---
phase: 16-ship-travel-mission
plan: 04
type: execute
wave: 4
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified:
  - client/src/components/MissionOverlay/MissionOverlay.tsx  # NEW
  - client/src/components/MissionOverlay/RhythmTapMission.tsx  # NEW
  - client/src/components/MissionOverlay/DefendMission.tsx  # NEW
  - client/src/components/MissionOverlay/HotSpotMission.tsx  # NEW
  - client/src/store/cosmic/slice.ts
  - client/src/App.tsx
autonomous: true
requirements:
  - MISSION-01
  - MISSION-02
  - MISSION-03
  - MISSION-04
  - MISSION-05
  - MISSION-06
  - MISSION-07
  - MISSION-08
  - CREW-04
  - CREW-08

must_haves:
  truths:
    - "При emit 'cosmic:start-mission' → MissionOverlay монтируется fullscreen"
    - "Overlay random выбирает один из 3 типов (rhythm/defend/hotspot)"
    - "Skip кнопка появляется через 1с (visible) → result='fail' → addBox без bonus"
    - "Score → result mapping: 1.0 perfect / 0.6+ good / else fail"
    - "Mission complete → consumeMissionCredit + addBox{element=elementFromPlanet, bonusRarity}"
    - "Box element детерминирован archetype/main-race планеты"
    - "Mission consumes 1 credit (CREW-04, CREW-07: only при consume)"
    - "Toast «Получен ящик NAME [Открыть]» эмитится при successful complete"
    - "fullscreen overlay имеет ship+planet preview как backdrop (MISSION-08)"
  artifacts:
    - path: "client/src/components/MissionOverlay/MissionOverlay.tsx"
      provides: "Главный overlay component: lifecycle intro→active→done, mission type dispatcher, skip handling"
      min_lines: 120
    - path: "client/src/components/MissionOverlay/RhythmTapMission.tsx"
      provides: "rhythm-tap challenge (15-30 кликов за 30с)"
      min_lines: 60
    - path: "client/src/components/MissionOverlay/DefendMission.tsx"
      provides: "defend challenge (3 вспышки за 15с)"
      min_lines: 70
    - path: "client/src/components/MissionOverlay/HotSpotMission.tsx"
      provides: "hot-spot challenge (5 точек за 20с)"
      min_lines: 70
    - path: "client/src/store/cosmic/slice.ts"
      provides: "investigatePlanet(planetId, result) action"
      contains: "investigatePlanet"
  key_links:
    - from: "App.tsx"
      to: "MissionOverlay (lazy mount)"
      via: "subscriber на 'cosmic:start-mission' → setActiveMissionPlanetId"
      pattern: "cosmic:start-mission|MissionOverlay"
    - from: "MissionOverlay onComplete"
      to: "useGameStore.investigatePlanet"
      via: "investigatePlanet(planetId, result)"
      pattern: "investigatePlanet"
    - from: "investigatePlanet"
      to: "elementFromPlanet + addBox"
      via: "resolve element from archetype, addBox с bonusRarity"
      pattern: "elementFromPlanet|addBox"
---

<objective>
Реализовать mini-clicker mission overlay с 3 типами challenges, scoring,
box generation, credit consumption.

Purpose: Без MissionOverlay loop scout→box→serum не замыкается. Это финальная
gameplay-фича Phase 16, делающая v2.0 fully-playable.

Output:
- 4 новых React components (overlay + 3 mission variants)
- Расширение slice с `investigatePlanet` action (atomic credit+box transaction)
- App.tsx subscriber на 'cosmic:start-mission' для mount/unmount overlay
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ship-travel-mission/PLAN.md
@.planning/phases/16-ship-travel-mission/16-03-SUMMARY.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slice.ts
@client/src/game/data/missionConfig.ts
@client/src/game/effects/elements/elementMapping.ts
@client/src/store/eventBus.ts
@client/src/locales/ru/translation.json

<interfaces>
<!-- Контракты после 16-01..03. -->

From cosmic/slice.ts (Phase 11/16-01):
```typescript
addBox(box: BoxData): void
consumeMissionCredit(): boolean
setHasFirstMission(v: boolean): void  // Phase 16-01 added
```

From missionConfig.ts (Phase 16-01):
```typescript
type MissionType = 'rhythm' | 'defend' | 'hotspot'
const MISSION_TYPES: readonly MissionType[]
pickRandomMissionType(): MissionType
type MissionResult = 'perfect' | 'good' | 'fail'
scoreToResult(score: number): MissionResult
bonusRarityForResult(result: MissionResult): number  // 0.15 / 0.05 / 0
findPlanetById, planetElementInputs
```

From elementMapping.ts (Phase 12):
```typescript
elementFromPlanet(archetype: string|undefined, mainRaceType: string|undefined): Element | null
```

From eventBus.ts:
```typescript
'cosmic:start-mission': { planetId: string }
'cosmic:mission-complete': { planetId: string; result: MissionResult }
'cosmic:mission-cancel': void
'cosmic:toast': CosmicToastPayload
```

From cosmic/types.ts (after 16-01):
```typescript
interface BoxData {
  id: string; element: Element; opened: boolean;
  sourceArchetype?: string; bonusRarity?: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить slice с investigatePlanet action (atomic credit+box transaction)</name>
  <files>client/src/store/cosmic/slice.ts</files>
  <behavior>
- guard: ship.state !== 'docked' OR ship.planetId !== planetId → no-op (false return)
- guard: missionsToday >= DAILY_CAP → no-op (caller проверяет, но belt-and-suspenders)
- atomic transaction (single set()):
  - missionsToday++
  - addBox с element from elementFromPlanet, bonusRarity from result, sourceArchetype
  - hasFirstMission = true (gates Боксы tab)
- emit 'cosmic:toast' { type: 'box-received', msg: 'Получен ящик NAME', action: открыть Боксы }
  </behavior>
  <action>
Модифицировать `client/src/store/cosmic/slice.ts`:

### A. Дополнительные импорты (если ещё не были)

```typescript
import {
  travelTimeMs, planetDistance, findPlanetById, getLocalDateString,
  DAILY_CAP, bonusRarityForResult, planetElementInputs,
  type MissionResult,
} from '../../game/data/missionConfig'
import { elementFromPlanet } from '../../game/effects/elements/elementMapping'
```

### B. Расширить interface

```typescript
export interface CosmicSliceActions {
  // ... existing ...

  // Phase 16-04: atomic investigate transaction
  investigatePlanet(planetId: string, result: MissionResult): boolean
    // returns true если успешно, false если guard не прошёл
}
```

### C. Имплементация

```typescript
investigatePlanet: (planetId, result) => {
  const s = get()
  // Guard 1: ship docked at this planet?
  if (!s.ship || s.ship.state !== 'docked' || s.ship.planetId !== planetId) {
    return false
  }
  // Guard 2: cap reached?
  if (s.crew.missionsToday >= DAILY_CAP) {
    return false
  }
  const planet = findPlanetById(planetId)
  if (!planet) return false

  // Resolve element
  const { archetype, mainRaceType } = planetElementInputs(planet)
  const element = elementFromPlanet(archetype, mainRaceType) ?? 'fire'  // fallback

  const bonus = bonusRarityForResult(result)

  // Atomic transaction: один set()
  const newBoxId = `box_${Date.now()}_${Math.floor(Math.random() * 1e6)}`
  set({
    crew: { ...s.crew, missionsToday: s.crew.missionsToday + 1 },
    boxes: [
      ...s.boxes,
      {
        id: newBoxId,
        element,
        opened: false,
        sourceArchetype: archetype ?? mainRaceType,
        bonusRarity: bonus,
      },
    ],
    hasFirstMission: true,  // unlock Боксы tab (REQ UX-09)
  })

  // Side-effect: toast (вне set, но в том же tick)
  eventBus.emit('cosmic:toast', {
    type: 'box-received',
    msg: `Получен ящик ${planet.name}`,  // i18n в caller wrapper если нужен — slice — pure logic
    action: {
      label: 'Открыть',
      onClick: () => {
        // Open Боксы tab (Phase 16-05 + Phase 15 wiring).
        // Сейчас просто переключим lastActiveTab; UI-side обработает на следующем mount.
        get().setLastActiveTab('boxes')
        // Можно дополнительно emit'нуть событие для immediate render если Hub открыт.
      },
    },
  })

  return true
},
```

**i18n note:** slice не знает про i18n (typically). Phase 16 решение: hardcode RU «Получен ящик» в slice. App.tsx Toast renderer может intercept type='box-received' и переводить через i18n key `mission.box_received_toast` на основе планет name. Это менее coupling. **Альтернатива (выбираем):** оставить hardcode RU в slice до Phase 19 i18n polish. Можно поправить в 16-05 при final i18n round.

### D. CREW-08 enforce

Pity counter (Phase 19) растёт только в `consumeMissionCredit` / `investigatePlanet`. Phase 16 этот counter не использует, но invariant: «pity не растёт от flights без mission». Documented в comment.

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    slice.ts экспортирует investigatePlanet с guards + atomic set + toast emit. tsc clean. grep `investigatePlanet` находит ≥2 (interface + implementation).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 mission variant components (Rhythm, Defend, HotSpot)</name>
  <files>client/src/components/MissionOverlay/RhythmTapMission.tsx, client/src/components/MissionOverlay/DefendMission.tsx, client/src/components/MissionOverlay/HotSpotMission.tsx</files>
  <behavior>
- RhythmTapMission: timer 30s, target = 15+random*15 taps, каждый tap → counter++, score = clamp(taps/target, 0, 1)
- DefendMission: 3 вспышки за 15s (interval ~5s), каждая live 1s window. Tap в окно → +1. Score = hits/3.
- HotSpotMission: 5 точек последовательно (4s each, 20s total). Tap внутри радиуса = found++. Score = found/5.
- Каждый компонент принимает props: { onComplete: (score: number) => void; onTimeout: (score: number) => void }
- Каждый закрывается timer'ом (timeout) либо tap'ом-out (если все targets hit раньше).
  </behavior>
  <action>
### A. `RhythmTapMission.tsx`

```tsx
// Phase 16: «Кликни планету N раз за 30 сек» (rhythm-tap mission).

import { useEffect, useState, useRef } from 'react'
import { useTranslation } from 'react-i18next'

interface Props {
  onComplete: (score: number) => void  // score: 0..1
}

export function RhythmTapMission({ onComplete }: Props) {
  const { t } = useTranslation()
  const targetRef = useRef<number>(15 + Math.floor(Math.random() * 16))  // 15..30
  const [taps, setTaps] = useState(0)
  const [remainingMs, setRemainingMs] = useState(30_000)
  const finishedRef = useRef(false)

  useEffect(() => {
    const startedAt = performance.now()
    const id = setInterval(() => {
      const elapsed = performance.now() - startedAt
      const remaining = 30_000 - elapsed
      if (remaining <= 0 && !finishedRef.current) {
        finishedRef.current = true
        clearInterval(id)
        const score = Math.min(1, taps / targetRef.current)
        onComplete(score)
      } else {
        setRemainingMs(Math.max(0, remaining))
      }
    }, 100)
    return () => clearInterval(id)
  }, [taps, onComplete])

  const handleTap = () => {
    if (finishedRef.current) return
    setTaps((n) => {
      const next = n + 1
      if (next >= targetRef.current) {
        finishedRef.current = true
        // Defer onComplete on next tick (избежать setState in setState warning)
        setTimeout(() => onComplete(1.0), 0)
      }
      return next
    })
  }

  return (
    <div className="flex flex-col items-center gap-4">
      <div className="text-white/80 text-sm">
        {t('mission.rhythm_label', { taps, target: targetRef.current })}
      </div>
      <button
        onClick={handleTap}
        className="w-32 h-32 rounded-full bg-gradient-to-br from-amber-400 to-orange-600 active:scale-95 transition-transform shadow-lg shadow-amber-500/30"
        aria-label={t('mission.rhythm_tap_button')}
      >
        <span className="text-3xl">🪐</span>
      </button>
      <div className="text-white/60 text-xs">
        {t('mission.time_left', { secs: Math.ceil(remainingMs / 1000) })}
      </div>
    </div>
  )
}
```

### B. `DefendMission.tsx`

```tsx
// Phase 16: «Защити корабль от 3 вспышек» (defend mission).

import { useEffect, useState, useRef } from 'react'
import { useTranslation } from 'react-i18next'

interface Props {
  onComplete: (score: number) => void
}

interface Flash {
  id: number
  x: number  // 0..1 (relative to overlay)
  y: number
  startMs: number
  durationMs: number  // 1000 (1 sec window)
  hit: boolean
}

export function DefendMission({ onComplete }: Props) {
  const { t } = useTranslation()
  const [flashes, setFlashes] = useState<Flash[]>([])
  const [hits, setHits] = useState(0)
  const finishedRef = useRef(false)

  useEffect(() => {
    const startedAt = performance.now()
    // Spawn 3 вспышки на t = 2s, 7s, 12s
    const spawnTimes = [2000, 7000, 12000]
    const timeouts: number[] = []
    spawnTimes.forEach((tms, i) => {
      timeouts.push(window.setTimeout(() => {
        if (finishedRef.current) return
        setFlashes((arr) => [...arr, {
          id: i,
          x: 0.2 + Math.random() * 0.6,
          y: 0.2 + Math.random() * 0.5,
          startMs: performance.now(),
          durationMs: 1000,
          hit: false,
        }])
      }, tms))
    })
    // Auto-complete на 15s
    const finalTimer = window.setTimeout(() => {
      if (finishedRef.current) return
      finishedRef.current = true
      const score = hits / 3
      onComplete(score)
    }, 15_000)
    timeouts.push(finalTimer)

    // Cleanup expired flashes (poll @ 100ms)
    const cleanupId = window.setInterval(() => {
      const now = performance.now()
      setFlashes((arr) => arr.filter((f) => now - f.startMs < f.durationMs || f.hit))
    }, 100)

    return () => {
      timeouts.forEach((id) => window.clearTimeout(id))
      window.clearInterval(cleanupId)
    }
  // hits captured in finalTimer closure — re-mount only on prop change.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  const handleFlashTap = (id: number) => {
    setFlashes((arr) => arr.map((f) => f.id === id ? { ...f, hit: true } : f))
    setHits((n) => {
      const next = n + 1
      if (next >= 3 && !finishedRef.current) {
        finishedRef.current = true
        setTimeout(() => onComplete(1.0), 0)
      }
      return next
    })
  }

  return (
    <div className="relative w-full h-64 bg-black/30 rounded-lg overflow-hidden">
      <div className="absolute top-2 left-2 text-white/80 text-sm">
        {t('mission.defend_label', { hits })}
      </div>
      {flashes.filter((f) => !f.hit).map((f) => (
        <button
          key={f.id}
          onClick={() => handleFlashTap(f.id)}
          className="absolute w-12 h-12 rounded-full bg-red-500 animate-pulse"
          style={{
            left: `${f.x * 100}%`,
            top: `${f.y * 100}%`,
            transform: 'translate(-50%, -50%)',
          }}
          aria-label={t('mission.defend_flash')}
        />
      ))}
    </div>
  )
}
```

### C. `HotSpotMission.tsx`

```tsx
// Phase 16: «Найди скрытое» (hot-spot mission).

import { useEffect, useState, useRef } from 'react'
import { useTranslation } from 'react-i18next'

interface Props {
  onComplete: (score: number) => void
}

interface Spot {
  x: number  // 0..1
  y: number
  found: boolean
}

export function HotSpotMission({ onComplete }: Props) {
  const { t } = useTranslation()
  const [spots, setSpots] = useState<Spot[]>(() =>
    Array.from({ length: 5 }, () => ({
      x: 0.1 + Math.random() * 0.8,
      y: 0.1 + Math.random() * 0.8,
      found: false,
    })),
  )
  const [activeIdx, setActiveIdx] = useState(0)
  const [remainingMs, setRemainingMs] = useState(20_000)
  const finishedRef = useRef(false)

  useEffect(() => {
    const startedAt = performance.now()
    const id = setInterval(() => {
      const elapsed = performance.now() - startedAt
      const remaining = 20_000 - elapsed
      // Auto-advance к next spot каждые 4с (если не найден)
      const expectedIdx = Math.min(4, Math.floor(elapsed / 4000))
      if (expectedIdx !== activeIdx) setActiveIdx(expectedIdx)
      if (remaining <= 0 && !finishedRef.current) {
        finishedRef.current = true
        clearInterval(id)
        const found = spots.filter((s) => s.found).length
        onComplete(found / 5)
      } else {
        setRemainingMs(Math.max(0, remaining))
      }
    }, 200)
    return () => clearInterval(id)
  }, [activeIdx, spots, onComplete])

  const handleAreaTap = (e: React.MouseEvent<HTMLDivElement>) => {
    const rect = e.currentTarget.getBoundingClientRect()
    const px = (e.clientX - rect.left) / rect.width
    const py = (e.clientY - rect.top) / rect.height
    const target = spots[activeIdx]
    if (!target || target.found) return
    const dx = px - target.x
    const dy = py - target.y
    const dist = Math.sqrt(dx * dx + dy * dy)
    if (dist < 0.08) {  // ~8% радиус — generous touch
      setSpots((arr) => arr.map((s, i) => i === activeIdx ? { ...s, found: true } : s))
      // Advance вручную
      const next = activeIdx + 1
      if (next >= 5 && !finishedRef.current) {
        finishedRef.current = true
        setTimeout(() => onComplete(1.0), 0)
      } else {
        setActiveIdx(next)
      }
    }
  }

  const found = spots.filter((s) => s.found).length

  return (
    <div className="flex flex-col items-center gap-3">
      <div className="text-white/80 text-sm">
        {t('mission.hotspot_label', { found, total: 5 })}
      </div>
      <div
        onClick={handleAreaTap}
        className="relative w-72 h-72 bg-gradient-to-br from-purple-900 to-blue-900 rounded-lg cursor-crosshair overflow-hidden"
      >
        {/* Hint: показать area-glow вокруг текущего spot */}
        {spots[activeIdx] && !spots[activeIdx].found && (
          <div
            className="absolute w-16 h-16 rounded-full bg-yellow-300/10 animate-pulse pointer-events-none"
            style={{
              left: `${spots[activeIdx].x * 100}%`,
              top: `${spots[activeIdx].y * 100}%`,
              transform: 'translate(-50%, -50%)',
            }}
          />
        )}
        {/* Found dots */}
        {spots.filter((s) => s.found).map((s, i) => (
          <div
            key={i}
            className="absolute w-3 h-3 rounded-full bg-emerald-400"
            style={{
              left: `${s.x * 100}%`,
              top: `${s.y * 100}%`,
              transform: 'translate(-50%, -50%)',
            }}
          />
        ))}
      </div>
      <div className="text-white/60 text-xs">
        {t('mission.time_left', { secs: Math.ceil(remainingMs / 1000) })}
      </div>
    </div>
  )
}
```

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "MissionOverlay/" | head -20</automated>
  </verify>
  <done>
    3 файла созданы, типизированы, Score формула match'ит spec (clamp 0..1, /target). tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MissionOverlay.tsx (dispatcher + skip + dispatch result) + App.tsx wiring</name>
  <files>client/src/components/MissionOverlay/MissionOverlay.tsx, client/src/App.tsx, client/src/locales/ru/translation.json, client/src/locales/en/translation.json, client/src/locales/es/translation.json</files>
  <behavior>
- App.tsx subscriber на 'cosmic:start-mission' → setActiveMissionPlanetId(planetId)
- При activeMissionPlanetId !== null → render MissionOverlay
- MissionOverlay: 1s intro → render random mission variant → onComplete(score) → resolve result + close + emit 'cosmic:mission-complete'
- Skip button visible с 1s после mount (REQ MISSION-04) → onComplete(0) → result='fail'
- App-level cleanup: при mission-complete event → call investigatePlanet(planetId, result), затем setActiveMissionPlanetId(null)
  </behavior>
  <action>
### A. `client/src/components/MissionOverlay/MissionOverlay.tsx` (NEW)

```tsx
// Phase 16: Mission overlay — fullscreen mini-clicker.
// REQ MISSION-01..08.

import { useEffect, useMemo, useState } from 'react'
import { useTranslation } from 'react-i18next'
import {
  pickRandomMissionType, scoreToResult, findPlanetById,
  type MissionType,
} from '../../game/data/missionConfig'
import { eventBus } from '../../store/eventBus'
import { RhythmTapMission } from './RhythmTapMission'
import { DefendMission } from './DefendMission'
import { HotSpotMission } from './HotSpotMission'

interface Props {
  planetId: string
  onClose: () => void  // unmount overlay (parent owns activeMissionPlanetId state)
  forceType?: MissionType  // dev override (Plan 16-05 dev panel)
}

export function MissionOverlay({ planetId, onClose, forceType }: Props) {
  const { t } = useTranslation()
  const planet = findPlanetById(planetId)
  const missionType: MissionType = useMemo(
    () => forceType ?? pickRandomMissionType(),
    [forceType],
  )

  const [phase, setPhase] = useState<'intro' | 'active' | 'done'>('intro')
  const [skipVisible, setSkipVisible] = useState(false)

  // 1s intro → active
  useEffect(() => {
    const id = setTimeout(() => setPhase('active'), 1000)
    return () => clearTimeout(id)
  }, [])

  // Skip button visible с 1s (REQ MISSION-04)
  useEffect(() => {
    const id = setTimeout(() => setSkipVisible(true), 1000)
    return () => clearTimeout(id)
  }, [])

  const finish = (score: number) => {
    if (phase === 'done') return
    setPhase('done')
    const result = scoreToResult(score)
    eventBus.emit('cosmic:mission-complete', { planetId, result })
    onClose()
  }

  const handleSkip = () => {
    eventBus.emit('cosmic:mission-cancel')
    finish(0)  // skip = score 0 → fail
  }

  const renderMission = () => {
    if (phase !== 'active') return null
    switch (missionType) {
      case 'rhythm': return <RhythmTapMission onComplete={finish} />
      case 'defend': return <DefendMission onComplete={finish} />
      case 'hotspot': return <HotSpotMission onComplete={finish} />
    }
  }

  return (
    <div className="fixed inset-0 z-[70] flex flex-col items-center justify-center bg-gradient-to-b from-gray-950 to-black">
      {/* Backdrop: ship + planet preview (REQ MISSION-08) */}
      <div className="absolute top-4 left-4 text-white/40 text-xs">
        🚀 → {planet?.name ?? planetId}
      </div>

      {/* Mission title */}
      <div className="absolute top-12 text-white text-lg font-bold">
        {t(`mission.title_${missionType}`)}
      </div>

      {/* Intro phase */}
      {phase === 'intro' && (
        <div className="text-white/80 text-sm animate-pulse">
          {t('mission.intro_warmup')}
        </div>
      )}

      {/* Active mission */}
      {renderMission()}

      {/* Skip button (REQ MISSION-04) */}
      {skipVisible && phase !== 'done' && (
        <button
          onClick={handleSkip}
          className="absolute bottom-4 right-4 px-4 py-2 bg-white/10 hover:bg-white/20 rounded text-sm text-white/80"
        >
          {t('mission.skip')}
        </button>
      )}
    </div>
  )
}
```

### B. App.tsx wiring

В App.tsx добавить:

```tsx
import { MissionOverlay } from './components/MissionOverlay/MissionOverlay'

// State
const [activeMissionPlanetId, setActiveMissionPlanetId] = useState<string | null>(null)

// Subscriber на start
useEffect(() => {
  const handleStart = ({ planetId }: { planetId: string }) => {
    // Validate ship docked + credits available
    const state = useGameStore.getState()
    if (!state.ship || state.ship.state !== 'docked' || state.ship.planetId !== planetId) return
    if (state.crew.missionsToday >= 4) return
    setActiveMissionPlanetId(planetId)
  }
  eventBus.on('cosmic:start-mission', handleStart)
  return () => { eventBus.off('cosmic:start-mission', handleStart) }
}, [])

// Subscriber на complete → call slice action
useEffect(() => {
  const handleComplete = ({ planetId, result }: { planetId: string; result: 'perfect' | 'good' | 'fail' }) => {
    useGameStore.getState().investigatePlanet(planetId, result)
    setActiveMissionPlanetId(null)
  }
  eventBus.on('cosmic:mission-complete', handleComplete)
  return () => { eventBus.off('cosmic:mission-complete', handleComplete) }
}, [])

// Render overlay
{activeMissionPlanetId && (
  <MissionOverlay
    planetId={activeMissionPlanetId}
    onClose={() => setActiveMissionPlanetId(null)}
  />
)}
```

### C. i18n strings

В RU/EN/ES translation.json добавить под "mission":

**RU:**
```json
"mission": {
  "title_rhythm": "Кликни планету",
  "title_defend": "Защити корабль",
  "title_hotspot": "Найди скрытое",
  "intro_warmup": "Приготовьтесь...",
  "skip": "Пропустить",
  "rhythm_label": "{{taps}} / {{target}}",
  "rhythm_tap_button": "Тап",
  "defend_label": "Отбито: {{hits}} / 3",
  "defend_flash": "Вспышка",
  "hotspot_label": "Найдено: {{found}} / {{total}}",
  "time_left": "{{secs}}с",
  "box_received_toast": "Получен ящик {{name}}"
}
```

**EN:**
```json
"mission": {
  "title_rhythm": "Tap the planet",
  "title_defend": "Defend ship",
  "title_hotspot": "Find hidden",
  "intro_warmup": "Get ready...",
  "skip": "Skip",
  "rhythm_label": "{{taps}} / {{target}}",
  "rhythm_tap_button": "Tap",
  "defend_label": "Hits: {{hits}} / 3",
  "defend_flash": "Flash",
  "hotspot_label": "Found: {{found}} / {{total}}",
  "time_left": "{{secs}}s",
  "box_received_toast": "Box received: {{name}}"
}
```

**ES:**
```json
"mission": {
  "title_rhythm": "Toca el planeta",
  "title_defend": "Defiende la nave",
  "title_hotspot": "Encuentra oculto",
  "intro_warmup": "Prepárate...",
  "skip": "Saltar",
  "rhythm_label": "{{taps}} / {{target}}",
  "rhythm_tap_button": "Toca",
  "defend_label": "Aciertos: {{hits}} / 3",
  "defend_flash": "Destello",
  "hotspot_label": "Encontrado: {{found}} / {{total}}",
  "time_left": "{{secs}}s",
  "box_received_toast": "Caja recibida: {{name}}"
}
```

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    MissionOverlay создан, App.tsx subscriber wires start → mount → complete → investigatePlanet. Skip button работает. i18n RU/EN/ES добавлены. tsc clean.
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` → 0 errors
- `grep -c "investigatePlanet" client/src/store/cosmic/slice.ts` → ≥2
- `grep -c "MissionOverlay" client/src/App.tsx` → ≥2 (import + usage)
- `grep -c "RhythmTapMission\|DefendMission\|HotSpotMission" client/src/components/MissionOverlay/MissionOverlay.tsx` → ≥3 (3 imports)
- Manual: trigger investigate at HOME → MissionOverlay появляется → играю → mission-complete → бокс в `cosmicSlice.boxes`.
- Manual: 5-я попытка после 4 успешных → investigatePlanet возвращает false (cap reached); UI button уже disabled из 16-03.
</verification>

<success_criteria>
1. tsc clean.
2. После «Изучить» (из ShipTab Plan 16-03) — MissionOverlay появляется fullscreen.
3. Random missionType (rhythm / defend / hotspot) монтируется per session.
4. Score → result → bonusRarity correct (perfect 0.15, good 0.05, fail 0).
5. Mission-complete → addBox с element=elementFromPlanet(archetype).
6. cosmic:toast «Получен ящик NAME» эмитится.
7. crew.missionsToday увеличивается ровно на 1 per mission.
8. hasFirstMission = true после первой successful mission.
9. Skip button visible с 1s, click → result='fail' → addBox с bonus=0.
</success_criteria>

<output>
После завершения создать `.planning/phases/16-ship-travel-mission/16-04-SUMMARY.md` с:
- Что реализовано (REQ-IDs ✓ / ◑ / ✗)
- Bundle delta gzip (cumulative)
- Atomic commits
  - `phase-16: add investigatePlanet atomic transaction in cosmic slice`
  - `phase-16: add 3 mission variant components (rhythm/defend/hotspot)`
  - `phase-16: add MissionOverlay dispatcher + App.tsx wiring + i18n`
- Open issues:
  - Toast i18n: slice использует hardcoded RU «Получен ящик» — Plan 16-05 i18n round может перенести в App-side render layer.
  - Box opening UX = Phase 15 dependency. Mission complete → бокс в инвентаре, но cascade reveal сейчас не работает (Phase 15 необходим). Toast action.onClick → open Hub on Боксы tab — функция-stub, ждёт Plan 16-05 wiring.
</output>
