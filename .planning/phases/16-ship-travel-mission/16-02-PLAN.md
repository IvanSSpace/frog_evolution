---
phase: 16-ship-travel-mission
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - client/src/game/effects/ShipSprite.ts  # NEW
  - client/src/game/scenes/StarMapScene.ts
  - client/src/App.tsx
autonomous: true
requirements:
  - SHIP-02
  - SHIP-04
  - SHIP-05
  - SHIP-06
  - SHIP-09
  - SHIP-10

must_haves:
  truths:
    - "На StarMap видна одна ракетка-ship у текущей planet (docked) либо на траектории (transit)"
    - "Particle-trail активен только в transit, отключается за 5% до прибытия"
    - "Sprite ротация совпадает с вектором движения (угол atan2)"
    - "Tween linear, completion вызывает arriveShipAt() через onArrive callback"
    - "Redirect mid-flight — старый tween killed, новый старт от current world pos"
    - "На arrival emit'ится 'cosmic:ship-arrived' (через slice action), App слышит → toast"
    - "ShipSprite.destroy() вызывается из StarMapScene.shutdown() (REQ INFRA-06)"
  artifacts:
    - path: "client/src/game/effects/ShipSprite.ts"
      provides: "Class ShipSprite — Phaser-native ship visual + transit tween + trail"
      min_lines: 130
    - path: "client/src/game/scenes/StarMapScene.ts"
      provides: "Создание ShipSprite в create(), subscribe на ship state changes, emit cosmic:request-flight в handlePlanetPress, cleanup в shutdown()"
      contains: "new ShipSprite"
    - path: "client/src/App.tsx"
      provides: "Subscriber на 'cosmic:ship-arrived' → emit grouped toast «Прибыли на NAME [Изучить]»"
      contains: "cosmic:ship-arrived"
  key_links:
    - from: "ShipSprite.ts"
      to: "Phaser.Tweens"
      via: "scene.tweens.add({ targets: container, x, y, ... onComplete })"
      pattern: "scene\\.tweens\\.add"
    - from: "StarMapScene.ts"
      to: "useGameStore.subscribe"
      via: "selector state.cosmicSlice.ship → onShipChange handler"
      pattern: "subscribe.*cosmicSlice\\.ship|state\\.ship"
    - from: "StarMapScene.handlePlanetPress"
      to: "eventBus.emit('cosmic:request-flight'"
      via: "после existing 'starmap:planet-tapped' emit, дополнительно request-flight"
      pattern: "cosmic:request-flight"
---

<objective>
Реализовать визуальную часть ship + интегрировать в StarMapScene.

Purpose: Без ShipSprite Phase 16 не имеет видимого корабля. Этот plan создаёт
class, подключает к StarMapScene через store-subscribe, и обеспечивает arrival
flow (toast + cosmic:ship-arrived event).

Output:
- `client/src/game/effects/ShipSprite.ts` (NEW) — Phaser class
- `client/src/game/scenes/StarMapScene.ts` (modified) — create/subscribe/cleanup
- `client/src/App.tsx` (modified) — eventBus subscriber на ship-arrived
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ship-travel-mission/PLAN.md
@.planning/phases/16-ship-travel-mission/16-01-SUMMARY.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slice.ts
@client/src/game/data/missionConfig.ts
@client/src/store/eventBus.ts

<interfaces>
<!-- Контракты Plan 16-01 (Wave 1 output). -->

From client/src/store/cosmic/types.ts (after 16-01):
```typescript
export type ShipState = ShipStateDocked | ShipStateTransit
export interface ShipStateDocked { state: 'docked'; planetId: string }
export interface ShipStateTransit {
  state: 'transit'; fromPlanetId: string; toPlanetId: string;
  startedAt: number; arrivesAt: number
}
```

From client/src/store/cosmic/slice.ts (after 16-01):
```typescript
ensureShipExists(): void
sendShipTo(toPlanetId: string): void
arriveShipAt(planetId: string): void
setShipPosition(x: number, y: number): void
```

From client/src/game/data/missionConfig.ts (after 16-01):
```typescript
findPlanetById(id: string): PlanetInfo | null
planetDistance(a, b): number
travelTimeMs(distance: number): number
```

From StarMapScene.ts (Phase 9):
```typescript
const WORLD_SIZE = 7000 * DPR  // line 38
private handlePlanetPress(sys: Race | BgSystem) { ... emit 'starmap:planet-tapped' ... }
// scene содержит worldContainer (см. existing patterns); planet positions = sys.x, sys.y в DPR-units
```

From client/src/store/eventBus.ts (after 16-01):
```typescript
'cosmic:request-flight': { planetId: string }
'cosmic:ship-arrived': { planetId: string }
```

Existing toast subscriber pattern (Phase 11 в App.tsx):
```typescript
useEffect(() => {
  const off = eventBus.on('cosmic:toast', (payload) => { /* render toast */ })
  return () => { eventBus.off('cosmic:toast', off) }
}, [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать ShipSprite класс (Phaser-native ракетка + trail + tween)</name>
  <files>client/src/game/effects/ShipSprite.ts</files>
  <behavior>
- new ShipSprite(opts) → создаёт container с body (triangle Graphics) + reservoirs trail
- setDocked(planetPos) → kill tween, скрыть trail, position рядом с planet (offset 30 px)
- startTransit(from, to, durationMs, onArrive) → tween linear, rotation = atan2 vector, trail enabled, onUpdate → store.setShipPosition (throttled)
- На completion → onArrive() callback (вызывает store.arriveShipAt в caller)
- redirect → kill currentTween, startTransit от текущей container.x/y
- destroy → killAllTweens + container.destroy + trail.destroy
  </behavior>
  <action>
Создать NEW `client/src/game/effects/ShipSprite.ts`:

```typescript
// Phase 16: Ship visual (REQ SHIP-02, SHIP-04, SHIP-05, SHIP-06).
// Phaser-native (Container + Graphics + ParticleEmitter). НЕ DOM.
// Singleton в StarMapScene; subscribed на cosmicSlice.ship через scene-side subscribe.

import Phaser from 'phaser'

export interface ShipSpriteOpts {
  scene: Phaser.Scene
  parent: Phaser.GameObjects.Container | null  // worldContainer or null = scene root
  initialPosition: { x: number; y: number }
  depth?: number  // default 1500
  onPositionUpdate?: (x: number, y: number) => void  // throttled @ 6 frames в caller
}

const TRAIL_TINT = 0xfde047  // желтый (matches plasma idle)
const SHIP_SCALE = 1
const TRAIL_TRIGGER_THROTTLE_MS = 16 * 6  // ~96ms (~6 frames @ 60fps)

export class ShipSprite {
  public state: 'docked' | 'transit' = 'docked'
  public worldX: number
  public worldY: number

  private scene: Phaser.Scene
  private container: Phaser.GameObjects.Container
  private body: Phaser.GameObjects.Graphics
  private particles: Phaser.GameObjects.Particles.ParticleEmitter | null = null
  private currentTween: Phaser.Tweens.Tween | null = null
  private opts: ShipSpriteOpts
  private destroyed = false
  private lastPosUpdateMs = 0

  constructor(opts: ShipSpriteOpts) {
    this.opts = opts
    this.scene = opts.scene
    this.worldX = opts.initialPosition.x
    this.worldY = opts.initialPosition.y

    this.container = this.scene.add.container(this.worldX, this.worldY)
    this.container.setDepth(opts.depth ?? 1500)
    if (opts.parent) {
      opts.parent.add(this.container)
    }

    // Body — простая ракетка-triangle.
    this.body = this.scene.add.graphics()
    this.drawBody(this.body)
    this.container.add(this.body)
  }

  /** Triangular ship silhouette pointing right (rotation=0). */
  private drawBody(g: Phaser.GameObjects.Graphics) {
    g.clear()
    // Корпус: triangle с базой 12 px, высотой 18 px, точка справа.
    g.fillStyle(0xe5e7eb, 1)  // светло-серый
    g.beginPath()
    g.moveTo(10 * SHIP_SCALE, 0)
    g.lineTo(-8 * SHIP_SCALE, -6 * SHIP_SCALE)
    g.lineTo(-8 * SHIP_SCALE, 6 * SHIP_SCALE)
    g.closePath()
    g.fillPath()
    // Окошко-cockpit
    g.fillStyle(0x60a5fa, 1)
    g.fillCircle(2 * SHIP_SCALE, 0, 2 * SHIP_SCALE)
    // Outline
    g.lineStyle(1, 0x111827, 1)
    g.strokePath()
  }

  /** Setup particle emitter for trail. Created lazily on first transit. */
  private ensureTrail() {
    if (this.particles || this.destroyed) return
    // Phaser 3.60+: scene.add.particles(x, y, texture, config) returns ParticleEmitter directly.
    // Если в проекте нет '__ship_trail' texture — используем встроенный 'particle' атлас или
    // на лету создаём 1×1 white texture. Проверяем через TextureManager.
    const TEX_KEY = '__ship_trail_dot'
    if (!this.scene.textures.exists(TEX_KEY)) {
      const g = this.scene.make.graphics({ x: 0, y: 0 }, false)
      g.fillStyle(0xffffff, 1)
      g.fillCircle(2, 2, 2)
      g.generateTexture(TEX_KEY, 4, 4)
      g.destroy()
    }
    // emitZone = trailing behind ship (offset -10 на X в local space — будет умножено rotation)
    this.particles = this.scene.add.particles(0, 0, TEX_KEY, {
      lifespan: 600,
      speed: 0,
      scale: { start: 0.6, end: 0 },
      alpha: { start: 0.7, end: 0 },
      tint: TRAIL_TINT,
      blendMode: Phaser.BlendModes.ADD,
      frequency: 40,  // emit every 40ms
      follow: this.container,
      followOffset: { x: -10, y: 0 },
    })
    this.particles.setDepth((this.opts.depth ?? 1500) - 1)
    this.particles.stop()  // запустим в startTransit
  }

  /** Hide trail (transit complete or set to docked). */
  private stopTrail() {
    if (!this.particles) return
    this.particles.stop()
  }

  setDocked(planetPos: { x: number; y: number }, planetSize?: number): void {
    if (this.destroyed) return
    this.killCurrentTween()
    this.stopTrail()
    this.state = 'docked'
    // Orbit offset: справа от планеты, на размере planet+30 px (минимум 50).
    const orbitR = Math.max(50, (planetSize ?? 60) * 0.6)
    const x = planetPos.x + orbitR
    const y = planetPos.y
    this.container.setPosition(x, y)
    this.container.setRotation(0)
    this.worldX = x
    this.worldY = y
  }

  startTransit(
    from: { x: number; y: number },
    to: { x: number; y: number },
    durationMs: number,
    onArrive: () => void,
  ): void {
    if (this.destroyed) return
    this.killCurrentTween()
    this.ensureTrail()

    this.container.setPosition(from.x, from.y)
    this.worldX = from.x
    this.worldY = from.y

    const angle = Math.atan2(to.y - from.y, to.x - from.x)
    this.container.setRotation(angle)

    this.state = 'transit'
    this.particles?.start()

    this.currentTween = this.scene.tweens.add({
      targets: this.container,
      x: to.x,
      y: to.y,
      duration: durationMs,
      ease: 'Linear',
      onUpdate: (tween) => {
        if (this.destroyed) return
        this.worldX = this.container.x
        this.worldY = this.container.y
        // Stop trail at t > 0.95 (REQ SHIP-05)
        if (tween.progress > 0.95) this.stopTrail()
        // Throttled position broadcast (REQ SHIP-06: redirect needs current pos)
        const now = performance.now()
        if (now - this.lastPosUpdateMs > TRAIL_TRIGGER_THROTTLE_MS) {
          this.lastPosUpdateMs = now
          this.opts.onPositionUpdate?.(this.worldX, this.worldY)
        }
      },
      onComplete: () => {
        if (this.destroyed) return
        this.currentTween = null
        this.stopTrail()
        this.state = 'docked'
        onArrive()
      },
    })
  }

  /** Mid-flight redirect: kill current, start fresh from current pos. */
  redirect(
    to: { x: number; y: number },
    durationMs: number,
    onArrive: () => void,
  ): void {
    if (this.destroyed) return
    const from = { x: this.worldX, y: this.worldY }
    this.startTransit(from, to, durationMs, onArrive)
  }

  /** Snap-position ship without tween (used for store-driven sync after store change). */
  syncFromState(transit: { from: { x: number; y: number }; to: { x: number; y: number }; startedAt: number; arrivesAt: number }, onArrive: () => void): void {
    if (this.destroyed) return
    const total = transit.arrivesAt - transit.startedAt
    const elapsed = Date.now() - transit.startedAt
    if (elapsed >= total) {
      onArrive()
      return
    }
    const remaining = total - elapsed
    // Compute current interpolated start point (linear)
    const t = Math.max(0, Math.min(1, elapsed / total))
    const curX = transit.from.x + (transit.to.x - transit.from.x) * t
    const curY = transit.from.y + (transit.to.y - transit.from.y) * t
    this.startTransit({ x: curX, y: curY }, transit.to, remaining, onArrive)
  }

  destroy(): void {
    if (this.destroyed) return
    this.destroyed = true
    this.killCurrentTween()
    if (this.particles) {
      this.particles.destroy()
      this.particles = null
    }
    this.container.destroy()
  }

  private killCurrentTween() {
    if (this.currentTween) {
      this.currentTween.stop()
      this.currentTween.remove()
      this.currentTween = null
    }
  }
}
```

**Phaser version compat note:** Phaser 3.60+ имеет нативный `scene.add.particles(x, y, texture, config)` (в отличие от 3.55 где был `createEmitter`). Если проект на 3.55 — использовать `scene.add.particles(TEX_KEY).createEmitter({...})` синтаксис. Проверить `package.json` перед start: смотрим `"phaser": "^3.6X.X"`.

Если depths коллизия с другими elements (e.g. NebulaBackground depth=-100, planets ~1000) — depth 1500 даёт ship поверх планет. Если StarMapScene использует worldContainer (camera), parent=worldContainer чтобы координаты в world-space.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "ShipSprite\.ts" | head -20</automated>
  </verify>
  <done>
    ShipSprite класс экспортируется, имеет методы setDocked, startTransit, redirect, syncFromState, destroy. Trail particles создаются лениво. Tween использует Linear ease. onUpdate throttled через TRAIL_TRIGGER_THROTTLE_MS. tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Интегрировать ShipSprite в StarMapScene + emit cosmic:request-flight</name>
  <files>client/src/game/scenes/StarMapScene.ts</files>
  <behavior>
- В create() после initial setup: useGameStore.getState().ensureShipExists(); создать ShipSprite с initialPosition = home planet coords; subscribe на ship state.
- onShipChange handler: docked → ship.setDocked(planetCoords(planetId)); transit → ship.syncFromState(planetCoords pairs, onArrive=() => store.arriveShipAt(toPlanetId))
- В handlePlanetPress(sys): после existing emit 'starmap:planet-tapped' дополнительно eventBus.emit('cosmic:request-flight', { planetId: sys.id }) — App-side subscriber решит, открыт ли Hub.
- В shutdown(): ship.destroy(), unsubscribe (kill subscribe handle)
  </behavior>
  <action>
Модифицировать `client/src/game/scenes/StarMapScene.ts`:

### A. Импорты (около line 1-30)

Добавить:
```typescript
import { ShipSprite } from '../effects/ShipSprite'
import { useGameStore } from '../../store/gameStore'  // если уже не импортирован
import { findPlanetById } from '../data/missionConfig'
import type { ShipState } from '../../store/cosmic/types'
```

### B. Class fields (около других private fields)

```typescript
private shipSprite: ShipSprite | null = null
private shipUnsubscribe: (() => void) | null = null
private lastShipStateSig = ''  // dedup для re-emit того же state object
```

### C. В `create()` после построения worldContainer + planets

Найти место где scene уже знает о worldContainer (грубо grep `worldContainer\|this.world`). Добавить блок:

```typescript
// Phase 16: Ship singleton.
// Гарантируем что ship существует (auto-spawn at home при первом open).
useGameStore.getState().ensureShipExists()
const initialShip = useGameStore.getState().ship
const homeId = initialShip?.state === 'docked' ? initialShip.planetId : 'home'
const homePlanet = findPlanetById(homeId) ?? findPlanetById('home')
if (homePlanet) {
  this.shipSprite = new ShipSprite({
    scene: this,
    parent: this.worldContainer ?? null,  // ИСПОЛЬЗОВАТЬ existing worldContainer reference
    initialPosition: { x: homePlanet.x, y: homePlanet.y },
    depth: 1500,
    onPositionUpdate: (x, y) => {
      // throttled выше; здесь — простой proxy в store для redirect calc
      useGameStore.getState().setShipPosition(x, y)
    },
  })
  // Sync initial state
  this.applyShipState(useGameStore.getState().ship)
}

// Subscribe — реагируем на изменения cosmicSlice.ship.
// Selector + dedup через JSON-сигнатуру (чтобы не reapply identical state).
this.shipUnsubscribe = useGameStore.subscribe(
  (s) => s.ship,
  (ship) => this.applyShipState(ship),
  { equalityFn: (a, b) => JSON.stringify(a) === JSON.stringify(b) },
)
```

**Note:** `useGameStore.subscribe` semantics зависят от используемого middleware. Phase 12 FrogOverlayManager использует subscribe pattern — проверить там как именно подписан (vanilla `useGameStore.subscribe(state => state.cosmicSlice, ...)` либо `subscribeWithSelector` middleware). Использовать тот же паттерн.

### D. Метод `applyShipState`

Добавить private метод:

```typescript
private applyShipState(ship: ShipState | null): void {
  if (!this.shipSprite || ship === null) return

  const sig = JSON.stringify(ship)
  if (sig === this.lastShipStateSig) return
  this.lastShipStateSig = sig

  if (ship.state === 'docked') {
    const p = findPlanetById(ship.planetId)
    if (!p) return
    this.shipSprite.setDocked({ x: p.x, y: p.y }, p.size)
  } else {
    const fp = findPlanetById(ship.fromPlanetId)
    const tp = findPlanetById(ship.toPlanetId)
    if (!fp || !tp) return
    const onArrive = () => {
      // Лучший ход: позвать слайс из тут (одно место — slice action emit'нет cosmic:ship-arrived)
      useGameStore.getState().arriveShipAt(ship.toPlanetId)
    }
    this.shipSprite.syncFromState(
      {
        from: { x: fp.x, y: fp.y },
        to: { x: tp.x, y: tp.y },
        startedAt: ship.startedAt,
        arrivesAt: ship.arrivesAt,
      },
      onArrive,
    )
  }
}
```

### E. handlePlanetPress модификация

Найти существующий метод `handlePlanetPress` (line ~810). После строки `eventBus.emit('starmap:planet-tapped', { ... })` (~line 824), добавить:

```typescript
// Phase 16: parallel emit для Cosmic Hub flight flow.
// App-side subscriber решает, открыт ли Hub, и показывает confirm dialog
// (slice ничего не делает до confirm).
eventBus.emit('cosmic:request-flight', { planetId: sys.id })
```

(Это **не блокирует** existing planet animation — обычный tap по-прежнему запускает animation. Confirm dialog появится поверх независимо.)

### F. shutdown / cleanup

Найти существующий cleanup (либо `shutdown()` либо в `destroy()` — Phase 9 refactor должен был кодифицировать; если нет — добавить shutdown override). Добавить:

```typescript
shutdown(): void {
  if (this.shipUnsubscribe) {
    this.shipUnsubscribe()
    this.shipUnsubscribe = null
  }
  if (this.shipSprite) {
    this.shipSprite.destroy()
    this.shipSprite = null
  }
  this.lastShipStateSig = ''
  // ... existing shutdown logic если есть ...
}
```

Если shutdown уже override'ен — добавить наши строки в существующий метод (не создавать дубликата).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "StarMapScene\.ts" | head -20</automated>
  </verify>
  <done>
    StarMapScene имеет shipSprite field + create-time spawn at home + applyShipState handler + cosmic:request-flight emit в handlePlanetPress + cleanup в shutdown. tsc clean. grep подтверждает ≥3 references «ShipSprite» и ≥1 «cosmic:request-flight» в StarMapScene.ts.
  </done>
</task>

<task type="auto">
  <name>Task 3: App.tsx subscriber для 'cosmic:ship-arrived' → toast «Прибыли на NAME [Изучить]»</name>
  <files>client/src/App.tsx</files>
  <action>
Модифицировать `client/src/App.tsx`:

### A. Импорты (наверху)

Добавить (если не уже):
```typescript
import { findPlanetById } from './game/data/missionConfig'
```

### B. Subscriber на 'cosmic:ship-arrived'

Найти существующий useEffect для `'cosmic:toast'` subscriber (Phase 11). Рядом или в новом useEffect — подписаться на ship-arrived:

```typescript
useEffect(() => {
  const handleArrived = ({ planetId }: { planetId: string }) => {
    const planet = findPlanetById(planetId)
    const name = planet?.name ?? planetId.toUpperCase()
    // Emit как обычный cosmic:toast чтобы переиспользовать grouped toast logic
    // (Phase 11: multi-toast grouping в App.tsx subscriber).
    eventBus.emit('cosmic:toast', {
      type: 'generic',
      msg: `Прибыли на ${name}`,
      action: {
        label: 'Изучить',
        onClick: () => {
          // Открыть Cosmic Hub на табе Корабль (Phase 16-03 wiring) +
          // emit start-mission для прямого перехода в overlay (если есть credit).
          // В Plan 16-03: useGameStore.getState().setLastActiveTab('scouts')
          //               + setCosmicHubOpen(true)
          //               + eventBus.emit('cosmic:start-mission', { planetId })
          // Для Plan 16-02 — заводим scaffold; реальная отправка в start-mission подготовлена в Plan 16-04.
          eventBus.emit('cosmic:start-mission', { planetId })
        },
      },
    })
  }
  eventBus.on('cosmic:ship-arrived', handleArrived)
  return () => { eventBus.off('cosmic:ship-arrived', handleArrived) }
}, [])
```

**Note:** В Plan 16-04 расширим `onClick` чтобы реально открывал Cosmic Hub modal — сейчас просто emit `cosmic:start-mission` (downstream listener в Plan 16-04 обработает).

### C. Опционально: i18n

Использовать t('cosmic_hub.ship_arrived', { name }) если i18n уже подключен в App.tsx. Если нет — hardcode RU «Прибыли на …» (i18n round завершится в 16-03/16-05).

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "App\.tsx" | head -20</automated>
  </verify>
  <done>
    App.tsx имеет subscriber на 'cosmic:ship-arrived', emit'ит cosmic:toast с msg «Прибыли на NAME» и action.onClick → emit 'cosmic:start-mission'. Cleanup useEffect возврат корректен. tsc clean.
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` → 0 errors
- `cd client && npm run build` → success (не обязательно для Plan 16-02 если 16-05 валидирует, но желательно)
- `grep -c "ShipSprite\|new ShipSprite" client/src/game/scenes/StarMapScene.ts` → ≥3
- `grep -c "cosmic:request-flight" client/src/game/scenes/StarMapScene.ts` → ≥1
- `grep -c "cosmic:ship-arrived" client/src/App.tsx` → ≥1
- Manual smoke (dev): открыть StarMap → ракетка видна у HOME planet с orbit offset.
</verification>

<success_criteria>
1. tsc clean.
2. Ship sprite видим у home planet при открытии StarMap (auto-spawn через ensureShipExists).
3. Manual: установить ship state в transit через `useGameStore.getState().sendShipTo('bliks')` — ship начинает движение, trail виден.
4. Прибытие → ship.setDocked у bliks → emit 'cosmic:ship-arrived' (через slice) → App subscriber → cosmic:toast.
5. Redirect: во время transit вызвать `sendShipTo('drave')` → ship меняет цель без визуальных артефактов (старый tween killed).
6. shutdown: переключить scene → no tween leak (visual: ship destroy'нут).
</success_criteria>

<output>
После завершения создать `.planning/phases/16-ship-travel-mission/16-02-SUMMARY.md` с:
- Что реализовано (REQ-IDs ✓ / ◑ / ✗)
- Bundle delta gzip
- Atomic commits
  - `phase-16: add ShipSprite Phaser-native ship visual + trail + transit tween`
  - `phase-16: integrate ship into StarMapScene with store subscribe + cleanup`
  - `phase-16: subscribe to cosmic:ship-arrived in App.tsx → arrival toast`
- Open issues: ship visible only когда StarMap открыт; в Plan 16-03 — текстовое отображение состояния в ShipTab покрывает offline-StarMap случай.
</output>
