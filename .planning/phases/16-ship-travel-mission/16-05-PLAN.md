---
phase: 16-ship-travel-mission
plan: 05
type: execute
wave: 5
depends_on:
  - 16-01
  - 16-02
  - 16-03
  - 16-04
files_modified:
  - client/src/components/CosmicHub/CosmicHubModal.tsx
  - client/src/store/gameStore.ts
  - client/src/App.tsx
  - client/src/locales/ru/translation.json
  - client/src/locales/en/translation.json
  - client/src/locales/es/translation.json
autonomous: true
requirements:
  - UX-09

must_haves:
  truths:
    - "В DEV mode все 4 таба Cosmic Hub видны (даже без прогресса)"
    - "В PRODUCTION mode + clean save: видны только Сыворотки + Бестиарий"
    - "После первой completed mission — Боксы tab unlocks (через hasFirstMission flag)"
    - "Корабль tab unlocks после первого feed (Phase 17 устанавливает hasFirstFeed)"
    - "Disabled таб render'ит gray + tooltip"
    - "Dev helpers `__forceMissionType(type)`, `__resetCrewToday()`, `__unlockAllTabs()` работают (DEV-only, tree-shaken в prod)"
    - "Final i18n round: все ключи присутствуют в RU/EN/ES (≤12 chars для tabs)"
    - "tsc clean, vite build success, bundle delta ≤ +40 KB gzip vs Phase 15 baseline"
  artifacts:
    - path: "client/src/components/CosmicHub/CosmicHubModal.tsx"
      provides: "Tab gating logic с enabled flag + disabled visual + tooltip"
      contains: "hasFirstMission|hasFirstFeed|hasOpenedAnyBox"
    - path: "client/src/store/gameStore.ts"
      provides: "Dev-mode init: при import.meta.env.DEV — sentinel flags = true"
      contains: "import.meta.env.DEV"
    - path: "client/src/App.tsx"
      provides: "Dev helpers exposed на window (__forceMissionType, __resetCrewToday, __unlockAllTabs)"
      contains: "__forceMissionType"
  key_links:
    - from: "CosmicHubModal.tsx"
      to: "useGameStore (sentinel flags selectors)"
      via: "useGameStore(s => s.hasFirstFeed)"
      pattern: "hasFirstFeed|hasFirstMission|hasOpenedAnyBox"
    - from: "gameStore.ts"
      to: "import.meta.env.DEV"
      via: "init-time toggle для dev unlock"
      pattern: "import\\.meta\\.env\\.DEV"
---

<objective>
Финализировать Phase 16: progressive disclosure tab gating (UX-09), dev helpers
для testing, final i18n полнота, full verification бакета.

Purpose: Без gating UX broken (показываются tabs без контента). Без dev helpers
тестирование не воспроизводимо. Без verify не доказываем что Phase 16 ship-ready.

Output:
- CosmicHubModal: gating logic + disabled tab UI
- gameStore: DEV-mode unlock-all sentinel flags
- App.tsx: window-exposed dev helpers
- i18n final pass
- full verify: tsc + build + bundle delta + smoke checklist
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ship-travel-mission/PLAN.md
@.planning/phases/16-ship-travel-mission/16-01-SUMMARY.md
@.planning/phases/16-ship-travel-mission/16-02-SUMMARY.md
@.planning/phases/16-ship-travel-mission/16-03-SUMMARY.md
@.planning/phases/16-ship-travel-mission/16-04-SUMMARY.md
@client/src/components/CosmicHub/CosmicHubModal.tsx
@client/src/store/gameStore.ts
@client/src/App.tsx

<interfaces>
<!-- Полный набор после 16-01..04. -->

From cosmic/types.ts (after 16-01):
```typescript
export interface CosmicSlice {
  // ...
  hasFirstFeed: boolean
  hasFirstMission: boolean
  hasOpenedAnyBox: boolean
}
```

From cosmic/slice.ts (after 16-01..04):
```typescript
setHasFirstFeed(v: boolean): void
setHasFirstMission(v: boolean): void
setHasOpenedAnyBox(v: boolean): void
investigatePlanet(planetId, result): boolean  // sets hasFirstMission = true
ensureShipExists(): void
sendShipTo(planetId): void
arriveShipAt(planetId): void
```

From CosmicHubModal.tsx (after 16-03):
```typescript
const TABS: Tab[] = [
  { id: 'scouts',   label: t('cosmic_hub.tab_ship'),     icon: '🚀' },
  { id: 'boxes',    label: t('cosmic_hub.tab_boxes'),    icon: '🎁' },
  { id: 'serums',   label: t('cosmic_hub.tab_serums'),   icon: '🧪' },
  { id: 'bestiary', label: t('cosmic_hub.tab_bestiary'), icon: '📖' },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab gating logic в CosmicHubModal + disabled tab visual + locked tooltip i18n</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx, client/src/locales/ru/translation.json, client/src/locales/en/translation.json, client/src/locales/es/translation.json, client/src/store/gameStore.ts</files>
  <action>
### A. CosmicHubModal.tsx — extend Tab interface + gating

```tsx
import { useGameStore } from '../../store/gameStore'

interface Tab {
  id: CosmicTab
  label: string
  icon: string
  enabled: boolean
  lockReason?: string  // i18n key для tooltip
}

export default function CosmicHubModal({ onClose }: Props) {
  const { t } = useTranslation()
  const hasFirstFeed = useGameStore((s) => s.hasFirstFeed)
  const hasFirstMission = useGameStore((s) => s.hasFirstMission)
  const hasOpenedAnyBox = useGameStore((s) => s.hasOpenedAnyBox)

  const isDev = import.meta.env.DEV

  // Tab build с enabled flags (REQ UX-09)
  const TABS: Tab[] = [
    {
      id: 'scouts',
      label: t('cosmic_hub.tab_ship'),
      icon: '🚀',
      enabled: isDev || hasFirstFeed,
      lockReason: 'cosmic_hub.lock_first_feed',
    },
    {
      id: 'boxes',
      label: t('cosmic_hub.tab_boxes'),
      icon: '🎁',
      enabled: isDev || hasFirstMission,
      lockReason: 'cosmic_hub.lock_first_mission',
    },
    {
      id: 'serums',
      label: t('cosmic_hub.tab_serums'),
      icon: '🧪',
      enabled: true,  // всегда unlock'ed
    },
    {
      id: 'bestiary',
      label: t('cosmic_hub.tab_bestiary'),
      icon: '📖',
      // Bestiary tab всегда видим, но в первой инсталляции — empty state.
      // Phase 18 polish решит — в Phase 16 ставим always enabled.
      enabled: true,
    },
  ]

  // Если активный таб теперь disabled (например save corrupt) — fall back на первый enabled
  useEffect(() => {
    const active = TABS.find((t) => t.id === activeTab)
    if (active && !active.enabled) {
      const firstEnabled = TABS.find((t) => t.enabled)
      if (firstEnabled) setActiveTab(firstEnabled.id)
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [hasFirstFeed, hasFirstMission])

  // ...

  return (
    <div ...>
      {/* Header — без изменений */}
      ...

      {/* Tab strip с disabled visual */}
      <div className="flex border-b border-white/10 flex-shrink-0">
        {TABS.map((tab) => (
          <button
            key={tab.id}
            onClick={() => tab.enabled && setActiveTab(tab.id)}
            disabled={!tab.enabled}
            title={!tab.enabled && tab.lockReason ? t(tab.lockReason) : undefined}
            className={[
              'flex-1 py-2 text-xs font-medium transition-colors',
              !tab.enabled
                ? 'text-white/20 cursor-not-allowed'
                : activeTab === tab.id
                  ? 'text-white border-b-2 border-emerald-400'
                  : 'text-white/40',
            ].join(' ')}
          >
            <span className="block text-base">{tab.enabled ? tab.icon : '🔒'}</span>
            <span>{tab.label}</span>
          </button>
        ))}
      </div>

      {/* Tab content — без изменений */}
      ...
    </div>
  )
}
```

### B. i18n locked tooltip keys (RU/EN/ES)

**RU translation.json — добавить под "cosmic_hub":**
```json
{
  "cosmic_hub": {
    "lock_first_feed": "Откроется после первого кормления",
    "lock_first_mission": "Откроется после первой миссии"
  }
}
```

**EN:**
```json
{
  "cosmic_hub": {
    "lock_first_feed": "Unlocks after first feed",
    "lock_first_mission": "Unlocks after first mission"
  }
}
```

**ES:**
```json
{
  "cosmic_hub": {
    "lock_first_feed": "Se desbloquea tras la primera alimentación",
    "lock_first_mission": "Se desbloquea tras la primera misión"
  }
}
```

### C. gameStore.ts — DEV-mode default unlock

Найти init место в `client/src/store/gameStore.ts` (где создаётся cosmic slice). После init добавить:

```typescript
// Phase 16: DEV-mode unlock all progressive disclosure flags для testability.
// Production: флаги управляются нормальной gameplay (Phase 17 hasFirstFeed,
// Phase 16 hasFirstMission, Phase 17/18 hasOpenedAnyBox).
if (import.meta.env.DEV) {
  // Defer на microtask чтобы dev tools видели init
  queueMicrotask(() => {
    const cur = useGameStore.getState()
    if (!cur.hasFirstFeed || !cur.hasFirstMission || !cur.hasOpenedAnyBox) {
      cur.setHasFirstFeed(true)
      cur.setHasFirstMission(true)
      cur.setHasOpenedAnyBox(true)
    }
  })
}
```

**Note:** где именно вставлять — зависит от current gameStore.ts архитектуры (zustand `create((set, get) => ({...slice}))`). Размещать после `useGameStore` экспорта в bottom файла. Если `gameStore.ts` слишком сложный — fallback: вставить в `App.tsx` useEffect on mount.

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "CosmicHubModal\.tsx|gameStore\.ts" | head -20</automated>
  </verify>
  <done>
    CosmicHubModal имеет enabled flag per tab, disabled tab показывает 🔒 + lock tooltip, fallback на первый enabled tab если active disabled. DEV mode unlocks all flags. i18n RU/EN/ES добавлены.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dev helpers + full verification (tsc + build + bundle + smoke)</name>
  <files>client/src/App.tsx</files>
  <action>
### A. Dev helpers в App.tsx

Добавить useEffect (DEV-only) который exposes window:

```tsx
useEffect(() => {
  if (!import.meta.env.DEV) return
  const w = window as any

  // Force mission type (тестирование 3 вариантов)
  w.__forceMissionType = (type: 'rhythm' | 'defend' | 'hotspot') => {
    // Хранить в localStorage и читать в MissionOverlay через prop forceType
    localStorage.setItem('__force_mission_type', type)
    console.log('[dev] forced mission type:', type)
  }

  // Reset crew today
  w.__resetCrewToday = () => {
    useGameStore.setState((s) => ({
      crew: { ...s.crew, missionsToday: 0 },
    }))
    console.log('[dev] crew reset')
  }

  // Unlock all tabs
  w.__unlockAllTabs = () => {
    const s = useGameStore.getState()
    s.setHasFirstFeed(true)
    s.setHasFirstMission(true)
    s.setHasOpenedAnyBox(true)
    console.log('[dev] all tabs unlocked')
  }

  // Lock all tabs (test prod gating)
  w.__lockAllTabs = () => {
    const s = useGameStore.getState()
    s.setHasFirstFeed(false)
    s.setHasFirstMission(false)
    s.setHasOpenedAnyBox(false)
    console.log('[dev] all tabs locked (simulating fresh prod install)')
  }

  // Teleport ship to planet (skip transit)
  w.__shipTo = (planetId: string) => {
    useGameStore.getState().arriveShipAt(planetId)
  }

  return () => {
    delete w.__forceMissionType
    delete w.__resetCrewToday
    delete w.__unlockAllTabs
    delete w.__lockAllTabs
    delete w.__shipTo
  }
}, [])
```

### B. MissionOverlay читает forceType из localStorage (если задано)

Изменить `MissionOverlay.tsx` чтобы перед `pickRandomMissionType()` проверял localStorage:

```tsx
const missionType: MissionType = useMemo(() => {
  if (forceType) return forceType
  if (import.meta.env.DEV) {
    const forced = localStorage.getItem('__force_mission_type')
    if (forced === 'rhythm' || forced === 'defend' || forced === 'hotspot') {
      return forced
    }
  }
  return pickRandomMissionType()
}, [forceType])
```

### C. Verification script

В Plan 16-05 запустить:

```bash
cd client && npx tsc --noEmit
cd client && npm run build 2>&1 | tail -30
```

**Bundle delta budget calculation:**
- Phase 15 baseline (estimated, может быть скорректирован после Phase 15 ship): зафиксировать в `16-05-SUMMARY.md` как pre-Phase-16 KB.
- Cap: ≤ +40 KB gzip vs Phase 15 baseline.

Если bundle delta > +40 KB:
1. Проверить — не дублируется ли planetMap.json import (it's already used elsewhere — должен tree-shake).
2. ParticleEmitter / Phaser tween могут требовать чанк — verify через `vite build --mode production` chunks output.

### D. Smoke checklist (manual + console-driven)

В terminal/dev console после dev start:

1. **Initial state**: open Cosmic Hub → Корабль tab visible (DEV) → ship docked at HOME → crew 0/4.
2. **Flight**: open StarMap → tap planet BLIKS → FlightConfirmDialog показывает Travel ~Xs → Confirm → ship transit.
3. **Wait/teleport**: либо подождать transit, либо `__shipTo('bliks')` → arrival toast «Прибыли на BLIKS [Изучить]».
4. **Investigate**: open Cosmic Hub → Корабль → click [Изучить] → MissionOverlay появляется.
5. **Force mission type**: `__forceMissionType('rhythm')` → reload page (или просто next mission) → rhythm spawns.
6. **Mission complete perfect**: tap rapidly to hit target → onComplete(1.0) → result='perfect' → check `useGameStore.getState().boxes.at(-1)` → bonusRarity = 0.15.
7. **Skip path**: trigger mission → wait 1s → click Skip → toast box received with bonusRarity = 0.
8. **Crew cap**: `__resetCrewToday()` → run 4 missions → 5th: «Изучить» disabled с tooltip.
9. **Production gating**: `__lockAllTabs()` → Корабль и Боксы tabs становятся 🔒 disabled. Серум и Бестиарий enabled.
10. **Daily reset**: change system date forward by 1 day OR call `useGameStore.getState().resetCrewIfNewDay()` after manually setting `lastResetDay = '2020-01-01'` → missionsToday resets to 0.

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit && cd client && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    tsc clean. vite build success. 5 dev helpers exposed (DEV-only). Smoke checklist passed (recorded в 16-05-SUMMARY.md). Bundle delta documented.
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` → 0 errors
- `cd client && npm run build` → success, output bundle size noted
- `grep -c "lock_first_feed\|lock_first_mission" client/src/locales/ru/translation.json` → 2
- `grep -c "import.meta.env.DEV" client/src/store/gameStore.ts` → ≥1
- `grep -c "__forceMissionType\|__resetCrewToday\|__unlockAllTabs" client/src/App.tsx` → ≥3
- Manual smoke checklist (10 steps) — записать pass/fail в SUMMARY.
- Bundle delta vs Phase 15 baseline (зафиксировать KB) — должно быть ≤ +40 KB gzip.
</verification>

<success_criteria>
1. tsc clean, build success.
2. Bundle delta ≤ +40 KB gzip vs Phase 15 baseline.
3. UX-09 enforced:
   - DEV: все 4 tabs visible.
   - PROD + clean save: только Сыворотки + Бестиарий visible.
   - После first mission complete: Боксы tab unlocks.
   - Корабль tab требует hasFirstFeed (Phase 17 toggle).
4. 5 dev helpers работают.
5. Smoke checklist pass на all 10 пунктов.
6. i18n RU/EN/ES coverage 100% для Phase 16 keys.
</success_criteria>

<output>
После завершения создать `.planning/phases/16-ship-travel-mission/16-05-SUMMARY.md` с:
- Что реализовано (REQ-IDs ✓ / ◑ / ✗)
- **Bundle delta gzip с absolute KB before/after** (Phase 15 baseline → Phase 16 final)
- Atomic commits
  - `phase-16: progressive disclosure tab gating in CosmicHubModal`
  - `phase-16: DEV-mode unlock + dev helpers + i18n final round`
  - `phase-16: verify build + bundle delta + smoke checklist`
- Smoke checklist results (10 пунктов)
- Open issues (если есть): toast i18n cleanup, Phase 17 hasFirstFeed wiring (out-of-scope для Phase 16), Phase 15 cascade integration ждёт Phase 15 ship.
- **Phase 16 final REQ table:** все 27 REQ-IDs с маркером ✓ / ◑ / ✗ + ссылка на plan где реализован.

После Phase 16 — обновить `STATE.md`:
- Row 16 → status: complete (date)
- Performance Metrics row для Phase 16 (5 plans, 5 waves, total tasks, files modified, bundle delta gzip)
- Notes: «Phase 16 закрыта; v2.0 первый fully-playable milestone достигнут — ship→mission→box loop работает (Phase 15 cascade integration в next phase)»
</output>
