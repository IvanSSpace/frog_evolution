---
phase: 16-ship-travel-mission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/cosmic/types.ts
  - client/src/store/cosmic/slice.ts
  - client/src/store/eventBus.ts
  - client/src/game/data/missionConfig.ts  # NEW
autonomous: true
requirements:
  - SHIP-01
  - SHIP-03
  - SHIP-06
  - CREW-01
  - CREW-02
  - CREW-03

must_haves:
  truths:
    - "ShipState — discriminated union (docked / transit) с required полями"
    - "travelTimeMs(distance) возвращает clamp(1500..120000)"
    - "consumeMissionCredit() атомарно проверяет cap=4 + увеличивает счётчик"
    - "resetCrewIfNewDay() использует ЛОКАЛЬНУЮ дату (не UTC)"
    - "ensureShipExists() инициализирует docked у home при ship === null"
    - "sendShipTo() создаёт transit либо redirect (kill+restart на текущую позицию)"
  artifacts:
    - path: "client/src/store/cosmic/types.ts"
      provides: "ShipState discriminated union + расширение CosmicSlice"
      contains: "type ShipStateDocked"
    - path: "client/src/store/cosmic/slice.ts"
      provides: "ensureShipExists, sendShipTo, arriveShipAt, setShipPosition, getLocalDateString fix"
      contains: "ensureShipExists"
    - path: "client/src/game/data/missionConfig.ts"
      provides: "WORLD_DIAGONAL, travelTimeMs, planetDistance, DAILY_CAP, MISSION_TYPES"
      min_lines: 40
    - path: "client/src/store/eventBus.ts"
      provides: "новые события cosmic:request-flight, ship-arrived, start-mission, mission-complete"
      contains: "'cosmic:request-flight'"
  key_links:
    - from: "cosmic/slice.ts"
      to: "missionConfig.ts"
      via: "import { travelTimeMs, planetDistance, DAILY_CAP }"
      pattern: "import.*from.*missionConfig"
    - from: "cosmic/slice.ts"
      to: "planetMap.json"
      via: "lookup x/y by planetId через helper findPlanetById (импорт-once)"
      pattern: "findPlanetById|planetMap"
    - from: "cosmic/types.ts"
      to: "cosmic/slice.ts"
      via: "ShipState discriminated union shape"
      pattern: "ShipState"
---

<objective>
Заложить foundation Phase 16: типы (ShipState discriminated union + новые
sentinel флаги), travel time math util, актиоры sendShipTo/arriveShipAt/
investigatePlanet skeleton, fix CREW-03 (локальный YYYY-MM-DD вместо UTC),
ensureShipExists, расширение eventBus для cosmic:request-flight/ship-arrived
семейства событий.

Purpose: Без этого все subsequent plans (ShipSprite, ShipTab, MissionOverlay)
не имеют типов и actions для wire-up.

Output:
- `client/src/store/cosmic/types.ts` (modified) — ShipState union + sentinel флаги
- `client/src/store/cosmic/slice.ts` (modified) — новые actions + LocalDate fix
- `client/src/game/data/missionConfig.ts` (NEW) — travel formula, constants
- `client/src/store/eventBus.ts` (modified) — новые типизированные события
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ship-travel-mission/PLAN.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slice.ts
@client/src/store/eventBus.ts
@client/src/game/effects/elements/elementMapping.ts

<interfaces>
<!-- Существующие типы и контракты Phase 11/12, на которые мы навешиваемся. -->

From client/src/store/cosmic/types.ts (Phase 11):
```typescript
export type Element = 'fire' | 'ice' | ... | 'void'  // 16 entries
export type Rarity = 'common' | 'rare' | 'epic' | 'legendary'

export interface BoxData {
  id: string
  element: Element
  opened: boolean
  sourceArchetype?: string
}

export interface ShipState {
  state: 'docked' | 'transit'
  dockedAt?: string
  from?: string
  to?: string
  startedAt?: number
  arrivesAt?: number
}  // ← устаревший плейсхолдер; заменяем на discriminated union

export interface CosmicSlice {
  serums: Record<Element, Record<Rarity, number>>
  boxes: BoxData[]
  scouts: ScoutData[]
  ship: ShipState | null
  carriers: CarrierData[]
  bestiaryBitset: number[]
  pityCounters: PityCounters
  lastActiveTab: CosmicTab
  crew: { missionsToday: number; lastResetDay: string }
  serumDragActive: boolean
  selectedSerum: { element: Element; rarity: Rarity } | null
}
```

From client/src/store/cosmic/slice.ts (Phase 11):
```typescript
export interface CosmicSliceActions {
  addSerum, removeSerum, setSerumDragActive, applySerum,
  addBox, openBox,
  addScout, removeScout,
  addCarrier, removeCarrier,
  setLastActiveTab,
  consumeMissionCredit(): boolean
  resetCrewIfNewDay(): void  // ← bug: использует UTC вместо локальной даты
}
```

From client/src/game/effects/elements/elementMapping.ts (Phase 12):
```typescript
export function elementFromPlanet(
  archetype: string | undefined,
  mainRaceType: string | undefined,
): Element | null
```

From client/src/game/scenes/StarMapScene.ts (Phase 9):
```typescript
const WORLD_SIZE = 7000 * DPR  // в DPR-units
```

From client/src/store/eventBus.ts (Phase 11):
```typescript
type Events = {
  'starmap:open': void
  'starmap:close': void
  'starmap:planet-tapped': { id: string; type: string; archetype?: string; durationMs: number; seed: number }
  'cosmic:toast': CosmicToastPayload
  // ... + Phase 14 cosmic:select-serum etc.
}
```

planetMap.json structure (top-level):
```json
{
  "meta": { "seed", "bgCount": 984, "mainCount": 16, "totalCount": 1000, ... },
  "planets": [
    { "id": "home", "name": "HOME", "kind": "main", "x": 3500, "y": 3500,
      "type": "home", "color": ..., "size": 75, "distFromHome": 0 },
    ...
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать missionConfig.ts (constants + travel utils + planet lookup)</name>
  <files>client/src/game/data/missionConfig.ts</files>
  <action>
Создать NEW файл `client/src/game/data/missionConfig.ts` со следующим содержимым:

```typescript
// Phase 16: ship navigation + crew + mission constants/utils.
// Изолированы от других модулей — pure helpers, нет cyclic imports.

import planetMapJson from './planetMap.json' with { type: 'json' }

// === World geometry ===
// WORLD_SIZE согласован с client/src/game/scenes/StarMapScene.ts:38 (DPR=1).
// Travel формула использует diagonal of "extended" world (×2): worst-case полёт
// от ~south-west до ~north-east bound.
export const WORLD_SIZE = 7000  // в DPR-units (как planetMap.json уже в DPR=1)
export const WORLD_DIAGONAL = Math.SQRT2 * WORLD_SIZE * 2  // ≈ 19_798

// === Travel time formula (REQ SHIP-03) ===
export const TRAVEL_MS_FOR_DIAGONAL = 120_000  // 2 минуты для самого далёкого
export const TRAVEL_MS_MIN = 1_500             // floor для близких полётов

/** Travel time в ms для заданной distance (DPR-units). Линейная интерполяция,
 * clamped в [TRAVEL_MS_MIN, TRAVEL_MS_FOR_DIAGONAL]. */
export function travelTimeMs(distance: number): number {
  const ratio = Math.min(Math.max(distance, 0) / WORLD_DIAGONAL, 1)
  return Math.max(TRAVEL_MS_MIN, ratio * TRAVEL_MS_FOR_DIAGONAL)
}

/** Euclidean distance между двумя точками в DPR-units. */
export function planetDistance(
  a: { x: number; y: number },
  b: { x: number; y: number },
): number {
  const dx = b.x - a.x
  const dy = b.y - a.y
  return Math.sqrt(dx * dx + dy * dy)
}

// === Crew (REQ CREW-02) ===
export const DAILY_CAP = 4  // максимум миссий в день

// === Mission types (REQ MISSION-02) ===
export type MissionType = 'rhythm' | 'defend' | 'hotspot'
export const MISSION_TYPES: readonly MissionType[] = ['rhythm', 'defend', 'hotspot']

/** Random mission type (helper для MissionOverlay). Tested через __forceMissionType. */
export function pickRandomMissionType(): MissionType {
  const i = Math.floor(Math.random() * MISSION_TYPES.length)
  return MISSION_TYPES[i]
}

// === Mission scoring → bonusRarity (REQ MISSION-03) ===
export type MissionResult = 'perfect' | 'good' | 'fail'

/** Score [0..1] → result bucket. */
export function scoreToResult(score: number): MissionResult {
  if (score >= 1.0) return 'perfect'
  if (score >= 0.6) return 'good'
  return 'fail'
}

/** Result → bonusRarity multiplier (additive к base rarity roll в Phase 15). */
export function bonusRarityForResult(result: MissionResult): number {
  switch (result) {
    case 'perfect': return 0.15
    case 'good': return 0.05
    case 'fail': return 0
  }
}

// === Local date helper (FIX CREW-03) ===
/** ЛОКАЛЬНАЯ дата YYYY-MM-DD (НЕ UTC). Phase 11 использовал toISOString().slice(0,10),
 * что нарушает CREW-03 (reset по 00:00 локального часового пояса). */
export function getLocalDateString(now: Date = new Date()): string {
  const y = now.getFullYear()
  const m = String(now.getMonth() + 1).padStart(2, '0')
  const d = String(now.getDate()).padStart(2, '0')
  return `${y}-${m}-${d}`
}

/** Время до следующей локальной полночи (ms). Используется в crew tooltip
 * countdown «До утра 14:32». */
export function msUntilLocalMidnight(now: Date = new Date()): number {
  const next = new Date(now.getFullYear(), now.getMonth(), now.getDate() + 1, 0, 0, 0, 0)
  return next.getTime() - now.getTime()
}

// === Planet lookup ===
export interface PlanetInfo {
  id: string
  name: string
  kind: 'main' | 'bg'
  x: number
  y: number
  type?: string
  archetype?: string
  size?: number
  distFromHome?: number
}

// Cast JSON to typed array; planetMap.json schema контролируется генератором.
const PLANETS = (planetMapJson as { planets: PlanetInfo[] }).planets

const PLANET_INDEX: Map<string, PlanetInfo> = new Map(
  PLANETS.map((p) => [p.id, p]),
)

/** Lookup planet by id; null если не найден (на случай stale storage). */
export function findPlanetById(id: string): PlanetInfo | null {
  return PLANET_INDEX.get(id) ?? null
}

/** Resolve archetype + mainRaceType для elementFromPlanet().
 * BG planets имеют `archetype`; main race имеют `kind === 'main'` и используют `type` как mainRaceType. */
export function planetElementInputs(p: PlanetInfo): {
  archetype: string | undefined
  mainRaceType: string | undefined
} {
  if (p.kind === 'main') {
    return { archetype: undefined, mainRaceType: p.type }
  }
  return { archetype: p.archetype ?? p.type, mainRaceType: undefined }
}
```

**Notes:**
- `import planetMapJson from './planetMap.json' with { type: 'json' }` — JSON import attribute (TS 5.3+; vite поддерживает). Если `tsconfig` не разрешает — fallback на `import planetMapJson from './planetMap.json'` (vite resolves сам). Проверить tsc после реализации.
- `bgCount=984 + mainCount=16 = 1000` (из meta) → `PLANETS.length === 1000`; PLANET_INDEX one-shot init.
- НЕ используем `distFromHome` для travel — нам нужен distance от **текущей** ship позиции, поэтому считаем full euclidean.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Файл создан, экспортирует все 11 имён (WORLD_SIZE, WORLD_DIAGONAL, TRAVEL_MS_FOR_DIAGONAL, TRAVEL_MS_MIN, travelTimeMs, planetDistance, DAILY_CAP, MISSION_TYPES, scoreToResult, bonusRarityForResult, getLocalDateString, msUntilLocalMidnight, findPlanetById, planetElementInputs, plus types MissionType, MissionResult, PlanetInfo). tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Расширить cosmic/types.ts (ShipState union + sentinel flags + BoxData.bonusRarity)</name>
  <files>client/src/store/cosmic/types.ts</files>
  <action>
Модифицировать `client/src/store/cosmic/types.ts`:

1. **Заменить existing ShipState** на discriminated union:

```typescript
// ===== Phase 16: ShipState discriminated union (REQ SHIP-01) =====
export interface ShipStateDocked {
  state: 'docked'
  planetId: string  // unique idx в planetMap.json (e.g. 'home')
}

export interface ShipStateTransit {
  state: 'transit'
  fromPlanetId: string  // для UI «В пути от X к Y»
  toPlanetId: string
  startedAt: number     // unix ms (Date.now())
  arrivesAt: number     // unix ms
}

export type ShipState = ShipStateDocked | ShipStateTransit
```

(Удалить старый ShipState с optional полями `dockedAt?, from?, to?, startedAt?, arrivesAt?`.)

2. **Добавить bonusRarity** в `BoxData`:

```typescript
export interface BoxData {
  id: string
  element: Element
  opened: boolean
  sourceArchetype?: string
  // Phase 16: bonus rarity (additive к base rarity roll), 0..0.15. Optional —
  // старые saved boxes без поля рассматриваются как 0. Phase 15 cascade
  // читает это поле в slot-machine roll.
  bonusRarity?: number
}
```

3. **Добавить sentinel flags + transient cached ship pos** в `CosmicSlice`:

```typescript
export interface CosmicSlice {
  // ... existing ...

  // Phase 16: progressive disclosure flags (REQ UX-09).
  // gates Корабль tab; toggle при первом feed (Phase 17).
  hasFirstFeed: boolean
  // gates Боксы tab; toggle при первой completed mission (Phase 16).
  hasFirstMission: boolean
  // gates Бестиарий visualization; toggle при первом opened box (Phase 17/18).
  hasOpenedAnyBox: boolean

  // Phase 16: transient cached ship world position для redirect calc.
  // НЕ persisted в localStorage (init на null после load → re-derived из planetCoords).
  latestShipPos: { x: number; y: number } | null
}
```

4. **Обновить `makeInitialCosmicSlice()`** — добавить дефолты:

```typescript
return {
  // ... existing ...
  hasFirstFeed: false,
  hasFirstMission: false,
  hasOpenedAnyBox: false,
  latestShipPos: null,
}
```

5. **CosmicTab — НЕ менять** (оставить `'scouts'` как ID; rename только UI label в Plan 16-03 / 16-05). Это conscious choice для backward compat sessionStorage.

**Self-check:** все existing fields сохранены (serums, boxes, scouts, ship, carriers, bestiaryBitset, pityCounters, lastActiveTab, crew, serumDragActive, selectedSerum). Discriminated union ShipState не имеет optional dockedAt/from/to (раньше были) — это **breaking change для localStorage**. Migration handle'ится в gameStore — добавим в Task 3 (slice).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "cosmic/(types|slice)\.ts" | head -10</automated>
  </verify>
  <done>
    types.ts содержит ShipStateDocked, ShipStateTransit, ShipState union; BoxData имеет optional bonusRarity; CosmicSlice имеет hasFirstFeed/hasFirstMission/hasOpenedAnyBox/latestShipPos; makeInitialCosmicSlice возвращает дефолты для новых полей. tsc clean (errors могут быть в slice.ts — будут пофикшены в Task 3, но types сами по себе чистые).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Расширить cosmic/slice.ts (ensureShipExists, sendShipTo, arriveShipAt, setShipPosition, fix resetCrewIfNewDay) + расширить eventBus</name>
  <files>client/src/store/cosmic/slice.ts, client/src/store/eventBus.ts</files>
  <behavior>
- ensureShipExists() при ship === null → ship = { state: 'docked', planetId: 'home' }, no-op если уже set
- sendShipTo(planetId) при state === 'docked' и planetId === current → no-op (already there, see SHIP-08 — UI handles confirm skip; slice также idempotent)
- sendShipTo(planetId) при state === 'docked' к другой planet → ship = transit с fromPlanetId=docked, startedAt=now, arrivesAt=now+travelTimeMs(distance)
- sendShipTo(planetId) при state === 'transit' (REDIRECT) → distance считается от latestShipPos (если есть) либо от fromPlanetId (fallback). startedAt=now, arrivesAt=now+travelTimeMs. fromPlanetId сохраняется как был (UI отображает «От X к Z»; редирект — конца переименовывает только to).
- arriveShipAt(planetId) → ship = { state: 'docked', planetId }, eventBus.emit('cosmic:ship-arrived', { planetId })
- setShipPosition(x, y) → atomic set latestShipPos
- consumeMissionCredit() — already exists; verify cap=DAILY_CAP=4 (Phase 11 hardcoded 4)
- resetCrewIfNewDay() — FIX: использовать getLocalDateString() вместо new Date().toISOString().slice(0,10)
  </behavior>
  <action>
Модифицировать `client/src/store/cosmic/slice.ts`:

### A. Импорты сверху

```typescript
import { eventBus } from '../eventBus'
import {
  travelTimeMs, planetDistance, findPlanetById, getLocalDateString,
  DAILY_CAP,
} from '../../game/data/missionConfig'
```

### B. Расширить интерфейс actions

```typescript
export interface CosmicSliceActions {
  // ... existing actions ...

  // Phase 16: Ship navigation
  ensureShipExists(): void
  sendShipTo(toPlanetId: string): void
  arriveShipAt(planetId: string): void
  setShipPosition(x: number, y: number): void

  // Phase 16: Sentinel flags toggles (used by Phase 17/19 + dev panel)
  setHasFirstFeed(v: boolean): void
  setHasFirstMission(v: boolean): void
  setHasOpenedAnyBox(v: boolean): void
}
```

### C. Имплементация (внутри createCosmicSlice)

```typescript
ensureShipExists: () => {
  const s = get()
  if (s.ship !== null) return
  set({ ship: { state: 'docked', planetId: 'home' } })
},

sendShipTo: (toPlanetId) => {
  const s = get()
  const target = findPlanetById(toPlanetId)
  if (!target) {
    // unknown planet — ignore (corrupt state guard, T-16-02)
    return
  }
  // Если ship === null — pre-init
  if (s.ship === null) {
    set({ ship: { state: 'docked', planetId: 'home' } })
  }
  const ship = get().ship!  // non-null после ensure

  // No-op: уже docked у этой planet (UI должен предотвратить — но slice idempotent)
  if (ship.state === 'docked' && ship.planetId === toPlanetId) return

  let fromPos: { x: number; y: number } | null = null
  let fromPlanetIdForUi: string

  if (ship.state === 'docked') {
    const fromPlanet = findPlanetById(ship.planetId)
    if (!fromPlanet) return  // corrupt — bail
    fromPos = { x: fromPlanet.x, y: fromPlanet.y }
    fromPlanetIdForUi = ship.planetId
  } else {
    // REDIRECT: используем cached latestShipPos если есть, иначе fromPlanetId
    if (s.latestShipPos) {
      fromPos = s.latestShipPos
    } else {
      const fp = findPlanetById(ship.fromPlanetId)
      if (!fp) return
      fromPos = { x: fp.x, y: fp.y }
    }
    fromPlanetIdForUi = ship.fromPlanetId  // сохраняем UI «откуда летели изначально»
  }

  const dist = planetDistance(fromPos, { x: target.x, y: target.y })
  const dur = travelTimeMs(dist)
  const now = Date.now()
  set({
    ship: {
      state: 'transit',
      fromPlanetId: fromPlanetIdForUi,
      toPlanetId,
      startedAt: now,
      arrivesAt: now + dur,
    },
  })
},

arriveShipAt: (planetId) => {
  const target = findPlanetById(planetId)
  if (!target) return  // защита от mismatch
  set({ ship: { state: 'docked', planetId } })
  // Notify subscribers (ShipTab, MissionOverlay enabler).
  eventBus.emit('cosmic:ship-arrived', { planetId })
},

setShipPosition: (x, y) => {
  set({ latestShipPos: { x, y } })
},

setHasFirstFeed: (v) => set({ hasFirstFeed: v }),
setHasFirstMission: (v) => set({ hasFirstMission: v }),
setHasOpenedAnyBox: (v) => set({ hasOpenedAnyBox: v }),
```

### D. Fix existing actions

**consumeMissionCredit** — заменить hardcoded 4 на `DAILY_CAP`:
```typescript
consumeMissionCredit: () => {
  const state = get()
  if (state.crew.missionsToday >= DAILY_CAP) return false
  set({ crew: { ...state.crew, missionsToday: state.crew.missionsToday + 1 } })
  return true
},
```

**resetCrewIfNewDay** — заменить UTC на локальную дату:
```typescript
resetCrewIfNewDay: () => {
  const today = getLocalDateString()
  const state = get()
  if (state.crew.lastResetDay !== today) {
    set({ crew: { missionsToday: 0, lastResetDay: today } })
  }
},
```

### E. Обновить eventBus.ts

В `client/src/store/eventBus.ts` extend `Events` type:

```typescript
type Events = {
  // ... existing ...

  // Phase 16: ship + mission events
  'cosmic:request-flight': { planetId: string }
    // emitted from StarMapScene tap when Cosmic Hub modal open OR ship-mode active
  'cosmic:ship-arrived': { planetId: string }
    // emitted from arriveShipAt(); subscribed by App-level toast manager + ShipTab
  'cosmic:start-mission': { planetId: string }
    // emitted by ShipTab «Изучить» button → triggers MissionOverlay show
  'cosmic:mission-complete': { planetId: string; result: 'perfect' | 'good' | 'fail' }
    // emitted by MissionOverlay onComplete → triggers investigatePlanet (Plan 16-04)
  'cosmic:mission-cancel': void
    // emitted by MissionOverlay close без отдачи result (e.g. user pressed back)
}
```

### F. Storage migration (best-effort safety net)

В create slice сразу после `const initial = makeInitialCosmicSlice()`:
- НЕ trogаем здесь миграции (gameStore делает heavy lifting; см. STORAGE_VERSION bump). Вместо этого slice **defensive guard**: если на rehydrate пришёл старый ShipState shape (с `dockedAt`/`from`/`to`), оборачивающий gameStore migration сбросит `ship: null`. В рамках Task 3 — просто доверяем new shape. (Bump STORAGE_VERSION + migration scaffold можно добавить inline в slice creator, но Phase 11 уже делал это — re-use существующего паттерна.)

### G. Тесты-смок

Phase 16 — без unit-тестов в slice (проект не имеет vitest setup для slice; используем typecheck + manual smoke). Verify через tsc + ad-hoc smoke в Task 1 of Plan 16-02.

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    slice.ts экспортирует ensureShipExists / sendShipTo / arriveShipAt / setShipPosition / setHasFirstFeed / setHasFirstMission / setHasOpenedAnyBox; resetCrewIfNewDay использует getLocalDateString; consumeMissionCredit использует DAILY_CAP константу. eventBus.ts типизирован для cosmic:request-flight | ship-arrived | start-mission | mission-complete | mission-cancel. tsc clean (0 errors).
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` → 0 errors
- `grep -c "ShipStateDocked\|ShipStateTransit" client/src/store/cosmic/types.ts` → ≥2
- `grep -c "ensureShipExists\|sendShipTo\|arriveShipAt" client/src/store/cosmic/slice.ts` → ≥3
- `grep -c "cosmic:request-flight\|cosmic:ship-arrived\|cosmic:start-mission\|cosmic:mission-complete" client/src/store/eventBus.ts` → ≥4
- `grep -v '^//' client/src/game/data/missionConfig.ts | grep -c "export"` → ≥10 экспортов
</verification>

<success_criteria>
1. tsc clean (`cd client && npx tsc --noEmit`).
2. Все 4 файла модифицированы/созданы (включая NEW missionConfig.ts).
3. ShipState — discriminated union; sendShipTo идемпотентна для same-planet docked.
4. resetCrewIfNewDay использует локальную дату (`getLocalDateString`).
5. consumeMissionCredit использует `DAILY_CAP` константу.
6. travelTimeMs(0) === TRAVEL_MS_MIN; travelTimeMs(WORLD_DIAGONAL) === TRAVEL_MS_FOR_DIAGONAL.
7. findPlanetById('home') возвращает PlanetInfo (не null).
8. eventBus.emit('cosmic:ship-arrived', { planetId: 'home' }) typecheck'ит.
</success_criteria>

<output>
После завершения создать `.planning/phases/16-ship-travel-mission/16-01-SUMMARY.md` с:
- Что реализовано (REQ-IDs ✓ / ◑ / ✗)
- Bundle delta gzip (cumulative от Phase 15 baseline)
- Atomic commits
  - `phase-16: add missionConfig (travel formula + DAILY_CAP + planet lookup)`
  - `phase-16: ShipState discriminated union + sentinel flags in cosmic types`
  - `phase-16: ship navigation actions + crew local-date fix in cosmic slice`
- Open issues: миграция localStorage старого ShipState shape — обработать в gameStore migration table перед testing Plan 16-02 (note для следующего plan'а)
</output>
