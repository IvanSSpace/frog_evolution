---
phase: 16-ship-travel-mission
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - client/src/components/CosmicHub/ShipTab.tsx  # NEW (renamed from ScoutsTab)
  - client/src/components/CosmicHub/CosmicHubModal.tsx
  - client/src/components/CosmicHub/FlightConfirmDialog.tsx  # NEW
  - client/src/components/CosmicHub/CrewIndicator.tsx  # NEW
  - client/src/App.tsx
  - client/src/locales/ru/translation.json
  - client/src/locales/en/translation.json
  - client/src/locales/es/translation.json
autonomous: true
requirements:
  - SHIP-07
  - SHIP-08
  - SHIP-09
  - SHIP-10
  - CREW-05
  - CREW-06
  - CREW-07

must_haves:
  truths:
    - "Cosmic Hub имеет таб «Корабль» (label из i18n) — старый ScoutsTab переименован/заменён"
    - "ShipTab показывает состояние: docked NAME / transit к NAME с countdown"
    - "Crew indicator показывает «N/4 миссий ⏱ до утра HH:MM»"
    - "Tap planet на StarMap (когда Cosmic Hub открыт) → FlightConfirmDialog с travel time"
    - "Confirm → useGameStore.sendShipTo(planetId); cancel → no-op"
    - "Если ship уже docked у этой planet — confirm пропускается (SHIP-08)"
    - "Crew indicator tap → tooltip с объяснением (CREW-07)"
    - "Кнопка «Изучить» enabled только при docked + crew credits > 0"
    - "Кнопка «Изучить» disabled с tooltip когда missionsToday >= cap"
    - "i18n RU/EN/ES для всех ship/mission/crew strings"
  artifacts:
    - path: "client/src/components/CosmicHub/ShipTab.tsx"
      provides: "Tab контент: state pill + crew indicator + кнопки [Открыть карту] [Изучить]"
      min_lines: 100
    - path: "client/src/components/CosmicHub/FlightConfirmDialog.tsx"
      provides: "Dialog «Лететь на NAME (~Xs)?» — confirm/cancel"
      min_lines: 60
    - path: "client/src/components/CosmicHub/CrewIndicator.tsx"
      provides: "Card «N/4 миссий ⏱ до утра HH:MM» + tooltip on tap"
      min_lines: 60
    - path: "client/src/components/CosmicHub/CosmicHubModal.tsx"
      provides: "Импорт ShipTab вместо ScoutsTab; tab label key 'cosmic_hub.tab_ship'"
      contains: "ShipTab"
    - path: "client/src/App.tsx"
      provides: "Subscriber на 'cosmic:request-flight' → state для FlightConfirmDialog rendering"
      contains: "cosmic:request-flight"
  key_links:
    - from: "ShipTab.tsx"
      to: "useGameStore (zustand selector)"
      via: "const ship = useGameStore(s => s.ship); const crew = useGameStore(s => s.crew)"
      pattern: "useGameStore\\(.*ship|useGameStore\\(.*crew"
    - from: "App.tsx FlightConfirmDialog"
      to: "useGameStore.getState().sendShipTo"
      via: "onConfirm callback"
      pattern: "sendShipTo"
    - from: "FlightConfirmDialog.tsx"
      to: "missionConfig.travelTimeMs"
      via: "import { travelTimeMs, planetDistance, findPlanetById } для preview"
      pattern: "travelTimeMs|planetDistance"
---

<objective>
Реализовать React UI для Phase 16: ShipTab (renamed from ScoutsTab),
FlightConfirmDialog (модалка подтверждения полёта), CrewIndicator (счётчик
миссий с countdown), wire-up через App.tsx subscriber на 'cosmic:request-flight'.

Purpose: Без UI игрок не может управлять кораблём. Этот plan — основной UX-слой
Phase 16 (state visualization + interaction surface).

Output:
- 3 новых React components + 1 rename + CosmicHubModal mod + App.tsx subscriber
- i18n keys RU/EN/ES (round 1; финал в 16-05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ship-travel-mission/PLAN.md
@.planning/phases/16-ship-travel-mission/16-01-SUMMARY.md
@.planning/phases/16-ship-travel-mission/16-02-SUMMARY.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slice.ts
@client/src/game/data/missionConfig.ts
@client/src/components/CosmicHub/CosmicHubModal.tsx
@client/src/components/CosmicHub/ScoutsTab.tsx
@client/src/components/CosmicHub/SerumsTab.tsx
@client/src/locales/ru/translation.json
@client/src/locales/en/translation.json
@client/src/locales/es/translation.json

<interfaces>
<!-- Контракты после 16-01/16-02. -->

From cosmic/types.ts:
```typescript
export type ShipState = ShipStateDocked | ShipStateTransit
export interface CosmicSlice {
  ship: ShipState | null
  crew: { missionsToday: number; lastResetDay: string }
  // ...
}
```

From cosmic/slice.ts (Plan 16-01):
```typescript
ensureShipExists(): void
sendShipTo(toPlanetId: string): void
arriveShipAt(planetId: string): void
consumeMissionCredit(): boolean
resetCrewIfNewDay(): void
```

From missionConfig.ts (Plan 16-01):
```typescript
travelTimeMs(distance: number): number
planetDistance(a, b): number
findPlanetById(id: string): PlanetInfo | null
DAILY_CAP = 4
msUntilLocalMidnight(): number
```

From eventBus.ts (Plan 16-01):
```typescript
'cosmic:request-flight': { planetId: string }
'cosmic:ship-arrived': { planetId: string }
'cosmic:start-mission': { planetId: string }
```

From CosmicHubModal.tsx (Phase 11):
```typescript
interface Tab { id: CosmicTab; label: string; icon: string }
const TABS: Tab[] = [
  { id: 'scouts', label: t('cosmic_hub.tab_scouts'), icon: '🚀' },
  { id: 'boxes', label: t('cosmic_hub.tab_boxes'), icon: '🎁' },
  { id: 'serums', label: t('cosmic_hub.tab_serums'), icon: '🧪' },
  { id: 'bestiary', label: t('cosmic_hub.tab_bestiary'), icon: '📖' },
]
function renderTab() { case 'scouts': return <ScoutsTab /> ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShipTab.tsx (renamed) + CosmicHubModal.tsx import update + i18n round 1</name>
  <files>client/src/components/CosmicHub/ShipTab.tsx, client/src/components/CosmicHub/ScoutsTab.tsx, client/src/components/CosmicHub/CosmicHubModal.tsx, client/src/locales/ru/translation.json, client/src/locales/en/translation.json, client/src/locales/es/translation.json</files>
  <action>
### A. Создать `client/src/components/CosmicHub/ShipTab.tsx` (NEW)

```tsx
// Phase 16: Tab «Корабль» в Cosmic Hub.
// Заменяет ScoutsTab из Phase 11 (placeholder). Tab id остаётся 'scouts' в
// CosmicTab union для backward compat sessionStorage (Phase 19 polish может
// сменить ID при необходимости миграции).

import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { useGameStore } from '../../store/gameStore'
import { eventBus } from '../../store/eventBus'
import {
  findPlanetById, DAILY_CAP, msUntilLocalMidnight,
} from '../../game/data/missionConfig'
import { CrewIndicator } from './CrewIndicator'

interface Props {
  onClose: () => void  // close Cosmic Hub modal (для «Открыть карту»)
}

export function ShipTab({ onClose }: Props) {
  const { t } = useTranslation()
  const ship = useGameStore((s) => s.ship)
  const crew = useGameStore((s) => s.crew)
  const ensureShipExists = useGameStore((s) => s.ensureShipExists)
  const resetCrewIfNewDay = useGameStore((s) => s.resetCrewIfNewDay)

  // Live tick для transit countdown (1s).
  const [, setTick] = useState(0)
  useEffect(() => {
    const id = setInterval(() => setTick((n) => n + 1), 1000)
    return () => clearInterval(id)
  }, [])

  // На mount: ensure ship exists + reset crew если новый день.
  useEffect(() => {
    ensureShipExists()
    resetCrewIfNewDay()
  }, [ensureShipExists, resetCrewIfNewDay])

  const handleOpenMap = () => {
    eventBus.emit('starmap:open')
    onClose()
  }

  const handleInvestigate = () => {
    if (!ship || ship.state !== 'docked') return
    if (crew.missionsToday >= DAILY_CAP) return  // disabled state
    eventBus.emit('cosmic:start-mission', { planetId: ship.planetId })
    onClose()  // overlay покажется на основном game canvas
  }

  // ========== Render branches ==========

  if (!ship) {
    // Empty state (никогда не должно случиться после ensureShipExists, но defensive)
    return (
      <div className="flex flex-col items-center justify-center h-full gap-3 text-white/60 p-4">
        <div className="text-4xl">🚀</div>
        <p className="text-sm">{t('ship.empty_state')}</p>
      </div>
    )
  }

  // Resolve names + countdown
  let stateLabel = ''
  let stateDetail: React.ReactNode = null
  let canInvestigate = false

  if (ship.state === 'docked') {
    const planet = findPlanetById(ship.planetId)
    const name = planet?.name ?? ship.planetId.toUpperCase()
    stateLabel = t('ship.state_docked', { name })
    canInvestigate = crew.missionsToday < DAILY_CAP
  } else {
    const target = findPlanetById(ship.toPlanetId)
    const name = target?.name ?? ship.toPlanetId.toUpperCase()
    const remainingMs = Math.max(0, ship.arrivesAt - Date.now())
    const mins = Math.floor(remainingMs / 60_000)
    const secs = Math.floor((remainingMs % 60_000) / 1000)
    stateLabel = t('ship.state_transit', { name })
    stateDetail = (
      <div className="text-sm text-white/70 mt-1">
        {t('ship.transit_eta', { mins, secs: String(secs).padStart(2, '0') })}
      </div>
    )
    canInvestigate = false  // нельзя изучать пока летим
  }

  return (
    <div className="flex flex-col h-full gap-4 p-4 text-white">
      {/* State pill */}
      <div className="bg-white/5 rounded-lg p-3 border border-white/10">
        <div className="text-xs text-white/40 uppercase tracking-wide">
          {t('ship.section_state')}
        </div>
        <div className="text-base font-medium">{stateLabel}</div>
        {stateDetail}
      </div>

      {/* Crew indicator (REQ CREW-06/07) */}
      <CrewIndicator
        used={crew.missionsToday}
        cap={DAILY_CAP}
        msUntilReset={msUntilLocalMidnight()}
      />

      {/* Action buttons */}
      <div className="flex flex-col gap-2">
        <button
          onClick={handleOpenMap}
          className="py-2 px-4 bg-emerald-600 hover:bg-emerald-700 active:bg-emerald-800 rounded-md text-sm font-medium"
        >
          {t('ship.open_map')}
        </button>

        <button
          onClick={handleInvestigate}
          disabled={!canInvestigate}
          title={
            !canInvestigate && ship.state === 'docked'
              ? t('ship.investigate_tooltip_capped')
              : !canInvestigate && ship.state === 'transit'
                ? t('ship.investigate_tooltip_transit')
                : undefined
          }
          className={[
            'py-2 px-4 rounded-md text-sm font-medium',
            canInvestigate
              ? 'bg-amber-500 hover:bg-amber-600 active:bg-amber-700 text-gray-900'
              : 'bg-gray-700 text-gray-400 cursor-not-allowed',
          ].join(' ')}
        >
          {t('ship.investigate')}
        </button>
      </div>
    </div>
  )
}
```

### B. Удалить старый `ScoutsTab.tsx` ИЛИ оставить как deprecated re-export

**Решение:** удалить `client/src/components/CosmicHub/ScoutsTab.tsx`, добавить `ShipTab.tsx`. Никто кроме CosmicHubModal не импортирует ScoutsTab (verify: `grep -rn "ScoutsTab\|from.*ScoutsTab" client/src/`).

Если grep показывает ещё ссылки в App.tsx или dev panel — заменить на ShipTab.

### C. Modify `CosmicHubModal.tsx`

```diff
- import { ScoutsTab } from './ScoutsTab'
+ import { ShipTab } from './ShipTab'

  const TABS: Tab[] = [
-    { id: 'scouts',   label: t('cosmic_hub.tab_scouts'),   icon: '🚀' },
+    { id: 'scouts',   label: t('cosmic_hub.tab_ship'),     icon: '🚀' },
     ...
  ]

  const renderTab = () => {
    switch (activeTab) {
-     case 'scouts':   return <ScoutsTab />
+     case 'scouts':   return <ShipTab onClose={onClose} />
      ...
    }
  }
```

(Тab `id` остаётся `'scouts'` — meta поле, не пользовательский label. UI label = `tab_ship`.)

### D. i18n round 1 — добавить новые ключи

В `client/src/locales/ru/translation.json` (а также en/es) — добавить под "cosmic_hub" + новый top-level "ship":

**RU (translation.json):**
```json
{
  "cosmic_hub": {
    "tab_ship": "Корабль",
    "...": "..."
  },
  "ship": {
    "empty_state": "Корабль пока не построен.",
    "section_state": "Состояние",
    "state_docked": "Пристыкован у {{name}}",
    "state_transit": "В пути → {{name}}",
    "transit_eta": "Прибытие через {{mins}}:{{secs}}",
    "open_map": "Открыть карту",
    "investigate": "Изучить",
    "investigate_tooltip_capped": "Экипаж устал. Возвращайтесь завтра.",
    "investigate_tooltip_transit": "Сначала прилетите на планету.",
    "arrived_toast": "Прибыли на {{name}}"
  },
  "crew": {
    "indicator_label": "Сегодня: {{used}}/{{cap}} миссий",
    "until_reset": "До утра {{hh}}:{{mm}}",
    "tap_for_info": "Нажмите для деталей",
    "explanation": "Экипаж выполняет до {{cap}} миссий в день. Сброс в полночь по локальному времени."
  },
  "flight_confirm": {
    "title": "Лететь на {{name}}?",
    "duration": "Время в пути: ~{{secs}}с",
    "current_planet": "Сейчас: {{name}}",
    "in_transit": "Вы редиректнёте курс с {{from}} на {{to}}",
    "confirm": "Лететь",
    "cancel": "Отмена"
  }
}
```

**EN (translation.json):**
```json
{
  "cosmic_hub": { "tab_ship": "Ship" },
  "ship": {
    "empty_state": "Ship not built yet.",
    "section_state": "Status",
    "state_docked": "Docked at {{name}}",
    "state_transit": "En route → {{name}}",
    "transit_eta": "Arrival in {{mins}}:{{secs}}",
    "open_map": "Open map",
    "investigate": "Investigate",
    "investigate_tooltip_capped": "Crew is tired. Come back tomorrow.",
    "investigate_tooltip_transit": "Arrive at the planet first.",
    "arrived_toast": "Arrived at {{name}}"
  },
  "crew": {
    "indicator_label": "Today: {{used}}/{{cap}} missions",
    "until_reset": "Until {{hh}}:{{mm}}",
    "tap_for_info": "Tap for details",
    "explanation": "Crew runs up to {{cap}} missions per day. Reset at local midnight."
  },
  "flight_confirm": {
    "title": "Fly to {{name}}?",
    "duration": "Travel time: ~{{secs}}s",
    "current_planet": "Currently: {{name}}",
    "in_transit": "You will redirect course from {{from}} to {{to}}",
    "confirm": "Fly",
    "cancel": "Cancel"
  }
}
```

**ES (translation.json):**
```json
{
  "cosmic_hub": { "tab_ship": "Nave" },
  "ship": {
    "empty_state": "Nave aún no construida.",
    "section_state": "Estado",
    "state_docked": "Atracada en {{name}}",
    "state_transit": "En ruta → {{name}}",
    "transit_eta": "Llegada en {{mins}}:{{secs}}",
    "open_map": "Abrir mapa",
    "investigate": "Investigar",
    "investigate_tooltip_capped": "Tripulación cansada. Vuelve mañana.",
    "investigate_tooltip_transit": "Llega al planeta primero.",
    "arrived_toast": "Llegada a {{name}}"
  },
  "crew": {
    "indicator_label": "Hoy: {{used}}/{{cap}} misiones",
    "until_reset": "Hasta {{hh}}:{{mm}}",
    "tap_for_info": "Toca para detalles",
    "explanation": "La tripulación realiza hasta {{cap}} misiones por día. Reinicio a medianoche local."
  },
  "flight_confirm": {
    "title": "¿Volar a {{name}}?",
    "duration": "Duración: ~{{secs}}s",
    "current_planet": "Actualmente: {{name}}",
    "in_transit": "Redirigirás de {{from}} a {{to}}",
    "confirm": "Volar",
    "cancel": "Cancelar"
  }
}
```

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "ShipTab\.tsx|CosmicHubModal\.tsx" | head -10</automated>
  </verify>
  <done>
    ShipTab.tsx создан, ScoutsTab.tsx удалён (или conviвирован в re-export), CosmicHubModal импортирует ShipTab, i18n RU/EN/ES имеют ключи cosmic_hub.tab_ship, ship.*, crew.*, flight_confirm.*. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: CrewIndicator.tsx + FlightConfirmDialog.tsx (NEW)</name>
  <files>client/src/components/CosmicHub/CrewIndicator.tsx, client/src/components/CosmicHub/FlightConfirmDialog.tsx</files>
  <action>
### A. `client/src/components/CosmicHub/CrewIndicator.tsx` (NEW)

```tsx
// Phase 16: Crew indicator card (REQ CREW-06, CREW-07).
// Показывает «Сегодня: N/4 миссий ⏱ до утра HH:MM».
// Tap → expand info tooltip с объяснением системы.

import { useState } from 'react'
import { useTranslation } from 'react-i18next'

interface Props {
  used: number
  cap: number
  msUntilReset: number
}

export function CrewIndicator({ used, cap, msUntilReset }: Props) {
  const { t } = useTranslation()
  const [expanded, setExpanded] = useState(false)

  // Compute HH:MM представление до reset.
  // msUntilReset → конкретное время дня (т.е. формула: nextMidnight - now → когда наступит).
  // Для простоты UI: показываем «через H:MM» (relative). Если хотим absolute «до 14:32» —
  // это локальное время полуночи, что всегда 00:00 → нерелевантно. Использовать relative.
  const totalMin = Math.floor(msUntilReset / 60_000)
  const hh = Math.floor(totalMin / 60)
  const mm = totalMin % 60

  return (
    <button
      onClick={() => setExpanded((v) => !v)}
      className="text-left bg-white/5 rounded-lg p-3 border border-white/10 hover:bg-white/10 transition-colors"
      aria-label={t('crew.tap_for_info')}
    >
      <div className="flex items-center justify-between">
        <span className="text-sm font-medium text-white">
          {t('crew.indicator_label', { used, cap })}
        </span>
        <span className="text-xs text-white/60">
          ⏱ {t('crew.until_reset', { hh: String(hh).padStart(2, '0'), mm: String(mm).padStart(2, '0') })}
        </span>
      </div>

      {/* Progress bar */}
      <div className="mt-2 h-1.5 bg-white/10 rounded-full overflow-hidden">
        <div
          className={used >= cap ? 'bg-red-400' : 'bg-emerald-400'}
          style={{ width: `${(used / cap) * 100}%`, height: '100%' }}
        />
      </div>

      {/* Expanded explanation (REQ CREW-07) */}
      {expanded && (
        <div className="mt-3 pt-3 border-t border-white/10 text-xs text-white/70 leading-relaxed">
          {t('crew.explanation', { cap })}
        </div>
      )}
    </button>
  )
}
```

### B. `client/src/components/CosmicHub/FlightConfirmDialog.tsx` (NEW)

```tsx
// Phase 16: FlightConfirmDialog (REQ SHIP-07).
// Показывается когда юзер тапает по planet на StarMap при открытом Cosmic Hub.
// Если ship уже docked у этой planet — caller не показывает dialog (SHIP-08).

import { useTranslation } from 'react-i18next'
import {
  findPlanetById, planetDistance, travelTimeMs,
} from '../../game/data/missionConfig'
import { useGameStore } from '../../store/gameStore'

interface Props {
  toPlanetId: string
  onConfirm: () => void
  onCancel: () => void
}

export function FlightConfirmDialog({ toPlanetId, onConfirm, onCancel }: Props) {
  const { t } = useTranslation()
  const ship = useGameStore((s) => s.ship)
  const latestPos = useGameStore((s) => s.latestShipPos)

  const target = findPlanetById(toPlanetId)
  if (!target) {
    // unknown planet — auto-cancel
    onCancel()
    return null
  }

  // Compute travel time preview based on ship's current state.
  let fromPos: { x: number; y: number } | null = null
  let fromName = '—'
  let inTransit = false

  if (!ship || ship.state === 'docked') {
    const fromPlanet = ship && ship.state === 'docked' ? findPlanetById(ship.planetId) : null
    if (fromPlanet) {
      fromPos = { x: fromPlanet.x, y: fromPlanet.y }
      fromName = fromPlanet.name
    }
  } else {
    inTransit = true
    fromPos = latestPos ?? null
    if (!fromPos) {
      const fp = findPlanetById(ship.fromPlanetId)
      if (fp) fromPos = { x: fp.x, y: fp.y }
    }
    fromName = findPlanetById(ship.fromPlanetId)?.name ?? ship.fromPlanetId
  }

  const dist = fromPos ? planetDistance(fromPos, { x: target.x, y: target.y }) : 0
  const durSecs = Math.round(travelTimeMs(dist) / 1000)

  return (
    <div
      className="fixed inset-0 z-[60] flex items-center justify-center bg-black/70"
      onClick={(e) => { if (e.target === e.currentTarget) onCancel() }}
    >
      <div className="bg-gray-900 border border-white/10 rounded-lg p-5 w-[90%] max-w-sm text-white">
        <h3 className="text-lg font-bold mb-2">
          {t('flight_confirm.title', { name: target.name })}
        </h3>
        <div className="text-sm text-white/70 mb-1">
          {t('flight_confirm.duration', { secs: durSecs })}
        </div>
        {inTransit ? (
          <div className="text-xs text-amber-300 mt-2">
            {t('flight_confirm.in_transit', { from: fromName, to: target.name })}
          </div>
        ) : (
          <div className="text-xs text-white/50 mt-2">
            {t('flight_confirm.current_planet', { name: fromName })}
          </div>
        )}
        <div className="flex gap-2 mt-4">
          <button
            onClick={onCancel}
            className="flex-1 py-2 bg-gray-700 hover:bg-gray-600 rounded text-sm"
          >
            {t('flight_confirm.cancel')}
          </button>
          <button
            onClick={onConfirm}
            className="flex-1 py-2 bg-emerald-600 hover:bg-emerald-700 rounded text-sm font-medium"
          >
            {t('flight_confirm.confirm')}
          </button>
        </div>
      </div>
    </div>
  )
}
```

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "CrewIndicator\.tsx|FlightConfirmDialog\.tsx" | head -20</automated>
  </verify>
  <done>
    Оба компонента созданы, типизированы, импортируют только из приватных + i18n. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: App.tsx subscriber на 'cosmic:request-flight' → render FlightConfirmDialog</name>
  <files>client/src/App.tsx</files>
  <action>
### A. State + subscriber

В `App.tsx` (внутри основного functional компонента):

```tsx
import { FlightConfirmDialog } from './components/CosmicHub/FlightConfirmDialog'
// (если еще не) import { useGameStore } from './store/gameStore'

// Внутри компонента:
const [pendingFlightPlanetId, setPendingFlightPlanetId] = useState<string | null>(null)
const [cosmicHubOpen, setCosmicHubOpen] = useState(false)  // НЕ менять: re-use existing state если есть

useEffect(() => {
  const handleRequestFlight = ({ planetId }: { planetId: string }) => {
    // Только если Cosmic Hub открыт ИЛИ ship-mode active.
    // Phase 16 решение: только когда modal открыт. Future: ship-mode флаг.
    if (!cosmicHubOpen) return

    const ship = useGameStore.getState().ship
    // SHIP-08: если уже docked у этой planet — пропускаем confirm
    if (ship && ship.state === 'docked' && ship.planetId === planetId) return

    setPendingFlightPlanetId(planetId)
  }
  eventBus.on('cosmic:request-flight', handleRequestFlight)
  return () => { eventBus.off('cosmic:request-flight', handleRequestFlight) }
}, [cosmicHubOpen])
```

### B. Render FlightConfirmDialog

Найти место где рендерится CosmicHubModal (через React.lazy + Suspense). Рядом или после — добавить:

```tsx
{pendingFlightPlanetId && (
  <FlightConfirmDialog
    toPlanetId={pendingFlightPlanetId}
    onConfirm={() => {
      useGameStore.getState().sendShipTo(pendingFlightPlanetId)
      setPendingFlightPlanetId(null)
    }}
    onCancel={() => setPendingFlightPlanetId(null)}
  />
)}
```

### C. **Existing cosmicHubOpen state**

Если в App.tsx уже есть state управления Cosmic Hub modal — re-use. Phase 11 commit добавил state setter. Если name отличается (например `hubOpen`) — использовать тот же.

Если у App.tsx нет state для open/close (например modal открывается через прямой setState внутри BottomBar) — добавить top-level `useState<boolean>(false)` вместе с handler `eventBus.on('starmap:open', () => setCosmicHubOpen(false))` чтобы при открытии StarMap закрывать Hub.

  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "App\.tsx" | head -10</automated>
  </verify>
  <done>
    App.tsx: subscriber на 'cosmic:request-flight' с гардами cosmicHubOpen + same-planet skip; render FlightConfirmDialog когда pendingFlightPlanetId set; onConfirm вызывает sendShipTo. tsc clean.
  </done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` → 0 errors
- `grep -c "ShipTab" client/src/components/CosmicHub/CosmicHubModal.tsx` → ≥1
- `! test -f client/src/components/CosmicHub/ScoutsTab.tsx` (file removed)
- `grep -c "tab_ship" client/src/locales/ru/translation.json` → ≥1
- `grep -c "tab_ship" client/src/locales/en/translation.json` → ≥1
- `grep -c "tab_ship" client/src/locales/es/translation.json` → ≥1
- `grep -c "FlightConfirmDialog" client/src/App.tsx` → ≥2 (import + usage)
- Manual: open Cosmic Hub → Tab «Корабль» → state pill показывает «Пристыкован у HOME».
- Manual: tap planet on StarMap (с Hub открытым) → FlightConfirmDialog появляется. Confirm → ship transit start.
</verification>

<success_criteria>
1. tsc clean.
2. ShipTab показывает state, crew indicator, кнопки.
3. Crew indicator tap раскрывает explanation.
4. FlightConfirmDialog показывает travel time preview, confirm/cancel работают.
5. SHIP-08 enforced: тап на already-docked planet не показывает dialog.
6. i18n RU/EN/ES round 1 покрывает все новые ключи.
</success_criteria>

<output>
После завершения создать `.planning/phases/16-ship-travel-mission/16-03-SUMMARY.md` с:
- Что реализовано (REQ-IDs ✓ / ◑ / ✗)
- Bundle delta gzip (cumulative)
- Atomic commits
  - `phase-16: rename ScoutsTab → ShipTab + state pill + i18n round 1`
  - `phase-16: add CrewIndicator + FlightConfirmDialog components`
  - `phase-16: wire cosmic:request-flight in App.tsx → confirm dialog`
- Open issues: cosmicHubOpen state может потребовать рефакторинг в App.tsx (если Phase 11 не вынес как top-level state). [Изучить] кнопка пока emit'ит cosmic:start-mission — overlay handler в Plan 16-04.
</output>
