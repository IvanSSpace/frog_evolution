---
phase: 22-carrier-merge-redesign
plan: 07
type: execute
wave: 6
depends_on: ["22-06"]
files_modified:
  - client/src/store/persistence.ts
  - client/src/store/migrations/phase22.ts
  - client/src/store/migrations/phase22.test.ts
  - client/src/store/gameStore.ts
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
  - .planning/phases/22-carrier-merge-redesign/SMOKE_TEST_22.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [PHASE22-MIGRATION, PHASE22-SMOKE]
estimated_hours: 2

must_haves:
  truths:
    - "Existing saves с carrier'ами имеющими rarity/stabilized/feedCount поля → strip these fields on load"
    - "Existing saves где discovered[19] true но hasCosmosUnlocked отсутствует → выставить true"
    - "Existing carriers с level undefined → default level = 1 (safe assumption)"
    - "Existing serums shape Record<Element, Record<Rarity, number>> → flatten в Record<Element, number> через sum"
    - "Migration applied только один раз (idempotent через version stamp)"
    - "Smoke test проигрывает полный flow от new game до cosmic shop purchase"
    - "ROADMAP.md обновлён: Phase 22 status = complete, plans listed"
    - "STATE.md обновлён: current_phase = 22, phase 22 metrics"
  artifacts:
    - path: "client/src/store/migrations/phase22.ts"
      provides: "Чистая функция migrate(legacyState) → newState"
      exports: ["migratePhase22"]
    - path: "client/src/store/migrations/phase22.test.ts"
      provides: "Unit-тесты на каждый класс legacy shape"
    - path: ".planning/phases/22-carrier-merge-redesign/SMOKE_TEST_22.md"
      provides: "Manual smoke checklist для всего phase"
  key_links:
    - from: "client/src/store/persistence.ts loadCosmicState"
      to: "migratePhase22(parsedJson)"
      via: "Called BEFORE дешифровка в strongly-typed CosmicSlice"
      pattern: "migratePhase22"
---

<objective>
Финальный plan: migration legacy state + i18n consistency + smoke checklist + ROADMAP/STATE
updates.

Migration критична: existing playtest саве могут иметь carrier'ов с rarity/stabilized полями.
Без migration на load возникнут TypeScript runtime mismatches (undefined .level, лишние поля
не вредят но создают confusion).

Purpose: чистый exit phase — все legacy shapes конвертированы, smoke-документация готова
для финальной приёмки, planning state-files отражают завершение фазы.

Output: migration функция + тесты, smoke checklist, ROADMAP + STATE updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-carrier-merge-redesign/22-CONTEXT.md
@.planning/phases/22-carrier-merge-redesign/22-01-SUMMARY.md
@.planning/phases/22-carrier-merge-redesign/22-06-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@client/src/store/persistence.ts
</context>

<interfaces>
Legacy shape (pre Phase 22):
```typescript
interface LegacyCarrier {
  frogId: string
  element: Element
  rarity: 'common' | 'rare' | 'epic' | 'legendary'  // REMOVED
  feedCount: number    // REMOVED
  stabilized: boolean  // REMOVED
  ceiling?: number     // REMOVED
  rollHistory?: any[]  // REMOVED
  level?: number       // KEEP, default to 1 if missing
}

interface LegacySerums {
  [element: string]: { common: number; rare: number; epic: number; legendary: number }
}
```

New shape (post Phase 22):
```typescript
interface CarrierData {
  frogId: string
  element: Element
  level: number  // 1..18
}
interface NewSerums {
  [element: string]: number  // flat count
}
```

Migration triggers:
- if `carrier.rarity` defined → drop, set level = carrier.level ?? 1
- if `serums[element]` is object → sum all rarity counts into single number
- if `state.discovered?.includes(19) === true && state.hasCosmosUnlocked === undefined` → hasCosmosUnlocked = true
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration функция + unit-тесты (TDD)</name>
  <files>
    client/src/store/migrations/phase22.ts,
    client/src/store/migrations/phase22.test.ts,
    client/src/store/persistence.ts,
    client/src/store/gameStore.ts
  </files>
  <behavior>
    Test 1 — strip rarity/feedCount/stabilized из carriers:
      Input: { carriers: [{ frogId:'f1', element:'fire', rarity:'epic', feedCount:5, stabilized:true, level:7 }] }
      Output: { carriers: [{ frogId:'f1', element:'fire', level:7 }] }

    Test 2 — carrier без level → default 1:
      Input: { carriers: [{ frogId:'f1', element:'fire', rarity:'common', feedCount:0, stabilized:false }] }
      Output: { carriers: [{ frogId:'f1', element:'fire', level:1 }] }

    Test 3 — flatten nested serums:
      Input: { serums: { fire: {common:5, rare:2, epic:1, legendary:0}, water: {common:0, rare:0, epic:0, legendary:0} } }
      Output: { serums: { fire: 8, water: 0 } } (sum across rarity tiers)

    Test 4 — серум shape уже flat → leave as-is:
      Input: { serums: { fire: 5, water: 3 } }
      Output: { serums: { fire: 5, water: 3 } }

    Test 5 — hasCosmosUnlocked inferred from legacy discovered[19]:
      Input: { discovered: [1,2,...,19], hasCosmosUnlocked: undefined }
      Output: { hasCosmosUnlocked: true }

    Test 6 — initial fresh state (no legacy fields):
      Input: { carriers: [], serums: {fire:0, ...}, hasCosmosUnlocked: false }
      Output: equal (no-op for fresh new state)

    Test 7 — initialise new Phase 22 fields if absent:
      Input: legacy state без essence, ascendedCarriers, permaSlotBonus, shopPurchaseCounts
      Output: эти поля initialised к 0 / [] / {}

    Test 8 — idempotency:
      migratePhase22(migratePhase22(state)) === migratePhase22(state) (deep equal).
  </behavior>
  <action>
    1. Создать `client/src/store/migrations/phase22.ts`:
       ```ts
       import type { CosmicSlice } from '../cosmic/types'

       /**
        * Migrate legacy localStorage cosmic state from pre-Phase-22 shape.
        * Idempotent: safe to call multiple times.
        */
       export function migratePhase22(legacy: any): any {
         if (!legacy || typeof legacy !== 'object') return legacy
         const out = { ...legacy }

         // 1. Carriers: strip rarity/feedCount/stabilized/ceiling/rollHistory
         if (Array.isArray(out.carriers)) {
           out.carriers = out.carriers.map((c: any) => ({
             frogId: c.frogId,
             element: c.element,
             level: typeof c.level === 'number' ? c.level : 1,
           }))
         }

         // 2. Serums: flatten nested rarity → single count
         if (out.serums && typeof out.serums === 'object') {
           const flat: Record<string, number> = {}
           for (const [el, val] of Object.entries(out.serums)) {
             if (typeof val === 'number') {
               flat[el] = val  // already flat
             } else if (val && typeof val === 'object') {
               flat[el] = Object.values(val).reduce<number>((a, b) => a + (typeof b === 'number' ? b : 0), 0)
             } else {
               flat[el] = 0
             }
           }
           out.serums = flat
         }

         // 3. hasCosmosUnlocked: infer from legacy discovered[19]
         if (out.hasCosmosUnlocked === undefined) {
           const discovered = out.discovered ?? out.discoveredLevels ?? []
           out.hasCosmosUnlocked = Array.isArray(discovered) && discovered.includes(19)
         }

         // 4. Initialise Phase 22 new fields if absent
         if (out.essence === undefined) out.essence = 0
         if (out.ascendedCarriers === undefined) out.ascendedCarriers = []
         if (out.permaSlotBonus === undefined) out.permaSlotBonus = 0
         if (out.permaShipSpeedBonus === undefined) out.permaShipSpeedBonus = 0
         if (out.permaSerumDropBonus === undefined) out.permaSerumDropBonus = 0
         if (out.shopPurchaseCounts === undefined) out.shopPurchaseCounts = {}

         return out
       }
       ```

    2. Создать `phase22.test.ts` с 8 тестами (Node test runner). RED → GREEN.

    3. В `persistence.ts` загрузке cosmic state:
       ```ts
       const raw = localStorage.getItem(COSMIC_KEY)
       if (!raw) return defaultCosmicSlice()
       try {
         const parsed = JSON.parse(raw)
         const migrated = migratePhase22(parsed)
         return migrated as CosmicSlice
       } catch { return defaultCosmicSlice() }
       ```

    4. В `gameStore.ts` загрузке top-level state — аналогично migrate hasCosmosUnlocked
       если он на top level а не в cosmic slice (что более вероятно по architecture).
       Решение по структуре — либо top-level либо cosmic.* — выбрать ОДНО и применить
       в обоих placeholders из Plan 22-06 + здесь. Default: top-level state.hasCosmosUnlocked.
  </action>
  <verify>
    <automated>cd client && node --test src/store/migrations/phase22.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    phase22.ts экспортирует migratePhase22 (pure function). phase22.test.ts: 8/8 PASS.
    persistence.ts вызывает migrate перед типизацией. Loading old state не throws и produces
    valid new shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n parity check + smoke test document</name>
  <files>
    client/src/i18n/ru.json,
    client/src/i18n/en.json,
    client/src/i18n/es.json,
    .planning/phases/22-carrier-merge-redesign/SMOKE_TEST_22.md
  </files>
  <action>
    1. Запустить `cd client && npm run check-translations 2>&1`. Если есть mismatches —
       поправить (Plan 22-04 и 22-05 уже добавили большую часть keys, тут final pass).

    2. Конкретные новые keys добавленные в Phase 22 (consolidated list для verification):
       - `hud.bonus.*` (Plan 22-04)
       - `shop.*` (Plan 22-05)
       - `cosmic_hub.tabs.shop` (Plan 22-05)
       - Любые tooltip/hint строки в SerumBar, BottomBar, LocationStack про cosmos unlock

    3. Создать `.planning/phases/22-carrier-merge-redesign/SMOKE_TEST_22.md`:
       ```markdown
       # Phase 22 — Manual Smoke Test Checklist

       ## Preconditions
       - Clear localStorage `frog_evolution_*` keys
       - `cd client && npm run dev`

       ## Scenario A: New player flow (pre-cosmos)
       - [ ] SerumBar НЕ виден
       - [ ] 🧬 Cosmic Hub button disabled/hidden
       - [ ] Ship controls на StarMap отсутствуют
       - [ ] Mega-box drops НЕ дают серум
       - [ ] Merge L1+L1 ... L17+L17 работает как раньше (no regression)

       ## Scenario B: Cosmos unlock trigger
       - [ ] Force L18+L18 normal merge (через `__forceMerge` или grind)
       - [ ] После sentinel: SerumBar появляется БЕЗ reload
       - [ ] 🧬 button становится active
       - [ ] Ship controls появляются на StarMap

       ## Scenario C: Carrier merge progression
       - [ ] `__giveSerum('fire')` → apply на frog L5 → carrier fire L5 с aura
       - [ ] `__giveCarrier('fire', 5, 'a')` + `__giveCarrier('fire', 5, 'b')` → merge → carrier L6
       - [ ] Target-wins проверка: carrier fire + carrier water (target) → carrier water

       ## Scenario D: Ascension
       - [ ] Carrier reach L18 → instant ascension tween (~1.5s)
       - [ ] ascendedCarriers.length увеличилось на 1
       - [ ] essence += 1
       - [ ] HUD ActiveBonusesBar показывает корректный bonus

       ## Scenario E: Shop purchases
       - [ ] Open Cosmic Hub → Shop tab
       - [ ] Buy «+1 slot» (cost=1 essence) → permaSlotBonus = 1
       - [ ] Second buy «+1 slot» (cost=2) → permaSlotBonus = 2 (scaling works)
       - [ ] Buy cosmic_box (3 essence) → 3 frogs L7 spawned
       - [ ] Buy serum trade-up: 3 fire → 1 random
       - [ ] Buy skip_ship_cooldown: ship становится docked мгновенно

       ## Scenario F: Persistence
       - [ ] Сделай ascension, buy slot+1, F5 → all preserved

       ## Scenario G: Legacy migration (если есть old playtest save)
       - [ ] Load page с legacy localStorage (rarity carriers, nested serums)
       - [ ] Console — no errors
       - [ ] Carriers конвертированы в { frogId, element, level }
       - [ ] Serums flat Record<Element, number> (sum across rarities)
       - [ ] discovered[19]=true → hasCosmosUnlocked = true

       ## Scenario H: i18n
       - [ ] RU → EN → ES — все HUD/shop labels переведены
       - [ ] npm run check-translations PASS

       ## Scenario I: Build chain
       - [ ] `npx tsc --noEmit` (client) — clean
       - [ ] `cd server && npx tsc --noEmit` — clean
       - [ ] `cd client && npx vite build` — succeeds
       - [ ] Bundle size delta ≤ +15 KB gzip (demo target, не strict cap)
       ```

    4. Закоммитить smoke document с phase прогрессом.
  </action>
  <verify>
    <automated>cd client && (npm run check-translations 2>&1 || true) | tail -10 && wc -l .planning/phases/22-carrier-merge-redesign/SMOKE_TEST_22.md</automated>
  </verify>
  <done>
    check-translations PASS. SMOKE_TEST_22.md существует, содержит scenarios A-I.
  </done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP + STATE updates + glossary placeholder note</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/phases/22-carrier-merge-redesign/GLOSSARY_UPDATES.md
  </files>
  <action>
    1. В `.planning/ROADMAP.md` обновить Phase 22 entry:
       - `**Plans:** 7 plans` (было 0)
       - Список plans:
         ```
         Plans:
         - [ ] 22-01-PLAN.md — Cleanup: убрать Rarity, mergeCarriers, feed-stabilize
         - [ ] 22-02-PLAN.md — Carrier merge rules (carrier+normal, carrier+carrier)
         - [ ] 22-03-PLAN.md — L18 ascension trigger + archetype bonus pool
         - [ ] 22-04-PLAN.md — HUD active bonuses bar + tooltip
         - [ ] 22-05-PLAN.md — Cosmic shop с essence + серум валютами
         - [ ] 22-06-PLAN.md — Cosmos gate (pre-cosmos hiding)
         - [ ] 22-07-PLAN.md — Migration + smoke + ROADMAP updates
         ```
       - `**Requirements**: PHASE22-CLEANUP, PHASE22-MERGE-RULES, PHASE22-ASCENSION,
         PHASE22-ARCHETYPE-POOL, PHASE22-HUD-BONUSES, PHASE22-COSMIC-SHOP,
         PHASE22-CURRENCIES, PHASE22-COSMOS-GATE, PHASE22-MIGRATION, PHASE22-SMOKE`
       - Убрать TBD placeholder и `(run /gsd-plan-phase 22 --prd ...)` hint.

    2. В `.planning/STATE.md` отметить:
       - `current_phase: 22` (после execution; для planning — пока остаётся predecessor)
       - Не обновляй completed_phases — это сделает execute-phase. Но добавь Phase 22 row
         в "Phase Progress" таблицу со статусом "planned":
         ```
         | 22 | Carrier merge redesign | **planned** (2026-05-17) — 7 plans, scope ~23h |
         ```

    3. Создать `.planning/phases/22-carrier-merge-redesign/GLOSSARY_UPDATES.md` —
       placeholder document с TODO list для obsidian glossary refresh после execution:
       ```markdown
       # Phase 22 Glossary Updates — TODO post-execution

       После реализации Phase 22 обновить frog_obsidian/Glossary/:

       - [ ] Сыворотка.md — убрать секцию Rarity. «В коде» обновить на flat Record<Element, number>.
       - [ ] Пробуждённая лягушка.md — пометить устаревшим (intent_status: removed). Linked Drift entry.
       - [ ] Стабилизация.md — пометить устаревшим.
       - [ ] Редкость сыворотки.md — пометить устаревшим.
       - [ ] Эффект архетипа.md — обновить под 4-категорную схему (Огонь/Вода/Камень/Тень/Прочее).
       - [ ] Capтан.md — обновить «Намерение» если нужно для cosmos-gate context.
       - [ ] NEW: Ascended carrier.md (или Carrier MAX.md) — новая страница, объясняет L18 ascension flow.
       - [ ] NEW: Эссенция.md — новая валюта.
       - [ ] NEW: Космический магазин.md — новый shop UI.

       Эти обновления — separate task в frog_obsidian/, не часть Phase 22 implementation
       (cross-repository edits). Запускать вручную после code review.
       ```

    4. В Plan SUMMARY должно быть упомянуто что glossary updates — separate manual pass.
  </action>
  <verify>
    <automated>grep -c "Plans:" .planning/ROADMAP.md && grep -c "22-01-PLAN\|22-07-PLAN" .planning/ROADMAP.md && wc -l .planning/phases/22-carrier-merge-redesign/GLOSSARY_UPDATES.md</automated>
  </verify>
  <done>
    ROADMAP.md содержит ссылки на 7 plans Phase 22 (grep matches). STATE.md содержит row для
    Phase 22 со статусом "planned". GLOSSARY_UPDATES.md существует со списком TODO для
    frog_obsidian/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Финальный cleanup: migration функция для legacy saves, smoke checklist, planning state
    files обновлены, glossary TODO placeholder создан.
  </what-built>
  <how-to-verify>
    1. `cd client && node --test src/store/migrations/phase22.test.ts` — 8/8 PASS
    2. `cd client && npm run check-translations` — PASS
    3. `cd client && npx tsc --noEmit` — clean
    4. `cd client && npx vite build` — succeeds
    5. Open SMOKE_TEST_22.md — пробежать пункты hands-on (минимум Scenarios A, B, C, D, E)
    6. Verify `.planning/ROADMAP.md` Phase 22 entry содержит 7 PLAN reference'ов
    7. Verify `.planning/STATE.md` содержит Phase 22 row
    8. Verify `.planning/phases/22-carrier-merge-redesign/GLOSSARY_UPDATES.md` существует

    Phase 22 ready to ship demo-build качества когда все scenario из SMOKE_TEST_22.md проходят.

    Type "approved" or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or описать issues</resume-signal>
</task>

</tasks>

<verification>
- migratePhase22 тесты 8/8 PASS
- check-translations PASS
- tsc clean
- vite build clean
- Smoke scenarios manual PASS
- ROADMAP/STATE отражают завершение фазы (planned status)
</verification>

<success_criteria>
1. Legacy localStorage save не вызывает runtime errors при load
2. Carriers сконвертированы в { frogId, element, level }
3. Serums flatten в Record<Element, number>
4. hasCosmosUnlocked inferred from discovered[19] для existing players
5. Migration idempotent — повторный вызов не ломает state
6. SMOKE_TEST_22.md покрывает все 9 scenarios (A-I)
7. ROADMAP.md и STATE.md актуальны
8. GLOSSARY_UPDATES.md создан как separate manual TODO для obsidian refresh
</success_criteria>

<output>
Создать `.planning/phases/22-carrier-merge-redesign/22-07-SUMMARY.md`. Зафиксировать:
final migration coverage, smoke pass/fail outcomes, любые deferred items для balance phase
(precise cost values, essence reward magnitude, archetype bonus magnitudes, serum drop rates).
Перечислить открытые knobs из Design Note → balance phase.
</output>
