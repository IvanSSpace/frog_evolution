---
phase: 22-carrier-merge-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/cosmic/types.ts
  - client/src/store/cosmic/slice.ts
  - client/src/store/cosmic/slices/carrierSlice.ts
  - client/src/store/cosmic/slices/serumSlice.ts
  - client/src/store/cosmic/slices/boxSlice.ts
  - client/src/store/cosmic/slices/shipSlice.ts
  - client/src/utils/rarityRoll.ts
  - client/src/utils/serumEligibility.ts
  - client/src/utils/carrierFeed.ts
  - client/src/utils/carrierEvolution.ts
  - client/src/components/SerumBar.tsx
  - client/src/components/CosmicHub/SerumInventoryTab.tsx
  - client/src/components/CosmicHub/SerumsTab.tsx
  - client/src/components/CosmicHub/CarriersTab.tsx
  - client/src/components/CosmicHub/CarrierInfoCard.tsx
  - client/src/components/CosmicHub/CeilingDisplay.tsx
  - client/src/components/CosmicHub/StabilizationModal.tsx
  - client/src/game/scenes/main/MergeController.ts
  - client/src/game/effects/FrogElementOverlay.ts
  - client/src/game/effects/FrogOverlayManager.ts
  - server/src/config/cosmic.ts
  - server/src/routes/cosmic.ts
  - client/src/store/cosmic/mergeCarriers.test.ts
  - client/src/store/cosmic/carriers.test.ts
autonomous: false
requirements: [PHASE22-CLEANUP]
estimated_hours: 3

must_haves:
  truths:
    - "Тип Rarity полностью удалён из cosmic state — TypeScript не компилирует ссылок на него"
    - "Серум инвентарь — плоский Record<Element, number>, не вложенный по rarity"
    - "Carrier не имеет полей rarity / stabilized / feedCount / ceiling / rollHistory"
    - "Action mergeCarriers удалён из carrierSlice"
    - "Action feedCarrier удалён из carrierSlice (вся feed-stabilize ветка вырезана)"
    - "Server endpoint POST /game/cosmic/apply-serum принимает любую обычную frog без rarity-валидации"
    - "Файлы serumEligibility.ts, carrierFeed.ts, carrierEvolution.ts удалены физически"
    - "StabilizationModal, CeilingDisplay, CarrierInfoCard удалены или сведены к stub'у"
    - "Существующие тесты mergeCarriers.test.ts и carriers.test.ts удалены"
    - "TypeScript build (tsc --noEmit) проходит без ошибок"
    - "Vite build проходит без ошибок"
  artifacts:
    - path: "client/src/store/cosmic/types.ts"
      provides: "Carrier без rarity/stabilized, серум как Record<Element, number>"
      must_not_contain: "Rarity\\|RARITIES\\|stabilized\\|feedCount\\|ceiling\\|rollHistory"
    - path: "server/src/config/cosmic.ts"
      provides: "Конфиг без RARITY_TO_STARTING_LEVEL"
      must_not_contain: "RARITY_TO_STARTING_LEVEL"
    - path: "client/src/utils/rarityRoll.ts"
      provides: "Boolean serum-drop роллер (упрощённый)"
      exports: ["rollSerumDrop"]
  key_links:
    - from: "server/src/routes/cosmic.ts"
      to: "any-frog acceptance"
      via: "applySerum принимает frogId без rarity-валидации"
      pattern: "apply-serum"
    - from: "client/src/store/cosmic/slices/serumSlice.ts"
      to: "applySerum (упрощённый)"
      via: "decrement inventory[element] && add carrier { frogId, element, level }"
      pattern: "applySerum"
---

<objective>
Удалить весь устаревший carrier-related код: Rarity-систему серумов, mergeCarriers action,
feed-stabilize awakening механику, ceiling/rollHistory/stabilized поля carrier'а.

Cleanup идёт ПЕРВЫМ, чтобы избежать double-state bugs при добавлении нового API.

Purpose: подготовить чистую surface для редизайна. Каждый последующий plan строится на
этой пустой основе, не борясь со старой логикой.

Output: cosmic state и UI вычищены от rarity/feed-stabilize концепций. Серум — единый тип
без rarity. Carrier — { frogId, element, level }, без stabilization. Build проходит.

ВАЖНО: это destructive cleanup. Тесты mergeCarriers.test.ts и carriers.test.ts удаляются
ВМЕСТЕ с кодом (тестируют функционал которого больше нет).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-carrier-merge-redesign/22-CONTEXT.md
@frog_obsidian/Design Notes/2026-05-17-carrier-merge-redesign.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slices/carrierSlice.ts
@client/src/store/cosmic/slices/serumSlice.ts
@server/src/routes/cosmic.ts
@server/src/config/cosmic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить Rarity из cosmic types + server config + утилит</name>
  <files>
    client/src/store/cosmic/types.ts,
    server/src/config/cosmic.ts,
    client/src/utils/rarityRoll.ts,
    client/src/utils/serumEligibility.ts
  </files>
  <action>
    1. В `client/src/store/cosmic/types.ts`:
       - Удалить `export type Rarity` и `export const RARITIES`.
       - Изменить `serums: Record<Element, Record<Rarity, number>>` → `serums: Record<Element, number>`.
       - Из `CarrierData` убрать поля `rarity`, `feedCount`, `stabilized`, `ceiling`, `rollHistory`.
       - Из `CarrierData` оставить: `frogId: string`, `element: Element`, `level: number` (level становится обязательным).
       - Убрать `RollResult` type целиком.
       - Из `CosmicSlice.selectedSerum` убрать `rarity`: `{ element: Element } | null`.
       - В `bestiaryBitset` оставить размер 144 байт (1152 бита = 16 × 4 × 18), но добавить TODO-комментарий «Phase 22: rarity dimension legacy — обсудить shrink в отдельном плане».
       - Обновить initial state factory (где `serums = {} as Record<Element, Record<Rarity, number>>` → плоский Record<Element, number>).
    2. В `server/src/config/cosmic.ts`:
       - Удалить `export type Rarity`, `RARITIES`, `RARITY_TO_STARTING_LEVEL`, `isValidRarity`.
       - Оставить только Element-related export'ы.
    3. `client/src/utils/serumEligibility.ts` — удалить файл целиком (`rm`).
    4. `client/src/utils/rarityRoll.ts` — упростить до:
       ```ts
       export function rollSerumDrop(chance: number, rng: () => number = Math.random): boolean {
         return rng() < chance
       }
       ```
       Убрать `rollRarity`, `updatePity`, `applyBonusFloor` и связанные тесты в `rarityRoll.test.ts`.
       Удалить `rarityRoll.test.ts` целиком (legacy тесты не релевантны).
    Используем простой Element-tag carrier без rarity per D-Serum Model.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>
    Файлы types.ts и cosmic.ts (server) не содержат символа `Rarity`. Файл serumEligibility.ts
    отсутствует. rarityRoll.ts содержит только функцию `rollSerumDrop`.
    `grep -rn "Rarity\b" client/src/store/cosmic/types.ts server/src/config/cosmic.ts` → 0 matches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Удалить mergeCarriers + feed-stabilize из store + server</name>
  <files>
    client/src/store/cosmic/slices/carrierSlice.ts,
    client/src/store/cosmic/slices/serumSlice.ts,
    client/src/store/cosmic/slices/boxSlice.ts,
    client/src/store/cosmic/slices/shipSlice.ts,
    client/src/store/cosmic/slice.ts,
    client/src/utils/carrierFeed.ts,
    client/src/utils/carrierEvolution.ts,
    client/src/store/cosmic/mergeCarriers.test.ts,
    client/src/store/cosmic/carriers.test.ts,
    server/src/routes/cosmic.ts
  </files>
  <action>
    1. В `client/src/store/cosmic/slices/carrierSlice.ts`:
       - Удалить action `mergeCarriers` целиком.
       - Удалить action `feedCarrier` целиком.
       - Удалить action `disposeCarrier` (станет частью ascension в Plan 22-03, пока убираем).
       - Оставить только `addCarrier` (используется applySerum), `removeCarrier(frogId)` (понадобится для ascension).
       - Обновить TypeScript ключи в `CarrierActionKey` union.
    2. В `client/src/store/cosmic/slices/serumSlice.ts`:
       - В `applySerum` optimistic update убрать `rarity`, `stabilized: false`, `feedCount: 0`.
       - Новый shape: `{ frogId, element, level: frogLevel }` где `frogLevel` = level лягушки на которую applied.
       - Декремент инвентаря: было `serums[element][rarity]--`, стало `serums[element]--`.
       - Rollback path — аналогично без rarity.
    3. В `client/src/store/cosmic/slices/boxSlice.ts` и `shipSlice.ts`:
       - Убрать все ссылки на `rollRarity`, `bonusRarity`, `updatePity` (BoxData больше не содержит rarity-полей в Phase 22; serum даётся как +1 к Record<Element, number>).
       - Изменить `commitOpenedBox` и mission completion path: вместо `addBox({ bonusRarity })` теперь `+1 серум` в `serums[element]`.
       - Если `bonusRarity` всё ещё нужен для UI cascade — оставить как cosmetic-only флаг (Plan 22-07 решит).
    4. В `client/src/store/cosmic/slice.ts`:
       - Удалить любые orphan-ссылки на `mergeCarriers`, `feedCarrier`, `RARITIES`.
       - Очистить imports.
    5. Удалить файлы целиком:
       - `client/src/utils/carrierFeed.ts`
       - `client/src/utils/carrierEvolution.ts` + соседний `.spec.ts`
       - `client/src/store/cosmic/mergeCarriers.test.ts`
       - `client/src/store/cosmic/carriers.test.ts`
    6. В `server/src/routes/cosmic.ts`:
       - В POST /game/cosmic/apply-serum убрать любую rarity-валидацию (если есть).
       - Убрать поле rarity из request body schema и newCarrier construction.
       - Carrier на сервере: `{ frogId, element, level: clientFrogLevel }`.

    Все эти удаления соответствуют D-Code Cleanup. Cascade box UI временно может сломаться —
    исправим в Plan 22-07. Главное чтобы tsc проходил.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -80 && cd ../server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Grep по `mergeCarriers\|feedCarrier\|stabilized\|RARITY_TO_STARTING_LEVEL` в client/src и
    server/src возвращает 0 совпадений. Файлы carrierFeed.ts, carrierEvolution.ts,
    mergeCarriers.test.ts, carriers.test.ts физически отсутствуют. TypeScript build clean
    обеих сторон.
  </done>
</task>

<task type="auto">
  <name>Task 3: Удалить feed-stabilize UI + переписать MergeController / overlay</name>
  <files>
    client/src/components/SerumBar.tsx,
    client/src/components/CosmicHub/SerumInventoryTab.tsx,
    client/src/components/CosmicHub/SerumsTab.tsx,
    client/src/components/CosmicHub/CarriersTab.tsx,
    client/src/components/CosmicHub/CarrierInfoCard.tsx,
    client/src/components/CosmicHub/CeilingDisplay.tsx,
    client/src/components/CosmicHub/StabilizationModal.tsx,
    client/src/components/CosmicHub/DisposeConfirmModal.tsx,
    client/src/game/scenes/main/MergeController.ts,
    client/src/game/effects/FrogElementOverlay.ts,
    client/src/game/effects/FrogOverlayManager.ts
  </files>
  <action>
    1. UI cleanup — удалить или заглушить компоненты завязанные на rarity/stabilize:
       - `StabilizationModal.tsx` — удалить файл + убрать mount в App.tsx (если есть).
       - `CeilingDisplay.tsx` — удалить.
       - `CarrierInfoCard.tsx` — упростить до { element, level } shape (Plan 22-04 расширит ссылкой на active bonuses).
       - `DisposeConfirmModal.tsx` — оставить как stub если используется, но убрать ссылку на feedCount / stabilized.
       - `CarriersTab.tsx` — простой список: element + level + frogId, без stabilized badge / ceiling progress.
       - `SerumsTab.tsx` + `SerumInventoryTab.tsx` — убрать rarity sections (common/rare/epic/legendary), оставить одну секцию «Серумы» с count per element.
       - `SerumBar.tsx` — отрисовка одного типа серума per element (без rarity stripes).
    2. `MergeController.ts`:
       - Удалить ВСЕ ветки связанные с feed / stabilize:
         - блок `if (cls.kind === 'blocked-unstabilized')` (line ~134)
         - блок `if (cls.kind === 'blocked-stabilized')` (line ~152)
         - блок `// Atomic feed — store mutates carrier` (line ~379) и вокруг
         - `'stabilize' — carrier остаётся, modal triggered через eventBus` (line ~472)
       - В `classifyDropTarget` оставить только: `normal+normal`, `carrier+normal` (станет TODO для 22-02), `carrier+carrier` (TODO), `L18+L18 sentinel` (existing).
       - Cosmos sentinel `markDiscovered(19)` оставить нетронутым (используется в 22-06).
       - Carrier+normal и carrier+carrier branches — заглушить (return early, `// TODO Plan 22-02`). НЕ удалять полностью — нужны точки расширения.
    3. `FrogElementOverlay.ts` / `FrogOverlayManager.ts`:
       - Удалить логику `carrier.stabilized ? awakened : dormant` ветвления.
       - Любой carrier теперь рисует одну aura (basic) — Plan 22-03 решит ascension tween.
       - Убрать `locked` flag если он был привязан к stabilized.
       - `elementCarriers.filter((c) => c.stabilized)` → просто `elementCarriers` (все carrier'ы одинаковы).

    Используем reuse existing ElementAuraOverlay per D-Specific Ideas. Тут просто упрощение,
    не добавление нового art.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -80 && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
    UI компоненты feed-stabilize удалены. MergeController feed-related ветки удалены, точки
    carrier+normal/carrier+carrier помечены TODO. Overlay rendering не зависит от stabilized.
    `grep -rn "stabilized\|feedCount\|StabilizationModal\|CeilingDisplay" client/src` → 0 matches.
    tsc clean, vite build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Полный destructive cleanup: Rarity, mergeCarriers, feed-stabilize, ceiling, stabilization
    UI — всё удалено. Carrier теперь = { frogId, element, level }. Серум = плоский Record.
    MergeController имеет TODO точки для carrier merge rules (заполнятся в Plan 22-02).
  </what-built>
  <how-to-verify>
    1. Запусти dev server: `cd client && npm run dev`
    2. Открой игру, тапни на 🧬 в нижнем баре → Cosmic Hub
    3. Открой вкладку «Сыворотки» — должна показывать ОДИН тип серума per element
       (без секций common/rare/epic/legendary)
    4. Открой вкладку «Carriers» — должна показывать пустой список (или существующих
       carrier'ов как простые element + level cards без stabilized badges)
    5. В dev-console выполни `__giveSerum('fire')` (если dev helper существует) или вручную
       зачисли серум через store devtools
    6. Apply серум на любую лягушку → должен появиться carrier с aura, БЕЗ feed progress UI
    7. Попробуй накормить carrier обычной лягушкой — НЕ должно быть feed-effect (просто merge
       будет заблокирован TODO-веткой; нормально)
    8. Открой console: не должно быть error'ов про undefined .stabilized или .feedCount

    Type "approved" если cleanup чист, или опиши что сломалось.
  </how-to-verify>
  <resume-signal>Type "approved" or описать issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript build clean (client + server)
- Vite build succeeds
- Grep returns 0 matches для удалённых концепций
- Dev-mode игра запускается без runtime error'ов
- UI carriers/serums отображают упрощённый shape
</verification>

<success_criteria>
1. Тип `Rarity` физически удалён из codebase (grep подтверждает)
2. Файлы serumEligibility.ts, carrierFeed.ts, carrierEvolution.ts, mergeCarriers.test.ts,
   carriers.test.ts отсутствуют
3. `CarrierData = { frogId, element, level }` — без rarity/stabilized/feedCount/ceiling
4. Серум инвентарь — плоский `Record<Element, number>`
5. MergeController имеет identifiable TODO точки для carrier-merge правил
6. Build chain (tsc client + tsc server + vite build) проходит без ошибок
7. Dev-mode игра запускается, Cosmic Hub открывается без runtime crash
</success_criteria>

<output>
После завершения создать `.planning/phases/22-carrier-merge-redesign/22-01-SUMMARY.md`
с перечнем удалённых файлов, изменённых типов, и оставшихся TODO точек в MergeController.
Зафиксировать какие тесты удалены (mergeCarriers.test.ts, carriers.test.ts, rarityRoll.test.ts,
carrierEvolution.spec.ts) для traceability при code review.
</output>
