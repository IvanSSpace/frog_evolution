---
phase: 22-carrier-merge-redesign
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - client/src/components/HUD/ActiveBonusesBar.tsx
  - client/src/components/HUD/ActiveBonusesTooltip.tsx
  - client/src/components/HUD/ActiveBonusesBar.module.css
  - client/src/components/CosmicHub/CarrierInfoCard.tsx
  - client/src/App.tsx
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: false
requirements: [PHASE22-HUD-BONUSES]
estimated_hours: 3

must_haves:
  truths:
    - "Активная HUD-строка с бонусами видна вверху экрана (постоянно когда что-то активно)"
    - "Строка отображает СУММУ full (ascended) + mini (carriers на поле) per bonus key"
    - "Mini bonus получает '·mini' бэйдж когда нет ascended той же категории"
    - "Строка автоматически обновляется при каждом ascension И при каждом изменении carriers (reactive)"
    - "Формат: компактный, лево-направо: `+10% gold, +5.5% box speed, +0.5% serum drop ·mini, ...`"
    - "Только ненулевые бонусы отображаются (пустой pool — bar скрыт)"
    - "Click на строку → tooltip/modal с двумя секциями: Mini (carriers на поле) + Full (ascended)"
    - "Tooltip показывает список ascended carriers (element badge + ascendedAt дата)"
    - "i18n RU/EN/ES для всех 5 bonus labels + mini/full section labels + mini_hint"
    - "Component реактивен — Zustand selector подписан на ascendedCarriers И carriers"
  artifacts:
    - path: "client/src/components/HUD/ActiveBonusesBar.tsx"
      provides: "Top-of-screen bonus summary bar (full + mini)"
      min_lines: 50
    - path: "client/src/components/HUD/ActiveBonusesTooltip.tsx"
      provides: "Click-tooltip с mini + full sections"
      min_lines: 40
  key_links:
    - from: "ActiveBonusesBar.tsx"
      to: "aggregateFullBonuses(ascendedCarriers) + aggregateMiniBonuses(carriers)"
      via: "Zustand useStore selectors на cosmic.ascendedCarriers и cosmic.carriers"
      pattern: "aggregateFullBonuses\\|aggregateMiniBonuses\\|ascendedCarriers\\|carriers"
    - from: "App.tsx"
      to: "ActiveBonusesBar"
      via: "render в top-level layout (fixed top)"
      pattern: "ActiveBonusesBar"
---

<objective>
Реализовать HUD строку активных бонусов от ascended carrier'ов. Одна тонкая полоса вверху
экрана, формат `+10% gold, +5% box speed, ...`. Click → tooltip с breakdown.

Purpose: дать игроку постоянную visibility что carrier'ы РАБОТАЮТ. Без HUD бонусы
невидимы и ascension теряет смысл retention loop'а.

Output: ActiveBonusesBar + ActiveBonusesTooltip компоненты, интегрированные в App layout,
с i18n RU/EN/ES.

Demo-build качество: tooltip — простая absolute-позиционированная карточка, не нужна сложная
positioning логика. Reduced effects не учитываем (Plan 22-07 опционально).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-carrier-merge-redesign/22-CONTEXT.md
@.planning/phases/22-carrier-merge-redesign/22-03-SUMMARY.md
@frog_obsidian/Design Notes/2026-05-17-carrier-merge-redesign.md
@client/src/utils/archetypeBonuses.ts
@client/src/store/cosmic/types.ts
@client/src/App.tsx
@client/src/components/SerumBar.tsx
@client/src/i18n/ru.json
</context>

<interfaces>
From client/src/utils/archetypeBonuses.ts (Plan 22-03):
```typescript
export interface AggregatedBonuses {
  boxDropSpeed: number  // 0.05 = +5%
  tractorGold: number
  offlineCap: number
  serumDrop: number
  flatGold: number
}
export function aggregateBonuses(ascended: AscendedCarrier[]): AggregatedBonuses
```

From client/src/store/cosmic/types.ts (Plan 22-03):
```typescript
export interface AscendedCarrier {
  id: string
  element: Element
  ascendedAt: number
}
// state.cosmic.ascendedCarriers: AscendedCarrier[]
```

Existing CSS module pattern: `.module.css` файлы рядом с .tsx (Phase 14-19 convention).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ActiveBonusesBar компонент + reactive selector</name>
  <files>
    client/src/components/HUD/ActiveBonusesBar.tsx,
    client/src/components/HUD/ActiveBonusesBar.module.css
  </files>
  <action>
    1. Создать `ActiveBonusesBar.tsx` (~50-70 lines):
       ```tsx
       import { useMemo, useState } from 'react'
       import { useTranslation } from 'react-i18next'
       import { useStore } from '../../store/gameStore'
       import { aggregateFullBonuses, aggregateMiniBonuses } from '../../utils/archetypeBonuses'
       import { ActiveBonusesTooltip } from './ActiveBonusesTooltip'
       import styles from './ActiveBonusesBar.module.css'

       export function ActiveBonusesBar() {
         const { t } = useTranslation()
         const ascended = useStore(s => s.cosmic.ascendedCarriers)
         const carriers  = useStore(s => s.cosmic.carriers)  // on-field carriers for mini bonus
         const [open, setOpen] = useState(false)

         const fullBonuses = useMemo(() => aggregateFullBonuses(ascended), [ascended])
         const miniBonuses = useMemo(() => aggregateMiniBonuses(carriers),  [carriers])

         // Total displayed = full + mini per bonus key
         const total = {
           boxDropSpeed: fullBonuses.boxDropSpeed + miniBonuses.boxDropSpeed,
           tractorGold:  fullBonuses.tractorGold  + miniBonuses.tractorGold,
           offlineCap:   fullBonuses.offlineCap   + miniBonuses.offlineCap,
           serumDrop:    fullBonuses.serumDrop    + miniBonuses.serumDrop,
           flatGold:     fullBonuses.flatGold     + miniBonuses.flatGold,
         }

         const items: { label: string; value: number; mini: number }[] = []
         if (total.flatGold > 0)     items.push({ label: t('hud.bonus.gold'),       value: total.flatGold,     mini: miniBonuses.flatGold })
         if (total.tractorGold > 0)  items.push({ label: t('hud.bonus.tractorGold'),value: total.tractorGold,  mini: miniBonuses.tractorGold })
         if (total.boxDropSpeed > 0) items.push({ label: t('hud.bonus.boxSpeed'),   value: total.boxDropSpeed, mini: miniBonuses.boxDropSpeed })
         if (total.offlineCap > 0)   items.push({ label: t('hud.bonus.offlineCap'), value: total.offlineCap,   mini: miniBonuses.offlineCap })
         if (total.serumDrop > 0)    items.push({ label: t('hud.bonus.serumDrop'),  value: total.serumDrop,    mini: miniBonuses.serumDrop })

         if (items.length === 0) return null  // hide когда нет бонусов

         return (
           <>
             <button
               type="button"
               className={styles.bar}
               onClick={() => setOpen(o => !o)}
               aria-label={t('hud.bonus.aria_open')}
             >
               {items.map((it, i) => (
                 <span key={i} className={styles.item}>
                   +{(it.value * 100).toFixed(1).replace(/\.0$/,'')}% {it.label}
                   {it.mini > 0 && it.value === it.mini && (
                     <span className={styles.miniBadge} title={t('hud.bonus.mini_hint')}>·mini</span>
                   )}
                 </span>
               ))}
             </button>
             {open && <ActiveBonusesTooltip onClose={() => setOpen(false)} />}
           </>
         )
       }
       ```

       Семантика отображения:
       - Если carrier на поле без ascensions той же категории → показывается mini value с
         `·mini` бэйджем (читается «затравка»).
       - Если есть ascended той же категории → mini растворяется в общую сумму без бэйджа.
       - Этот UX даёт игроку сигнал «бонус сейчас слабый — доведи до L18 чтобы раскрылся».

    2. Создать `ActiveBonusesBar.module.css`:
       ```css
       .bar {
         position: fixed;
         top: 4px;
         left: 50%;
         transform: translateX(-50%);
         z-index: 50;
         display: flex;
         gap: 8px;
         padding: 4px 12px;
         border: none;
         background: rgba(20, 20, 30, 0.85);
         color: #fbbf24;
         font-size: 11px;
         font-weight: 600;
         border-radius: 999px;
         cursor: pointer;
         max-width: 95vw;
         overflow-x: auto;
         white-space: nowrap;
       }
       .item { flex-shrink: 0; }
       .miniBadge {
         margin-left: 4px;
         padding: 1px 4px;
         font-size: 9px;
         font-weight: 500;
         color: rgba(251, 191, 36, 0.6);
         border: 1px solid rgba(251, 191, 36, 0.3);
         border-radius: 4px;
         vertical-align: middle;
       }
       .bar:hover { background: rgba(30, 30, 45, 0.95); }
       ```

    3. Важно — НЕ tween'ить frog.container alpha (см. memory `feedback_frog_container_alpha`).
       Этот компонент сам по себе DOM overlay над Phaser canvas, frog'ов не трогает.

    4. Click cliclability: button с `type="button"` (избежать form submit). `z-index: 50` выше
       любых Phaser DOM overlay'ев (см. memory `feedback_clickability`).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -20 && grep -c "ActiveBonusesBar\|aggregateBonuses\|ascendedCarriers" src/components/HUD/ActiveBonusesBar.tsx</automated>
  </verify>
  <done>
    ActiveBonusesBar.tsx >= 40 lines, использует useStore selector на ascendedCarriers,
    рендерит только ненулевые бонусы, click toggles tooltip. CSS module создан.
  </done>
</task>

<task type="auto">
  <name>Task 2: ActiveBonusesTooltip + per-carrier breakdown</name>
  <files>
    client/src/components/HUD/ActiveBonusesTooltip.tsx
  </files>
  <action>
    1. Создать `ActiveBonusesTooltip.tsx`:
       ```tsx
       import { useTranslation } from 'react-i18next'
       import { useStore } from '../../store/gameStore'
       import { ELEMENT_TO_CATEGORY, BONUS_PER_CATEGORY, MINI_BONUS_PER_CATEGORY } from '../../utils/archetypeBonuses'

       interface Props { onClose: () => void }

       export function ActiveBonusesTooltip({ onClose }: Props) {
         const { t } = useTranslation()
         const ascended = useStore(s => s.cosmic.ascendedCarriers)
         const carriers = useStore(s => s.cosmic.carriers)  // on-field for mini section
         const essence = useStore(s => s.cosmic.essence)

         // FULL — group ascended by category
         const grouped = new Map<string, typeof ascended>()
         for (const a of ascended) {
           const cat = ELEMENT_TO_CATEGORY[a.element]
           if (!grouped.has(cat)) grouped.set(cat, [])
           grouped.get(cat)!.push(a)
         }

         // MINI — unique categories present on field
         const miniCategories = new Set<string>()
         for (const c of carriers) miniCategories.add(ELEMENT_TO_CATEGORY[c.element])

         return (
           <div
             onClick={onClose}
             style={{
               position: 'fixed', top: 0, left: 0, right: 0, bottom: 0,
               background: 'rgba(0,0,0,0.5)', zIndex: 100,
               display: 'flex', alignItems: 'flex-start', justifyContent: 'center',
               paddingTop: 48,
             }}
           >
             <div
               onClick={e => e.stopPropagation()}
               style={{
                 background: '#1a1a2e', color: '#fff', padding: 16,
                 borderRadius: 8, maxWidth: 360, width: '90vw',
                 maxHeight: '70vh', overflowY: 'auto',
               }}
             >
               <h3 style={{ marginTop: 0 }}>{t('hud.bonus.tooltip_title')}</h3>
               <p style={{ fontSize: 12, opacity: 0.7 }}>
                 {t('hud.bonus.essence')}: {essence}
               </p>

               {/* MINI section — teaser bonus от carriers на поле */}
               {miniCategories.size > 0 && (
                 <div style={{ marginBottom: 12, padding: 8, background: 'rgba(251,191,36,0.08)', borderRadius: 4 }}>
                   <strong style={{ fontSize: 11, color: '#fbbf24' }}>
                     {t('hud.bonus.mini_section_title')}
                   </strong>
                   <p style={{ margin: '4px 0', fontSize: 10, opacity: 0.7 }}>
                     {t('hud.bonus.mini_hint')}
                   </p>
                   {Array.from(miniCategories).map(cat => {
                     const mini = MINI_BONUS_PER_CATEGORY[cat as keyof typeof MINI_BONUS_PER_CATEGORY]
                     return (
                       <div key={cat} style={{ fontSize: 11, marginLeft: 8 }}>
                         · {t(`hud.bonus.category.${cat}`)}
                         {' '}+{(mini.amount * 100).toFixed(1).replace(/\.0$/,'')}% {t(`hud.bonus.${mini.key}`)}
                       </div>
                     )
                   })}
                 </div>
               )}

               {/* FULL section — permanent ascended bonuses */}
               {grouped.size > 0 && (
                 <div style={{ marginBottom: 8 }}>
                   <strong style={{ fontSize: 11 }}>{t('hud.bonus.full_section_title')}</strong>
                 </div>
               )}
               {Array.from(grouped.entries()).map(([cat, items]) => {
                 const bonus = BONUS_PER_CATEGORY[cat as keyof typeof BONUS_PER_CATEGORY]
                 return (
                   <div key={cat} style={{ marginBottom: 8 }}>
                     <strong>{t(`hud.bonus.category.${cat}`)}</strong>
                     {' '}× {items.length}
                     {' '}(+{Math.round(items.length * bonus.amount * 100)}% {t(`hud.bonus.${bonus.key}`)})
                     <ul style={{ margin: '4px 0 0 16px', padding: 0, fontSize: 11 }}>
                       {items.map(a => (
                         <li key={a.id}>
                           {a.element} — {new Date(a.ascendedAt).toLocaleDateString()}
                         </li>
                       ))}
                     </ul>
                   </div>
                 )
               })}
               <button
                 type="button"
                 onClick={onClose}
                 style={{ marginTop: 12, padding: '6px 16px', cursor: 'pointer' }}
               >
                 {t('common.close')}
               </button>
             </div>
           </div>
         )
       }
       ```

    2. Cliclability check (memory `feedback_clickability`):
       - Backdrop onClick → close.
       - Inner div onClick → stopPropagation (чтобы клик внутри не закрывал).
       - Close button type="button".
       - z-index 100 — выше bar (50).

    3. Не использовать Lottie — CSS-only стили per memory `feedback_animations`.
       Простая absolute карточка достаточна для demo.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -20 && grep -c "stopPropagation\|onClose" src/components/HUD/ActiveBonusesTooltip.tsx</automated>
  </verify>
  <done>
    ActiveBonusesTooltip.tsx >= 30 lines. Backdrop click закрывает. Inner stopPropagation работает.
    Группирует ascended по category. Показывает count + total bonus per category + список
    individual ascensions с датой.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount в App + i18n RU/EN/ES</name>
  <files>
    client/src/App.tsx,
    client/src/i18n/ru.json,
    client/src/i18n/en.json,
    client/src/i18n/es.json
  </files>
  <action>
    1. В `client/src/App.tsx` добавить:
       ```tsx
       import { ActiveBonusesBar } from './components/HUD/ActiveBonusesBar'

       // Где-то в root layout (вне Phaser canvas wrapper, но в том же overflow:none container):
       <ActiveBonusesBar />
       ```
       Render ОДИН раз на top level. Bar самостоятельно скрывается если bonuses пусты.

    2. В `client/src/i18n/ru.json` добавить:
       ```json
       "hud": {
         "bonus": {
           "aria_open": "Активные бонусы — открыть детали",
           "tooltip_title": "Активные бонусы",
           "essence": "Эссенция",
           "mini_section_title": "Затравка (carrier'ы на поле)",
           "full_section_title": "Постоянные (ascended)",
           "mini_hint": "Доведи carrier до L18 чтобы бонус раскрылся полностью (×10)",
           "gold": "к золоту",
           "tractorGold": "к трактору",
           "boxSpeed": "к скорости боксов",
           "offlineCap": "к offline-капу",
           "serumDrop": "к дропу серума",
           "category": {
             "fire": "Огонь",
             "water": "Вода",
             "stone": "Камень",
             "shadow": "Тень",
             "other": "Прочее"
           }
         }
       }
       ```

    3. В `en.json`:
       ```json
       "hud": {
         "bonus": {
           "aria_open": "Active bonuses — open details",
           "tooltip_title": "Active bonuses",
           "essence": "Essence",
           "mini_section_title": "Teaser (carriers on field)",
           "full_section_title": "Permanent (ascended)",
           "mini_hint": "Reach L18 to unlock the full bonus (×10)",
           "gold": "gold",
           "tractorGold": "tractor gold",
           "boxSpeed": "box speed",
           "offlineCap": "offline cap",
           "serumDrop": "serum drop",
           "category": { "fire": "Fire", "water": "Water", "stone": "Stone", "shadow": "Shadow", "other": "Other" }
         }
       }
       ```

    4. В `es.json`:
       ```json
       "hud": {
         "bonus": {
           "aria_open": "Bonificaciones activas",
           "tooltip_title": "Bonificaciones activas",
           "essence": "Esencia",
           "mini_section_title": "Adelanto (en el campo)",
           "full_section_title": "Permanentes (ascendidos)",
           "mini_hint": "Llega a L18 para revelar el bono completo (×10)",
           "gold": "oro",
           "tractorGold": "oro tractor",
           "boxSpeed": "velocidad cajas",
           "offlineCap": "límite offline",
           "serumDrop": "drop suero",
           "category": { "fire": "Fuego", "water": "Agua", "stone": "Piedra", "shadow": "Sombra", "other": "Otro" }
         }
       }
       ```

    5. Запустить `npm run check-translations` (если script существует из Phase 19) —
       должен PASS (RU/EN/ES parity).

    6. CarrierInfoCard.tsx (от Plan 22-01) — добавить hint в самом низу:
       «Доведи до L18 для ascension».
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -10 && (npm run check-translations 2>&1 || true) | tail -10 && grep -c "ActiveBonusesBar" src/App.tsx</automated>
  </verify>
  <done>
    App.tsx импортирует и рендерит ActiveBonusesBar. i18n содержит hud.bonus.* keys в RU/EN/ES.
    check-translations PASS. tsc clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    HUD bonus bar: тонкая полоса вверху экрана, отображает все активные бонусы (+X% gold,
    +Y% box speed, ...) в одну линию, click → tooltip с breakdown по category и per-carrier.
    i18n RU/EN/ES.
  </what-built>
  <how-to-verify>
    1. `cd client && npm run dev`
    2. Открыть игру — HUD bar НЕ виден (нет carriers и нет ascended)
    3. В console: `window.__giveCarrier('fire', 1, 'a')` — создать L1 fire carrier на поле
    4. HUD bar появляется: `+0.5% к скорости боксов ·mini` (mini badge)
    5. `window.__giveCarrier('plasma', 1, 'b')` (другой fire-category carrier)
    6. HUD bar остаётся: `+0.5% к скорости боксов ·mini` (не стэкается — max per category)
    7. `window.__giveCarrier('water', 1, 'c')` (другая категория)
    8. HUD bar: `+0.5% к скорости боксов ·mini, +0.5% к трактору ·mini`
    9. Click на bar → tooltip открывается, секция «Затравка» показывает 2 категории
       с hint «Доведи carrier до L18...»
    10. Прогресс carrier 'a' до L18 (через `__simulateMerge` chain или дев hotkey).
        Ascension tween играет → carrier 'a' исчезает.
    11. HUD bar: `+5% к скорости боксов` (без ·mini badge, потому что есть ascended той же
        категории, mini растворился) + `+0.5% к трактору ·mini` (только water carrier на поле)
    12. Tooltip → секция «Постоянные» показывает Огонь × 1, секция «Затравка» — только water
    13. Click outside (backdrop) → tooltip закрывается
    14. Click внутри tooltip — НЕ закрывается (stopPropagation)
    15. Switch language RU → EN → ES — все строки переведены, включая mini_hint и section titles
    16. F5 → ascended carrier остаётся (persisted), carriers на поле тоже (existing persistence)

    Type "approved" или опиши issue.
  </how-to-verify>
  <resume-signal>Type "approved" or описать issues</resume-signal>
</task>

</tasks>

<verification>
- tsc clean
- check-translations PASS (RU/EN/ES parity)
- Manual smoke: bar реактивен к ascendedCarriers
- Tooltip open/close через click + backdrop
- z-index correctness (bar над Phaser, tooltip над bar)
</verification>

<success_criteria>
1. HUD bar виден когда есть хотя бы один carrier (mini) или ascended carrier (full)
2. Bar показывает sum(full + mini) per bonus key
3. Mini-only бонусы получают `·mini` бэйдж (визуальный teaser маркер)
4. Mini растворяется в общую сумму когда есть ascended той же категории (badge снимается)
5. Bar обновляется автоматически при ascension И при carrier add/remove (reactive)
6. Tooltip показывает 2 секции: Mini (категории carriers на поле) + Full (per-category aggregation + per-carrier list)
7. i18n полностью покрывает 5 bonus labels + 5 categories + mini/full section titles + mini_hint в 3 локалях
8. Bar/tooltip имеют корректную clickability (memory `feedback_clickability` checklist)
9. Demo-build качество — не enterprise UI, простой и читаемый
</success_criteria>

<output>
Создать `.planning/phases/22-carrier-merge-redesign/22-04-SUMMARY.md`. Зафиксировать
финальный i18n key list, z-index layering, и edge case "bar hidden когда pool пуст".
</output>
