---
phase: 22-carrier-merge-redesign
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - client/src/store/cosmic/slices/carrierSlice.ts
  - client/src/game/scenes/main/MergeController.ts
  - client/src/store/cosmic/slice.ts
  - client/src/store/cosmic/types.ts
  - client/src/store/cosmic/carrierMerge.test.ts
autonomous: true
requirements: [PHASE22-MERGE-RULES]
estimated_hours: 3

must_haves:
  truths:
    - "Carrier Ln + normal Ln → carrier L(n+1), element наследуется от carrier"
    - "Carrier Ln + carrier Ln (одинаковый element) → carrier L(n+1)"
    - "Carrier Ln + carrier Ln (разные elements) → carrier L(n+1) с element TARGET (drop-on)"
    - "Carrier меняет уровень через стандартный merge — не через отдельный action"
    - "Normal L18 + normal L18 sentinel cosmos-unlock работает как раньше (regression-free)"
    - "Unit-тест carrierMerge.test.ts покрывает 4 ветки + target-wins rule"
    - "TypeScript build clean"
  artifacts:
    - path: "client/src/store/cosmic/slices/carrierSlice.ts"
      provides: "Actions mergeCarrierWithNormal + mergeCarrierWithCarrier"
      exports: ["mergeCarrierWithNormal", "mergeCarrierWithCarrier"]
    - path: "client/src/game/scenes/main/MergeController.ts"
      provides: "Полная логика drop classification с carrier ветками"
      contains: "carrier-with-normal\\|carrier-with-carrier"
    - path: "client/src/store/cosmic/carrierMerge.test.ts"
      provides: "Unit-тесты для всех 4 веток + target-wins"
      contains: "describe.*carrier merge"
  key_links:
    - from: "client/src/game/scenes/main/MergeController.ts"
      to: "carrierSlice.mergeCarrierWith*"
      via: "drop classification → atomic store action"
      pattern: "mergeCarrierWith"
    - from: "carrier+carrier merge"
      to: "target-element-survives rule"
      via: "passing targetFrogId = drop-on frog"
      pattern: "target.*element"
---

<objective>
Реализовать carrier merge rules в store + MergeController. Заменить TODO точки из Plan 22-01
полной логикой:

- carrier + normal → carrier L+1, element от carrier
- carrier + carrier same element → carrier L+1
- carrier + carrier разные elements → carrier L+1, element TARGET (drop-on) survives

Purpose: дать carrier'у стандартный merge-progression от L1 до L17 (L18 ascension — Plan 22-03).

Output: 2 новых store actions, обновлённый classifyDropTarget, unit-тест на все ветки.

ВАЖНО: target-wins rule для carrier+carrier — player контролирует drag direction (drag A на B
→ B's element wins). Это game-design решение per D-Carrier+carrier merge rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-carrier-merge-redesign/22-CONTEXT.md
@.planning/phases/22-carrier-merge-redesign/22-01-SUMMARY.md
@frog_obsidian/Design Notes/2026-05-17-carrier-merge-redesign.md
@client/src/store/cosmic/slices/carrierSlice.ts
@client/src/game/scenes/main/MergeController.ts
@client/src/store/cosmic/types.ts
</context>

<interfaces>
<!-- Post-Plan-22-01 CarrierData shape (executor должен использовать как есть) -->

From client/src/store/cosmic/types.ts (after Plan 22-01):
```typescript
export interface CarrierData {
  frogId: string
  element: Element
  level: number  // 1..18
}
```

From client/src/game/scenes/main/MergeController.ts:
```typescript
// classifyDropTarget возвращает discriminated union;
// после Plan 22-01 carrier-ветки помечены TODO.
// MAX_LEVEL = 18 (existing constant).
```

From client/src/store/cosmic/slices/carrierSlice.ts (after Plan 22-01):
```typescript
// Существующие actions: addCarrier, removeCarrier
// Plan 22-02 ДОБАВЛЯЕТ: mergeCarrierWithNormal, mergeCarrierWithCarrier
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store actions для carrier merge + unit-тесты (TDD)</name>
  <files>
    client/src/store/cosmic/slices/carrierSlice.ts,
    client/src/store/cosmic/slice.ts,
    client/src/store/cosmic/carrierMerge.test.ts
  </files>
  <behavior>
    Тесты ПЕРЕД реализацией:

    Test 1 — mergeCarrierWithNormal happy path:
      State: carriers = [{ frogId: 'f1', element: 'fire', level: 3 }]
      Call: mergeCarrierWithNormal('f1', 'f2-normal', 'f3-new', 4)
      Expect: carriers = [{ frogId: 'f3-new', element: 'fire', level: 4 }]
      (старый carrier удалён, новый создан с element=fire, level=4)

    Test 2 — mergeCarrierWithCarrier same element:
      State: carriers = [
        { frogId: 'a', element: 'water', level: 5 },
        { frogId: 'b', element: 'water', level: 5 }
      ]
      Call: mergeCarrierWithCarrier(droppedFrogId='a', targetFrogId='b', newFrogId='c', newLevel=6)
      Expect: carriers = [{ frogId: 'c', element: 'water', level: 6 }]

    Test 3 — mergeCarrierWithCarrier разные elements (target wins):
      State: carriers = [
        { frogId: 'a', element: 'fire', level: 7 },
        { frogId: 'b', element: 'water', level: 7 }
      ]
      Call: mergeCarrierWithCarrier(droppedFrogId='a', targetFrogId='b', newFrogId='c', newLevel=8)
      Expect: carriers = [{ frogId: 'c', element: 'water', level: 8 }]
      (target = drop-on = 'b' → его element survives)

    Test 4 — invariant invalid input:
      Call: mergeCarrierWithNormal('unknown', 'f2', 'f3', 4) → action — no-op, state unchanged

    Test 5 — mergeCarrierWithCarrier при отсутствующем target → no-op.

    Test 6 — level cap: при newLevel=18, action всё равно срабатывает (создаёт carrier L18).
      Ascension trigger — отдельный Plan 22-03, тут не проверяем.
  </behavior>
  <action>
    1. Создать `client/src/store/cosmic/carrierMerge.test.ts` со всеми 6 тестами (Node test runner,
       тот же стиль что и slice.test.ts: `describe/it` + `assert`).

    2. Запустить тест — ДОЛЖЕН FAIL (actions не существуют).
       Команда: `cd client && node --test src/store/cosmic/carrierMerge.test.ts` (или through
       npm script если есть). Зафиксировать RED commit.

    3. Реализовать в `carrierSlice.ts`:
       ```ts
       mergeCarrierWithNormal: (carrierFrogId, normalFrogId, newFrogId, newLevel) => {
         set((state) => {
           const carrier = state.carriers.find(c => c.frogId === carrierFrogId)
           if (!carrier) return state
           const nextCarriers = state.carriers
             .filter(c => c.frogId !== carrierFrogId)
             .concat({ frogId: newFrogId, element: carrier.element, level: newLevel })
           return { ...state, carriers: nextCarriers }
         })
       },

       mergeCarrierWithCarrier: (droppedFrogId, targetFrogId, newFrogId, newLevel) => {
         set((state) => {
           const target = state.carriers.find(c => c.frogId === targetFrogId)
           const dropped = state.carriers.find(c => c.frogId === droppedFrogId)
           if (!target || !dropped) return state
           // target element survives per D-Carrier+carrier merge rule
           const nextCarriers = state.carriers
             .filter(c => c.frogId !== droppedFrogId && c.frogId !== targetFrogId)
             .concat({ frogId: newFrogId, element: target.element, level: newLevel })
           return { ...state, carriers: nextCarriers }
         })
       },
       ```

    4. Добавить новые action keys в `CarrierActionKey` union и экспорт из slice.ts.

    5. Запустить тест — должен PASS. Зафиксировать GREEN commit.
  </action>
  <verify>
    <automated>cd client && node --test src/store/cosmic/carrierMerge.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Файл carrierMerge.test.ts существует с 6 describe/it блоков. Все 6 тестов pass.
    carrierSlice.ts экспортирует mergeCarrierWithNormal и mergeCarrierWithCarrier.
    `grep -c "it(" client/src/store/cosmic/carrierMerge.test.ts` >= 6.
  </done>
</task>

<task type="auto">
  <name>Task 2: MergeController — заполнить carrier merge ветки</name>
  <files>
    client/src/game/scenes/main/MergeController.ts
  </files>
  <action>
    1. В `classifyDropTarget` обновить discriminated union:
       ```ts
       type DropClass =
         | { kind: 'normal-normal'; level: number }
         | { kind: 'carrier-normal'; carrierFrogId: string; normalFrogId: string; nextLevel: number }
         | { kind: 'carrier-carrier'; droppedFrogId: string; targetFrogId: string; nextLevel: number }
         | { kind: 'sentinel-l18-l18' }  // existing cosmos unlock
         | { kind: 'invalid'; reason: string }
       ```

    2. В onDrop / handleMerge:
       - Если оба normal same-level → existing normal merge path.
       - Если dropped — carrier, target — normal, levels match → call `mergeCarrierWithNormal`,
         удалить normal frog с поля, заменить target frog'у sprite/id на newFrogId, level+1,
         сохранить element. Acquire ElementAuraOverlay для нового carrier'а.
       - Если оба carrier same-level → call `mergeCarrierWithCarrier(dropped, target, newFrogId, level+1)`.
         Target frog ID и его element выживает per rule. Удалить dropped frog. Overlay
         re-acquire с element TARGET'а (если был mismatch).
       - L18+L18 normal sentinel: НЕ ТРОГАТЬ (existing flow). Cosmos unlock через `markDiscovered(19)`.
       - L18 carrier reaching — НЕ ОБРАБАТЫВАТЬ в этом плане (Plan 22-03 добавит ascension hook
         в success path). Сейчас при carrier merging до L18 — просто создаётся carrier L18 как
         любой другой; ascension trigger Plan 22-03 подцепит через subscribe или направляющий emit.

    3. Drag direction = source of truth:
       - В drag/drop handler: `droppedFrogId` = тот что player таскает, `targetFrogId` = на котором
         dropped. Используй существующие FrogInteraction вызовы (см. FrogInteraction.ts).
       - НЕ менять targetFrogId/droppedFrogId местами. Это semantic source для target-wins rule.

    4. Удалить любые orphan TODO из Plan 22-01 — все carrier merge ветки теперь имеют impl.

    5. Logging для отладки: добавить `console.log('[carrier-merge]', {kind, droppedElement, targetElement, newElement})`
       обёрнутый в `if (import.meta.env.DEV)` блок. Demo-build качество — можно оставить.

    Используем reuse существующего ElementAuraOverlay per D-Specific Ideas.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | head -40 && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>
    MergeController.ts содержит все 4 ветки classifyDropTarget без TODO остатков.
    `grep -c "carrier-normal\|carrier-carrier" client/src/game/scenes/main/MergeController.ts` >= 2.
    `grep -c "TODO Plan 22-02" client/src/game/scenes/main/MergeController.ts` == 0.
    tsc clean, vite build OK.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke test — проиграть merge сценарии в dev mode</name>
  <files>
    client/src/utils/devCarriers.ts
  </files>
  <action>
    1. В `devCarriers.ts` добавить (или обновить) три dev helper'а:
       - `window.__giveCarrier(element, level, frogId?)` — создаёт carrier на существующей frog
         (через addCarrier action), либо spawn'ит новую frog указанного level если frogId не дан.
       - `window.__listCarriers()` — печатает в console текущий список carrier'ов.
       - `window.__simulateMerge(droppedFrogId, targetFrogId)` — симулирует drop через прямой вызов
         MergeController public API (если есть) или через eventBus emit, чтобы тестировать без UI.

    2. Документировать в JSDoc usage:
       ```
       window.__giveCarrier('fire', 5, 'frog-a')
       window.__giveCarrier('water', 5, 'frog-b')
       window.__simulateMerge('frog-a', 'frog-b')  // → carrier водный L6
       window.__listCarriers()  // [{frogId: 'new-...', element: 'water', level: 6}]
       ```

    3. Smoke test acceptance — задокументировать в SUMMARY (не в коде):
       - carrier + normal: создаёт carrier element=carrier.element, level+1 ✓
       - carrier + carrier same element: создаёт один carrier, level+1 ✓
       - carrier + carrier different element: target element survives ✓
       - normal L18 + normal L18: existing cosmos sentinel unchanged ✓

    Это demo-build — UI smoke ручной, формальный e2e не делаем.
  </action>
  <verify>
    <automated>cd client && grep -c "__giveCarrier\|__simulateMerge" src/utils/devCarriers.ts</automated>
  </verify>
  <done>
    devCarriers.ts экспортирует __giveCarrier, __listCarriers, __simulateMerge (>= 3 совпадения grep).
    Smoke test scenarios описаны в Plan SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `node --test carrierMerge.test.ts` — все 6 PASS
- tsc clean (client)
- vite build succeeds
- Dev console __simulateMerge корректно срабатывает (manual smoke)
- L18+L18 normal sentinel cosmos unlock — regression check (manual)
</verification>

<success_criteria>
1. Actions mergeCarrierWithNormal и mergeCarrierWithCarrier существуют и atomic
2. Unit-тесты (6 шт) покрывают: happy path normal, happy path same-element, target-wins
   different element, invalid carrier id, invalid target id, level cap behavior
3. MergeController classifyDropTarget полностью покрывает 4 валидные ветки + invalid
4. Existing cosmos sentinel (L18+L18 normal) НЕ regress
5. Dev helpers позволяют простую proof через console
</success_criteria>

<output>
Создать `.planning/phases/22-carrier-merge-redesign/22-02-SUMMARY.md`. Зафиксировать
финальный список merge ветвей и behavior на edge cases (carrier+normal с level mismatch =
не срабатывает; carrier+carrier с level mismatch = не срабатывает). Smoke scenarios + dev helpers usage.
</output>
