---
phase: 27-contacts-messages-relationships
plan: 06
type: execute
wave: 4
depends_on: [27-01, 27-02, 27-03, 27-04, 27-05]
files_modified:
  - client/SMOKE_TEST_27.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PHASE27-I18N-PARITY
  - PHASE27-SMOKE
  - PHASE27-FINALIZE

must_haves:
  truths:
    - "client/SMOKE_TEST_27.md exists with ≥6 numbered scenarios (A-F) covering tab visibility, list view, race detail navigation, reply UX, event toast, persistence + dev helpers"
    - "Full build chain green: cd client && npx tsc --noEmit + npx vitest run + node scripts/check-translations.cjs + npx vite build"
    - "Bundle delta gzip main recorded vs Phase 26 baseline (209.17 KB) — captured in SMOKE_TEST_27.md notes"
    - "ROADMAP.md Phase 27 entry: Requirements TBD replaced with 25 actual REQ-IDs, Plans count = 6, plan list checked, outcome paragraph added, Last updated timestamp updated"
    - "STATE.md current_phase: 27 (complete); progress totals updated; Phase Progress table row added"
    - "Per-plan SUMMARY existence verified (27-01..05 files present)"
  artifacts:
    - path: "client/SMOKE_TEST_27.md"
      provides: "Manual smoke test scenarios A-F + regression sanity + cliclability + i18n + build chain"
      contains: "Phase 27"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 27 entry finalized with REQ-IDs + plans + outcome paragraph"
      contains: "Phase 27"
    - path: ".planning/STATE.md"
      provides: "current_phase: 27 (complete) + Phase Progress row + progress totals updated"
      contains: "Phase 27"
  key_links:
    - from: ".planning/ROADMAP.md Phase 27"
      to: ".planning/phases/27-contacts-messages-relationships/27-0?-SUMMARY.md"
      via: "Plans block lists [x] for each completed plan"
      pattern: "27-0..PLAN.md"
    - from: ".planning/STATE.md Phase Progress"
      to: "client/SMOKE_TEST_27.md"
      via: "STATE row references scenarios covered + bundle delta from smoke test"
      pattern: "27.*complete"
---

<objective>
Finalize Phase 27: write SMOKE_TEST_27.md with manual QA scenarios, run full build chain (tsc + vitest + check-translations + vite build), gate on human approval via checkpoint, then update ROADMAP.md + STATE.md with completion entry + REQ-IDs.

Purpose: All 25 Phase 27 REQ-IDs map to plans 01-05 — this plan validates them via SMOKE + closes the phase in planning docs. After this plan ships, Phase 27 is "complete" and Phase 28 can start.

Output: 1 new SMOKE_TEST file + 2 updated planning docs. Per-plan SUMMARYs from 01-05 verified present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-contacts-messages-relationships/27-CONTEXT.md
@.planning/phases/26-cosmos-races-foundation/26-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build chain validation + bundle delta capture</name>
  <read_first>
    - client/SMOKE_TEST_26.md (Phase 26 SMOKE format — mirror structure)
    - .planning/phases/26-cosmos-races-foundation/26-06-PLAN.md (Phase 26 finalize pattern)
  </read_first>
  <files>(no file edits in this task — measurement only; output captured for Task 2)</files>
  <behavior>
    - Run `npx tsc --noEmit` from client/ — expect 0 errors
    - Run `npx vitest run` from client/ — expect all PASS modulo 3 pre-existing Phase 22 failures (slice.test.ts / slice.openBox.test.ts / cosmicSettings.test.ts — documented in deferred-items.md). Phase 27 added 11 new tests in pendingEngine.test.ts — these MUST be green
    - Run `node scripts/check-translations.cjs` (from repo root) — expect parity clean, ~117 new keys per locale vs Phase 26 baseline of 402
    - Run `cd client && npx vite build` — production build success
    - Capture main bundle gzip size — compare to Phase 26 baseline (209.17 KB main); expect delta cap around +5-8 KB (modest — contacts UI is DOM-only, no heavy deps added; pendingEngine pure logic; 100 chain items add ~5-10 KB raw, gzip-compressed much less)
  </behavior>
  <action>
    Run these in order:

    ```bash
    cd client && npx tsc --noEmit 2>&1 | tail -10
    cd client && npx vitest run 2>&1 | tail -10
    node scripts/check-translations.cjs 2>&1 | tail -5
    cd client && npx vite build 2>&1 | tail -20
    cd client && ls -la dist/assets/index-*.js 2>&1 | tail -5
    cd client && gzip -c dist/assets/index-*.js | wc -c
    ```

    Capture for Task 2:
    - tsc result (errors? expected 0)
    - vitest counts: passed / failed / skipped
    - check-translations parity result + key count per locale
    - vite build success + warnings
    - main bundle gzip size in bytes → convert to KB: bytes / 1024

    Compare gzip size against Phase 26 baseline 209.17 KB:
    - Delta = current_kb - 209.17
    - Expected: 0 < delta < 12 KB (modest because contacts is DOM + pure-engine; no new Phaser textures, no new images, just JS)

    If any check fails, note the failure but DO NOT block Task 2 (Task 3 human-verify can decide). Record in SMOKE_TEST_27.md notes section.

    No commit in this task — measurement only.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -5 ; echo "---vitest---" ; cd client && npx vitest run 2>&1 | tail -5 ; echo "---i18n---" ; node scripts/check-translations.cjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tsc exits 0 (no `error TS` lines)
    - vitest: pre-existing failures documented; new pendingEngine.test.ts shows `Test Files  1 passed` for that file
    - check-translations.cjs reports parity clean (0 missing, 0 extra)
    - vite build exits 0 (no error messages, no chunk-size warnings beyond Phase 26 baseline)
    - main bundle gzip size captured (numeric value recorded for Task 2)
  </acceptance_criteria>
  <done>
    All build chain commands logged. Bundle delta captured. Ready for SMOKE_TEST_27 authorship.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write SMOKE_TEST_27.md (6 scenarios A-F)</name>
  <read_first>
    - client/SMOKE_TEST_26.md (Phase 26 SMOKE — exact structure to mirror)
    - .planning/phases/27-contacts-messages-relationships/27-CONTEXT.md
  </read_first>
  <files>client/SMOKE_TEST_27.md (NEW)</files>
  <behavior>
    - 6 numbered scenarios A-F (after Phase 26's 7 scenarios A-G; Phase 27 = simpler surface, 6 sufficient)
    - Each scenario: clear goal + ordered checklist items + dev helper callouts
    - Sections: Prerequisites / A. Tab visibility / B. Contacts list / C. Race detail navigation / D. Reply UX (msg/dialog/quest_hook) / E. Event toast / F. Persistence + DEV helpers
    - Final sections: i18n parity / build chain / regression sanity / bundle delta record
    - Total length ≥120 lines (Phase 26 SMOKE is ~110 lines; comparable surface)
  </behavior>
  <action>
    Create `client/SMOKE_TEST_27.md` with this structure (verbatim):

    ```markdown
    # Phase 27: Contacts + Messages + Relationships — Manual Smoke Test

    **Phase:** 27-contacts-messages-relationships
    **Date:** 2026-05-18+
    **Tester:** _________

    Run order: A → B → C → D → E → F. Каждое scenario независимо в смысле state setup (используйте dev helpers для reset между scenarios).

    ## Prerequisites

    - [ ] `npm run dev` running, app loaded в browser
    - [ ] DEV console available (`window.__listRaces`, `__addPending`, etc. present)
    - [ ] Player state с cosmos unlocked (`useGameStore.getState().hasCosmosUnlocked === true`)
      - Если не unlocked: trigger через L18+L18 merge OR `useGameStore.setState({hasCosmosUnlocked: true})`
    - [ ] Reset Phase 27 state: `__resetRelationships()` → relationships=2, chainProgress=0, pendingItems=[]

    ## Scenario A: Tab visibility + cosmos gate

    **Goal:** «Контакты» 📡 tab appears after cosmos unlock; hidden before.

    - [ ] Open Cosmic Hub (post-cosmos) → see 7 tabs: 🚀 / 🏭 / 📖 / 🐸 / 🛒 / 🎒 / 📡
    - [ ] 7th tab label = «Контакты» (RU) / "Contacts" (EN) / "Contactos" (ES); switch locale via settings to verify
    - [ ] Reset cosmos: `useGameStore.setState({hasCosmosUnlocked: false}); window.location.reload()`
    - [ ] Open Cosmic Hub → see lock screen (Phase 22-06 pattern) — НЕТ tab strip visible
    - [ ] Restore: `useGameStore.setState({hasCosmosUnlocked: true})`

    ## Scenario B: Contacts list view

    **Goal:** List shows 10 races with relationship indicators.

    - [ ] `__resetRelationships()` — start fresh
    - [ ] Open Cosmic Hub → 📡 → see 10 race rows in canonical order (crystalloids first, cometfolk last)
    - [ ] Each row shows: race emoji + name + tier label OR "—" + tier badge с value/10 OR "?"
    - [ ] До first contact все races dimmed (opacity 0.6) + tier='—' + badge='?'
    - [ ] Header: «Очередь: 0/3» (pending count)
    - [ ] `__markFirstContact('crystalloids')` (Phase 26 helper) → reload OR `__resetRelationships()` again then `__addPending('crystalloids')` → crystalloids row gets pink unread dot + opacity 1 + tier badge color
    - [ ] All other rows still dimmed

    ## Scenario C: Race detail navigation

    **Goal:** Tap row → detail view; back arrow returns to list.

    - [ ] Tap crystalloids row → tab content swaps to detail view (NOT modal)
    - [ ] Detail view shows: ← back arrow + 💎 emoji + «Кристаллозиды» name in header
    - [ ] Lore card visible: «Силикасос» home planet (gold), «Холодные, мудрые, медленные» personality (italic), lore_short paragraph
    - [ ] RelationshipBar visible: tier badge «враждебный» (red — tier=hostile at value=2) + filled portion ~20% + numeric "2 / 10"
    - [ ] Если pending exists: pending interaction card shown below bar
    - [ ] Tap ← back arrow → returns to list view, scrolls/state preserved

    ## Scenario D: Reply UX (msg / dialog / quest_hook)

    **Goal:** Each pending item type renders correct buttons + applies delta correctly.

    **D1. msg (item.type === 'msg'):**
    - [ ] `__resetRelationships()`; `__addPending('crystalloids')` — chain item 0 = msg
    - [ ] Open contacts → crystalloids row has unread dot → tap → detail shows msg text
    - [ ] Single «Понятно» button visible (pink CTA mini)
    - [ ] Tap «Понятно» → item disappears, relationship unchanged (still 2), chainProgress crystalloids → 1
    - [ ] Engine auto-pulls next item (chain item 1 also = msg) → another msg appears

    **D2. dialog (item.type === 'dialog'):**
    - [ ] `__resetRelationships()`; `__advanceChain('crystalloids')` 2 times; `__addPending('crystalloids')` — chain item 2 = dialog
    - [ ] Detail shows dialog text + TWO buttons: «Отказать (-1)» (neutral grey) and «Поддержать (+1)» (pink CTA)
    - [ ] Tap «Поддержать» → relationship +1 (2 → 3, tier transitions hostile → cool), bar fills more, tier badge updates AND pulses (CSS keyframe contacts-tier-pulse 800ms)
    - [ ] Engine auto-pulls next item (chain item 3 = msg)

    **D3. quest_hook (item.type === 'quest_hook'):**
    - [ ] `__resetRelationships()`; `__advanceChain('crystalloids')` 5 times; `__addPending('crystalloids')` — chain item 5 = quest_hook
    - [ ] Detail shows quest_hook text + «Поддержать (+1)» / «Отказать (-1)» buttons + GOLD italic «Запрос принят. Детали скоро прояснятся.» hint below text (cosmic_hub.contacts.quest_stub)
    - [ ] Tap «Поддержать» → relationship +1, item resolves, next pull
    - [ ] Verify quest_id reserved for Phase 28: `useGameStore.getState().pendingItems` — after resolve, this quest_hook item gone; nothing else affected

    **D4. empty state:**
    - [ ] `__advanceChain('crystalloids')` until chainProgress=10 (chain end)
    - [ ] Open crystalloids detail → no pending interaction → «Все сообщения прочитаны» empty state shown

    ## Scenario E: Event toast (inline event auto-apply)

    **Goal:** Step 6 of every chain is inline event — auto-applies delta + fires toast banner.

    - [ ] `__resetRelationships()`; `__markFirstContact('crystalloids')` (Phase 26) OR `__addPending('crystalloids')`
    - [ ] `__advanceChain('crystalloids')` 6 times (now at chain item 6 = event target=self delta=-1)
    - [ ] `__addPending('crystalloids')` triggers pull → engine processes event automatically
    - [ ] TOAST BANNER appears at top of screen: 💎 emoji + RU text «Кристаллозиды кристаллический резонанс затронул их сны: -1 к отношениям» + red -1 value
    - [ ] After ~3 seconds toast fades out (CSS keyframe contacts-toast-fade) and disappears
    - [ ] `useGameStore.getState().raceRelationships.crystalloids` decremented by 1 (clamped to RELATIONSHIP_MIN=1 if already at floor)
    - [ ] chainProgress.crystalloids = 7 (advanced past event)
    - [ ] Engine auto-pulled chain item 7 → pendingItems contains new msg/dialog item

    **E2. Multiple events queue (max 3 visible):**
    - [ ] Rapid: `__resetRelationships()`; for each of 5 races, run `__markFirstContact(...)` then `__advanceChain(raceId)` 6 times then `__addPending(raceId)`
    - [ ] Up to 3 toasts stacked vertically at top-center
    - [ ] 4th and 5th events: oldest fades, newer appears at bottom of stack (FIFO trim)
    - [ ] Toast z-index 150 — appears ABOVE Cosmic Hub (100) and Star Map (50)

    ## Scenario F: Persistence + DEV helpers

    **Goal:** State survives reload; DEV helpers work.

    - [ ] `__addPending('crystalloids')` → check `useGameStore.getState().pendingItems.length === 1`
    - [ ] `__advanceChain('fireworms')` 2 times → check chainProgress.fireworms === 2
    - [ ] Reply to one pending: tap «Поддержать» on crystalloids dialog → relationship increments to 3
    - [ ] **Reload browser** (full page refresh)
    - [ ] After reload: `useGameStore.getState().raceRelationships.crystalloids === 3` (persisted via localStorage cosmic blob)
    - [ ] `useGameStore.getState().chainProgress.fireworms === 2` (persisted)
    - [ ] `useGameStore.getState().pendingItems.length` matches pre-reload (modulo new pulls triggered after reload)
    - [ ] `__dumpContacts()` → console.table shows all 10 races with current state
    - [ ] Server sync: open Network tab → next PUT /game/state includes cosmic.raceRelationships / chainProgress / pendingItems
    - [ ] DEV helpers tree-shake check: `npx vite build` → grep -c "__addPending" dist/assets/index-*.js returns 0 (helpers excluded from production bundle)

    ## i18n parity

    - [ ] `node scripts/check-translations.cjs` → 0 missing, 0 extra (parity preserved)
    - [ ] Switch locale RU → all cosmic_hub.contacts.* + cosmos.event.* + races.<id>.chain.<N>.* render in Russian
    - [ ] Switch locale EN → same in English ("Contacts", "Support", "Refuse", "OK", tier names hostile/cool/neutral/friendly/ally, race chain texts)
    - [ ] Switch locale ES → same in Spanish ("Contactos", "Apoyar", "Rechazar", etc.)
    - [ ] Key count per locale: 402 (Phase 26 baseline) + 15 (Plan 27-01) + 105 (Plan 27-02) ≈ 522 keys per locale

    ## Build chain

    - [ ] `cd client && npx tsc --noEmit` clean (0 errors)
    - [ ] `cd client && npx vitest run` — passing (modulo 3 pre-existing Phase 22 failures documented in deferred-items.md; new pendingEngine.test.ts: 11 PASS)
    - [ ] `cd client && npx vite build` — success, no chunk-size warnings beyond Phase 26 baseline
    - [ ] Bundle delta gzip main: ______ KB recorded (baseline Phase 26: 209.17 KB; expected delta ~+5-8 KB)

    ## Regression sanity (no breakage of prior phases)

    - [ ] Phase 26 First Contact flow: tap habitable planet → cinematic + modal play (still works after engine integration)
    - [ ] Phase 26 Inventory tab: open Hub → 🎒 → 4 sections render unchanged (relationships placeholder still shows "?" — Phase 27 contacts is separate tab, doesn't replace inventory placeholder)
    - [ ] Phase 25 CosmicHub restyle: pink underline + dark cosmic shell unchanged
    - [ ] Phase 24 Captain Birth flow: L18+L18 merge → cinematic + modal still works
    - [ ] Phase 22 Cosmic Shop tab: open Hub → 🛒 → items rendered с essence/serum balances
    - [ ] Phase 18 Bestiary tab: open → 4 locations × virtualized grids visible

    ---

    **Tester signature / date:** _________
    ```

    Commit message: `docs(27-06): SMOKE_TEST_27.md (6 scenarios A-F + i18n + build chain + regression sanity)`
  </action>
  <verify>
    <automated>test -f client/SMOKE_TEST_27.md && wc -l client/SMOKE_TEST_27.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/SMOKE_TEST_27.md` exits 0
    - `wc -l client/SMOKE_TEST_27.md` reports ≥120
    - `grep -c "## Scenario [A-F]:" client/SMOKE_TEST_27.md` returns 6
    - `grep -c "i18n parity\|Build chain\|Regression sanity" client/SMOKE_TEST_27.md` returns at least 3
    - File contains literal "Phase 27" string at least 3 times
    - All checkbox items use `- [ ]` (uncheckable initially) — `grep -c '\- \[ \]' client/SMOKE_TEST_27.md` returns ≥30
  </acceptance_criteria>
  <done>
    SMOKE_TEST_27.md authored with 6 scenarios + i18n + build chain + regression sanity. ≥120 lines. Atomic commit pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human approval after smoke test</name>
  <what-built>
    Phase 27 ships all 5 implementation plans (27-01..05):
    - Plan 27-01: foundation types + state + persistence + i18n skeleton
    - Plan 27-02: race chain data (10 × 10 items) + i18n texts (RU/EN/ES parity)
    - Plan 27-03: pure pendingEngine + slice actions + 2 eventBus events + dev helpers
    - Plan 27-04: ContactsTab + RaceDetailView + RelationshipBar + 7th tab wiring
    - Plan 27-05: EventToast + EventToastController + App.tsx wiring
  </what-built>
  <how-to-verify>
    Open `client/SMOKE_TEST_27.md` and execute scenarios A through F sequentially. Minimum critical scenarios for approval:

    1. **Scenario A** (Tab visibility + cosmos gate) — 7th tab appears post-cosmos, hidden pre-cosmos
    2. **Scenario C** (Race detail navigation) — tap row → detail view + back arrow returns to list
    3. **Scenario D2-D3** (dialog + quest_hook reply UX) — buttons render correct delta annotations + apply to relationship + tier badge pulses on tier change
    4. **Scenario E** (Event toast) — auto-applied event delta + top-screen toast banner with auto-dismiss
    5. **Scenario F** (Persistence) — state survives reload + server sync includes new cosmic fields
    6. **Regression sanity** — Phase 26 first contact flow still works (no breakage)

    If issues:
    - Tab not visible → recheck CosmicHubModal TABS array + sessionStorage validation + cosmosUnlocked gate
    - Pending interaction buttons wrong delta → recheck Plan 27-02 RACE_CHAINS data accept_delta/refuse_delta values
    - Toast not appearing → recheck eventBus subscription in eventToastController + Plan 27-03 emit on contacts:event-applied
    - tier badge doesn't pulse → recheck RelationshipBar useEffect for relationship-delta subscription
    - Build fails → recheck imports + TypeScript errors per plan tsc verify steps
  </how-to-verify>
  <resume-signal>Type "approved" if all critical scenarios passed, OR describe issues for revision</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: ROADMAP.md + STATE.md finalize</name>
  <read_first>
    - .planning/ROADMAP.md (find Phase 27 entry near line 704)
    - .planning/STATE.md (Phase Progress table + frontmatter progress totals)
    - .planning/phases/26-cosmos-races-foundation/26-06-PLAN.md (Task 4 pattern reference)
    - .planning/phases/27-contacts-messages-relationships/27-0?-SUMMARY.md (verify all 5 exist)
  </read_first>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <behavior>
    - ROADMAP.md Phase 27 entry updated: TBD replaced with 25 REQ-IDs, Plans count + list, outcome paragraph appended, Last updated line updated
    - STATE.md frontmatter: current_phase: 27 (complete); completed_phases incremented; total_plans + completed_plans incremented; last_updated bumped
    - STATE.md Phase Progress table: new row for Phase 27 with concise multi-line summary
    - Per-plan SUMMARY existence verified
  </behavior>
  <action>
    **A. Verify per-plan SUMMARYs exist:**
    ```bash
    ls .planning/phases/27-contacts-messages-relationships/27-0?-SUMMARY.md
    ```
    Expected: 5 files (27-01..05). If any missing — flag in commit message but do not block finalize.

    **B. Update `.planning/ROADMAP.md` Phase 27 entry:**

    Find `### Phase 27: Contacts + Messages + Relationships foundation` (around line 704). Replace section:
    - `**Requirements**: TBD (resolved при /gsd-plan-phase 27)` → with full list:
      ```
      **Requirements**: PHASE27-CONTACTS-TAB, PHASE27-CONTACTS-LIST, PHASE27-RACE-DETAIL, PHASE27-RELATIONSHIP-STATE, PHASE27-RELATIONSHIP-TIERS, PHASE27-CHAIN-CONFIG, PHASE27-CHAIN-DATA-10-RACES, PHASE27-PENDING-ENGINE, PHASE27-PENDING-CAP-3, PHASE27-REPLY-UX, PHASE27-EVENT-INLINE, PHASE27-TOAST-SYSTEM, PHASE27-QUEST-HOOK-STUB, PHASE27-PERSISTENCE, PHASE27-SERVER-SYNC, PHASE27-I18N-RU, PHASE27-I18N-EN, PHASE27-I18N-ES, PHASE27-I18N-PARITY, PHASE27-COSMOS-GATE, PHASE27-FIRST-CONTACT-DEP, PHASE27-DEV-HELPERS, PHASE27-CLICLABILITY, PHASE27-SMOKE, PHASE27-FINALIZE
      ```
      (25 REQ-IDs)
    - `**Plans:** TBD plans` → `**Plans:** 6 plans`
    - Replace Plans block (currently `- [ ] TBD (run /gsd-plan-phase 27 to break down)`) with:
      ```
      Plans:
      - [x] 27-01-PLAN.md — Foundation types + state + persistence + i18n skeleton
      - [x] 27-02-PLAN.md — Race chain data (10 races × 10 ChainItem) + i18n texts (RU/EN/ES parity)
      - [x] 27-03-PLAN.md — Pure pendingEngine + slice actions + 2 eventBus contacts:* events + dev helpers
      - [x] 27-04-PLAN.md — ContactsTab + RaceDetailView + RelationshipBar + 7th tab wiring
      - [x] 27-05-PLAN.md — EventToast + EventToastController + App.tsx wiring
      - [x] 27-06-PLAN.md — SMOKE_TEST_27 (6 scenarios) + ROADMAP/STATE finalize
      ```
    - Append outcome paragraph immediately AFTER the Plans block, BEFORE the `---` separator:
      ```
      **Outcome:** Relationship-driven contacts foundation для multi-phase космической экспансии. 7-я tab «Контакты» 📡 в Cosmic Hub (cosmos-gated, наследует Phase 22-06 modal-level gate) с list view 10 рас (emoji + name + tier badge + pink unread dot if pending) + race detail view (back arrow + lore card с home planet/personality/lore_short + RelationshipBar 1-10 с 5 tiers через TIER_COLORS + pending interaction renderer для msg/dialog/quest_hook). RACE_CHAINS data 10 рас × 10 ChainItem each (40 msg + 30 dialog + 20 quest_hook + 10 event) с уникальным narrative tone matching personality (Огнечервы aggressive demands, Кристаллозиды patient geometric, Кометники cheerful traveling). Pure pendingEngineTick (deterministic side-effect-free; lowest-progress-first pull с alphabetical tiebreak; cap CHAIN_PENDING_CAP=3; event ChainItems auto-apply delta + emit toast NOT push to inbox; firstContactsSeen + cosmosUnlocked gate) + 11 vitest unit tests. cosmic slice extended raceRelationships/chainProgress/pendingItems (INITIAL_RELATIONSHIP=2 low threshold; defensive load clamps to [1,10]) + 4 actions (resolveAccept/Refuse/Acknowledge/triggerPendingPull) + 2 typed eventBus events ('contacts:relationship-delta' для tier pulse + 'contacts:event-applied' для toast subscription). EventToast top-center stack (max 3 visible, auto-dismiss 3s, CSS keyframes contacts-toast-slide + contacts-toast-fade, z-index 150 между Cosmic Hub 100 и modal 200, без Lottie memory feedback_animations). Persistence + server sync routes new fields через cosmic blob (snapshotForSave + loadGameState hydrate + defensive load). DEV helpers __addPending/__resetRelationships/__advanceChain/__dumpContacts (DEV-gated через import.meta.env.DEV — Vite tree-shake в production). RelationshipBar pulse на tier change через 'contacts:relationship-delta' subscription. i18n RU/EN/ES parity 402 (Phase 26) → ~522 keys × 3 locales (+15 contacts.* + 105 chain texts + 5 event descriptions + 1 cosmos.event.notification template). Quest hooks = STUB (accept = +1 relationship + cosmic_hub.contacts.quest_stub hint; quest_id field reserved для Phase 28 wiring). NO Lottie (CSS keyframes для tier pulse + toast slide/fade), НЕ trogает frog.container.alpha (DOM-only), cliclability checklist (type='button' + touchAction: manipulation + stopPropagation + z-index hierarchy preserved). Bundle delta gzip main _____ KB (Phase 26 baseline 209.17 KB; cap ~+10 KB). SMOKE_TEST_27.md 6 scenarios A-F + i18n + build chain + regression sanity. 25/25 ✓ REQ-IDs (PHASE27-*).
      ```
      Replace `_____ KB` with the actual measured value from Task 1.
    - Update final line `**Last updated:** 2026-05-18 — Phase 27 added ...` to `**Last updated:** 2026-05-18 — Phase 27 complete (6 plans, contacts + relationships foundation, _____ KB gzip delta)`

    **C. Update `.planning/STATE.md`:**

    Frontmatter:
    - `current_phase: 26 (complete)` → `current_phase: 27 (complete); Phase 20 (Pre-release safety net) deferred до prod-релиза`
    - `last_updated: "2026-05-18T11:00:00.000Z"` → bump to current timestamp (e.g. `"2026-05-18T15:00:00.000Z"`)
    - `progress.total_phases: 15` → 16 (add Phase 27 to count)
    - `progress.completed_phases: 5` → 6
    - `progress.total_plans: 42` → 48 (42 + 6 new plans)
    - `progress.completed_plans: 34` → 40 (34 + 6 new completed)
    - `progress.percent: 81` → recalculate: 40/48 = 83 (round)

    Phase Progress table — add new row AFTER existing Phase 26 row (between Phase 26 row and the next section heading `## v1.0 Achievement Summary`):
    ```
    | 27 | Contacts + Messages + Relationships foundation | **complete** (2026-05-18) — 6 plans, ~22h scope; 7-я tab «Контакты» 📡 в Cosmic Hub (cosmos-gated, наследует Phase 22-06 modal-level gate) с list view 10 рас + race detail (back arrow + lore + RelationshipBar + pending interaction); RACE_CHAINS 10 рас × 10 ChainItem (40 msg + 30 dialog + 20 quest_hook + 10 event); pure pendingEngineTick (lowest-progress-first pull, cap=3, event auto-apply, cosmos + firstContact gate) + 11 vitest; cosmic slice +3 fields (raceRelationships INITIAL=2 / chainProgress / pendingItems) + 4 actions (resolveAccept/Refuse/Acknowledge/triggerPendingPull); 2 typed eventBus events (contacts:relationship-delta + contacts:event-applied); EventToast top-center stack (max 3, auto-dismiss 3s, CSS keyframes, z-index 150); persistence + server sync через cosmic blob (defensive load clamps relationship to [1,10]); DEV helpers __addPending/__resetRelationships/__advanceChain/__dumpContacts; RelationshipBar pulse на tier change; quest hooks STUB для Phase 28 (cosmic_hub.contacts.quest_stub hint); SMOKE_TEST_27.md 6 scenarios A-F; bundle delta gzip main _____ KB (Phase 26 baseline 209.17 KB); i18n RU/EN/ES parity 402 → ~522 keys × 3 locales (+15 cosmic_hub.contacts.* + 105 race chain texts + 5 cosmos.event.* + 1 notification template); никакого Lottie (CSS keyframes для tier pulse + toast slide/fade); НЕ trogает frog.container.alpha (DOM-only); cliclability checklist (type='button' + touchAction: manipulation + stopPropagation). 25/25 ✓ REQ-IDs (PHASE27-CONTACTS-TAB, PHASE27-CONTACTS-LIST, PHASE27-RACE-DETAIL, PHASE27-RELATIONSHIP-STATE, PHASE27-RELATIONSHIP-TIERS, PHASE27-CHAIN-CONFIG, PHASE27-CHAIN-DATA-10-RACES, PHASE27-PENDING-ENGINE, PHASE27-PENDING-CAP-3, PHASE27-REPLY-UX, PHASE27-EVENT-INLINE, PHASE27-TOAST-SYSTEM, PHASE27-QUEST-HOOK-STUB, PHASE27-PERSISTENCE, PHASE27-SERVER-SYNC, PHASE27-I18N-RU/EN/ES/PARITY, PHASE27-COSMOS-GATE, PHASE27-FIRST-CONTACT-DEP, PHASE27-DEV-HELPERS, PHASE27-CLICLABILITY, PHASE27-SMOKE, PHASE27-FINALIZE). |
    ```
    Replace `_____ KB` with the actual measured value from Task 1.

    Also update the lead-in text near the top of STATE.md:
    - `**Status:** Complete — Phase 26 closed ...` → `**Status:** Complete — Phase 27 closed (Contacts + Messages + Relationships foundation — 7-я tab в Cosmic Hub, 10 race chains, relationship 1-10 с 5 tiers, pending engine cap=3, inline event toasts, quest hook stub). All 25 PHASE27-* REQ-IDs covered.`
    - `**Current Phase:** 26 (complete) ...` → `**Current Phase:** 27 (complete); Phase 20 (Pre-release safety net) deferred до prod-релиза`
    - `**Last Updated:** 2026-05-18` (keep same date)

    **D. Validate YAML parse-ability after edits:**
    ```bash
    python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])" && echo "YAML OK"
    ```
    Expected: `YAML OK` — frontmatter still valid.

    **E. Commit:**
    ```
    git add .planning/ROADMAP.md .planning/STATE.md
    git commit -m "docs(27-06): finalize Phase 27 contacts + messages + relationships foundation"
    ```
  </action>
  <verify>
    <automated>grep -n "Phase 27.*complete" .planning/STATE.md | head -3 ; echo "---" ; grep -n "27-06-PLAN.md" .planning/ROADMAP.md | head -3 ; echo "---" ; python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])" && echo "YAML OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 27.*complete" .planning/STATE.md` returns at least 1
    - `grep -c "27-06-PLAN.md" .planning/ROADMAP.md` returns 1 (Plans block updated)
    - `grep -c "PHASE27-CONTACTS-TAB" .planning/ROADMAP.md` returns at least 1 (REQ-IDs filled in)
    - `grep -c "current_phase: 27" .planning/STATE.md` returns 1
    - `grep -c "completed_phases: 6" .planning/STATE.md` returns 1
    - `python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` exits 0 (YAML valid)
    - git log shows commit with `docs(27-06): finalize Phase 27` message
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 27 entry finalized with 25 REQ-IDs + 6 plans + outcome paragraph. STATE.md frontmatter current_phase=27 complete + Phase Progress row added + progress totals updated. YAML still parses cleanly. Bundle delta recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual smoke test trust | Tester may miss visual edge cases — mitigation: 6 explicit scenarios + checkbox list |
| ROADMAP/STATE.md frontmatter | Edit may break GSD planning tooling if YAML syntax broken |
| Bundle delta accuracy | Recorded value influences future budget planning — must reflect actual gzip output |
| Per-plan SUMMARY existence | Plans 01-05 must each produce SUMMARY.md (their <output> sections require it); finalize task verifies presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Repudiation | Phase shipped without verified smoke | mitigate | Task 3 checkpoint blocks ROADMAP update until human approval |
| T-27-06-02 | Tampering | STATE.md frontmatter broken YAML syntax | mitigate | Validate YAML parse-ability post-edit via python3 yaml.safe_load |
| T-27-06-03 | Info Disclosure | Outcome paragraph contains incorrect bundle metric | accept | Recorded during Task 1 verification, not fabricated |
| T-27-06-04 | Repudiation | Per-plan SUMMARY missing for one of 01-05 plans | mitigate | Task 4A verification step lists files; missing flagged in commit message |
</threat_model>

<verification>
- `npm run check-translations` (or `node scripts/check-translations.cjs`) clean
- `cd client && npx tsc --noEmit` clean
- `cd client && npx vitest run` — green modulo 3 pre-existing Phase 22 failures
- `cd client && npx vite build` success
- `client/SMOKE_TEST_27.md` exists, ≥120 lines, 6 scenarios A-F + i18n + build chain + regression sanity
- `python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` exits 0
- All 5 per-plan SUMMARYs (27-01..05) present
- Git log shows: 1 smoke test commit + 1 finalize commit (+ optional human-approved revision commits if Task 3 triggered re-work)
</verification>

<success_criteria>
- SMOKE_TEST_27.md authored (6 scenarios + i18n + build chain + regression sanity)
- Human approval obtained via Task 3 checkpoint
- ROADMAP.md Phase 27 entry: 25 REQ-IDs + 6 plans + outcome paragraph
- STATE.md current_phase=27 complete + Phase Progress row + progress totals
- Bundle delta gzip main recorded
- YAML still parses
- 2 atomic commits (smoke test + finalize)
</success_criteria>

<output>
After completion, create `.planning/phases/27-contacts-messages-relationships/27-06-SUMMARY.md` per templates/summary.md format, including:
- SMOKE_TEST_27 scenarios summary table
- Build chain results (tsc / vitest / check-translations / vite build / gzip)
- Bundle delta vs Phase 26 baseline (209.17 KB)
- i18n key count delta (Phase 26 baseline 402 → Phase 27 ~522)
- ROADMAP + STATE finalization diff summary
- Self-check: file existence + commit hashes + REQ-ID coverage 25/25
</output>
