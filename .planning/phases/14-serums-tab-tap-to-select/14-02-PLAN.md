---
phase: 14-serums-tab-tap-to-select
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/CosmicHub/SerumsTab.tsx
  - client/src/components/CosmicHub/ElementGrid.tsx
  - client/src/components/CosmicHub/CosmicHubModal.tsx
autonomous: true
requirements:
  - SERUM-02
  - SERUM-03
  - UX-07

must_haves:
  truths:
    - "SerumsTab показывает 4 секции (common/rare/epic/legendary), каждая с 16 cells по элементам"
    - "Каждый cell отображает count из serums[element][rarity]; count===0 → визуально faded; count>0 → tappable"
    - "Tap на cell с count>0 → emit 'cosmic:select-serum' event + setSerumDragActive(true, payload) + close CosmicHubModal"
    - "При serums.все=0 → placeholder 'Нет сывороток' (Phase 15 produce их)"
    - "ElementGrid — переиспользуемый компонент: принимает rarity + counts, рендерит 16-cell grid"
  artifacts:
    - path: "client/src/components/CosmicHub/SerumsTab.tsx"
      provides: "4-section serum inventory UI с tap handler"
      contains: "section_common.*section_rare.*section_epic.*section_legendary"
    - path: "client/src/components/CosmicHub/ElementGrid.tsx"
      provides: "Переиспользуемый 16-cell grid для одной rarity-секции"
      exports:
        - "ElementGrid"
  key_links:
    - from: "SerumsTab.tsx"
      to: "useGameStore"
      via: "select(s => s.serums)"
      pattern: "useGameStore.*serums"
    - from: "SerumsTab.tsx"
      to: "eventBus"
      via: "emit('cosmic:select-serum', { element, rarity })"
      pattern: "cosmic:select-serum"
    - from: "SerumsTab.tsx"
      to: "CosmicHubModal closeHandler"
      via: "prop onClose passed by CosmicHubModal или через event"
      pattern: "onClose|setOpen"
---

<objective>
SerumsTab UI: 4 секции по rarity, в каждой grid из 16 элементов с count badges.
Tap на cell с count>0 — entry point в selection mode (Plan 14-03 подхватывает
event и активирует MainScene flow).

Purpose: Юзер видит инвентарь и инициирует apply flow через нативный mobile-first
тап-интерфейс (SERUM-02, SERUM-03 entry).

Output: 1 new component (ElementGrid) + 1 rewrite (SerumsTab) + 1 small modify
(CosmicHubModal — pass closeHandler / extend event types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-serums-tab-tap-to-select/PLAN.md
@.planning/REQUIREMENTS.md
@client/src/components/CosmicHub/SerumsTab.tsx
@client/src/components/CosmicHub/CosmicHubModal.tsx
@client/src/store/eventBus.ts
@client/src/i18n/ru.json
@client/src/utils/telegram.ts

<interfaces>
<!-- existing eventBus types — нужно extend -->

From client/src/store/eventBus.ts:
```typescript
type Events = {
  // ...
  'cosmic:toast': CosmicToastPayload
  // NEW в этом плане:
  'cosmic:select-serum': { element: Element; rarity: Rarity }
}
```

From client/src/store/cosmic/types.ts:
```typescript
export type Element = 'fire' | 'ice' | ... (16 элементов)
export type Rarity = 'common' | 'rare' | 'epic' | 'legendary'
export const ELEMENTS: readonly Element[]   // 16 элементов
export const RARITIES: readonly Rarity[]    // 4 редкости
```

From client/src/utils/telegram.ts:
```typescript
export function hapticImpact(style: 'light'|'medium'|'heavy'|'rigid'|'soft'): void
export function hapticNotification(type: 'error'|'success'|'warning'): void
```

From client/src/game/effects/elements/elementTints.ts (если экспортирует tint
table): использовать для colour cell'ов. Если нет — inline TINT_TABLE из
REQUIREMENTS.md строки 80-97.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend eventBus with 'cosmic:select-serum' event</name>
  <files>client/src/store/eventBus.ts, client/src/store/cosmic/types.ts</files>
  <behavior>
    - eventBus.emit('cosmic:select-serum', { element: 'fire', rarity: 'common' }) → typed без any
    - eventBus.on('cosmic:select-serum', handler) → handler получает { element, rarity }
    - Type CosmicToastPayload расширен полем type: 'serum-applied' | 'serum-mistap' | (existing)
  </behavior>
  <action>
    1. В `client/src/store/eventBus.ts` добавить в `Events`:
       ```ts
       'cosmic:select-serum': { element: Element; rarity: Rarity }
       'cosmic:cancel-serum': void  // для cancel button или back-tap
       ```
       — добавить import `Element, Rarity` из `./cosmic/types`.

    2. В `client/src/store/cosmic/types.ts` расширить `CosmicToastPayload.type`:
       ```ts
       export interface CosmicToastPayload {
         type: 'scout-returned' | 'box-received' | 'mission-complete'
             | 'serum-applied' | 'serum-mistap' | 'generic'
         msg: string
         action?: { label: string; onClick: () => void }
         count?: number
         duration?: number  // optional override (default 4000ms)
       }
       ```
       — добавить поле `duration?: number` для SERUM-10 (undo toast 4с)
       — App.tsx subscriber в Plan 14-04 будет honor этот override.
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error|cosmic:select-serum" ; echo "exit=$?"</automated>
  </verify>
  <done>
    Events типизированы; CosmicToastPayload расширен. tsc clean.
    Commit: `phase-14: add cosmic:select-serum event + extend toast types`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ElementGrid component (reusable per rarity)</name>
  <files>client/src/components/CosmicHub/ElementGrid.tsx</files>
  <behavior>
    - Render: 16 cells (4×4 grid) — по одному cell на каждый из ELEMENTS
    - Каждый cell показывает: element icon (emoji или tint dot) + count badge
    - count===0 → cell faded (opacity 0.3), не tappable (cursor: default, no onClick fire)
    - count>0 → cell active, tappable, hover/active states
    - При tap на active cell → onSelect(element, rarity) callback
    - Cell tint берётся из TINT TABLE (locked в REQUIREMENTS.md)
  </behavior>
  <action>
    Создать `client/src/components/CosmicHub/ElementGrid.tsx`:

    ```tsx
    import { useTranslation } from 'react-i18next'
    import { ELEMENTS, type Element, type Rarity } from '../../store/cosmic/types'
    import { hapticImpact } from '../../utils/telegram'

    /** TINT TABLE из REQUIREMENTS.md (locked palette, colorblind-safe). */
    const ELEMENT_TINT: Record<Element, string> = {
      fire: '#fb923c',
      ice: '#a5f3fc',
      water: '#38bdf8',
      forest: '#4ade80',
      toxic: '#86efac',
      plasma: '#fde047',
      shadow: '#6b7280',
      crystal: '#ddd6fe',
      desert: '#fde68a',
      gas: '#fdba74',
      ring: '#c4b5fd',
      binary: '#fca5a5',
      arcane: '#a78bfa',
      mechanical: '#fde68a',
      war: '#dc2626',
      void: '#1f2937',
    }

    interface Props {
      rarity: Rarity
      counts: Record<Element, number>  // serums[element][rarity] для всех 16 elements
      onSelect: (element: Element, rarity: Rarity) => void
    }

    export function ElementGrid({ rarity, counts, onSelect }: Props) {
      const { t } = useTranslation()
      return (
        <div className="grid grid-cols-4 gap-2">
          {ELEMENTS.map((el) => {
            const count = counts[el] ?? 0
            const active = count > 0
            return (
              <button
                key={el}
                disabled={!active}
                onClick={() => {
                  if (!active) return
                  hapticImpact('light')
                  onSelect(el, rarity)
                }}
                className={
                  `relative aspect-square rounded-lg border-2 flex items-center justify-center
                   ${active
                     ? 'border-white/40 bg-white/5 hover:bg-white/10 active:scale-95 transition'
                     : 'border-white/10 bg-white/5 opacity-30 cursor-default'
                  }`
                }
                style={{ borderColor: active ? ELEMENT_TINT[el] : undefined }}
                aria-label={t(`cosmic_hub.elements.${el}`)}
                title={t(`cosmic_hub.elements.${el}`)}
              >
                {/* Tint dot — основной visual; emoji не используется чтобы избежать font issues */}
                <div
                  className="w-6 h-6 rounded-full"
                  style={{ backgroundColor: ELEMENT_TINT[el] }}
                />
                {count > 0 && (
                  <span className="absolute -top-1 -right-1 bg-emerald-500 text-white
                                 text-xs font-bold rounded-full min-w-[1.25rem] h-5
                                 flex items-center justify-center px-1">
                    {count > 99 ? '99+' : count}
                  </span>
                )}
              </button>
            )
          })}
        </div>
      )
    }
    ```

    Не импортировать i18n keys, которых нет — `cosmic_hub.elements.fire` уже
    определены в Phase 11 (см. ru.json:163-180).

    **Бюджетный contract:** ≤ 80 строк tsx; чистая визуальная функция; никаких
    эффектов или субскрайберов.
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error|ElementGrid" ; echo "exit=$?"</automated>
    Manual: после Task 3 (SerumsTab использует ElementGrid) → открыть Cosmic Hub
    → таб «Сыворотки» → видеть 4 grid'а из 16 cells каждый.
  </verify>
  <done>
    ElementGrid.tsx экспортирует Props + component; tsc clean; visual в Storybook
    not required (нет setup) — verified через SerumsTab integration в Task 3.
    Commit: `phase-14: add ElementGrid reusable component`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite SerumsTab with 4 sections + tap-to-select integration</name>
  <files>client/src/components/CosmicHub/SerumsTab.tsx, client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <behavior>
    - SerumsTab subscribes на serums Record через useGameStore
    - Rendered 4 секции в порядке legendary → epic → rare → common (показ "лучшее наверху")
    - Если total count === 0 → placeholder с emoji + текст "Сыворотки появятся после открытия боксов"
    - Если total count > 0 → 4 секции; пустая секция (e.g. legendary count=0) рендерится — но collapsed visual: header с "0/16 элементов открыто" мелким текстом
    - Tap на cell → emit 'cosmic:select-serum' + setSerumDragActive(true, payload) + close CosmicHubModal
    - CosmicHubModal принимает onClose — передан в SerumsTab через context или prop
    - Section header показывает rarity name (RU: "Легендарные / Эпические / Редкие / Обычные") в i18n
  </behavior>
  <action>
    1. **SerumsTab.tsx** — full rewrite:

    ```tsx
    import { useTranslation } from 'react-i18next'
    import { useGameStore } from '../../store/gameStore'
    import { eventBus } from '../../store/eventBus'
    import { ELEMENTS, RARITIES, type Element, type Rarity } from '../../store/cosmic/types'
    import { ElementGrid } from './ElementGrid'

    interface Props {
      onClose: () => void  // from CosmicHubModal — закрыть на select
    }

    export function SerumsTab({ onClose }: Props) {
      const { t } = useTranslation()
      const serums = useGameStore((s) => s.serums)
      const setSerumDragActive = useGameStore((s) => s.setSerumDragActive)

      // total count для placeholder check
      const totalCount = RARITIES.reduce((sum, r) => {
        return sum + ELEMENTS.reduce((es, e) => es + (serums[e][r] ?? 0), 0)
      }, 0)

      const handleSelect = (element: Element, rarity: Rarity) => {
        // Atomic: store + event. Order matters — store сначала, event потом
        // (MainScene subscriber читает state из store, event — fallback uplink).
        setSerumDragActive(true, { element, rarity })
        eventBus.emit('cosmic:select-serum', { element, rarity })
        onClose()  // закрываем modal — юзер должен видеть ферму
      }

      if (totalCount === 0) {
        return (
          <div className="flex flex-col items-center justify-center h-full gap-3 text-white/60">
            <div className="text-4xl">🧪</div>
            <p className="text-sm text-center px-6">
              {t('cosmic_hub.serums_empty')}
            </p>
          </div>
        )
      }

      // Order: legendary → epic → rare → common (best first)
      const orderedRarities: Rarity[] = ['legendary', 'epic', 'rare', 'common']

      return (
        <div className="flex flex-col gap-4 px-3 py-3 overflow-y-auto h-full">
          {orderedRarities.map((rarity) => {
            // Build counts Record<Element, number> для этой rarity
            const counts = {} as Record<Element, number>
            for (const el of ELEMENTS) counts[el] = serums[el][rarity] ?? 0
            const sectionTotal = ELEMENTS.reduce((s, el) => s + counts[el], 0)
            const discoveredCount = ELEMENTS.filter((el) => counts[el] > 0).length

            return (
              <section key={rarity} className="flex flex-col gap-2">
                <header className="flex items-baseline justify-between">
                  <h3 className={`text-sm font-bold ${rarityColorClass(rarity)}`}>
                    {t(`cosmic_hub.serums.section_${rarity}`)}
                  </h3>
                  <span className="text-xs text-white/40">
                    {sectionTotal > 0
                      ? t('cosmic_hub.serums.section_count', { count: sectionTotal, kinds: discoveredCount })
                      : t('cosmic_hub.serums.section_empty')}
                  </span>
                </header>
                <ElementGrid rarity={rarity} counts={counts} onSelect={handleSelect} />
              </section>
            )
          })}
        </div>
      )
    }

    function rarityColorClass(r: Rarity): string {
      switch (r) {
        case 'legendary': return 'text-yellow-300'
        case 'epic': return 'text-purple-300'
        case 'rare': return 'text-blue-300'
        case 'common': return 'text-white/80'
      }
    }
    ```

    2. **CosmicHubModal.tsx** — изменить вызов SerumsTab чтобы передать
       `onClose`. Прочитать существующий файл, найти место где `<SerumsTab />`
       рендерится, заменить на `<SerumsTab onClose={() => setOpen(false)} />`
       (или эквивалент — там должна быть state для open/close).

       Если CosmicHubModal не имеет очевидного onClose handler — найти в
       Phase 11 BottomBar / App.tsx где открывается modal, прокинуть handler
       вниз через prop или context.

    3. **i18n strings** — НЕ добавляются в этом task'е (см. Plan 14-04 task i18n).
       Пока что использовать text fallback через `t(key, { defaultValue: 'Common' })`
       или временные строки прямо в коде с TODO комментарием:
       ```tsx
       // TODO Plan 14-04: replace with t() i18n keys
       ```

       **Альтернатива (предпочтительно):** добавить минимальные keys в этом
       task'е (только что нужно для отрисовки) в ru.json/en.json/es.json:
       ```json
       "cosmic_hub": {
         "serums_empty": "Сыворотки появятся после открытия боксов",
         "serums": {
           "section_common": "Обычные",
           "section_rare": "Редкие",
           "section_epic": "Эпические",
           "section_legendary": "Легендарные",
           "section_count": "{{kinds}} видов · {{count}} шт",
           "section_empty": "—"
         }
       }
       ```
       Английский / испанский — placeholder идентичный по shape; финал в Plan 14-04.
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error|SerumsTab|ElementGrid" ; echo "exit=$?"</automated>
    Manual:
    1. `cd client && npm run dev` → открыть приложение
    2. Открыть DevTools → `useGameStore.getState().addSerum('fire', 'common', 3)`
    3. Тап на 🧬 (Cosmic Hub) → таб «Сыворотки» → видеть 4 секции, в "Обычные"
       видеть fire cell с badge "3"
    4. Тап на fire cell → modal закрывается + `useGameStore.getState().serumDragActive === true`
       + `useGameStore.getState().selectedSerum === { element: 'fire', rarity: 'common' }`
    5. Тап на cell с count=0 → ничего не происходит (visual disabled).
  </verify>
  <done>
    SerumsTab показывает 4 секции; tap на active cell закрывает modal + ставит
    serumDragActive=true. Empty state placeholder работает. tsc + build clean.
    Commit: `phase-14: rewrite SerumsTab with 4 rarity sections + tap select`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Описание |
|----------|----------|
| React render → store mutation | `setSerumDragActive` вызывается из click handler |
| eventBus emit → MainScene subscriber | Нелишнее emit'нуть тысячу раз |

## STRIDE Threat Register

| ID | Категория | Component | Disposition | Mitigation |
|----|-----------|-----------|-------------|------------|
| T-14-02-01 | DoS | Spam-tap на cell → race conditions | mitigate | `setSerumDragActive(true)` идемпотентный (повторный set не делает ничего деструктивного); первый emit закрывает modal, повторные тапы blocked unmounted component |
| T-14-02-02 | Tampering | `serums[el][rarity]` показан как огромное число (e.g. 1e9) | mitigate | UI отрисует `{count > 99 ? '99+' : count}` — clamps на отображение |
| T-14-02-03 | Validation | `onSelect(element, rarity)` invoked с invalid Element | accept | TypeScript prevents at compile; runtime — useGameStore action additionally валидирует через ELEMENTS list (existing in slice) |
</threat_model>

<verification>
1. `cd client && npm run typecheck` → 0 errors
2. `cd client && npm run build 2>&1 | tail -5` → success
3. Bundle delta ≤ +6 KB gzip vs Phase 13 baseline (UI компонент + 16-cell grid x4)
4. Manual smoke (выше в Task 3)
5. tap на disabled cell — НЕ closes modal, НЕ обновляет store
</verification>

<success_criteria>
1. ElementGrid.tsx + SerumsTab.tsx render корректно
2. Tap на active cell → setSerumDragActive(true, payload) + emit + onClose()
3. Tap на disabled cell → no-op
4. Empty state placeholder при total count === 0
5. 4 секции в порядке legendary → epic → rare → common
6. tsc + build clean
7. 3 commits — `phase-14: <action>`
</success_criteria>

<output>
`.planning/phases/14-serums-tab-tap-to-select/14-02-SUMMARY.md`:
- REQ-IDs: SERUM-02 ✓, SERUM-03 entry ✓ (apply в Plan 14-03)
- Bundle delta gzip
- New components count
- Open: i18n полный набор → Plan 14-04
</output>
