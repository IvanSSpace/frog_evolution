---
phase: 14-serums-tab-tap-to-select
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - client/src/game/scenes/MainScene.ts
  - client/src/game/effects/SerumSelectionLayer.ts
autonomous: true
requirements:
  - SERUM-03
  - SERUM-05
  - SERUM-06
  - SERUM-07
  - SERUM-09
  - UX-07

must_haves:
  truths:
    - "MainScene subscribed на serumDragActive — на true → halo на eligible frogs + pause magnet/merge"
    - "Tap на eligible frog (когда selection active) → 2-сек pulse + applySerum + close selection"
    - "Tap на ineligible frog (когда selection active) → red outline pulse + error toast + selection остаётся active"
    - "Tap в пустое место (когда selection active) → cancel selection (clear)"
    - "Magnet НЕ спавнится во время selection mode"
    - "Merge НЕ выполняется ни через tap-merge, ни через drag-drop во время selection mode"
    - "На scene shutdown — SerumSelectionLayer cleanup (kill tweens + destroy graphics)"
  artifacts:
    - path: "client/src/game/effects/SerumSelectionLayer.ts"
      provides: "Класс что управляет зелёными halo вокруг eligible frogs + red outline pulse + cleanup"
      exports:
        - "SerumSelectionLayer"
    - path: "client/src/game/scenes/MainScene.ts"
      provides: "Integration: subscribe serumDragActive, override frog tap, pause magnet/merge"
      contains: "serumDragActive"
  key_links:
    - from: "MainScene"
      to: "useGameStore.subscribe"
      via: "watch state.serumDragActive + state.selectedSerum"
      pattern: "useGameStore\\.subscribe"
    - from: "MainScene.onFrogTapped"
      to: "applySerum action OR misTap toast"
      via: "guard if (serumDragActive) → handleSerumTap"
      pattern: "serumDragActive"
    - from: "SerumSelectionLayer"
      to: "frog containers"
      via: "Phaser.Graphics added to frog.container, depth -1"
      pattern: "container\\.add\\(graphics\\)"
---

<objective>
Реализовать selection mode в Phaser scene: реактивная подсветка eligible
лягушек, override tap handler для apply/mis-tap, auto-pause магнита/мерджа.
Это центральный plan фазы — он связывает store (Plan 14-01) и UI (Plan 14-02)
с реальной игровой механикой.

Purpose: SERUM-03 (tap-to-select primary apply), SERUM-05 (drop zone glow),
SERUM-06 (auto-pause), SERUM-07 (mis-tap toast), SERUM-09 (2s pulse), UX-07
(haptics на ключевых действиях).

Output: 1 new effects helper (SerumSelectionLayer) + 1 modify (MainScene).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-serums-tab-tap-to-select/PLAN.md
@.planning/phases/14-serums-tab-tap-to-select/14-01-PLAN.md
@.planning/phases/14-serums-tab-tap-to-select/14-02-PLAN.md
@.planning/REQUIREMENTS.md
@client/src/game/scenes/MainScene.ts
@client/src/game/effects/FrogOverlayManager.ts
@client/src/utils/serumEligibility.ts
@client/src/utils/telegram.ts

<interfaces>
<!-- Готовые contracts после 14-01 + 14-02 -->

From client/src/store/cosmic/types.ts (после 14-01):
```typescript
export interface CosmicSlice {
  serums: Record<Element, Record<Rarity, number>>
  carriers: CarrierData[]
  serumDragActive: boolean              // NEW
  selectedSerum: { element: Element; rarity: Rarity } | null  // NEW
}
```

From client/src/store/cosmic/slice.ts (после 14-01):
```typescript
setSerumDragActive: (active: boolean, payload?: { element: Element; rarity: Rarity } | null) => void
applySerum: (frogId: string, element: Element, rarity: Rarity, level: number) => void
addCarrier: (carrier: CarrierData) => void   // existing — нужно для undo
removeCarrier: (frogId: string) => void      // existing — нужно для undo
```

From client/src/utils/serumEligibility.ts (создан в 14-01):
```typescript
export const RARITY_TO_STARTING_LEVEL: Record<Rarity, number>
export function isEligible(frog: FrogLike, element: Element, rarity: Rarity, carriers: ReadonlyArray<CarrierData>): boolean
export function getEligibilityHint(rarity: Rarity): { level: number; locationId: number }
```

From client/src/store/eventBus.ts (после 14-02):
```typescript
'cosmic:select-serum': { element: Element; rarity: Rarity }
'cosmic:cancel-serum': void
'cosmic:toast': CosmicToastPayload
```

From client/src/game/effects/elements/burstEffect.ts (Phase 13):
```typescript
export function burstEffect(scene: Phaser.Scene, container: Phaser.GameObjects.Container, element: Element): void
// 200-400ms self-cleaning visual blast
```

From client/src/utils/telegram.ts:
```typescript
hapticImpact('light'|'medium'|'heavy')
hapticNotification('error'|'success')
```

From MainScene FrogData interface (line 64):
```typescript
interface FrogData {
  container: Phaser.GameObjects.Container
  body: Phaser.GameObjects.Image
  level: number
  id: string
  isDragging: boolean
  isMerging: boolean
  isAttracted: boolean
  // ...
}
```

From MainScene magnet logic (lines 1721-1739): magnet spawn в update() if
`location.magnetEnabled && magnetLevel > 0 && store.magnetEnabled`. Мы добавим
`&& !store.serumDragActive` гард.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SerumSelectionLayer (halo + red outline + cleanup)</name>
  <files>client/src/game/effects/SerumSelectionLayer.ts</files>
  <behavior>
    - new SerumSelectionLayer(scene) → готов к использованию, без активных tweens
    - .show(eligibleFrogs: FrogLike[]) → для каждого frog добавить зелёный halo Graphics в frog.container с pulse tween (yoyo, repeat -1)
    - .hide() → killTweensOf + destroy всех halo Graphics, clean state
    - .flashRed(frog: FrogLike) → 200ms red outline pulse в этом frog (НЕ постоянный halo, одноразовый flash)
    - .dispose() — full cleanup на scene shutdown
    - Idempotent: повторный show() сначала hide() предыдущий
  </behavior>
  <action>
    Создать `client/src/game/effects/SerumSelectionLayer.ts`:

    ```ts
    // Phase 14: визуальный layer для tap-to-select serum flow.
    // Управляет:
    //  - зелёные halo вокруг eligible frogs (pulse, repeat -1)
    //  - one-shot red flash вокруг ineligible frogs (mis-tap, 200ms)
    //  - cleanup всего на hide() / dispose() (REQ INFRA-06)
    //
    // НЕ subscribed на store — manager Plan 14-03 (MainScene) вызывает методы
    // явно. Это упрощает testability (pure visual API).

    import Phaser from 'phaser'

    export interface FrogLike {
      id: string
      container: Phaser.GameObjects.Container
      body: Phaser.GameObjects.Image
    }

    interface HaloEntry {
      frogId: string
      graphics: Phaser.GameObjects.Graphics
      tween: Phaser.Tweens.Tween | null
    }

    const HALO_COLOR_GREEN = 0x4ade80   // emerald-400 (matches forest tint)
    const HALO_COLOR_RED = 0xef4444     // red-500
    const HALO_RADIUS = 38               // px @ DPR=1; масштаб через container.scale

    export class SerumSelectionLayer {
      private scene: Phaser.Scene
      private halos = new Map<string, HaloEntry>()

      constructor(scene: Phaser.Scene) {
        this.scene = scene
      }

      /** Показать halos над eligible frogs. Сначала hide() previous. */
      show(eligibleFrogs: ReadonlyArray<FrogLike>): void {
        this.hide()
        for (const f of eligibleFrogs) {
          const g = this.scene.add.graphics()
          g.lineStyle(3, HALO_COLOR_GREEN, 0.9)
          g.strokeCircle(0, 0, HALO_RADIUS)
          // Background slight fill
          g.fillStyle(HALO_COLOR_GREEN, 0.15)
          g.fillCircle(0, 0, HALO_RADIUS)
          // depth -1 относительно body, body at depth 0 inside container
          g.setDepth(-1)
          f.container.add(g)
          // Pulse: scale 1.0 ↔ 1.15, 800ms cycle
          const tw = this.scene.tweens.add({
            targets: g,
            scaleX: 1.15,
            scaleY: 1.15,
            duration: 800,
            yoyo: true,
            repeat: -1,
            ease: 'Sine.easeInOut',
          })
          this.halos.set(f.id, { frogId: f.id, graphics: g, tween: tw })
        }
      }

      /** Скрыть все halos: kill tweens + destroy graphics. */
      hide(): void {
        for (const [, entry] of this.halos) {
          if (entry.tween) {
            this.scene.tweens.remove(entry.tween)
          }
          entry.graphics.destroy()
        }
        this.halos.clear()
      }

      /** One-shot red flash на frog (mis-tap feedback, 200ms). */
      flashRed(frog: FrogLike): void {
        const g = this.scene.add.graphics()
        g.lineStyle(4, HALO_COLOR_RED, 1)
        g.strokeCircle(0, 0, HALO_RADIUS)
        g.setDepth(-1)
        frog.container.add(g)
        // Quick scale pulse + fade
        this.scene.tweens.add({
          targets: g,
          scaleX: 1.3,
          scaleY: 1.3,
          alpha: 0,
          duration: 220,
          ease: 'Cubic.easeOut',
          onComplete: () => g.destroy(),
        })
      }

      /** Сколько halos сейчас активно. Dev/test introspection. */
      get activeHaloCount(): number {
        return this.halos.size
      }

      /** Full cleanup на scene shutdown. */
      dispose(): void {
        this.hide()
      }
    }
    ```

    Не используем `compHaloFlash` (Phase 9 helper) — он принимает `AnimSys` и
    designed для StarMap planet effects (один-шот, не pulse). Для SERUM-05
    нужен `repeat: -1` pulse.
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error|SerumSelectionLayer" ; echo "exit=$?"</automated>
    Manual после Task 2 integration: открыть Cosmic Hub → tap на серум →
    видеть зелёные pulse'ующие halo вокруг подходящих лягушек. Cancel selection
    → halos исчезли.
  </verify>
  <done>
    SerumSelectionLayer.ts экспортирует класс с show/hide/flashRed/dispose. tsc
    clean. Используется в Task 2.
    Commit: `phase-14: add SerumSelectionLayer (halo + red flash)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate selection mode into MainScene</name>
  <files>client/src/game/scenes/MainScene.ts</files>
  <behavior>
    - При serumDragActive=false → MainScene ведёт себя как Phase 13 (магнит, мерж, тап = coin/burst)
    - При serumDragActive=true:
      - magnet НЕ спавнится (existing magnet, если есть, может догореть — НО isAttracted сбрасывается чтобы лягушки замерзли)
      - drag-drop merge заблокирован (drag enabled, но performMerge не зовётся)
      - tap-merge заблокирован (onFrogTapped → handleSerumTap)
      - eligible frogs имеют зелёный halo
      - tap на eligible frog → 2-сек pulse + applySerum + carriers updated → halo исчезает (через unsub) + toast
      - tap на ineligible frog → red flash + error toast + selection остаётся
      - tap на пустое место (background) → cancel selection
    - Cleanup: scene shutdown / location change → SerumSelectionLayer.dispose()
  </behavior>
  <action>
    Открыть `client/src/game/scenes/MainScene.ts` и сделать следующие правки:

    **2.1 Imports (top of file):**
    ```ts
    import { SerumSelectionLayer } from '../effects/SerumSelectionLayer'
    import { isEligible, getEligibilityHint } from '../../utils/serumEligibility'
    import { hapticNotification } from '../../utils/telegram'  // если ещё не imported
    ```

    **2.2 Поля класса:**
    После `private overlayManager: FrogOverlayManager | null = null` (line ~99) добавить:
    ```ts
    private selectionLayer: SerumSelectionLayer | null = null
    private unsubSerum: (() => void) | null = null
    private cachedSerumDragActive = false
    ```

    **2.3 В create() — после init overlayManager:**
    ```ts
    this.selectionLayer = new SerumSelectionLayer(this)

    // Subscribe на serumDragActive + selectedSerum через useGameStore.subscribe.
    // На каждый change — пересчитать eligible set + show/hide halos.
    this.unsubSerum = useGameStore.subscribe((state) => {
      const active = state.serumDragActive
      const dragChanged = active !== this.cachedSerumDragActive
      this.cachedSerumDragActive = active

      if (active) {
        const sel = state.selectedSerum
        if (!sel) {
          // active без payload — defensive, скрываем halos
          this.selectionLayer?.hide()
          return
        }
        const eligible = this.frogs.filter((f) =>
          isEligible(
            { id: f.id, level: f.level },
            sel.element,
            sel.rarity,
            state.carriers,
          ),
        )
        this.selectionLayer?.show(
          eligible.map((f) => ({ id: f.id, container: f.container, body: f.body })),
        )
      } else if (dragChanged) {
        // Just deactivated — hide halos
        this.selectionLayer?.hide()
      }
    })
    ```

    **2.4 onFrogTapped — guard в начало (line 829):**
    ```ts
    private onFrogTapped(frog: FrogData, tapX: number = frog.container.x, tapY: number = frog.container.y) {
      // Phase 14: serum selection mode переопределяет normal tap behavior.
      const state = useGameStore.getState()
      if (state.serumDragActive) {
        return this.handleSerumTap(frog)
      }
      // ...existing logic (tap-merge / coin / burstEffect)...
    }
    ```

    **2.5 Новый метод handleSerumTap:**
    ```ts
    private handleSerumTap(frog: FrogData) {
      const state = useGameStore.getState()
      const sel = state.selectedSerum
      if (!sel) {
        // Race condition fallback — clear selection
        useGameStore.getState().setSerumDragActive(false)
        return
      }

      const eligible = isEligible(
        { id: frog.id, level: frog.level },
        sel.element,
        sel.rarity,
        state.carriers,
      )

      if (!eligible) {
        // SERUM-07: mis-tap → red flash + error toast + haptic error
        this.selectionLayer?.flashRed({ id: frog.id, container: frog.container, body: frog.body })
        hapticNotification('error')
        const hint = getEligibilityHint(sel.rarity)
        eventBus.emit('cosmic:toast', {
          type: 'serum-mistap',
          msg: `Эта сыворотка работает только на L${hint.level} лягушку (локация ${hint.locationId})`,
        })
        return
        // Selection остаётся active — юзер может попробовать другую лягушку
      }

      // SERUM-09: eligible → 2-сек tween apply + carrier создан
      this.applySerumToFrog(frog, sel.element, sel.rarity)
    }

    private applySerumToFrog(frog: FrogData, element: Element, rarity: Rarity) {
      // Lock frog from tap during animation
      frog.isMerging = true  // переиспользуем флаг — все existing блоки respect его
      this.tweens.killTweensOf(frog.body)

      // 2-секундная pulse: scale 1.0 → 1.15 → 1.0 (yoyo, 1 cycle)
      this.tweens.add({
        targets: frog.body,
        scaleY: 1.15,
        scaleX: 1.15,
        duration: 1000,
        yoyo: true,
        ease: 'Sine.easeInOut',
        onComplete: () => {
          frog.isMerging = false
          // Возобновить idle
          if (!frog.isMoving) this.startIdleAnim(frog)
        },
      })

      // Burst effect at midpoint (1с) для драматичности (ELEMENT-10 reuse)
      this.time.delayedCall(1000, () => {
        if (!this.frogs.includes(frog)) return  // killed mid-anim
        burstEffect(this, frog.container, element)
      })

      // Apply atomic mutation — clears selection, decrements serum, adds carrier.
      // FrogOverlayManager (Phase 12) автоматически нарисует overlay tier.
      useGameStore.getState().applySerum(frog.id, element, rarity, frog.level)

      hapticNotification('success')

      // SERUM-10: success toast с undo action (Plan 14-04 wires real undo)
      eventBus.emit('cosmic:toast', {
        type: 'serum-applied',
        msg: 'Сыворотка применена',
        action: {
          label: 'Откатить',
          onClick: () => {
            // Plan 14-04 расширяет: removeCarrier + addSerum обратно
            useGameStore.getState().removeCarrier(frog.id)
            useGameStore.getState().addSerum(element, rarity, 1)
          },
        },
        duration: 4000,
      })
    }
    ```

    **2.6 Background tap для cancel selection — в create():**
    Найти где scene настраивает input (вероятно `this.input.on('pointerdown', ...)`). Если такой нет — добавить:
    ```ts
    this.input.on('pointerdown', (pointer: Phaser.Input.Pointer, currentlyOver: Phaser.GameObjects.GameObject[]) => {
      // Cancel selection ТОЛЬКО если tap не попал ни в одну frog body
      if (currentlyOver.length === 0 && useGameStore.getState().serumDragActive) {
        useGameStore.getState().setSerumDragActive(false)
        eventBus.emit('cosmic:cancel-serum')
      }
    })
    ```

    Если existing pointerdown handler есть — добавить ту же логику в начало
    (early-return после cancel).

    **2.7 Magnet pause в update() (line ~1721):**
    ```ts
    const location = getLocationById(store.currentLocation)
    const magnetLevel = store.upgrades.magnet
    const serumPaused = store.serumDragActive  // Phase 14
    if (!serumPaused && location.magnetEnabled && magnetLevel > 0 && store.magnetEnabled) {
      // ...existing magnet logic...
    } else {
      this.magnetSpawnMs = 0
    }
    if (this.magnets.length > 0 && !serumPaused) this.updateMagnets()
    ```

    **2.8 Drag-drop merge guard в performMerge / drag handlers:**
    Найти существующий drop-merge handler (вероятно `this.input.on('dragend', ...)` или внутри `dragend` callback в drag setup). Добавить early return:
    ```ts
    if (useGameStore.getState().serumDragActive) {
      return  // Phase 14: drop-merge заблокирован во время serum selection
    }
    ```

    Также в `findMergeTarget` ничего не меняем — он pure. Блокируем call site
    (onFrogTapped уже guarded в 2.4; drop-merge — здесь).

    **2.9 Cleanup в clearField (line ~228) и destroy():**
    ```ts
    // в clearField():
    this.selectionLayer?.dispose()
    this.selectionLayer = null
    this.unsubSerum?.()
    this.unsubSerum = null
    this.cachedSerumDragActive = false

    // в destroy():
    this.selectionLayer?.dispose()
    this.selectionLayer = null
    this.unsubSerum?.()
    this.unsubSerum = null
    ```

    Re-init `this.selectionLayer = new SerumSelectionLayer(this)` после
    `this.overlayManager = new FrogOverlayManager(...)` в location-transition
    callback (тот же блок где Phase 12 пересоздаёт overlayManager).

    Также re-subscribe на store после re-init (тот же `this.unsubSerum = useGameStore.subscribe(...)` блок).

    **2.10 Race-condition guard для frogs spawn after selection start:**
    В `spawnFrog(...)` после `this.frogs.push(frog)` добавить:
    ```ts
    // Phase 14: если selection active — пере-вычислить eligible set
    // (новая лягушка может или не может быть eligible).
    if (this.cachedSerumDragActive) {
      // Force re-render halos через subscribe trigger:
      const state = useGameStore.getState()
      if (state.serumDragActive && state.selectedSerum) {
        const sel = state.selectedSerum
        const eligible = this.frogs.filter((f) =>
          isEligible({ id: f.id, level: f.level }, sel.element, sel.rarity, state.carriers),
        )
        this.selectionLayer?.show(
          eligible.map((f) => ({ id: f.id, container: f.container, body: f.body })),
        )
      }
    }
    ```
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error" | head -10 ; echo "---"; cd client && npm run build 2>&1 | tail -10</automated>
    Manual smoke (REQ-by-REQ):
    1. **SERUM-03 + SERUM-09:** `addSerum('fire', 'common', 1)` → open Cosmic Hub
       → Сыворотки → tap fire/common cell → modal закрывается → видно зелёные
       halo на L1 frog'ах → tap на L1 frog → 2-сек pulse → carrier создан, fire
       overlay появляется (через Phase 12 manager).
    2. **SERUM-07:** во время selection → tap на L2+ frog → red flash + error
       toast → selection остаётся, halos на L1 ещё видны.
    3. **SERUM-06:** во время selection → проверить что magnet НЕ спавнится
       (magnetSpawnMs=0). Если уже был магнит — тоже paused (frogs не
       attractются).
    4. Cancel: tap в пустое место → halos исчезают, magnet работает снова.
    5. Re-tap на серум → re-show halos (idempotent).
  </verify>
  <done>
    Selection mode полностью работает: tap-to-select → halo → apply → carrier.
    Mis-tap → red flash + error. Magnet/merge paused. Cancel работает. Cleanup
    на scene shutdown. tsc + build clean. Bundle delta cumulative ≤ +12 KB.
    Commit: `phase-14: integrate selection mode into MainScene`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke verify selection mode end-to-end</name>
  <what-built>
    - SerumsTab показывает 4 секции с inventory
    - Tap на серум → ферма с зелёными halo на eligible
    - Tap на eligible frog → 2с pulse + carrier created → overlay появляется
    - Tap на ineligible → red flash + error toast
    - Magnet/merge paused во время selection
    - Cancel selection (tap в пустое место) → halos исчезают
  </what-built>
  <how-to-verify>
    Run: `cd client && npm run dev`. Open in browser.

    1. **Setup:** DevTools console:
       ```js
       useGameStore.getState().addSerum('fire', 'common', 2)
       useGameStore.getState().addSerum('water', 'rare', 1)
       ```

    2. **Common flow (SERUM-08 common→L1):**
       - Если на ферме нет L1 frog'и — спавнить через UI (купить L1 или
         dev: `__listFrogIds()` чтобы убедиться что есть L1)
       - Tap 🧬 → таб Сыворотки → видеть 2 sections: "Обычные" с fire×2 и "Редкие" с water×1
       - Tap fire/common cell → modal закрывается → ферма с зелёным halo на каждой L1 frog
       - Tap на L1 frog → 2-сек pulse → fire overlay (orange tint) появляется
       - В DevTools: `useGameStore.getState().serums.fire.common === 1` (decremented)
       - В DevTools: `useGameStore.getState().carriers.length === 1`
       - Toast "Сыворотка применена" с кнопкой "Откатить" (Plan 14-04 wire'ит full undo)

    3. **Mis-tap:**
       - Tap 🧬 → Сыворотки → tap water/rare → halos на L7 frog'ах (если есть)
       - Tap на L1 frog (НЕ L7) → red flash на L1 frog + error toast "работает только на L7" + selection остаётся (halos на L7 ещё видны)

    4. **Cancel:**
       - Tap в пустое место экрана → halos исчезают, `serumDragActive === false`

    5. **Magnet/merge pause (SERUM-06):**
       - До select: magnet спавнится при наличии mergeable pair
       - Tap серум → во время selection magnetSpawnMs=0, нет нового магнита
       - Cancel → magnet timer возобновляется

    6. **Bundle delta:**
       ```bash
       cd client && npm run build 2>&1 | grep -E "kB.*gzip" | head -5
       ```
       Сравнить с Phase 13 baseline 209.45 KB → delta ≤ +12 KB.
  </how-to-verify>
  <resume-signal>Type "approved" если все 6 пунктов работают, или describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Описание |
|----------|----------|
| store subscribe → scene render | Reactive subscribe может race с frog spawn/destroy |
| Phaser tap event → store mutation | applySerum atomic, FrogOverlayManager subscribed |
| Magnet/merge timer → guard reads store | Read-only, atomic snapshot via getState() |

## STRIDE Threat Register

| ID | Категория | Component | Disposition | Mitigation |
|----|-----------|-----------|-------------|------------|
| T-14-03-01 | Race | spawnFrog while selection active → новая frog не highlighted | mitigate | Re-show halos в spawnFrog (см. action 2.10) |
| T-14-03-02 | Race | tap на frog между applySerum mutation и subscribe trigger | mitigate | applySerum atomic single-set; isMerging флаг блокирует повторный tap |
| T-14-03-03 | Memory | halos Graphics не destroyed на location change | mitigate | clearField() вызывает selectionLayer.dispose() |
| T-14-03-04 | DoS | Spam tap на ineligible frog → много red Graphics | mitigate | flashRed создаёт self-destructing Graphics (onComplete: g.destroy); 220ms duration ⇒ max 5 на 1с |
| T-14-03-05 | Tampering | `selectedSerum` подделан (e.g. element='unknown') | mitigate | applySerum re-checks через ELEMENTS list; isEligible — pure, returns false на bad data |
| T-14-03-06 | Information | Mis-tap toast leaks "ваша лягушка слишком слабая" | accept | Сообщение полезно (UX-07 hint); single-player game |
</threat_model>

<verification>
1. tsc clean
2. vite build success, bundle delta ≤ +12 KB cumulative
3. Smoke checklist (Task 3 verify) — все 6 пунктов
4. Manual race test: open Сыворотки → tap fire/common → быстро купить новую L1
   frog в shop → новая halo появляется на ней
5. Memory: открыть selection 10 раз и cancel → DevTools Memory не растёт
   (Graphics destroyed)
</verification>

<success_criteria>
1. SerumSelectionLayer класс создан + cleanup корректный
2. MainScene subscribed на serumDragActive, реактивно показывает halos
3. handleSerumTap routes на applyToFrog или mis-tap
4. Magnet/merge guarded (`!serumDragActive`)
5. Background tap cancels selection
6. Cleanup в clearField + destroy + location transition
7. Bundle delta cumulative ≤ +12 KB gzip
8. 2 commits — `phase-14: <action>`
</success_criteria>

<output>
`.planning/phases/14-serums-tab-tap-to-select/14-03-SUMMARY.md`:
- REQ-IDs: SERUM-03 ✓, SERUM-05 (зелёный halo + red flash) ✓, SERUM-06 ✓,
  SERUM-07 ✓, SERUM-09 ✓, UX-07 (haptic error+success) ✓
- Bundle delta cumulative
- Smoke results 6/6
- Open: SERUM-10 undo full impl + SERUM-11 desktop DnD → Plan 14-04
</output>
