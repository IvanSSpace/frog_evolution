---
phase: 14-serums-tab-tap-to-select
plan: 04
type: execute
wave: 3
depends_on:
  - 14-03
files_modified:
  - client/src/App.tsx
  - client/src/game/scenes/MainScene.ts
  - client/src/components/CosmicHub/SerumsTab.tsx
  - client/src/utils/devSerums.ts
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: false
requirements:
  - SERUM-02
  - SERUM-05
  - SERUM-07
  - SERUM-10
  - SERUM-11
  - UX-07

must_haves:
  truths:
    - "Toast subscriber в App.tsx honors payload.duration override (4000ms для serum-applied)"
    - "Toast subscriber renders payload.action.onClick handler — undo работает"
    - "Desktop mouse pointer down on serum cell → ghost element follows cursor → drop on eligible frog applies, drop on empty returns serum"
    - "Mobile (touch) flow остаётся tap-to-select (drag не активируется на touchstart)"
    - "Все user-facing strings экстернализованы в i18n (RU/EN/ES)"
    - "Dev helper: window.__addSerum(element, rarity, count) tree-shaken в prod"
    - "Bundle delta ≤ +20 KB gzip vs Phase 13 baseline (209.45 KB)"
  artifacts:
    - path: "client/src/utils/devSerums.ts"
      provides: "Dev-only window.__addSerum helper (gated by import.meta.env.DEV)"
      exports:
        - "(side-effect import)"
    - path: "client/src/i18n/ru.json"
      provides: "Все Phase 14 user strings (RU)"
      contains: "serums\\.section_common.*serums\\.mis_tap_msg.*serums\\.applied"
  key_links:
    - from: "App.tsx toast handler"
      to: "payload.duration + payload.action.onClick"
      via: "AUTO_HIDE_MS = payload.duration ?? 4000"
      pattern: "duration.*payload"
    - from: "MainScene serum cell drag handler (desktop)"
      to: "applySerum при drop on eligible frog"
      via: "raw addEventListener pointermove/up на window"
      pattern: "addEventListener.*pointermove"
---

<objective>
Финальный plan фазы: undo toast полная реализация, desktop Pointer Events DnD
secondary mode (SERUM-11), dev helpers, i18n, и финальный verify (tsc + build +
smoke + bundle).

Purpose: Закрыть все оставшиеся REQ-IDs, обеспечить parity RU/EN/ES, доказать
≤ +20 KB cap.

Output: 1 dev util (devSerums.ts) + i18n updates (3 files) + 2 modify (App.tsx
toast subscriber + MainScene drag layer) + 1 modify (SerumsTab pointer-down
hook).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-serums-tab-tap-to-select/PLAN.md
@.planning/phases/14-serums-tab-tap-to-select/14-01-PLAN.md
@.planning/phases/14-serums-tab-tap-to-select/14-02-PLAN.md
@.planning/phases/14-serums-tab-tap-to-select/14-03-PLAN.md
@.planning/REQUIREMENTS.md
@client/src/App.tsx
@client/src/i18n/ru.json
@client/src/i18n/en.json
@client/src/i18n/es.json
@client/src/utils/devCarriers.ts

<interfaces>
<!-- Готовые contracts после Plan 14-03 -->

App.tsx toast handler (existing, lines 110-170):
- subscribed на 'cosmic:toast'
- AUTO_HIDE_MS = 4000 (hardcoded)
- groups events в 1с window
- ToastBanner renders { msg, action } — нужно расширить отображение action.onClick

MainScene (после 14-03):
- handleSerumTap → mis-tap или applySerumToFrog (apply emit'ит toast с undo
  action — Plan 14-03 уже добавил skeleton)

dev pattern (Phase 12+13):
- `if (import.meta.env.DEV) { window.__foo = ... }` — tree-shaken в production
- См. utils/devCarriers.ts как образец
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App.tsx toast subscriber honors payload.duration + renders action</name>
  <files>client/src/App.tsx</files>
  <behavior>
    - cosmic:toast с payload.duration=4000 → toast скрывается через 4с (не дефолтные)
    - cosmic:toast без duration → используется существующий AUTO_HIDE_MS=4000
    - cosmic:toast с payload.action → ToastBanner показывает кнопку с label, on tap вызывает action.onClick + закрывает toast
    - Grouped toast с разных duration → используется max() (длиннее побеждает) или duration первого payload (фиксируем — first wins для simplicity)
  </behavior>
  <action>
    Открыть `client/src/App.tsx` (lines 110-170 — existing handler).

    1. Прочитать существующий handler:
       ```ts
       const AUTO_HIDE_MS = 4000  // line 114 — hardcoded
       ```
    2. Заменить на:
       ```ts
       const DEFAULT_AUTO_HIDE_MS = 4000
       ```
    3. В onComplete handler (line 152 примерно):
       ```ts
       const hideMs = next.duration ?? DEFAULT_AUTO_HIDE_MS
       toastHideTimerRef.current = setTimeout(() => {
         setCosmicToast(null)
         toastHideTimerRef.current = null
       }, hideMs)
       ```
    4. ToastBanner integration: найти где `<ToastBanner ... />` рендерится (line ~226 — `setCosmicToast(null)`). Если ToastBanner уже принимает `onAction` prop — pass:
       ```tsx
       <ToastBanner
         payload={cosmicToast}
         onClose={() => setCosmicToast(null)}
         onAction={() => {
           cosmicToast?.action?.onClick()
           setCosmicToast(null)  // close on action
         }}
       />
       ```
    5. Если ToastBanner не имеет onAction — найти его файл (вероятно
       `client/src/components/ToastBanner.tsx` или похожий), добавить prop:
       ```tsx
       interface Props {
         payload: CosmicToastPayload | null
         onClose: () => void
         onAction?: () => void
       }
       // в render:
       {payload?.action && (
         <button onClick={() => onAction?.()} className="ml-2 underline text-emerald-300">
           {payload.action.label}
         </button>
       )}
       ```
       Стилизовать в соответствии с уже существующим toast styling.

    **Не ломать existing scout/box toast behavior** — они работают через
    `payload.action.onClick` уже сейчас (Phase 11 ship'ил multi-toast). Если
    ToastBanner уже корректно рендерит action — задача упрощается до
    `payload.duration ?? 4000`.
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error" | head -5</automated>
    Manual:
    1. `eventBus.emit('cosmic:toast', { type: 'generic', msg: 'test', duration: 1000 })` — toast скрывается через 1с
    2. `eventBus.emit('cosmic:toast', { type: 'generic', msg: 'test', action: { label: 'Click', onClick: () => alert('ok') } })` — кнопка работает
  </verify>
  <done>
    payload.duration honored; payload.action.onClick wires correctly. Existing
    scout-returned toast не сломан.
    Commit: `phase-14: toast subscriber honors duration + action callbacks`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: i18n strings for Phase 14 (RU + EN + ES)</name>
  <files>client/src/i18n/ru.json, client/src/i18n/en.json, client/src/i18n/es.json</files>
  <behavior>
    - Все 3 локали содержат идентичный набор ключей (parity)
    - Ключи: cosmic_hub.serums.section_{rarity}, .section_count, .section_empty,
      .empty_state, .applied, .undo_label, .undo_hint, .mis_tap_msg
    - Pluralization где нужно — i18next interpolation {{count}}, {{level}}, {{location}}
    - Валидно как JSON (no trailing commas)
  </behavior>
  <action>
    Добавить в каждый из 3 файлов под существующий блок `cosmic_hub`:

    **ru.json** (под `cosmic_hub`, добавить или extend):
    ```json
    "cosmic_hub": {
      ...existing keys...,
      "serums_empty": "Сыворотки появятся после открытия боксов",
      "serums": {
        "section_common": "Обычные",
        "section_rare": "Редкие",
        "section_epic": "Эпические",
        "section_legendary": "Легендарные",
        "section_count": "{{kinds}} видов · {{count}} шт",
        "section_empty": "—",
        "applied": "Сыворотка применена",
        "undo_label": "Откатить",
        "undo_hint": "Отменить последнее применение",
        "mis_tap_msg": "Эта сыворотка работает только на L{{level}} лягушку ({{location}})",
        "location_swamp": "Болото",
        "location_forest": "Лес",
        "location_continent": "Континент",
        "location_planet": "Планета"
      }
    }
    ```

    **en.json:**
    ```json
    "cosmic_hub": {
      ...existing,
      "serums_empty": "Serums will appear after opening boxes",
      "serums": {
        "section_common": "Common",
        "section_rare": "Rare",
        "section_epic": "Epic",
        "section_legendary": "Legendary",
        "section_count": "{{kinds}} types · {{count}} qty",
        "section_empty": "—",
        "applied": "Serum applied",
        "undo_label": "Undo",
        "undo_hint": "Revert last apply",
        "mis_tap_msg": "This serum works only on a level {{level}} frog ({{location}})",
        "location_swamp": "Swamp",
        "location_forest": "Forest",
        "location_continent": "Continent",
        "location_planet": "Planet"
      }
    }
    ```

    **es.json:**
    ```json
    "cosmic_hub": {
      ...existing,
      "serums_empty": "Los sueros aparecerán después de abrir cajas",
      "serums": {
        "section_common": "Comunes",
        "section_rare": "Raras",
        "section_epic": "Épicas",
        "section_legendary": "Legendarias",
        "section_count": "{{kinds}} tipos · {{count}} u.",
        "section_empty": "—",
        "applied": "Suero aplicado",
        "undo_label": "Deshacer",
        "undo_hint": "Revertir último uso",
        "mis_tap_msg": "Este suero solo funciona en una rana de nivel {{level}} ({{location}})",
        "location_swamp": "Pantano",
        "location_forest": "Bosque",
        "location_continent": "Continente",
        "location_planet": "Planeta"
      }
    }
    ```

    Update SerumsTab.tsx и MainScene.handleSerumTap чтобы использовать эти keys
    (`t('cosmic_hub.serums.section_common')` etc.). Заменить временные
    `'Сыворотка применена'` строки на `t('cosmic_hub.serums.applied')`.

    Для i18n в MainScene (вне React) — использовать `i18next.t(...)` напрямую
    (см. как уже сделано в Phase 12+13 — grep `i18next.t` для образца). Если
    нет precedent — emit toast с key и i18n происходит в App.tsx subscriber
    (упростить: переместить `t()` в App.tsx через payload.i18nKey + .i18nVars).

    **Альтернативный simpler путь:** в MainScene использовать i18next direct:
    ```ts
    import i18next from 'i18next'
    // ...
    const msg = i18next.t('cosmic_hub.serums.mis_tap_msg', {
      level: hint.level,
      location: i18next.t(`cosmic_hub.serums.location_${locKey}`),
    })
    ```
    где `locKey = ['swamp','forest','continent','planet'][hint.locationId-1]`.
  </action>
  <verify>
    <automated>cd client && python3 -c "import json; [json.load(open(f)) for f in ['src/i18n/ru.json','src/i18n/en.json','src/i18n/es.json']]; print('valid')"</automated>
    Manual: переключить язык в settings → видеть переведённые тексты в SerumsTab
    headers и в mis-tap toast.
  </verify>
  <done>
    Все 3 локали — валидный JSON с identical key shape. SerumsTab + MainScene
    используют t(). tsc clean.
    Commit: `phase-14: add i18n strings (RU/EN/ES)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Desktop Pointer Events DnD (SERUM-11) + Dev helper + bundle verify</name>
  <files>client/src/components/CosmicHub/SerumsTab.tsx, client/src/utils/devSerums.ts, client/src/main.tsx</files>
  <behavior>
    - **Mobile (touchstart):** flow остаётся tap-to-select (никаких ghost'ов; touchstart НЕ инициирует drag)
    - **Desktop (mousedown с button=0):** mousedown на active cell → создать DOM ghost (div.absolute.pointer-events-none), follow cursor через document pointermove
    - При dragмove: hapticImpact('medium') если cursor над eligible frog (snap radius 80px от frog.container.x/y, projected to screen coords)
    - При mouseup over eligible frog → applySerum (тот же путь что tap-to-select)
    - При mouseup в пустое место → ghost destroyed, серум возвращается (НЕ списан)
    - Detection mobile vs desktop: navigator.maxTouchPoints > 0 ИЛИ pointerType === 'touch' → skip drag, использовать только tap
    - Dev helper: `window.__addSerum(element, rarity, count=1)` доступен только в DEV build
  </behavior>
  <action>
    **3.1 Dev helper — `client/src/utils/devSerums.ts`:**

    Создать на образце `devCarriers.ts`:
    ```ts
    // Phase 14: dev-only helper для smoke testing serum apply flow.
    // Tree-shaken в prod (import.meta.env.DEV — статический бранч у Vite).

    import { useGameStore } from '../store/gameStore'
    import { ELEMENTS, RARITIES, type Element, type Rarity } from '../store/cosmic/types'

    declare global {
      interface Window {
        __addSerum?: (element?: Element, rarity?: Rarity, count?: number) => void
        __clearSerums?: () => void
        __listSerums?: () => void
      }
    }

    if (import.meta.env.DEV) {
      window.__addSerum = (element, rarity, count = 1) => {
        const e: Element = element ?? ELEMENTS[Math.floor(Math.random() * ELEMENTS.length)]
        const r: Rarity = rarity ?? 'common'
        useGameStore.getState().addSerum(e, r, count)
        console.log('[dev] serum added', { element: e, rarity: r, count })
      }

      window.__clearSerums = () => {
        const state = useGameStore.getState()
        for (const e of ELEMENTS) {
          for (const r of RARITIES) {
            const cur = state.serums[e][r]
            if (cur > 0) state.removeSerum(e, r, cur)
          }
        }
        console.log('[dev] all serums cleared')
      }

      window.__listSerums = () => {
        const state = useGameStore.getState()
        const rows: Array<{ element: string; rarity: string; count: number }> = []
        for (const e of ELEMENTS) {
          for (const r of RARITIES) {
            const c = state.serums[e][r]
            if (c > 0) rows.push({ element: e, rarity: r, count: c })
          }
        }
        if (rows.length === 0) console.log('[dev] inventory empty')
        else console.table(rows)
      }
    }
    ```

    Добавить import в `client/src/main.tsx` рядом с existing devCarriers:
    ```ts
    if (import.meta.env.DEV) {
      import('./utils/devCarriers')
      import('./utils/devSerums')  // NEW
    }
    ```
    Или если devCarriers уже imported eagerly — добавить аналогично.

    **3.2 Desktop Pointer Events DnD в SerumsTab.tsx:**

    Расширить `handleSelect` чтобы при desktop pointer-down НЕ закрывать modal
    сразу, а инициировать drag. На mouseup — либо apply (если над eligible),
    либо cancel (mobile-style fallback).

    Лучший паттерн: **отдельный handler `handlePointerDown` рядом с onClick**.
    onClick остаётся для tap-flow (mobile). pointerdown — desktop drag.

    ```tsx
    function isPointerTouch(e: React.PointerEvent): boolean {
      return e.pointerType === 'touch'
    }

    // В ElementGrid Props extend:
    interface Props {
      rarity: Rarity
      counts: Record<Element, number>
      onSelect: (element: Element, rarity: Rarity) => void
      onPointerDragStart?: (
        element: Element,
        rarity: Rarity,
        startX: number,
        startY: number,
      ) => void
    }

    // в button:
    onPointerDown={(e) => {
      if (!active) return
      if (isPointerTouch(e)) return  // mobile → use onClick path
      e.preventDefault()  // prevent text selection
      onPointerDragStart?.(el, rarity, e.clientX, e.clientY)
    }}
    onClick={() => {
      if (!active) return
      hapticImpact('light')
      onSelect(el, rarity)
    }}
    ```

    В SerumsTab — добавить handler:
    ```tsx
    const handlePointerDragStart = (
      element: Element,
      rarity: Rarity,
      x: number,
      y: number,
    ) => {
      // Создаём ghost DOM element
      const ghost = document.createElement('div')
      ghost.style.cssText = `
        position: fixed; left: ${x - 16}px; top: ${y - 16}px;
        width: 32px; height: 32px; border-radius: 50%;
        background: ${ELEMENT_TINT[element]};
        box-shadow: 0 0 12px ${ELEMENT_TINT[element]};
        pointer-events: none; z-index: 99999;
        transition: transform 80ms ease-out;
      `
      document.body.appendChild(ghost)

      // Включаем selection mode (highlights eligible frogs)
      setSerumDragActive(true, { element, rarity })
      eventBus.emit('cosmic:select-serum', { element, rarity })
      onClose()  // закрываем modal — frogs visible

      let lastHapticHover = false

      const onMove = (ev: PointerEvent) => {
        ghost.style.left = `${ev.clientX - 16}px`
        ghost.style.top = `${ev.clientY - 16}px`

        // Snap detection: pointer над eligible frog?
        // Phaser canvas виден через document.querySelector('canvas') — bounding rect
        // Hit-test через MainScene — emit event с pointer client coord, scene
        // конвертит в world coord и проверяет.
        eventBus.emit('cosmic:serum-pointer-move' as any, {  // typed extend ниже
          x: ev.clientX,
          y: ev.clientY,
        })
      }

      const onUp = (ev: PointerEvent) => {
        document.removeEventListener('pointermove', onMove)
        document.removeEventListener('pointerup', onUp)
        ghost.remove()
        // Apply / cancel handled через event:
        eventBus.emit('cosmic:serum-pointer-up' as any, {
          x: ev.clientX,
          y: ev.clientY,
        })
      }

      document.addEventListener('pointermove', onMove, { passive: true })
      document.addEventListener('pointerup', onUp, { passive: true })
    }
    ```

    **3.3 MainScene listener для pointer events:**

    В MainScene.create() добавить:
    ```ts
    // Phase 14 (SERUM-11): desktop DnD secondary mode.
    eventBus.on('cosmic:serum-pointer-move', this.onSerumPointerMove)
    eventBus.on('cosmic:serum-pointer-up', this.onSerumPointerUp)
    ```

    Implement:
    ```ts
    private onSerumPointerMove = (p: { x: number; y: number }) => {
      const state = useGameStore.getState()
      if (!state.serumDragActive || !state.selectedSerum) return
      const sel = state.selectedSerum

      // Convert client coord → world coord
      const canvas = this.game.canvas
      const rect = canvas.getBoundingClientRect()
      const cx = p.x - rect.left
      const cy = p.y - rect.top
      const worldPoint = this.cameras.main.getWorldPoint(cx, cy)

      // Find frog within snap radius (80 * DPR)
      const SNAP = 80 * DPR
      let hit: FrogData | null = null
      for (const f of this.frogs) {
        const d = Phaser.Math.Distance.Between(
          worldPoint.x, worldPoint.y,
          f.container.x, f.container.y,
        )
        if (d <= SNAP) { hit = f; break }
      }

      if (hit) {
        const eligible = isEligible(
          { id: hit.id, level: hit.level },
          sel.element, sel.rarity, state.carriers,
        )
        if (eligible && !this.lastHaptiHover) {
          hapticImpact('medium')
          this.lastHaptiHover = true
        } else if (!eligible) {
          // No haptic on invalid hover — only on actual mis-drop
        }
      } else {
        this.lastHaptiHover = false
      }
    }

    private onSerumPointerUp = (p: { x: number; y: number }) => {
      const state = useGameStore.getState()
      if (!state.serumDragActive || !state.selectedSerum) return
      const sel = state.selectedSerum

      const canvas = this.game.canvas
      const rect = canvas.getBoundingClientRect()
      const cx = p.x - rect.left
      const cy = p.y - rect.top
      const worldPoint = this.cameras.main.getWorldPoint(cx, cy)

      const SNAP = 80 * DPR
      let hit: FrogData | null = null
      for (const f of this.frogs) {
        const d = Phaser.Math.Distance.Between(
          worldPoint.x, worldPoint.y,
          f.container.x, f.container.y,
        )
        if (d <= SNAP) { hit = f; break }
      }

      if (!hit) {
        // Drop в пустое — cancel selection, серум не списан
        useGameStore.getState().setSerumDragActive(false)
        return
      }

      const eligible = isEligible(
        { id: hit.id, level: hit.level },
        sel.element, sel.rarity, state.carriers,
      )

      if (eligible) {
        this.applySerumToFrog(hit, sel.element, sel.rarity)
      } else {
        this.selectionLayer?.flashRed({ id: hit.id, container: hit.container, body: hit.body })
        hapticNotification('error')
        const hint = getEligibilityHint(sel.rarity)
        eventBus.emit('cosmic:toast', {
          type: 'serum-mistap',
          msg: i18next.t('cosmic_hub.serums.mis_tap_msg', {
            level: hint.level,
            location: i18next.t(`cosmic_hub.serums.location_${
              ['swamp','forest','continent','planet'][hint.locationId - 1]
            }`),
          }),
        })
        // Selection остаётся active (как в tap-flow)
      }

      this.lastHaptiHover = false
    }

    private lastHaptiHover = false
    ```

    Добавить eventBus types:
    ```ts
    // в client/src/store/eventBus.ts:
    'cosmic:serum-pointer-move': { x: number; y: number }
    'cosmic:serum-pointer-up': { x: number; y: number }
    ```

    Cleanup в destroy() и clearField():
    ```ts
    eventBus.off('cosmic:serum-pointer-move', this.onSerumPointerMove)
    eventBus.off('cosmic:serum-pointer-up', this.onSerumPointerUp)
    ```

    **3.4 Bundle delta verify:**
    ```bash
    cd client && npm run build 2>&1 | grep -E "kB.*gzip|index-.*\.js" | head -10
    ```
    Compare with Phase 13 baseline 209.45 KB. Final delta МУСТ ≤ +20 KB.
    Если превышает — найти biggest contributor (vite output): vendored Phaser
    plotted? Если нет — code-split SerumsTab if needed (но Phase 11 уже lazy
    CosmicHubModal, so SerumsTab внутри chunk'а который не входит в main).
  </action>
  <verify>
    <automated>cd client && npm run typecheck 2>&1 | grep -E "error" | head -5 ; echo "---"; cd client && npm run build 2>&1 | tail -15</automated>
    Manual smoke (полный flow):
    1. **Mobile-style tap-flow** (на mobile или desktop через DevTools touch
       emulation): tap-to-select → tap eligible → apply (как Plan 14-03)
    2. **Desktop drag-flow** (mouse): mousedown на active cell → ghost появляется
       → modal закрывается → halos видны → drag mouse over L1 frog → haptic
       medium (если эмулятор) → mouseup over L1 → apply (carrier created); или
       drag над L2 → mouseup → red flash + error toast; или drag в пустое →
       mouseup → cancel
    3. **Dev helper:** `window.__addSerum('water', 'rare', 5)` → DevTools видеть
       state. `window.__listSerums()` → table.
    4. **i18n:** переключить язык EN → текст в SerumsTab переключается. ES — то же.
    5. **Undo toast:** apply → toast "Сыворотка применена" с "Откатить" → tap
       Откатить → carrier удалён, серум +1, overlay исчез.
    6. **Bundle:** delta ≤ +20 KB gzip vs 209.45 KB.
  </verify>
  <done>
    Desktop DnD работает (drag → ghost → drop). Dev helper exposed in DEV.
    i18n parity. Undo wires correctly. Bundle ≤ +20 KB. tsc + build clean.
    Commit: `phase-14: add desktop DnD + dev helper + bundle verify`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 14 final acceptance — все REQ-IDs ✓</name>
  <what-built>
    Полная Phase 14: SerumsTab UI, tap-to-select primary apply, mis-tap toast,
    auto-pause magnet/merge, undo toast, desktop Pointer Events DnD secondary,
    haptics, i18n RU/EN/ES, dev helpers.
  </what-built>
  <how-to-verify>
    Run: `cd client && npm run dev`. В DevTools console:

    1. **REQ SERUM-02 — UI 4 секции:**
       - `__addSerum('fire', 'common', 2); __addSerum('water', 'rare', 1); __addSerum('plasma', 'epic', 1); __addSerum('arcane', 'legendary', 1)`
       - 🧬 → Сыворотки → 4 секции с counts. ✓

    2. **REQ SERUM-03 + SERUM-04 + SERUM-09 — tap-to-select primary:**
       - tap fire/common cell → modal закрывается + halos видны
       - tap L1 frog → 2-сек pulse → carrier created → fire overlay появляется
       - serums.fire.common decremented to 1. ✓

    3. **REQ SERUM-05 — drop zone glow + haptic:**
       - В режиме selection: видно зелёный halo вокруг каждого eligible
       - Mis-tap → red flash 220ms + haptic error
       - Desktop drag → mouseover eligible → haptic medium ✓

    4. **REQ SERUM-06 — auto-pause magnet/merge:**
       - До select: дать времени на спавн магнита (см. что magnet появляется)
       - tap серум → магнит больше не спавнится; existing magnet — лягушки не
         притягиваются (isAttracted reset)
       - cancel selection → magnet снова работает ✓

    5. **REQ SERUM-07 — mis-tap toast:**
       - selection активен → tap L2+ frog → red flash + toast "работает только
         на L1 лягушку (Болото)" ✓

    6. **REQ SERUM-08 — eligibility table:**
       - common серум → halos только на L1 (Болото)
       - rare серум → halos только на L7 (Лес)
       - epic → halos только на L13 (Континент)
       - legendary → halos только на L19 (Планета)
       - L1 frog который УЖЕ carrier → NOT eligible (no halo) ✓

    7. **REQ SERUM-10 — undo toast:**
       - apply → toast "Сыворотка применена" + "Откатить" кнопка → 4с visible
       - tap Откатить → carrier removed, serum +1, overlay disappears ✓

    8. **REQ SERUM-11 — desktop DnD secondary:**
       - mousedown на серум cell → ghost (32px coloured circle) follows cursor
       - drag over L1 → mouseup → apply
       - drag в пустое → mouseup → cancel, serum NOT consumed ✓

    9. **REQ UX-07 — haptic feedback:**
       - light при выборе серума
       - medium при hover (desktop drag) над eligible
       - error при mis-tap
       - success при apply ✓
       (визуально через `console.log` в Telegram.WebApp.HapticFeedback mock —
       Telegram WebApp в localhost devtools может ругаться, что норм)

    10. **i18n parity:**
        - Settings → Язык → EN → текст в Сыворотки на английском
        - ES → испанский ✓

    11. **Bundle:**
        - `cd client && npm run build 2>&1 | grep -E "kB.*gzip" | head -5`
        - delta vs 209.45 KB ≤ +20 KB. ✓

    12. **Cleanup / no leaks:**
        - Открыть/закрыть selection 20 раз → DevTools Memory → не растёт
        - Сменить локацию → halos исчезают, новые лягушки в новой локации
          корректно подсвечиваются если selection re-activate
  </how-to-verify>
  <resume-signal>Type "approved — phase-14 complete" если все 12 пунктов ✓, или describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Описание |
|----------|----------|
| DOM pointer events → eventBus → MainScene | Untyped pointer event payloads converted на границе |
| Toast action.onClick | User-supplied callback — могут быть side effects |
| i18n loaded from /locales | static bundle — trusted |

## STRIDE Threat Register

| ID | Категория | Component | Disposition | Mitigation |
|----|-----------|-----------|-------------|------------|
| T-14-04-01 | DoS | Spam pointer-move events (1000+/s) | mitigate | event handler читает single getState() snapshot — O(n) frogs (n ≤ 30); throttle через `passive: true` listener; haptic ratelimited через `lastHaptiHover` flag |
| T-14-04-02 | XSS | i18n strings содержат HTML | accept | i18next по умолчанию escape'ит {{vars}}; ключи прописаны нами в bundle (no user input в i18n loader) |
| T-14-04-03 | Tampering | Dev helper exposed в production | mitigate | `if (import.meta.env.DEV)` static branch — Vite tree-shakes в prod build (verified Phase 12+13) |
| T-14-04-04 | Resource leak | Pointer move/up listeners не unsub'нуты | mitigate | onUp handler removes both listeners; ghost element removed onUp |
| T-14-04-05 | Repudiation | Toast undo onClick не работает (silent fail) | mitigate | Plan 14-03 emit'ит toast с undo; Task 1 здесь wires onAction → onClick; manual smoke 5 verifies |
| T-14-04-06 | Tampering | Toast payload.duration = -1 | accept | setTimeout с negative timeout fires immediately — toast мгновенно скрывается (UX glitch, не security) |
</threat_model>

<verification>
1. tsc clean, build success
2. Bundle delta ≤ +20 KB gzip vs 209.45 KB baseline
3. Smoke checklist (Task 4) — 12/12
4. JSON validation: `python3 -c "import json; ..."` для 3 i18n файлов
5. Tree-shaking: `cd client && npm run build && grep "__addSerum" dist/assets/*.js` → 0 hits в production bundle
</verification>

<success_criteria>
1. Toast subscriber в App.tsx honors duration + action.onClick
2. desktop DnD работает (mousedown→ghost→drop→apply/cancel)
3. Dev helper `__addSerum/__clearSerums/__listSerums` доступны в DEV
4. i18n RU/EN/ES — parity, валидный JSON
5. Bundle delta ≤ +20 KB gzip
6. Все 11 REQ-IDs Phase 14 ✓ (SERUM-02..11 + UX-07)
7. 3 atomic commits — `phase-14: <action>`
</success_criteria>

<output>
`.planning/phases/14-serums-tab-tap-to-select/14-04-SUMMARY.md`:
- Все 11 REQ-IDs ✓
- Bundle delta gzip final
- Atomic commits hash list (8-9 commits across 4 plans)
- Smoke checklist 12/12
- Open: SERUM-10 undo через 4с window — sufficient на v2.0; интеграция с
  CARRIER-* в Phase 17
- Update STATE.md row 14 → "complete (date)"
</output>
