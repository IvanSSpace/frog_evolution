---
phase: 19-balance-tutorial-i18n-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/gameStore.ts
  - client/src/ui/components/SettingsModal.tsx
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - UX-04
  - UX-05
  - UX-06

must_haves:
  truths:
    - "useGameStore.settings содержит 3 новых boolean: calmFarmMode, reducedEffects, openBoxesInstantly"
    - "Все 3 default false (UX-05 default OFF — Locked Decision)"
    - "SettingsModal.SettingsTab показывает новую секцию «КОСМОС» с 3 toggles"
    - "Toggles persist в localStorage (через существующий Zustand persist)"
    - "i18n RU/EN/ES coverage для каждого toggle label"
    - "Phase 19-04 НЕ wires consumer-side для toggles — только state + UI surface"
    - "Consumer wiring отдельный todo для других places (FrogOverlayManager, awakened presets, SerumSlotMachine) — будет finalized в 19-07 audit"
  artifacts:
    - path: "client/src/store/gameStore.ts"
      provides: "settings shape расширен 3 новыми boolean полями + actions"
      contains: "calmFarmMode"
    - path: "client/src/ui/components/SettingsModal.tsx"
      provides: "Cosmic settings section с 3 toggle rows"
      contains: "calm_farm"
    - path: "client/src/i18n/ru.json"
      provides: "settings.calm_farm, settings.reduced_effects, settings.open_boxes_instantly + descriptions"
      contains: "calm_farm"
  key_links:
    - from: "SettingsModal.tsx"
      to: "useGameStore"
      via: "useGameStore(s => s.settings.calmFarmMode) + setSettings action"
      pattern: "calmFarmMode|reducedEffects|openBoxesInstantly"
    - from: "gameStore.ts"
      to: "localStorage"
      via: "Zustand persist middleware (existing)"
      pattern: "settings.*calmFarm|persist"
---

<objective>
Добавить 3 Cosmic-related Settings toggles в существующий
`SettingsModal.SettingsTab` UI:

- **UX-04 «Calm farm mode»** — отключает aura/idle particles на ферме
- **UX-05 «Reduced effects»** — clamp animation tier на dormant (default OFF — Locked)
- **UX-06 «Open boxes instantly»** — minimizes slot-machine drama до 1с

Phase 19-04 покрывает **state + UI surface only**. Consumer wiring
(`FrogOverlayManager` reads `calmFarmMode`, `awakenedPresets dispatcher` reads
`reducedEffects`, `SerumSlotMachine timing` reads `openBoxesInstantly`) —
**не часть** этого plan'а; будет verify'ено в 19-07 audit. Причина разделения:
consumer wiring требует знания Phase 12/13/15 internals (которые могут не
существовать ещё, если мы выполняем Phase 19 до полного завершения Phase 17/18 —
но в нашем случае Phase 19 идёт последним, так что consumers есть). Тем не
менее, мы держим plan focused: state + UI here, audit + wire-checks в 19-07.

Phase 19-04 запускается **в wave 1 параллельно с 19-01** потому что
файлы не overlap (gameStore.ts settings keys + SettingsModal.tsx vs
cosmic/slice.ts + rarityRoll.ts).

Purpose: Locked Decision UX-05 «Reduced effects default OFF» + новые toggles
для cosmic system перед v2.0 ship.

Output:
- gameStore settings shape расширен (3 boolean fields)
- SettingsModal с новой section «КОСМОС»
- 6+ новых i18n keys × 3 локали
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/REQUIREMENTS.md
@client/src/store/gameStore.ts
@client/src/ui/components/SettingsModal.tsx
@client/src/i18n/ru.json
@client/src/i18n/en.json
@client/src/i18n/es.json

<interfaces>
<!-- Existing settings pattern в useGameStore. -->

From SettingsModal.tsx (existing usage pattern):
```tsx
const numberFormat = useGameStore((s) => s.numberFormat)
const setNumberFormat = useGameStore((s) => s.setNumberFormat)
```

Existing SettingsRow helper:
```tsx
function SettingsRow({ label, children }: { label: string; children: ReactNode }) {
  return (
    <div className="ff-card p-3 flex items-center justify-between gap-3">
      <span className="ff-body font-bold text-emerald-900 text-sm flex-shrink-0">{label}</span>
      {children}
    </div>
  )
}
```

ON/OFF toggle pattern (existing for sound):
```tsx
<button
  onClick={() => sfx.setMuted(!sfxMuted)}
  className={`ff-btn text-xs px-3 py-1.5 ${!sfxMuted ? 'ff-btn-green' : 'ff-btn-grey'}`}
>
  {!sfxMuted ? t('player.on') : t('player.off')}
</button>
```

Locked Decisions (REQUIREMENTS.md):
- UX-04: Calm farm mode toggle exists
- UX-05: Reduced effects toggle, **default OFF**
- UX-06: Open boxes instantly toggle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useGameStore settings shape with 3 cosmic toggles + actions</name>
  <files>client/src/store/gameStore.ts</files>
  <action>
    1. Открыть `client/src/store/gameStore.ts`. Найти существующий settings shape
       (обычно flat в GameState либо вложен в state.settings). Стиль зависит от
       существующего кода:

       **Если settings — отдельный объект** (например `state.settings = { numberFormat, ... }`):
       Расширить shape:
       ```typescript
       settings: {
         numberFormat: 'full' | 'short'
         // Phase 19-04: cosmic toggles
         calmFarmMode: boolean
         reducedEffects: boolean
         openBoxesInstantly: boolean
       }
       ```

       **Если flat в GameState** (например `numberFormat` напрямую в state):
       Добавить 3 новых поля рядом + 3 setter actions:
       ```typescript
       // в GameState interface:
       calmFarmMode: boolean
       reducedEffects: boolean
       openBoxesInstantly: boolean
       setCalmFarmMode: (v: boolean) => void
       setReducedEffects: (v: boolean) => void
       setOpenBoxesInstantly: (v: boolean) => void
       ```

       Implementation actions (в createStore body):
       ```typescript
       calmFarmMode: false,
       reducedEffects: false,
       openBoxesInstantly: false,
       setCalmFarmMode: (v) => set({ calmFarmMode: v }),
       setReducedEffects: (v) => set({ reducedEffects: v }),
       setOpenBoxesInstantly: (v) => set({ openBoxesInstantly: v }),
       ```

    2. Persist: проверить что localStorage save/load logic (обычно в `partialize`
       Zustand persist либо в `loadState` функции) включает 3 новых полей. Если
       persist использует whitelist подход — добавить ключи. Если использует
       blacklist (исключает только transient) — defaults к include.

       Найти существующий persist pattern (Phase 11 уже завёл cosmic state
       persistence) и follow ту же структуру. Например:
       ```typescript
       // в saveState (или partialize):
       calmFarmMode: state.calmFarmMode,
       reducedEffects: state.reducedEffects,
       openBoxesInstantly: state.openBoxesInstantly,
       // в loadState:
       calmFarmMode: parsed.calmFarmMode ?? false,
       reducedEffects: parsed.reducedEffects ?? false,
       openBoxesInstantly: parsed.openBoxesInstantly ?? false,
       ```

       НЕ bump'ать STORAGE_VERSION — три новых **boolean defaults** не ломают
       старые saves (fallback на false через `?? false`).

    3. tsc clean: `cd client && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd client && grep -c "calmFarmMode\|reducedEffects\|openBoxesInstantly" src/store/gameStore.ts</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error\|gameStore" | head -10</automated>
  </verify>
  <done>
    - Все 3 new fields присутствуют в gameStore (≥ 6 совпадений: 3 reads + 3 setters
      или 3 declarations + 3 default values).
    - tsc clean.
    - В localStorage save/load оба места покрыты (проверить grep'ом или manual review).
    - STORAGE_VERSION НЕ bumped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cosmic settings section to SettingsModal + i18n RU/EN/ES</name>
  <files>
    client/src/ui/components/SettingsModal.tsx
    client/src/i18n/ru.json
    client/src/i18n/en.json
    client/src/i18n/es.json
  </files>
  <action>
    1. Открыть `client/src/ui/components/SettingsModal.tsx`. Найти `SettingsTab`
       function (line ~183).

    2. Добавить state hooks (рядом с existing useGameStore selectors):
       ```tsx
       const calmFarmMode = useGameStore((s) => s.calmFarmMode)
       const setCalmFarmMode = useGameStore((s) => s.setCalmFarmMode)
       const reducedEffects = useGameStore((s) => s.reducedEffects)
       const setReducedEffects = useGameStore((s) => s.setReducedEffects)
       const openBoxesInstantly = useGameStore((s) => s.openBoxesInstantly)
       const setOpenBoxesInstantly = useGameStore((s) => s.setOpenBoxesInstantly)
       ```

    3. Найти JSX return — после блока `{/* Sound effects */}` (закрытие
       `</SettingsRow>` для sounds) и перед `{/* Bug report */}`. Вставить
       новый Cosmic section:

       ```tsx
       {/* Phase 19-04: Cosmic settings (UX-04, UX-05, UX-06) */}
       <div
         className="ff-body text-xs font-bold text-center py-1 rounded mt-2"
         style={{
           background: '#7e22ce', color: '#e9d5ff', letterSpacing: '0.05em',
         }}
       >
         {t('settings.cosmic_section')}
       </div>

       <SettingsRow label={t('settings.calm_farm')}>
         <button
           onClick={() => setCalmFarmMode(!calmFarmMode)}
           className={`ff-btn text-xs px-3 py-1.5 ${calmFarmMode ? 'ff-btn-green' : 'ff-btn-grey'}`}
           title={t('settings.calm_farm_desc')}
         >
           {calmFarmMode ? t('player.on') : t('player.off')}
         </button>
       </SettingsRow>

       <SettingsRow label={t('settings.reduced_effects')}>
         <button
           onClick={() => setReducedEffects(!reducedEffects)}
           className={`ff-btn text-xs px-3 py-1.5 ${reducedEffects ? 'ff-btn-green' : 'ff-btn-grey'}`}
           title={t('settings.reduced_effects_desc')}
         >
           {reducedEffects ? t('player.on') : t('player.off')}
         </button>
       </SettingsRow>

       <SettingsRow label={t('settings.open_boxes_instantly')}>
         <button
           onClick={() => setOpenBoxesInstantly(!openBoxesInstantly)}
           className={`ff-btn text-xs px-3 py-1.5 ${openBoxesInstantly ? 'ff-btn-green' : 'ff-btn-grey'}`}
           title={t('settings.open_boxes_instantly_desc')}
         >
           {openBoxesInstantly ? t('player.on') : t('player.off')}
         </button>
       </SettingsRow>
       ```

    4. Добавить i18n keys в `client/src/i18n/ru.json` settings секцию:
       ```json
       "cosmic_section": "🧬 КОСМОС",
       "calm_farm": "Тихая ферма",
       "calm_farm_desc": "Отключает ауру и частицы вокруг лягушек",
       "reduced_effects": "Меньше эффектов",
       "reduced_effects_desc": "Упрощает анимации высоких рангов",
       "open_boxes_instantly": "Мгновенные боксы",
       "open_boxes_instantly_desc": "Сокращает анимацию слот-машины до 1с"
       ```

    5. То же в `en.json`:
       ```json
       "cosmic_section": "🧬 COSMIC",
       "calm_farm": "Calm farm mode",
       "calm_farm_desc": "Disables aura and particles around frogs",
       "reduced_effects": "Reduced effects",
       "reduced_effects_desc": "Simplifies high-tier animations",
       "open_boxes_instantly": "Open boxes instantly",
       "open_boxes_instantly_desc": "Shortens slot machine animation to 1s"
       ```

    6. То же в `es.json`:
       ```json
       "cosmic_section": "🧬 CÓSMICO",
       "calm_farm": "Granja tranquila",
       "calm_farm_desc": "Desactiva aura y partículas alrededor de las ranas",
       "reduced_effects": "Efectos reducidos",
       "reduced_effects_desc": "Simplifica animaciones de alto rango",
       "open_boxes_instantly": "Abrir cajas al instante",
       "open_boxes_instantly_desc": "Acorta la animación a 1s"
       ```

       Все 7 keys. RU использует «Тихая ферма» (краткое + понятное;
       альтернатива «Тихий режим фермы» — длиннее, плохо для mobile UI).

    7. tsc clean: `cd client && npx tsc --noEmit`.

    8. Manual verify: запуск `npm run dev` → открыть SettingsModal → Settings tab
       → видна новая фиолетовая «🧬 КОСМОС» секция с 3 toggles. Каждый toggle
       cycles между ON (зелёный) / OFF (серый). После reload — состояние сохраняется.
  </action>
  <verify>
    <automated>cd client && grep -c "cosmic_section\|calm_farm\|reduced_effects\|open_boxes_instantly" src/ui/components/SettingsModal.tsx</automated>
    <automated>cd client && python3 -c "import json; ru=json.load(open('src/i18n/ru.json')); en=json.load(open('src/i18n/en.json')); es=json.load(open('src/i18n/es.json')); keys=['cosmic_section','calm_farm','reduced_effects','open_boxes_instantly']; missing=[k for k in keys if not (k in ru.get('settings',{}) and k in en.get('settings',{}) and k in es.get('settings',{}))]; print('missing:',missing); exit(1 if missing else 0)"</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error" | head -5</automated>
  </verify>
  <done>
    - SettingsModal содержит 3 toggle rows + section header.
    - Все 7 i18n keys (cosmic_section + 3 toggle labels + 3 descriptions) присутствуют
      в RU/EN/ES.
    - tsc clean.
    - Toggle ON/OFF работает (manual smoke).
    - State persists в localStorage (manual verify через reload).
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx tsc --noEmit` — 0 errors
2. `cd client && grep -c "calmFarmMode\|reducedEffects\|openBoxesInstantly" src/store/gameStore.ts` — ≥ 6
3. `cd client && python3 -c "..."` (key check) — exit 0
4. Manual smoke:
   - Settings tab показывает Cosmic секцию (фиолетовая полоска)
   - 3 toggle rows; на старте все OFF (серые)
   - Tap toggle → переключается на ON (зелёный)
   - Reload → состояние сохранено (toggle остаётся ON)
   - i18n: switch language ru/en/es → labels меняются
</verification>

<success_criteria>
1. **State shape:** useGameStore имеет 3 boolean fields + 3 setter actions.
2. **Defaults OFF:** Все 3 поля default false (UX-05 default OFF — Locked Decision).
3. **Persist:** localStorage сохраняет values; reload восстанавливает.
4. **No version bump:** STORAGE_VERSION не меняется (additive boolean defaults).
5. **UI surface:** SettingsModal Settings tab показывает Cosmic section с 3 toggles.
6. **i18n complete:** RU/EN/ES coverage для cosmic_section + 3 toggle labels +
   3 descriptions = 7 keys × 3 locales = 21 entries.
7. **No consumer wiring:** Phase 19-04 НЕ изменяет FrogOverlayManager / awakened
   presets / SerumSlotMachine. Эти consumers будут verified в 19-07.
8. **tsc clean, build clean.**
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-04-SUMMARY.md`
с разделами:
- Что изменено: 1 store, 1 modal, 3 i18n
- Settings shape перед/после (3 boolean + 3 actions)
- 21 i18n entries (7 keys × 3 locales)
- Что НЕ закрыто: consumer wiring (calmFarm read by FrogOverlayManager и т.д.) →
  verify in 19-07 audit
- Atomic commits: `phase-19: add cosmic settings toggles state (UX-04/05/06)`,
  `phase-19: add cosmic settings UI section + i18n RU/EN/ES`
</output>
