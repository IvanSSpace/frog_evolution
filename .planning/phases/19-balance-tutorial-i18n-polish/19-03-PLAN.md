---
phase: 19-balance-tutorial-i18n-polish
plan: 03
type: execute
wave: 3
depends_on: [19-01]
files_modified:
  - client/src/components/CosmicHub/PityCounterDisplay.tsx  # NEW
  - client/src/components/CosmicHub/CosmicHubModal.tsx
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "PityCounterDisplay рендерит null когда openedBoxesCount < 3"
    - "При openedBoxesCount ∈ [3, 4] показывает «Удача растёт» + 3-dot indicator (●●○)"
    - "При openedBoxesCount ≥ 5 показывает exact: «До rare: N», «До epic: N», «До legendary: N»"
    - "Числа считаются от current pityCounters: max(0, threshold - pity[tier])"
    - "Component mount'ится в footer CosmicHubModal (видно во всех табах)"
    - "i18n keys покрыты RU/EN/ES (cosmic_hub.pity_growing, pity_rare, pity_epic, pity_legendary)"
  artifacts:
    - path: "client/src/components/CosmicHub/PityCounterDisplay.tsx"
      provides: "footer-component с progressive pity counter (UX-01)"
      min_lines: 60
    - path: "client/src/components/CosmicHub/CosmicHubModal.tsx"
      provides: "modal mount'ит PityCounterDisplay в footer slot"
      contains: "PityCounterDisplay"
    - path: "client/src/i18n/ru.json"
      provides: "новые keys cosmic_hub.pity_*"
      contains: "pity_growing"
  key_links:
    - from: "PityCounterDisplay.tsx"
      to: "useGameStore"
      via: "useGameStore(s => s.pityCounters); useGameStore(s => s.boxes)"
      pattern: "pityCounters"
    - from: "CosmicHubModal.tsx"
      to: "PityCounterDisplay.tsx"
      via: "import + JSX в footer area под TabContent"
      pattern: "PityCounterDisplay"
---

<objective>
Создать **видимый progressive pity counter** в footer CosmicHubModal,
показывающий игроку прогресс к pity guarantees согласно UX research 5.1-5.2:

- Hidden first 3 opened boxes — не overwhelming новых игроков (anti dark-pattern)
- 3-dot indicator при 3-4 opened — soft hint что нечто отслеживается
- Exact numbers при 5+ opened — full transparency (наш differentiator vs Genshin)

Почему footer: research 6.x указывает что counter должен быть actual во всех
табах (Скауты/Боксы/Сыворотки/Бестиарий) — игрок отслеживает прогресс
независимо от текущей активности.

Phase 19-03 запускается в wave 3 — после 19-01 (pity wired) и 19-04 (toggles
и settings, но не depends on UI). Независим от 19-02 (Monte Carlo) — Plan 19-02
runtime-only, не trips UI.

Purpose: Реализовать Locked Decision UX-01 (visible pity counter с progressive
reveal). Дать игроку trust + регуляцию + retention loop.

Output:
- `PityCounterDisplay.tsx` — pure presentational component
- `CosmicHubModal.tsx` — mount footer (16-32px полоска внизу)
- 4-7 новых i18n keys × 3 локали (RU/EN/ES) с уже-locked Element Names паттерном
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/phases/19-balance-tutorial-i18n-polish/19-01-PLAN.md
@.planning/research/v2.0/UX.md
@client/src/components/CosmicHub/CosmicHubModal.tsx
@client/src/store/cosmic/types.ts
@client/src/store/gameStore.ts
@client/src/i18n/ru.json
@client/src/i18n/en.json
@client/src/i18n/es.json

<interfaces>
<!-- Phase 19-01 wires pityCounters; 19-03 reads from same shape. -->

From client/src/store/cosmic/types.ts (Phase 11 + Phase 19-01):
```typescript
export interface PityCounters {
  common: number    // placeholder (always 0)
  rare: number      // 0..3 — boxes без rare+ streak
  epic: number      // 0..10 — boxes без epic+ streak
  legendary: number // 0..25 — boxes без legendary streak (hard cap 25)
}

export interface BoxData {
  id: string
  opened: boolean
  // ... rest
}
```

From useGameStore (after Phase 19-01):
```typescript
useGameStore(s => s.pityCounters)  // PityCounters
useGameStore(s => s.boxes)         // BoxData[]
```

Pity thresholds (from rarityRoll.ts + 19-01):
- rare guarantee: 3 (when pity.rare >= 3)
- epic guarantee: 10 (when pity.epic >= 10)
- legendary hard: 25 (when pity.legendary >= 25)
- legendary soft: +3% at 15, +7% at 20

UI rules (UX research 5.1-5.2):
- < 3 opened boxes: HIDDEN (return null)
- 3-4 opened: dot indicator (3 dots) showing partial progress
- ≥ 5 opened: exact «До X через N»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PityCounterDisplay.tsx component + add i18n keys</name>
  <files>
    client/src/components/CosmicHub/PityCounterDisplay.tsx
    client/src/i18n/ru.json
    client/src/i18n/en.json
    client/src/i18n/es.json
  </files>
  <action>
    1. Создать `client/src/components/CosmicHub/PityCounterDisplay.tsx`:

       ```tsx
       // Phase 19-03 (UX-01): progressive pity counter footer.
       // Mounted в CosmicHubModal footer (видно во всех табах).
       // Reveal rules (UX research 5.1-5.2):
       //   openedCount < 3 → HIDDEN
       //   3 ≤ openedCount < 5 → DotIndicator (3 dots, partial fill)
       //   openedCount ≥ 5 → exact numbers «До rare: N», «До epic: N», «До legendary: N»

       import { useTranslation } from 'react-i18next'
       import { useGameStore } from '../../store/gameStore'

       const PITY_RARE_THRESHOLD = 3
       const PITY_EPIC_THRESHOLD = 10
       const PITY_LEGENDARY_HARD = 25

       export function PityCounterDisplay() {
         const { t } = useTranslation()
         const pity = useGameStore(s => s.pityCounters)
         const openedCount = useGameStore(
           s => s.boxes.filter(b => b.opened).length,
         )

         if (openedCount < 3) return null

         if (openedCount < 5) {
           // Dot indicator (3 dots filled per progress to hard pity 25).
           // legendary 0-8 → 0 dots; 9-16 → 1; 17-24 → 2; 25 → 3.
           const dotsFilled = Math.min(3, Math.floor(pity.legendary / 9))
           const dots = '●'.repeat(dotsFilled) + '○'.repeat(3 - dotsFilled)
           return (
             <div
               className="ff-body text-xs flex items-center gap-2 px-3 py-1"
               style={{
                 background: 'rgba(0, 0, 0, 0.3)',
                 color: '#bef264',
                 borderTop: '1px solid #4d7c0f',
                 letterSpacing: 1,
               }}
               aria-label={t('cosmic_hub.pity_growing')}
             >
               <span>{t('cosmic_hub.pity_growing')}</span>
               <span style={{ letterSpacing: 3, fontSize: 14 }}>{dots}</span>
             </div>
           )
         }

         // openedCount >= 5: exact numbers
         const toRare = Math.max(0, PITY_RARE_THRESHOLD - pity.rare)
         const toEpic = Math.max(0, PITY_EPIC_THRESHOLD - pity.epic)
         const toLegendary = Math.max(0, PITY_LEGENDARY_HARD - pity.legendary)

         return (
           <div
             className="ff-body text-xs flex flex-wrap items-center gap-3 px-3 py-1"
             style={{
               background: 'rgba(0, 0, 0, 0.3)',
               color: '#bef264',
               borderTop: '1px solid #4d7c0f',
             }}
           >
             <span>{t('cosmic_hub.pity_rare', { n: toRare })}</span>
             <span>{t('cosmic_hub.pity_epic', { n: toEpic })}</span>
             <span style={{ color: '#fde047' }}>
               {t('cosmic_hub.pity_legendary', { n: toLegendary })}
             </span>
           </div>
         )
       }
       ```

    2. Добавить i18n keys в `client/src/i18n/ru.json`. Найти `cosmic_hub` секцию
       (создаётся уже в Phase 11/15/16/17/18) и добавить:
       ```json
       "pity_growing": "Удача растёт",
       "pity_rare": "До редкой: {{n}}",
       "pity_epic": "До эпической: {{n}}",
       "pity_legendary": "До легендарной: {{n}}"
       ```

    3. То же в `en.json`:
       ```json
       "pity_growing": "Luck rising",
       "pity_rare": "To rare: {{n}}",
       "pity_epic": "To epic: {{n}}",
       "pity_legendary": "To legendary: {{n}}"
       ```

    4. То же в `es.json`:
       ```json
       "pity_growing": "Suerte creciendo",
       "pity_rare": "Hasta rara: {{n}}",
       "pity_epic": "Hasta épica: {{n}}",
       "pity_legendary": "Hasta legendaria: {{n}}"
       ```

    5. tsc clean: `cd client && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error\|PityCounterDisplay" | head -10</automated>
    <automated>grep -l "pity_growing\|pity_rare\|pity_epic\|pity_legendary" client/src/i18n/ru.json client/src/i18n/en.json client/src/i18n/es.json</automated>
    <automated>wc -l client/src/components/CosmicHub/PityCounterDisplay.tsx</automated>
  </verify>
  <done>
    - File exists, ≥60 LOC.
    - tsc --noEmit clean.
    - Все 4 keys (pity_growing, pity_rare, pity_epic, pity_legendary) присутствуют
      в каждом из ru.json/en.json/es.json (3 файла × 4 keys = 12 совпадений).
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount PityCounterDisplay in CosmicHubModal footer</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <action>
    1. Открыть `client/src/components/CosmicHub/CosmicHubModal.tsx`. Найти
       структурный root (обычно root flex column с tab header → tab content).

    2. Добавить import:
       ```tsx
       import { PityCounterDisplay } from './PityCounterDisplay'
       ```

    3. Найти место **после** tab content (обычно перед closing root `</div>`)
       и добавить mount:
       ```tsx
       {/* Phase 19-03 (UX-01): progressive pity counter footer */}
       <PityCounterDisplay />
       ```

       Если CosmicHubModal использует absolute-positioned элементы вместо
       flex column — обернуть в footer-стиль div:
       ```tsx
       <div style={{ position: 'absolute', bottom: 0, left: 0, right: 0, zIndex: 5 }}>
         <PityCounterDisplay />
       </div>
       ```
       Точное позиционирование зависит от существующей разметки modal'а — выбрать
       минимально-инвазивный вариант (один div sibling под TabContent).

    4. Verify visual smoke (manual):
       ```
       cd client && npm run dev
       ```
       Открыть modal → если есть < 3 opened боксов, footer пуст. Открыть 3 бокса
       (через dev panel или mock state) → появляется dot indicator. После 5 →
       точные числа.

    5. tsc clean: `cd client && npx tsc --noEmit`.

       NOTE: Если PERF-08 уже сделал code-split (CascadeReveal/SerumSlotMachine)
       — НЕ нарушать это. PityCounterDisplay должен оставаться в основном bundle
       CosmicHubModal chunk (потому что используется во всех табах + lightweight).
  </action>
  <verify>
    <automated>cd client && grep -c "PityCounterDisplay" src/components/CosmicHub/CosmicHubModal.tsx</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error\|CosmicHubModal" | head -10</automated>
  </verify>
  <done>
    - CosmicHubModal импортирует и рендерит PityCounterDisplay.
    - tsc clean.
    - grep PityCounterDisplay в CosmicHubModal.tsx → ≥ 2 строки (import + JSX).
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx tsc --noEmit` — 0 errors
2. `cd client && npm run build` — succeeds; нет warnings про PityCounterDisplay
3. Manual smoke (dev panel mock):
   - openedCount = 0 → footer null (no DOM render)
   - openedCount = 3, pity.legendary = 0 → dot indicator «●●●○○○» pattern (filled=0)
   - openedCount = 3, pity.legendary = 12 → 1 dot filled
   - openedCount = 5, pity = {rare: 1, epic: 4, legendary: 17} → exact:
     «До редкой: 2 До эпической: 6 До легендарной: 8»
4. Visual: footer 24-28px polosha с alphabet-rendering (один-row на desktop,
   wrap на mobile <360px).
</verification>

<success_criteria>
1. **Reveal rules:**
   - openedCount < 3 → component returns null (no DOM)
   - 3 ≤ openedCount < 5 → DotIndicator showing pity.legendary progress to hard 25
   - openedCount ≥ 5 → exact «До rare/epic/legendary через N»
2. **Numbers correct:** displayed n = max(0, threshold - pity[tier]) для каждого tier.
3. **i18n complete:** RU/EN/ES все 4 keys; no missing-translation warnings в
   browser console.
4. **Footer mount:** видно на всех 4 табах CosmicHubModal (Скауты/Боксы/Сыворотки/Бестиарий).
5. **No bundle bloat:** PityCounterDisplay не добавляет > 1 KB gzip к
   CosmicHubModal chunk (lightweight presentational).
6. **tsc clean, build clean.**
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-03-SUMMARY.md`
с разделами:
- Что создано: 1 новый файл (PityCounterDisplay.tsx) + 1 modified (CosmicHubModal.tsx) + 3 i18n
- Reveal table: openedCount → отображение
- 12 i18n entries (4 keys × 3 locales) — все ключи listed
- Bundle delta для CosmicHubModal chunk (если измеримо)
- Atomic commits: `phase-19: add progressive pity counter UI (UX-01)`,
  `phase-19: i18n RU/EN/ES for pity counter`
</output>
