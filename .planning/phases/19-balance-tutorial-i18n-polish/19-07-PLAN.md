---
phase: 19-balance-tutorial-i18n-polish
plan: 07
type: execute
wave: 4
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05, 19-06]
files_modified:
  - client/scripts/check-bundle-delta.cjs  # NEW
  - client/scripts/.bundle-baseline-v1.json  # NEW
  - package.json
  - SMOKE_TEST.md
autonomous: false
requirements:
  - PERF-01
  - PERF-05
  - PERF-07

must_haves:
  truths:
    - "production build completes successfully (vite build) с 0 errors / 0 warnings"
    - "client/dist/assets содержит 1 main index-*.js + ≥ 2 separate chunks (CosmicHubModal-*, BestiaryV2Tab-* или эквивалент)"
    - "check-bundle-delta.cjs reports gzip delta vs v1.0 baseline ≤ 50 KB"
    - "baseline json file checked-in с v1.0 reference numbers"
    - "lazy chunk verification: import('./CosmicHubModal') ⇒ separate file (не в main bundle)"
    - "FPS smoke checklist в SMOKE_TEST.md обновлён (60 desktop / 50 mid-tier mobile / 30 low)"
    - "settings consumer wiring verified: calmFarmMode читается FrogOverlayManager, reducedEffects — awakened presets, openBoxesInstantly — SerumSlotMachine timing"
  artifacts:
    - path: "client/scripts/check-bundle-delta.cjs"
      provides: "production build size validator vs locked v1.0 baseline"
      min_lines: 80
    - path: "client/scripts/.bundle-baseline-v1.json"
      provides: "checked-in v1.0 main bundle gzip size baseline"
      contains: "indexJsGzip"
    - path: "package.json"
      provides: "новый script check-bundle"
      contains: "check-bundle"
    - path: "SMOKE_TEST.md"
      provides: "Phase 19 final smoke checklist (FPS, perceived perf, settings wiring)"
      contains: "Phase 19-07"
  key_links:
    - from: "package.json"
      to: "client/scripts/check-bundle-delta.cjs"
      via: "scripts.check-bundle entry"
      pattern: "check-bundle"
    - from: "client/scripts/check-bundle-delta.cjs"
      to: "client/dist/assets/"
      via: "fs.readdirSync(distDir).filter(f => /^index-.+\\.js$/.test(f))"
      pattern: "dist.*assets"
    - from: "client/scripts/check-bundle-delta.cjs"
      to: "client/scripts/.bundle-baseline-v1.json"
      via: "JSON.parse(fs.readFileSync(baseline))"
      pattern: "bundle-baseline-v1"
---

<objective>
Финальный gate Phase 19: production build + bundle audit + FPS smoke checklist.

Plan 19-07 выполняется в **wave 4** — после всех остальных Phase 19 plans
(19-01 wires pity, 19-04 adds settings, 19-05 adds tutorial — все impact на
bundle size). 19-07 проверяет что **сумма** всех изменений Phase 9..19 не
превышает Locked Decision +50 KB gzip cap vs v1.0 baseline.

Three sub-objectives:

1. **PERF-05 — Bundle delta cap +50 KB gzip vs v1.0 baseline:** create
   `check-bundle-delta.cjs` script + checked-in baseline JSON. Run script,
   verify delta ≤ 50 KB.

2. **PERF-07 — Lazy load Cosmic Hub modal verify:** check-bundle script verifies
   что `CosmicHubModal-*.js`, `BestiaryV2Tab-*.js` (если PERF-08 был выполнен)
   присутствуют как **separate chunks** (не bundled в main index).

3. **PERF-01 — FPS targets manual smoke:** не automated (требует real device
   measurement). Add SMOKE_TEST.md checklist для manual verification 60/50/30
   FPS targets.

Plus: **settings consumer wiring verification.** Phase 19-04 добавил state +
UI surface, но не wire'ил consumers. 19-07 проверяет grep'ом что три consumers
читают settings (если **не** читают — это known TODO для Phase 20 но
documented здесь).

Phase 19-07 — **autonomous: false** потому что финальный FPS check требует
human на real Android device. Остальные tasks полностью autonomous.

Output:
- check-bundle-delta.cjs script
- .bundle-baseline-v1.json (checked-in)
- npm run check-bundle
- SMOKE_TEST.md Phase 19-07 final smoke section
- Audit report: settings consumers status
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/phases/19-balance-tutorial-i18n-polish/19-04-PLAN.md
@.planning/STATE.md
@client/vite.config.ts
@package.json

<interfaces>
<!-- Phase tracking metrics из STATE.md (cumulative bundle deltas) -->

Phase tracking (STATE.md):
- Phase 11: +3.05 KB gzip (cumulative)
- Phase 12: +2.53 KB
- Phase 13: +1.58 KB
- Phase 14: +2.14 KB (main 211.59 KB)
- Phase 16: +8.20 KB (main 218.70 KB after Phase 16)
- Phase 15/17/18: pending — каждая cap'нута на ~+10-15 KB

v1.0 baseline (из MILESTONES.md): main `index.js` ~196 KB gzip (estimate).
Реальное число — confirmed снятое до Phase 9 commit (надо найти в git log
или snapshot). Если неизвестно — Phase 19-07 task 1 включает baseline
discovery step.

Settings consumers (Phase 19-04 deferred):
- `FrogOverlayManager.acquire()` — должна читать `useGameStore.getState().calmFarmMode`
  и early-return если true
- `awakenedPresets` dispatcher — должна clamp `requestedTier` на 'dormant' если
  `reducedEffects === true`
- `SerumSlotMachine` timing — total duration multiplied by ratio если
  `openBoxesInstantly === true` (legendary 9c → 1c)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish v1.0 bundle baseline + create check-bundle-delta.cjs</name>
  <files>
    client/scripts/.bundle-baseline-v1.json
    client/scripts/check-bundle-delta.cjs
    package.json
  </files>
  <action>
    1. Discover v1.0 baseline. Несколько подходов в порядке preference:

       a. **Если git log содержит Phase 9 commit с reported bundle size** (Phase 9
          STATE.md note: «bundle delta -2 bytes» — то есть Phase 9 = v1.0 baseline):
          ```bash
          cd /Users/shar/Documents/frog_evolution/frog_evolution_code
          git log --all --oneline | grep -i "phase 9\|phase-9\|v1\.0" | head -5
          ```
          Если найден v1.0 commit — checkout его в temp branch, run `npm run build`,
          measure index gzip size:
          ```bash
          git checkout <v1.0-commit> -- .
          cd client && npm run build
          # Find index-*.js in dist/assets, gzip size:
          for f in dist/assets/index-*.js; do
            gzip -c "$f" | wc -c
          done
          # Restore current state:
          git checkout HEAD -- .
          ```

       b. **Если нельзя checkout** — use STATE.md / MILESTONES.md numbers как
          best estimate:
          - Phase 14 closed: main `index.js` 211.59 KB gzip → Phase 14 baseline
          - Phase 14 - Phase 9-13 cumulative deltas (3.05 + 2.53 + 1.58 + 2.14 = 9.30 KB)
          - v1.0 baseline ≈ 211.59 - 9.30 = **~202 KB gzip** (приближение)

       c. **Если оба недоступны** — установить baseline = текущий size **до**
          Phase 19 commits (i.e. snapshot перед началом Phase 19 work). Document
          ограничение в JSON file.

    2. Создать `client/scripts/.bundle-baseline-v1.json`:
       ```json
       {
         "indexJsGzip": 202000,
         "version": "v1.0",
         "savedAt": "2026-05-08",
         "method": "estimated_from_phase_14_delta_chain",
         "note": "v1.0 main bundle baseline. Phase 14 measured 211.59 KB gzip; subtracting Phase 9-14 cumulative deltas (~9.3 KB) gives ~202 KB. If actual v1.0 commit можно checkout — refresh с real measurement.",
         "capDeltaBytes": 51200
       }
       ```
       (`capDeltaBytes` = 50 KB = 51200 bytes)

       Если нашёл реальный v1.0 number — заменить indexJsGzip + установить
       method = "measured_from_v1.0_commit".

    3. Создать `client/scripts/check-bundle-delta.cjs`:

       ```javascript
       #!/usr/bin/env node
       // Phase 19-07 (PERF-05, PERF-07): bundle delta vs v1.0 baseline + lazy chunk verify.
       //
       // Usage:
       //   1. cd client && npm run build  (сначала)
       //   2. node client/scripts/check-bundle-delta.cjs
       //
       // Exits 0 если:
       //   - delta (current - baseline) ≤ capDeltaBytes (50 KB)
       //   - expected lazy chunks (CosmicHubModal, BestiaryV2Tab) присутствуют

       const fs = require('fs')
       const path = require('path')
       const zlib = require('zlib')

       const baselinePath = path.join(__dirname, '.bundle-baseline-v1.json')
       const baseline = JSON.parse(fs.readFileSync(baselinePath, 'utf8'))

       const distDir = path.join(__dirname, '..', 'dist', 'assets')
       if (!fs.existsSync(distDir)) {
         console.error(`dist/assets not found: ${distDir}`)
         console.error('Run `cd client && npm run build` first')
         process.exit(1)
       }

       const allFiles = fs.readdirSync(distDir).filter(f => f.endsWith('.js'))
       const indexFiles = allFiles.filter(f => /^index-.+\.js$/.test(f))
       if (indexFiles.length !== 1) {
         console.error(`expected 1 index-*.js, found ${indexFiles.length}: ${indexFiles}`)
         process.exit(1)
       }

       const mainPath = path.join(distDir, indexFiles[0])
       const mainBuf = fs.readFileSync(mainPath)
       const mainGzip = zlib.gzipSync(mainBuf).length

       const otherChunks = allFiles.filter(f => !f.startsWith('index-'))
       const chunkInfo = otherChunks.map(f => {
         const buf = fs.readFileSync(path.join(distDir, f))
         return { name: f, gzip: zlib.gzipSync(buf).length }
       })

       const expectedChunks = ['CosmicHubModal', 'BestiaryV2Tab']
       const missingChunks = expectedChunks.filter(name =>
         !otherChunks.some(f => f.includes(name))
       )

       const delta = mainGzip - baseline.indexJsGzip
       const cap = baseline.capDeltaBytes ?? 50 * 1024
       const capPassed = delta <= cap

       const report = {
         baseline_v1_gzip: baseline.indexJsGzip,
         current_main_gzip: mainGzip,
         delta_bytes: delta,
         delta_kb: +(delta / 1024).toFixed(2),
         cap_bytes: cap,
         cap_kb: cap / 1024,
         cap_passed: capPassed,
         expected_lazy_chunks: expectedChunks,
         missing_chunks: missingChunks,
         actual_chunks: chunkInfo.sort((a, b) => b.gzip - a.gzip),
       }

       console.log(JSON.stringify(report, null, 2))

       const errors = []
       if (!capPassed) {
         errors.push(`Bundle delta ${report.delta_kb}KB exceeds cap ${cap / 1024}KB`)
       }
       if (missingChunks.length > 0) {
         errors.push(`Missing expected lazy chunks: ${missingChunks.join(', ')}`)
       }

       if (errors.length > 0) {
         console.error('\n❌ Bundle audit FAILED')
         for (const e of errors) console.error('  -', e)
         process.exit(1)
       }
       console.error(`\n✓ Bundle delta ${report.delta_kb}KB ≤ cap ${cap / 1024}KB`)
       console.error(`✓ Lazy chunks present: ${expectedChunks.join(', ')}`)
       ```

    4. В `package.json` добавить script:
       ```json
       "check-bundle": "cd client && npm run build && node ../client/scripts/check-bundle-delta.cjs"
       ```
       (Wait — путь относительный от `package.json` location. Если monorepo
       root содержит `package.json`, а client subdir — separate, тогда:
       ```json
       "check-bundle": "node client/scripts/check-bundle-delta.cjs"
       ```
       и call'er сам делает `npm run build` в client first.)

       Выбрать вариант который соответствует существующим scripts patterns.

    5. Run end-to-end:
       ```bash
       cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client
       npm run build
       cd ..
       node client/scripts/check-bundle-delta.cjs
       ```

       Если exit 1 — log report. Если delta превышает cap — это **blocker**.
       Возможные действия:
       - Не доделанные code-splits — push'ить тяжёлые компоненты в lazy chunks
       - Tree-shake unused exports
       - Если cap exceeded даже после optimizations — escalate как Phase 19
         decision: defer some features или request user-approved cap increase.

       Если missing chunks (CosmicHubModal не выделен) — investigate vite
       config + dynamic import patterns в App.tsx / lazy() helper. Phase 11
       уже сделал CosmicHubModal lazy через `React.lazy(() => import(...))`,
       так что должно работать.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npm run build 2>&1 | tail -10</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && node client/scripts/check-bundle-delta.cjs 2>&1 | tail -20</automated>
    <automated>grep -c "check-bundle" /Users/shar/Documents/frog_evolution/frog_evolution_code/package.json</automated>
    <automated>ls /Users/shar/Documents/frog_evolution/frog_evolution_code/client/scripts/.bundle-baseline-v1.json</automated>
  </verify>
  <done>
    - .bundle-baseline-v1.json checked-in с indexJsGzip + capDeltaBytes
    - check-bundle-delta.cjs ≥ 80 LOC
    - npm run check-bundle works
    - vite build succeeds (exit 0)
    - bundle audit exits 0 (delta ≤ cap, expected chunks present)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify settings consumer wiring (calmFarmMode, reducedEffects, openBoxesInstantly)</name>
  <files>
    SMOKE_TEST.md
  </files>
  <action>
    Phase 19-04 добавил state + UI surface для 3 toggles. Сейчас проверяем
    что **consumer side** реально читает эти flags. Если не читает — записать
    как known TODO в SMOKE_TEST.md (для Phase 20 или для пост-релиз patch).

    1. Grep settings reads:
       ```bash
       cd /Users/shar/Documents/frog_evolution/frog_evolution_code
       grep -rn "calmFarmMode" client/src --include="*.ts" --include="*.tsx"
       grep -rn "reducedEffects" client/src --include="*.ts" --include="*.tsx"
       grep -rn "openBoxesInstantly" client/src --include="*.ts" --include="*.tsx"
       ```

    2. Ожидаемые consumers:

       | Toggle              | Expected consumer location                               |
       |---------------------|----------------------------------------------------------|
       | calmFarmMode        | `client/src/game/effects/elementOverlayPool.ts` или      |
       |                     | `FrogOverlayManager` (Phase 12) acquire()                 |
       | reducedEffects      | `awakenedPresets.ts` dispatcher (Phase 13) — clamp tier   |
       | openBoxesInstantly  | `SerumSlotMachine.tsx` timing function (Phase 15)         |

    3. Для каждого toggle:

       **Если consumer читает toggle:** ✓ записать в SMOKE_TEST.md как verified.

       **Если consumer НЕ читает (или Phase 15/17 ещё не closed Phase 19-04
       тогда сразу wire'ил):** Записать в SMOKE_TEST.md как **known limitation**.
       Это НЕ blocker для Phase 19-07 — toggle UI работает (state + persistence),
       и можно patch'нуть consumer wiring отдельным commit'ом или в Phase 20.

       Phase 19-07 не требует wiring (это было бы scope creep). Только audit.

    4. Открыть `/Users/shar/Documents/frog_evolution/frog_evolution_code/SMOKE_TEST.md`
       и в Phase 19-06 audit section добавить новую subsection:

       ```markdown
       ### Phase 19-07 — Settings consumer wiring audit

       Phase 19-04 added state + UI; consumer wiring verified ниже:

       - [ ] **calmFarmMode → FrogOverlayManager:**
             `grep "calmFarmMode" client/src/game/effects/elementOverlayPool.ts` returns ≥1 line.
             Smoke: enable toggle → лягушки на ферме теряют ауру/idle particles.
             Status (auto-fill via grep): {WIRED|TODO}
       - [ ] **reducedEffects → awakened presets dispatcher:**
             `grep "reducedEffects" client/src/game/effects/elements/*.ts` returns ≥1 line.
             Smoke: enable toggle → carrier с epic+ rarity показывает только dormant tier.
             Status: {WIRED|TODO}
       - [ ] **openBoxesInstantly → SerumSlotMachine:**
             `grep "openBoxesInstantly" client/src/components/CosmicHub/*.tsx` returns ≥1 line.
             Smoke: enable toggle → opening legendary box завершается за ~1с (не 9-10c).
             Status: {WIRED|TODO}

       Если status = TODO — добавить TODO в Phase 20 PLAN или create issue для
       post-release patch.
       ```

    5. Заполнить status'ы по результату grep:
       - WIRED если grep returns ≥ 1 строку в expected file
       - TODO если grep returns 0 строк

       Documenting TODO ≠ failure Phase 19-07. Toggle persists в localStorage и
       UI работает. Wiring может быть патчем-в-час.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -rn "calmFarmMode" client/src --include="*.ts" --include="*.tsx" | wc -l</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -rn "reducedEffects" client/src --include="*.ts" --include="*.tsx" | wc -l</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -rn "openBoxesInstantly" client/src --include="*.ts" --include="*.tsx" | wc -l</automated>
    <automated>grep -c "Settings consumer wiring audit" /Users/shar/Documents/frog_evolution/frog_evolution_code/SMOKE_TEST.md</automated>
  </verify>
  <done>
    - Grep results documented в SMOKE_TEST.md.
    - Each toggle status: WIRED или TODO (with TODO entry).
    - SMOKE_TEST.md содержит «Settings consumer wiring audit» section.
    - Если все TODO — note для Phase 20 / post-release.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FPS smoke on real device + visual audit checkpoint</name>
  <files>SMOKE_TEST.md</files>
  <what-built>
    Phase 19 closed all autonomous tasks: pity wired (19-01), Monte Carlo (19-02),
    pity UI (19-03), settings toggles (19-04), tutorial overlays (19-05),
    visual+i18n audit (19-06), bundle delta verified (19-07 Task 1), settings
    consumer audit (19-07 Task 2).

    Финальный manual gate: FPS targets (PERF-01) — Locked Decision 60 desktop /
    50 mid-tier mobile / 30 minimum low-tier. Нужен real device.
  </what-built>
  <action>
    Manual checkpoint — нужен human review. Claude НЕ продолжает после этой
    задачи без явного approve. Open SMOKE_TEST.md и follow Phase 19-06 + 19-07
    audit sections; затем follow «how-to-verify» инструкции ниже.
  </action>
  <how-to-verify>
    **Open SMOKE_TEST.md и пройти Phase 19-06 + 19-07 sections:**

    1. **Two-axis visualization (UX-02):** Mounted в Cosmic Hub.
       - Открой CosmicHubModal → SerumsTab → подтверди 4 rarity sections с
         distinct shapes/borders. Внутри каждой — 16 element-tints через hue.
       - BoxesTab → подтверди element-specific particles на боксах.
       - BestiaryTab → подтверди 4 location tabs показывают rarity-styled cells.

    2. **Colorblind-safe palette (UX-03):**
       - Просмотреть Бестиарий — все 16 element-tints различимы.
       - (Optional) Sim Daltonism / Color Oracle на macOS — simulation
         protanopia/deuteranopia. Tints должны distinguishable хотя бы по
         saturation.

    3. **i18n round-trip (I18N-02/03):**
       - Settings → switch language ru → en → es.
       - Каждый Cosmic Hub tab — все strings переведены, no fallback ключей
         типа «cosmic_hub.title» в UI.
       - Trigger tutorial overlay (новый browser profile / clear localStorage) —
         текст в текущей локали.
       - Toast'ы (mis-tap serum, crew exhausted, mission complete) — переведены.

    4. **FPS targets (PERF-01):**
       - **Desktop Chrome:**
         - DevTools Performance → record 30s session
         - 4 carriers + farming + Cosmic Hub open → average FPS ≥ 60
       - **Mid-tier Android (если доступно):**
         - Open Telegram WebApp / direct browser
         - 4 carriers + 30s farm → average FPS ≥ 50
       - **Low-tier Android (если доступно):**
         - 30 FPS minimum threshold

    5. **Settings consumer smoke** (на основе SMOKE_TEST.md статусов):
       - Если calmFarmMode = WIRED: enable toggle → ауры/частицы пропадают.
         Если TODO — skip (документировано).
       - Если reducedEffects = WIRED: enable → epic+ carriers показывают только
         dormant tier. Если TODO — skip.
       - Если openBoxesInstantly = WIRED: enable → legendary box opens ≤ 1с.
         Если TODO — skip.

    6. **Tutorial overlay full sequence:**
       - Clear localStorage → reload → verify no overlays
       - Send first scout → mission complete → first openBox → видим first-box overlay
       - tap CTA → reload → НЕ показывается снова
       - Repeat для first-serum, first-feed, first-stabilize

    7. **Pity UI:**
       - openedCount = 0 → footer hidden
       - openedCount = 3 → footer shows DotIndicator с правильным fill
       - openedCount = 5 → footer shows exact «До rare/epic/legendary через N»
       - Numbers consistent с pity counter actual values
  </how-to-verify>
  <verify>
    Manual checkpoint — verification содержится в `<how-to-verify>` блоке.
    Approve via resume-signal.
  </verify>
  <done>
    User signed off на FPS targets (PERF-01) + visual smoke (UX-02/03/08) +
    i18n round-trip (I18N-02/03). Phase 19 готов к close.
  </done>
  <resume-signal>
    Type "approved" если все checks pass.
    Если что-то fails — describe issue, plan creates patch tasks (либо в Phase 19
    либо в Phase 20 если non-blocking).
  </resume-signal>
</task>

</tasks>

<verification>
1. `cd client && npm run build` — succeeds, 0 errors
2. `node client/scripts/check-bundle-delta.cjs` — exit 0 (delta ≤ 50 KB cap)
3. Expected lazy chunks present (CosmicHubModal, BestiaryV2Tab если applicable)
4. SMOKE_TEST.md updated с Phase 19-07 settings audit + Phase 19-06 visual audit
5. CHECKPOINT (Task 3): user signs off на FPS + visual smoke
</verification>

<success_criteria>
1. **Production build:** `npm run build` succeeds, 0 errors, 0 warnings про
   bundle size.
2. **Bundle delta cap (PERF-05):** main `index.js` gzip ≤ v1.0 baseline + 50 KB.
3. **Lazy chunks (PERF-07):** CosmicHubModal-*.js separate file. (BestiaryV2Tab,
   SerumSlotMachine, CascadeRevealModal — separate если PERF-08 был implemented;
   audit reports actual state.)
4. **FPS targets (PERF-01) verified by user:** 60 desktop / 50 mid-tier mobile /
   30 low-tier (manual smoke на available devices).
5. **Settings consumer audit:** все 3 toggles documented (WIRED или TODO) в
   SMOKE_TEST.md.
6. **CHECKPOINT signed off:** user approves UX-02/UX-03/UX-08/I18N-02/I18N-03
   visual + tutorial sequence.
7. **No regressions:** все previous Phase 11-18 functionality continues to work
   (manual smoke).
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-07-SUMMARY.md`
с разделами:
- Что создано: check-bundle-delta.cjs (NEW), .bundle-baseline-v1.json (NEW),
  SMOKE_TEST.md updates, package.json
- Bundle metrics:
  - v1.0 baseline (KB)
  - Phase 19 final main bundle (KB)
  - Delta (KB) — должен быть ≤ 50
- Lazy chunks list (file names + size)
- Settings consumer status (WIRED / TODO для каждого из 3)
- FPS smoke results (per device): desktop / mid-tier / low-tier
- Phase 19 totals:
  - 7 plans, X commits, Y files modified, Z files created
  - 17/17 REQ-IDs ✓
- Atomic commits: `phase-19: add bundle delta check script + v1.0 baseline`,
  `phase-19: audit settings consumer wiring`, `phase-19: final FPS smoke + visual audit`
- Phase 19 closure: ready to ship v2.0 (с known TODO для Phase 20 safety net)
</output>
