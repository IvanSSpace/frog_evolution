---
phase: 19-balance-tutorial-i18n-polish
plan: 05
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - client/src/components/Tutorial/TutorialOverlay.tsx  # NEW
  - client/src/components/Tutorial/tutorialSteps.ts  # NEW
  - client/src/store/cosmic/types.ts
  - client/src/store/cosmic/slice.ts
  - client/src/store/gameStore.ts
  - client/src/App.tsx
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - UX-08

must_haves:
  truths:
    - "tutorialState shape: 4 boolean seenFlags (seenFirstBox, seenFirstSerum, seenFirstFeed, seenFirstStabilize) — все default false"
    - "TutorialOverlay subscribes к sentinel флагам + seen-flags; рендерит первый не-seen step"
    - "Tap «Понятно» вызывает markTutorialSeen(stepId) → set seen=true → overlay unmount"
    - "Persist seen flags в localStorage (через существующий cosmic persist)"
    - "Все 4 step-content i18n RU/EN/ES (title + body для каждого)"
    - "Triggers использует существующие sentinel: hasOpenedAnyBox, serumDragActive, hasFirstFeed, carrier.stabilized"
    - "Только один step показывается за раз (priority order: first-box > first-serum > first-feed > first-stabilize)"
  artifacts:
    - path: "client/src/components/Tutorial/TutorialOverlay.tsx"
      provides: "fullscreen translucent overlay с modal box + tap-to-dismiss"
      min_lines: 70
    - path: "client/src/components/Tutorial/tutorialSteps.ts"
      provides: "TUTORIAL_STEPS array конфигурация (id, trigger, contentKey)"
      min_lines: 40
    - path: "client/src/store/cosmic/types.ts"
      provides: "tutorialState extension в CosmicSlice"
      contains: "seenFirstBox"
    - path: "client/src/store/cosmic/slice.ts"
      provides: "markTutorialSeen action"
      contains: "markTutorialSeen"
    - path: "client/src/App.tsx"
      provides: "TutorialOverlay mounted на верхнем уровне"
      contains: "TutorialOverlay"
    - path: "client/src/i18n/ru.json"
      provides: "tutorial.first_box.{title,body}, first_serum.*, first_feed.*, first_stabilize.* + cta"
      contains: "first_box"
  key_links:
    - from: "TutorialOverlay.tsx"
      to: "tutorialSteps.ts"
      via: "import { TUTORIAL_STEPS }"
      pattern: "TUTORIAL_STEPS"
    - from: "TutorialOverlay.tsx"
      to: "useGameStore"
      via: "subscribe sentinel flags + seen flags + markTutorialSeen"
      pattern: "tutorialState\\."
    - from: "App.tsx"
      to: "TutorialOverlay.tsx"
      via: "JSX mount рядом с CosmicHubModal"
      pattern: "<TutorialOverlay"
---

<objective>
Реализовать **progressive tutorial overlay system** для 4 first-time событий
v2.0 cosmic loop:

| Step              | Trigger sentinel                         | What it teaches                            |
|-------------------|------------------------------------------|--------------------------------------------|
| first-box         | hasOpenedAnyBox=true (Phase 19-01 sets)   | про slot-machine + skip + ⭐ редкости      |
| first-serum       | serumDragActive=true (Phase 14)           | про tap-to-select + eligibility + DnD     |
| first-feed        | hasFirstFeed=true (Phase 17)              | про hidden ceiling + roll outcomes        |
| first-stabilize   | первый carrier.stabilized=true (Phase 17) | про merge above ceiling                    |

UX research 5.1-5.4: tutorial overlay с **persisted seen flags** (играем 1
раз и навсегда — не показывать снова даже после reload).

Phase 19-05 запускается **в wave 2** параллельно с 19-02 (Monte Carlo) — оба
не пересекаются по файлам. Не depends on 19-01 strictly (sentinels уже есть из
Phase 14/16/17), но 19-01 wires hasOpenedAnyBox в openBox — и без 19-01
first-box trigger может не fire'нуться. Поэтому wave 2 (после 19-01).

Purpose: UX-08 — onboarding cognitive overload mitigation. Игрок впервые
открывает бокс → видит slot-machine → кратко объясняем что происходит,
не блокируя interaction (overlay с большим CTA «Понятно»).

Output:
- TutorialOverlay.tsx — fullscreen translucent layer с message card + dismiss
- tutorialSteps.ts — конфигурация 4 шагов
- cosmic/types.ts расширен tutorialState shape
- cosmic/slice.ts: markTutorialSeen action
- App.tsx: mount TutorialOverlay
- i18n RU/EN/ES: 8 keys (4 steps × {title, body}) + cta
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/research/v2.0/UX.md
@client/src/store/cosmic/types.ts
@client/src/store/cosmic/slice.ts
@client/src/store/gameStore.ts
@client/src/App.tsx
@client/src/i18n/ru.json

<interfaces>
<!-- Sentinel flags уже существующие — Phase 19-05 НЕ заводит новые. -->

From cosmic/types.ts (existing):
```typescript
hasFirstFeed: boolean        // Phase 17 sets at first feed
hasFirstMission: boolean     // Phase 16 sets at first completed mission
hasOpenedAnyBox: boolean     // Phase 19-01 sets at first openBox

serumDragActive: boolean     // Phase 14 sets при tap-to-select
selectedSerum: { element: Element; rarity: Rarity } | null

carriers: CarrierData[]      // включает stabilized: boolean — индикатор для first-stabilize
```

CarrierData shape (Phase 14/17):
```typescript
interface CarrierData {
  frogId: string
  element: Element
  rarity: Rarity
  feedCount: number
  stabilized: boolean
  level?: number
}
```

Phase 19-05 ADDS to CosmicSlice:
```typescript
tutorialState: {
  seenFirstBox: boolean
  seenFirstSerum: boolean
  seenFirstFeed: boolean
  seenFirstStabilize: boolean
}
```

Phase 19-05 ADDS to CosmicSliceActions:
```typescript
markTutorialSeen: (step: TutorialStepId) => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend cosmic types + slice with tutorialState + markTutorialSeen action; persist</name>
  <files>
    client/src/store/cosmic/types.ts
    client/src/store/cosmic/slice.ts
    client/src/store/gameStore.ts
  </files>
  <action>
    1. В `client/src/store/cosmic/types.ts`:
       a. Добавить экспорт `TutorialStepId`:
          ```typescript
          export type TutorialStepId =
            | 'first-box' | 'first-serum' | 'first-feed' | 'first-stabilize'
          ```
       b. В `CosmicSlice` interface добавить:
          ```typescript
          tutorialState: {
            seenFirstBox: boolean
            seenFirstSerum: boolean
            seenFirstFeed: boolean
            seenFirstStabilize: boolean
          }
          ```
       c. В `makeInitialCosmicSlice()` добавить:
          ```typescript
          tutorialState: {
            seenFirstBox: false,
            seenFirstSerum: false,
            seenFirstFeed: false,
            seenFirstStabilize: false,
          },
          ```

    2. В `client/src/store/cosmic/slice.ts`:
       a. Добавить import `TutorialStepId`:
          ```typescript
          import { type TutorialStepId, ... } from './types'
          ```
       b. В `CosmicSliceActions` interface (между existing actions, в логичной
          группировке — рядом с tab persistence):
          ```typescript
          // Phase 19-05: tutorial overlay seen-flags (UX-08)
          markTutorialSeen: (step: TutorialStepId) => void
          ```
       c. В `createCosmicSlice` body — добавить implementation:
          ```typescript
          markTutorialSeen: (step) => {
            const s = get()
            const ts = s.tutorialState
            const next = { ...ts }
            if (step === 'first-box') next.seenFirstBox = true
            else if (step === 'first-serum') next.seenFirstSerum = true
            else if (step === 'first-feed') next.seenFirstFeed = true
            else if (step === 'first-stabilize') next.seenFirstStabilize = true
            // Idempotent: если already true — set всё равно безопасный (no harm)
            set({ tutorialState: next })
          },
          ```

    3. В `client/src/store/gameStore.ts`:
       a. В save/load logic — добавить tutorialState сериализацию. Найти где
          `pityCounters` сохраняется (line ~322 — существующий load) и
          добавить рядом:
          ```typescript
          tutorialState: parsed.tutorialState ?? defaults.tutorialState,
          ```
       b. В save (line ~599 — где `pityCounters: state.pityCounters`):
          ```typescript
          tutorialState: state.tutorialState,
          ```
       c. В comparison guard (line ~585, где `state.pityCounters !== prev.pityCounters`)
          добавить:
          ```typescript
          state.tutorialState !== prev.tutorialState ||
          ```

       Это standard pattern Phase 19-05 не bumps STORAGE_VERSION (additive
       fields с safe defaults через `??`).

    4. tsc clean: `cd client && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd client && grep -c "tutorialState\|markTutorialSeen\|TutorialStepId" src/store/cosmic/types.ts src/store/cosmic/slice.ts src/store/gameStore.ts</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error" | head -10</automated>
  </verify>
  <done>
    - types.ts: TutorialStepId exported; tutorialState shape in CosmicSlice;
      defaults в makeInitialCosmicSlice.
    - slice.ts: markTutorialSeen action implemented; signature в actions interface.
    - gameStore.ts: load/save/diff guard покрывают tutorialState.
    - STORAGE_VERSION НЕ bumped.
    - tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TUTORIAL_STEPS config + TutorialOverlay component + i18n RU/EN/ES</name>
  <files>
    client/src/components/Tutorial/tutorialSteps.ts
    client/src/components/Tutorial/TutorialOverlay.tsx
    client/src/i18n/ru.json
    client/src/i18n/en.json
    client/src/i18n/es.json
  </files>
  <action>
    1. Создать `client/src/components/Tutorial/tutorialSteps.ts`:

       ```typescript
       // Phase 19-05 (UX-08): tutorial step configuration.
       // Static config — triggers reference cosmic state, content via i18n keys.

       import type { TutorialStepId } from '../../store/cosmic/types'

       export interface TutorialStep {
         id: TutorialStepId
         /** i18n key prefix; expands to {prefix}.title и {prefix}.body */
         contentKey: string
         /** Priority order (lower = shown first, при collision) */
         priority: number
       }

       export const TUTORIAL_STEPS: readonly TutorialStep[] = [
         { id: 'first-box',       contentKey: 'tutorial.first_box',       priority: 1 },
         { id: 'first-serum',     contentKey: 'tutorial.first_serum',     priority: 2 },
         { id: 'first-feed',      contentKey: 'tutorial.first_feed',      priority: 3 },
         { id: 'first-stabilize', contentKey: 'tutorial.first_stabilize', priority: 4 },
       ]
       ```

    2. Создать `client/src/components/Tutorial/TutorialOverlay.tsx`:

       ```tsx
       // Phase 19-05 (UX-08): progressive tutorial overlay.
       // - Subscribes к cosmic sentinels + tutorialState
       // - Показывает только ОДИН step (наименьший priority с trigger=true & seen=false)
       // - Tap CTA → markTutorialSeen → overlay unmount
       //
       // Triggers:
       //   first-box        → hasOpenedAnyBox && !seenFirstBox
       //   first-serum      → serumDragActive && !seenFirstSerum
       //   first-feed       → hasFirstFeed && !seenFirstFeed
       //   first-stabilize  → carriers.some(c => c.stabilized) && !seenFirstStabilize

       import { useTranslation } from 'react-i18next'
       import { useGameStore } from '../../store/gameStore'
       import { TUTORIAL_STEPS, type TutorialStep } from './tutorialSteps'
       import type { TutorialStepId } from '../../store/cosmic/types'

       export function TutorialOverlay() {
         const { t } = useTranslation()

         // Sentinel флаги
         const hasOpenedAnyBox = useGameStore(s => s.hasOpenedAnyBox)
         const serumDragActive = useGameStore(s => s.serumDragActive)
         const hasFirstFeed = useGameStore(s => s.hasFirstFeed)
         const hasFirstStabilize = useGameStore(s =>
           s.carriers.some(c => c.stabilized),
         )

         // Seen flags
         const tutorialState = useGameStore(s => s.tutorialState)

         const markSeen = useGameStore(s => s.markTutorialSeen)

         // Determine what step (if any) активен сейчас. Priority order.
         const activeStep = pickActiveStep({
           hasOpenedAnyBox,
           serumDragActive,
           hasFirstFeed,
           hasFirstStabilize,
           tutorialState,
         })

         if (!activeStep) return null

         const titleKey = `${activeStep.contentKey}.title`
         const bodyKey = `${activeStep.contentKey}.body`

         return (
           <div
             className="ff-fade"
             role="dialog"
             aria-modal="true"
             aria-labelledby="tutorial-title"
             style={{
               position: 'fixed', inset: 0, zIndex: 200,
               display: 'flex', alignItems: 'center', justifyContent: 'center',
               background: 'rgba(0, 0, 0, 0.65)',
               padding: 16,
             }}
             onClick={() => markSeen(activeStep.id)}
           >
             <div
               className="ff-card p-4 max-w-sm"
               style={{
                 background: '#1f2937',
                 border: '2px solid #7e22ce',
                 borderRadius: 12,
                 boxShadow: '0 8px 32px rgba(126, 34, 206, 0.4)',
               }}
               onClick={(e) => e.stopPropagation()}
             >
               <div
                 id="tutorial-title"
                 className="ff-display text-base font-bold mb-2"
                 style={{ color: '#e9d5ff' }}
               >
                 {t(titleKey)}
               </div>
               <div
                 className="ff-body text-sm leading-snug mb-3"
                 style={{ color: '#d1d5db' }}
               >
                 {t(bodyKey)}
               </div>
               <button
                 onClick={() => markSeen(activeStep.id)}
                 className="ff-btn ff-btn-green text-sm w-full"
               >
                 {t('tutorial.cta_understood')}
               </button>
             </div>
           </div>
         )
       }

       interface PickArgs {
         hasOpenedAnyBox: boolean
         serumDragActive: boolean
         hasFirstFeed: boolean
         hasFirstStabilize: boolean
         tutorialState: {
           seenFirstBox: boolean
           seenFirstSerum: boolean
           seenFirstFeed: boolean
           seenFirstStabilize: boolean
         }
       }

       function pickActiveStep(a: PickArgs): TutorialStep | null {
         const conditions: Record<TutorialStepId, boolean> = {
           'first-box': a.hasOpenedAnyBox && !a.tutorialState.seenFirstBox,
           'first-serum': a.serumDragActive && !a.tutorialState.seenFirstSerum,
           'first-feed': a.hasFirstFeed && !a.tutorialState.seenFirstFeed,
           'first-stabilize': a.hasFirstStabilize && !a.tutorialState.seenFirstStabilize,
         }
         // Priority order (TUTORIAL_STEPS уже sorted by priority).
         for (const step of TUTORIAL_STEPS) {
           if (conditions[step.id]) return step
         }
         return null
       }
       ```

    3. Добавить i18n keys в `client/src/i18n/ru.json` (новая `tutorial` секция):
       ```json
       "tutorial": {
         "cta_understood": "Понятно!",
         "first_box": {
           "title": "🎰 Слот-машина запущена!",
           "body": "Длительность вращения подсказывает редкость. Тапни в любом месте после первой секунды чтобы пропустить."
         },
         "first_serum": {
           "title": "🧪 Применение сыворотки",
           "body": "Тапни на сыворотку чтобы выделить, потом тапни на eligible лягушку. Common — болото, rare — лес, epic — континент, legendary — планета."
         },
         "first_feed": {
           "title": "🍽️ Корм и потолок",
           "body": "У каждого carrier'а есть скрытый потолок. Первые feed'ы — «???». После 3-4 кормлений — цветовой hint. После 5 — точное число."
         },
         "first_stabilize": {
           "title": "✨ Стабилизация",
           "body": "Carrier стабилизировался! Чтобы поднять выше потолка — слей двух same-element same-level carriers. Это даёт guaranteed S-roll следующего уровня."
         }
       }
       ```

    4. То же в `en.json`:
       ```json
       "tutorial": {
         "cta_understood": "Got it!",
         "first_box": {
           "title": "🎰 Slot machine started!",
           "body": "Spin duration hints rarity. Tap anywhere after the first second to skip."
         },
         "first_serum": {
           "title": "🧪 Applying serum",
           "body": "Tap a serum to select, then tap an eligible frog. Common — swamp, rare — forest, epic — continent, legendary — planet."
         },
         "first_feed": {
           "title": "🍽️ Feeding and ceiling",
           "body": "Each carrier has a hidden ceiling. First feeds — «???». After 3-4 — color hint. After 5 — exact number."
         },
         "first_stabilize": {
           "title": "✨ Stabilization",
           "body": "Carrier stabilized! To go above the ceiling, merge two same-element same-level carriers — guaranteed S-roll for next level."
         }
       }
       ```

    5. То же в `es.json`:
       ```json
       "tutorial": {
         "cta_understood": "¡Entendido!",
         "first_box": {
           "title": "🎰 ¡Tragaperras iniciada!",
           "body": "La duración del giro indica la rareza. Toca en cualquier sitio tras el primer segundo para saltar."
         },
         "first_serum": {
           "title": "🧪 Aplicar suero",
           "body": "Toca un suero para seleccionar, luego toca una rana elegible. Común — pantano, raro — bosque, épico — continente, legendario — planeta."
         },
         "first_feed": {
           "title": "🍽️ Alimentar y techo",
           "body": "Cada portador tiene un techo oculto. Primeras alimentaciones — «???». Tras 3-4 — pista de color. Tras 5 — número exacto."
         },
         "first_stabilize": {
           "title": "✨ Estabilización",
           "body": "¡Portador estabilizado! Para superar el techo, fusiona dos portadores del mismo elemento y nivel — S-roll garantizado al siguiente nivel."
         }
       }
       ```

    6. tsc clean.
  </action>
  <verify>
    <automated>cd client && wc -l src/components/Tutorial/TutorialOverlay.tsx src/components/Tutorial/tutorialSteps.ts</automated>
    <automated>cd client && python3 -c "import json; ru=json.load(open('src/i18n/ru.json'))['tutorial']; en=json.load(open('src/i18n/en.json'))['tutorial']; es=json.load(open('src/i18n/es.json'))['tutorial']; steps=['first_box','first_serum','first_feed','first_stabilize']; assert 'cta_understood' in ru and 'cta_understood' in en and 'cta_understood' in es; [assert_each := (lambda d: all(s in d and 'title' in d[s] and 'body' in d[s] for s in steps))][0](ru); [assert_each := (lambda d: all(s in d and 'title' in d[s] and 'body' in d[s] for s in steps))][0](en); print('OK')"</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error\|tutorial" | head -10</automated>
  </verify>
  <done>
    - tutorialSteps.ts ≥ 40 LOC; TUTORIAL_STEPS array length === 4.
    - TutorialOverlay.tsx ≥ 70 LOC; pickActiveStep helper присутствует.
    - i18n: tutorial.cta_understood + 4 step entries (each title + body) × 3 locales =
      27 entries.
    - tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount TutorialOverlay in App.tsx</name>
  <files>client/src/App.tsx</files>
  <action>
    1. Открыть `client/src/App.tsx`. Найти где CosmicHubModal или другие top-level
       overlays mounted (обычно сразу под main content area, или в `Suspense`).

    2. Добавить import:
       ```tsx
       import { TutorialOverlay } from './components/Tutorial/TutorialOverlay'
       ```

    3. Добавить mount **после** existing CosmicHubModal mount (или рядом с ним
       в той же позиции). TutorialOverlay z-index=200 уже выше modal'ов (CosmicHubModal
       обычно zIndex=150-200), но ниже toast (если есть).

       Пример:
       ```tsx
       <Suspense fallback={null}>
         {showCosmicHub && <CosmicHubModal onClose={...} />}
       </Suspense>
       <TutorialOverlay />  {/* Phase 19-05 (UX-08): always mounted; conditional null-render */}
       ```

       NOTE: TutorialOverlay сама проверяет conditions; mount permanent.
       Performance — 4 useGameStore subscriptions, low cost.

    4. tsc clean: `cd client && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd client && grep -c "TutorialOverlay" src/App.tsx</automated>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -i "error\|App.tsx" | head -10</automated>
  </verify>
  <done>
    - App.tsx содержит import + JSX mount TutorialOverlay (≥ 2 grep matches).
    - tsc clean.
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx tsc --noEmit` — 0 errors
2. Sequence smoke (manual в dev panel):
   a. clean state (clear localStorage) → reload → no overlay
   b. trigger first openBox (через UI или dev addBox+openBox) → видим
      «🎰 Слот-машина запущена!» overlay
   c. tap «Понятно» → overlay dismiss
   d. reload → overlay НЕ возвращается (persistence works)
   e. activate first serum (selectSerum) → видим «🧪 Применение сыворотки»
   f. tap «Понятно» → dismiss + reload → не возвращается
3. `python3` JSON keys check — все 27 entries присутствуют RU/EN/ES.
</verification>

<success_criteria>
1. **Trigger conditions:** 4 step trigger проходят правильно (см. interfaces table).
2. **Persistence:** seen flags survive reload; once seen — НЕ показывается снова.
3. **Single step at time:** даже если несколько trigger conditions true, видно
   ровно ОДИН step (priority order).
4. **Idempotent:** markTutorialSeen на уже-seen step — no-op.
5. **Tap-to-dismiss:** background click И CTA «Понятно» оба dismiss overlay.
6. **i18n complete:** tutorial.cta_understood + 4 × {title, body} × 3 locales = 27 entries.
7. **No race conditions:** trigger пропавший (e.g. serumDragActive стал false до
   markSeen) → overlay просто disappears, никакого throw.
8. **tsc clean, build clean.**
9. **Bundle impact:** TutorialOverlay + tutorialSteps ≤ 2 KB gzip (lightweight,
   stays в main bundle — НЕ выделяем lazy chunk потому что tutorial обычно triggers
   рано и lazy-load создал бы FOUC).
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-05-SUMMARY.md`
с разделами:
- Что создано: 2 новых компонента (TutorialOverlay, tutorialSteps), 4 modified
  (types.ts, slice.ts, gameStore.ts, App.tsx), 3 i18n
- Trigger table (sentinel → step)
- Priority order (first-box > first-serum > first-feed > first-stabilize)
- 27 i18n entries (cta + 4×{title,body}) × 3 locales
- Atomic commits: `phase-19: add tutorial state shape + markTutorialSeen action`,
  `phase-19: add TutorialOverlay component + steps config`,
  `phase-19: i18n RU/EN/ES tutorial keys`,
  `phase-19: mount TutorialOverlay in App.tsx`
</output>
