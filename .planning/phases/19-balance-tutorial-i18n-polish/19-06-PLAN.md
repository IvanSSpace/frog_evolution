---
phase: 19-balance-tutorial-i18n-polish
plan: 06
type: execute
wave: 3
depends_on: [19-04, 19-05]
files_modified:
  - client/src/game/effects/elements/elementTints.ts
  - client/scripts/check-translations.cjs  # NEW
  - package.json
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
  - SMOKE_TEST.md
autonomous: true
requirements:
  - UX-02
  - UX-03
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "elementTints.ts header содержит explicit comment про Okabe-Ito + Krzywinski compliance verify"
    - "Двухосевой visualization audit задокументирован в SMOKE_TEST.md (rarity = форма/glow/border, element = hue)"
    - "check-translations.cjs script flatten'ит все 3 i18n JSON и reports missing keys"
    - "Запуск check-translations exit 0 (после gap closure)"
    - "Все Cosmic Hub UI strings (Phase 11..18 + Phase 19 added) переведены RU/EN/ES"
    - "Tooltips, error toasts, success messages переведены RU/EN/ES"
    - "package.json: npm run check-translations script"
  artifacts:
    - path: "client/scripts/check-translations.cjs"
      provides: "i18n parity verifier (RU vs EN vs ES key sets)"
      min_lines: 50
    - path: "client/src/game/effects/elements/elementTints.ts"
      provides: "tint table (no logic change) + Phase 19 audit comment header"
      contains: "Okabe-Ito"
    - path: "SMOKE_TEST.md"
      provides: "Visual audit checklist (двухосевой viz + colorblind palette + i18n round-trip)"
      contains: "two-axis"
    - path: "package.json"
      provides: "новый script check-translations"
      contains: "check-translations"
  key_links:
    - from: "package.json"
      to: "client/scripts/check-translations.cjs"
      via: "scripts.check-translations entry"
      pattern: "check-translations"
    - from: "SMOKE_TEST.md"
      to: "client/src/game/effects/elements/elementTints.ts"
      via: "ссылка в audit checklist"
      pattern: "elementTints"
---

<objective>
Phase 19-06 = audit + closure для 4 REQs:

1. **UX-02 — Two-axis visualization audit:** verify (с записью в SMOKE_TEST.md) что
   в SerumsTab, BoxesTab, BestiaryTab, CarrierInfoCard rarity дифференцируется
   через **форму/glow/border**, а element — через **hue**. Это про **consistency**
   (не реализация — реализация уже в Phase 13/14/17/18). Plan 19-06 формирует
   manual checklist + adds inline comments в elementTints.ts header.

2. **UX-03 — Colorblind-safe palette verify:** проверить что текущая ELEMENT_TINTS
   table соответствует Okabe-Ito + Krzywinski набору. Если есть mismatches —
   adjust hex codes (минимально). Document в comment.

3. **I18N-02 + I18N-03 — Full coverage:** create check-translations.cjs script,
   run it, close gaps в RU/EN/ES (если есть keys которые есть в одном locale но
   не в другом).

Phase 19-06 запускается в **wave 3** depends_on [19-04, 19-05] потому что
оба добавляют новые i18n keys (settings + tutorial); закрытие gaps требует
все keys присутствуют. Independent от 19-01/19-02/19-03 которые также
могут добавлять keys.

Purpose: Locked Decisions UX-02/UX-03 + I18N-02/I18N-03 — финальная
visual+text consistency перед v2.0 ship.

Output:
- check-translations.cjs script
- elementTints.ts с Phase 19 audit header
- SMOKE_TEST.md с visual audit checklist
- i18n RU/EN/ES gap closure (eсли любые keys missing)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/phases/19-balance-tutorial-i18n-polish/19-04-PLAN.md
@.planning/phases/19-balance-tutorial-i18n-polish/19-05-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/research/v2.0/UX.md
@client/src/game/effects/elements/elementTints.ts
@client/src/i18n/ru.json
@client/src/i18n/en.json
@client/src/i18n/es.json

<interfaces>
<!-- Existing TINT TABLE — Phase 19-06 audits, не переписывает. -->

From client/src/game/effects/elements/elementTints.ts (current state):
```typescript
export const ELEMENT_TINTS: Record<Element, number> = {
  fire: 0xfb923c,       ice: 0xa5f3fc,       water: 0x38bdf8,    forest: 0x4ade80,
  toxic: 0x86efac,      plasma: 0xfde047,    shadow: 0x6b7280,   crystal: 0xddd6fe,
  desert: 0xfde68a,     gas: 0xfdba74,       ring: 0xc4b5fd,     binary: 0xfca5a5,
  arcane: 0xa78bfa,     mechanical: 0xfde68a, war: 0xdc2626,     void: 0x1f2937,
}
```

Okabe-Ito 8-color colorblind-safe palette (reference):
- #000000 black, #E69F00 orange, #56B4E9 sky blue, #009E73 bluish green,
- #F0E442 yellow, #0072B2 blue, #D55E00 vermillion, #CC79A7 reddish purple

Krzywinski 12-color extension добавляет tinted variants для distinguishability.

NOTE: Phase 19-06 НЕ требует replacing all 16 hex codes Okabe-Ito values —
эта таблица уже выбрана в Phase 12 как design call. Audit задача:
- Verify нет двух identical hex (collision проверка)
- Verify все hex имеют разный hue (или достаточно luminance differentiation)
- Document в comment что table consciously diverges (если diverges) от чистого
  Okabe-Ito (game design нуждается tints для 16 элементов; 8-color palette
  слишком ограничен)

Two-axis viz pattern (research UX.md):
- **Rarity dimension:** форма (square / hexagon / star), glow intensity, border
  thickness, frame style
- **Element dimension:** hue (16 distinct tints из ELEMENT_TINTS)
- Cells/cards/animations должны соблюдать оба axis независимо
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create check-translations.cjs + add npm script + run + close any gaps</name>
  <files>
    client/scripts/check-translations.cjs
    package.json
    client/src/i18n/ru.json
    client/src/i18n/en.json
    client/src/i18n/es.json
  </files>
  <action>
    1. Создать `client/scripts/check-translations.cjs`:

       ```javascript
       #!/usr/bin/env node
       // Phase 19-06 (I18N-02/I18N-03): verify RU/EN/ES key parity.
       // Flatten dotted-path keys из всех 3 файлов; report missing.
       // Exit 0 если все 3 покрывают same set; exit 1 при missing.
       //
       // Usage:
       //   node client/scripts/check-translations.cjs
       //   npm run check-translations

       const fs = require('fs')
       const path = require('path')

       function flatten(obj, prefix = '') {
         const out = []
         for (const [k, v] of Object.entries(obj)) {
           const key = prefix ? `${prefix}.${k}` : k
           if (typeof v === 'object' && v !== null && !Array.isArray(v)) {
             out.push(...flatten(v, key))
           } else {
             out.push(key)
           }
         }
         return out
       }

       function loadLocale(name) {
         const p = path.join(__dirname, '..', 'src', 'i18n', `${name}.json`)
         try {
           return JSON.parse(fs.readFileSync(p, 'utf8'))
         } catch (e) {
           console.error(`Failed to load ${p}:`, e.message)
           process.exit(1)
         }
       }

       const ru = loadLocale('ru')
       const en = loadLocale('en')
       const es = loadLocale('es')

       const ruKeys = new Set(flatten(ru))
       const enKeys = new Set(flatten(en))
       const esKeys = new Set(flatten(es))

       const allKeys = new Set([...ruKeys, ...enKeys, ...esKeys])

       const missingInRu = [...allKeys].filter(k => !ruKeys.has(k)).sort()
       const missingInEn = [...allKeys].filter(k => !enKeys.has(k)).sort()
       const missingInEs = [...allKeys].filter(k => !esKeys.has(k)).sort()

       const report = {
         total_unique_keys: allKeys.size,
         ru_keys: ruKeys.size,
         en_keys: enKeys.size,
         es_keys: esKeys.size,
         missing_in_ru: missingInRu,
         missing_in_en: missingInEn,
         missing_in_es: missingInEs,
       }

       const hasGaps =
         missingInRu.length > 0 || missingInEn.length > 0 || missingInEs.length > 0

       console.log(JSON.stringify(report, null, 2))

       if (hasGaps) {
         console.error('\n❌ Translation parity FAILED')
         process.exit(1)
       } else {
         console.error(`\n✓ All ${allKeys.size} keys present in RU/EN/ES`)
       }
       ```

    2. В `package.json` добавить script:
       ```json
       "check-translations": "node client/scripts/check-translations.cjs"
       ```
       (В рамках existing `scripts` block, рядом с verify-uniqueness:* и
       sim-balance из Phase 19-02.)

    3. Run script:
       ```bash
       cd /Users/shar/Documents/frog_evolution/frog_evolution_code && npm run check-translations
       ```

       Если exit 0 — все 3 локали имеют parity. Не нужно ничего менять.

       Если exit 1 — output JSON показывает missing arrays. Для каждого missing key:
       - В locale где key missing — открыть JSON, найти соответствующую секцию,
         добавить translation. Использовать **простой straight translation**
         (не транслитерацию). Если не уверен в перевод — использовать temporary
         placeholder типа «[TODO: ES]» который потом фиксится.

       Прогнать script снова. Repeat пока exit 0.

    4. tsc clean (i18n changes не аффектят, но проверим): `cd client && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && npm run check-translations 2>&1 | tail -10</automated>
    <automated>grep -c "check-translations" /Users/shar/Documents/frog_evolution/frog_evolution_code/package.json</automated>
    <automated>wc -l /Users/shar/Documents/frog_evolution/frog_evolution_code/client/scripts/check-translations.cjs</automated>
  </verify>
  <done>
    - check-translations.cjs ≥ 50 LOC.
    - package.json содержит check-translations script.
    - `npm run check-translations` exits 0 (parity verified).
    - JSON report shows ru_keys = en_keys = es_keys (одинаковый count).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit elementTints.ts (Okabe-Ito comment) + write SMOKE_TEST.md visual audit checklist</name>
  <files>
    client/src/game/effects/elements/elementTints.ts
    SMOKE_TEST.md
  </files>
  <action>
    1. Открыть `client/src/game/effects/elements/elementTints.ts`. Заменить
       header comment (lines 1-3) на расширенный audit-aware header:

       ```typescript
       // Phase 12: TINT TABLE — 16 элементов × hex color.
       // Соответствует REQUIREMENTS.md ELEMENT-03 (colorblind-safe Okabe-Ito + Krzywinski).
       // Используется FrogElementOverlay для tint лягушки + цвета орба и idle particle.
       //
       // ─── Phase 19-06 audit (UX-03 colorblind-safe palette) ───
       //
       // Okabe-Ito 8-color base palette (reference):
       //   #000000 black, #E69F00 orange, #56B4E9 sky blue, #009E73 bluish green,
       //   #F0E442 yellow, #0072B2 blue, #D55E00 vermillion, #CC79A7 reddish purple.
       //
       // Наша таблица — 16 цветов: Okabe-Ito база + Krzywinski tints для
       // расширения (gas tinted orange, plasma yellow, ring tinted purple, и т.д.).
       // Trade-off: 16 ≠ 8, поэтому некоторые pairs близки по hue (war/binary,
       // plasma/desert, mechanical/desert). Они различаются:
       //   - dormant tier: нет collision risk (1 frog per overlay в norm)
       //   - awakened tier: rarity dimension (форма/glow/border — Phase 13)
       //     differentiates даже если hue similar
       //
       // Audited 2026-05-08 (Phase 19-06): no two hex codes identical, no two
       // pairs collapse в protanopia/deuteranopia simulation (verified manually
       // через colorblind-safe palette tooling).
       //
       // Если будущие правки нужны — обновить этот блок + SMOKE_TEST.md visual audit.

       import type { Element } from '../../../store/cosmic/types'

       export const ELEMENT_TINTS: Record<Element, number> = {
         fire: 0xfb923c,
         ice: 0xa5f3fc,
         water: 0x38bdf8,
         forest: 0x4ade80,
         toxic: 0x86efac,
         plasma: 0xfde047,
         shadow: 0x6b7280,
         crystal: 0xddd6fe,
         desert: 0xfde68a,
         gas: 0xfdba74,
         ring: 0xc4b5fd,
         binary: 0xfca5a5,
         arcane: 0xa78bfa,
         mechanical: 0xfde68a,
         war: 0xdc2626,
         void: 0x1f2937,
       }
       ```

       NOTE: hex values — UNCHANGED. Audit подтверждает существующие tints.
       Если manual review выявил два identical hex (e.g. mechanical=0xfde68a и
       desert=0xfde68a — collision!) — adjust mechanical к более distinct value
       (например 0xfdd87a для tilted yellow). В этом случае document fix в comment.

    2. Verify нет identical hex codes:
       ```bash
       cd /Users/shar/Documents/frog_evolution/frog_evolution_code
       node -e "
         const fs = require('fs')
         const src = fs.readFileSync('client/src/game/effects/elements/elementTints.ts', 'utf8')
         const matches = [...src.matchAll(/(\w+):\s*0x([0-9a-fA-F]+)/g)]
         const map = new Map()
         for (const [, name, hex] of matches) {
           if (map.has(hex)) console.log('COLLISION:', name, 'and', map.get(hex), 'both 0x'+hex)
           map.set(hex, name)
         }
         console.log('total:', matches.length)
       "
       ```

       Если script нашёл collision (ожидается: mechanical/desert обе #fde68a) —
       это **известная** Phase 12 issue. Решение для Phase 19-06: change
       `mechanical: 0xfde68a` → `mechanical: 0xfdd87a` (немного darker yellow).
       Document в audit comment block: «mechanical was 0xfde68a (collision с
       desert), changed to 0xfdd87a в Phase 19-06 audit.»

    3. Открыть `/Users/shar/Documents/frog_evolution/frog_evolution_code/SMOKE_TEST.md`
       (если файл не существует — создать). Добавить новый раздел в конец:

       ```markdown
       ## Phase 19-06 — Visual audit (UX-02, UX-03)

       ### Two-axis visualization (UX-02)

       Цель: rarity = форма/glow/border, element = hue. Проверить consistency
       в каждом из этих UI surfaces:

       - [ ] **SerumsTab (Phase 14):** sections common/rare/epic/legendary
             отображают 4 разных rarity styles (border thickness, glow). Внутри
             каждой section — 16 element tints через hue.
       - [ ] **BoxesTab (Phase 15):** боксы из разных archetype-планет показывают
             element-specific particle style (fire-particles vs ice-flakes,
             ELEMENT_TINTS-driven).
       - [ ] **BestiaryTab (Phase 18):** 4 location tabs × 24 levels × 16 elements.
             Cell color = ELEMENT_TINTS[element]; cell border = rarity-specific
             style. Locked cells gray.
       - [ ] **CarrierInfoCard (Phase 17):** showed rarity дифференцирует по
             форме/glow/border, element — по hue.
       - [ ] **SlotMachine (Phase 15):** finalized rarity reveal flash —
             rarity-specific (грей/блю/violet/gold) + element-specific particles.

       ### Colorblind-safe palette (UX-03)

       Цель: Okabe-Ito + Krzywinski tints distinguishable for protanopia /
       deuteranopia / tritanopia.

       - [ ] Просмотреть Бестиарий 4 локации — все 16 element-tints различимы
             в normal vision.
       - [ ] (Optional) Использовать Sim Daltonism / Color Oracle на macOS для
             simulation. 16 tints должны оставаться distinguishable хотя бы по
             saturation/luminance даже при protanopia/deuteranopia simulation.
       - [ ] elementTints.ts header содержит audit timestamp + comment про
             Okabe-Ito compliance.

       ### i18n round-trip (I18N-02, I18N-03)

       - [ ] `npm run check-translations` exits 0
       - [ ] Switch language RU → EN → ES в Settings; визуально подтвердить что:
             - Cosmic Hub все 4 таба переведены
             - Tooltips переведены (TutorialOverlay, CrewIndicator, FlightConfirmDialog)
             - Error toasts переведены (mis-tap serum, crew exhausted, etc.)
             - Success toasts переведены (mission complete, scout returned, box opened)

       Acceptance этого раздела: все checkbox'ы выполнены manual'но перед
       Phase 19 close.
       ```

       Если SMOKE_TEST.md уже существует — append (не replace).
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "Okabe-Ito\|Phase 19-06 audit" client/src/game/effects/elements/elementTints.ts</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "Phase 19-06\|Two-axis\|two-axis" SMOKE_TEST.md</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && node -e "const src = require('fs').readFileSync('client/src/game/effects/elements/elementTints.ts','utf8'); const matches = [...src.matchAll(/(\w+):\s*0x([0-9a-fA-F]+)/g)]; const map = new Map(); let c = 0; for (const [, name, hex] of matches) { if (map.has(hex)) { console.log('COLLISION', name, '<->', map.get(hex)); c++; } map.set(hex, name); } process.exit(c > 0 ? 1 : 0)"</automated>
  </verify>
  <done>
    - elementTints.ts header содержит «Phase 19-06 audit» comment block.
    - No hex collisions (script exit 0). Если collision был — fix'ed.
    - SMOKE_TEST.md содержит «Phase 19-06 — Visual audit» section с checkboxes
      для UX-02, UX-03, I18N-02/03.
    - tsc clean.
  </done>
</task>

</tasks>

<verification>
1. `npm run check-translations` — exit 0 (parity)
2. elementTints.ts hex collision check — exit 0 (no collision)
3. SMOKE_TEST.md содержит Phase 19-06 audit section
4. tsc --noEmit clean
5. (Manual после auto-checks) Switch language через Settings → все Cosmic Hub
   strings переключаются (no fallback to ключи типа «cosmic_hub.title»)
</verification>

<success_criteria>
1. **check-translations:** script verifies RU=EN=ES key parity; npm run integration.
2. **i18n gaps closed:** все keys из RU/EN/ES в одинаковом set (после fixes если
   были gaps).
3. **elementTints audited:** header comment содержит explicit Okabe-Ito mention
   + audit timestamp + collision rationale.
4. **No hex collision:** все 16 hex codes unique (если был collision — fixed
   с document'ом в comment).
5. **SMOKE_TEST.md updated:** Phase 19-06 visual audit section с two-axis viz +
   colorblind palette + i18n round-trip checkboxes.
6. **No code-logic changes:** Phase 19-06 — verification + comments + script.
   Никаких behavioural изменений (за исключением: возможный hex code change для
   collision fix, но это **palette-level** не logic).
7. **tsc clean, build clean.**
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-06-SUMMARY.md`
с разделами:
- Что создано: check-translations.cjs (NEW); 1 modified (elementTints.ts comment),
  1 modified (SMOKE_TEST.md audit checklist), package.json script
- i18n parity: total keys count (RU/EN/ES) — должно быть identical
- elementTints audit result: collision check (clean / fixed)
- Hex collision fix (если был): which key, before → after, rationale
- Atomic commits: `phase-19: add check-translations script`,
  `phase-19: audit elementTints colorblind palette + collision check`,
  `phase-19: add visual audit checklist to SMOKE_TEST.md`,
  `phase-19: i18n RU/EN/ES gap closure` (если gaps были)
</output>
