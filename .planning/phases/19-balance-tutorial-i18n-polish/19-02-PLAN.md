---
phase: 19-balance-tutorial-i18n-polish
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - client/scripts/simulate_balance.cjs  # NEW
  - client/scripts/_shared.cjs
  - package.json
autonomous: true
requirements:
  - BALANCE-08

must_haves:
  truths:
    - "Standalone CommonJS скрипт без React/Phaser/Zustand deps реплицирует rollRarity/updatePity 1:1 from rarityRoll.ts"
    - "10K iterations default; configurable через CLI argument"
    - "Output JSON содержит: distribution, pct, avgLegendaryPer100, timeToFirstLegendary, pityHard25Breaches, legendaryGap{min,max,median}"
    - "На любом seed: pityHard25Breaches === 0 (max legendary gap ≤ 25)"
    - "avgLegendaryPer100 ∈ [2.7, 3.3] на 10K iterations (Locked Decision baseline)"
    - "package.json содержит npm run sim-balance script"
    - "Header comment script-файла содержит закоммиченные baseline numbers (single 10K run snapshot)"
  artifacts:
    - path: "client/scripts/simulate_balance.cjs"
      provides: "Monte Carlo simulation runner for rarity weights + pity guarantees"
      min_lines: 130
    - path: "package.json"
      provides: "новая npm-скрипт-команда sim-balance"
      contains: "sim-balance"
  key_links:
    - from: "client/scripts/simulate_balance.cjs"
      to: "client/src/utils/rarityRoll.ts"
      via: "1:1 mirror logic в comment header указано «keep in sync»"
      pattern: "MIRROR.*rarityRoll"
    - from: "package.json"
      to: "client/scripts/simulate_balance.cjs"
      via: "scripts.sim-balance entry"
      pattern: "sim-balance"
---

<objective>
Создать standalone Monte Carlo simulation script `client/scripts/simulate_balance.cjs`,
который validates что Locked Decisions (50/35/12/3 + pity 3/10/15/20/25) действительно
дают баланс в спецификации (avg legendary 3.0 ± 0.3 per 100 boxes, max gap ≤ 25).

Phase 19-02 запускается только в wave 2 (после 19-01) потому что 19-01 wire
`openBox` к `rollRarity` — без этого мы не уверены что utility action реально
работает. Сам skript использует **mirror** logic (skopiрованный 1:1 из
`rarityRoll.ts`) чтобы skript мог запускаться без vite/tsconfig — pure node.

Purpose: Защита от **silent balance drift** при будущих правках
`rarityRoll.ts`. Скрипт должен быть idempotent + воспроизводимый — checked-in
baseline numbers в header комментария позволяют сравнивать после любой правки.

Output:
- `client/scripts/simulate_balance.cjs` — standalone runner
- baseline numbers зафиксированы как комментарий в header script-файла
- `npm run sim-balance` запускает default 10K iterations
- Optional `npm run sim-balance 100000` запускает 100K (для high-confidence runs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-balance-tutorial-i18n-polish/PLAN.md
@.planning/phases/19-balance-tutorial-i18n-polish/19-01-PLAN.md
@client/src/utils/rarityRoll.ts
@client/scripts/_shared.cjs
@client/scripts/verify_anim_uniqueness_strict.cjs
@package.json

<interfaces>
<!-- Reference logic из rarityRoll.ts. Script мирорит этот код 1:1. -->

From client/src/utils/rarityRoll.ts:
```javascript
// (TS — но логика портируется 1:1 в JS)
const RARITY_WEIGHTS = { common: 50, rare: 35, epic: 12, legendary: 3 }
const ORDER = ['common', 'rare', 'epic', 'legendary']

function rollRarity(pity, bonusRarity, rng = Math.random) {
  // 1. Hard guarantees
  if (pity.legendary >= 25) return 'legendary'
  if (pity.epic >= 10) return rollWeighted(rng, ['epic', 'legendary'], [80, 20])
  if (pity.rare >= 3 && !bonusRarity) return rollWeighted(rng, ['rare', 'epic', 'legendary'], [70, 25, 5])

  // 2. Soft pity boost
  let weights = { ...RARITY_WEIGHTS }
  if (pity.legendary >= 20) weights = { common: 45, rare: 33, epic: 12, legendary: 10 }
  else if (pity.legendary >= 15) weights = { common: 48, rare: 34, epic: 12, legendary: 6 }

  // 3. Weighted random
  let rolled = weightedRandom(weights, rng)

  // 4. bonusRarity floor
  if (bonusRarity && ORDER.indexOf(rolled) < ORDER.indexOf(bonusRarity)) {
    rolled = bonusRarity
  }
  return rolled
}

function updatePity(pity, rolled) {
  return {
    rare: rolled === 'common' ? pity.rare + 1 : 0,
    epic: rolled === 'common' || rolled === 'rare' ? pity.epic + 1 : 0,
    legendary: rolled === 'legendary' ? 0 : pity.legendary + 1,
  }
}
```

From package.json (existing scripts pattern — заглавный пример):
```json
"scripts": {
  "verify-uniqueness:anim": "node client/scripts/verify_anim_uniqueness_strict.cjs",
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simulate_balance.cjs Monte Carlo runner with mirrored rollRarity/updatePity</name>
  <files>client/scripts/simulate_balance.cjs</files>
  <action>
    Создать `client/scripts/simulate_balance.cjs`:

    ```javascript
    #!/usr/bin/env node
    // Phase 19-02 — Monte Carlo simulation for box-open rarity distribution.
    //
    // MIRROR of client/src/utils/rarityRoll.ts (pure logic, no React/Phaser).
    // Keep in sync — last verified against rarityRoll.ts: 2026-05-08 (Phase 19-02).
    //
    // ─── BASELINE NUMBERS (10K iterations, seed = Math.random) ───
    // distribution:           ~50.0 / 35.0 / 12.0 / 3.0  (target 50/35/12/3)
    // avgLegendaryPer100:     ~3.0  (acceptance: 2.7..3.3)
    // pityHard25Breaches:     0      (acceptance: must be 0)
    // legendaryGap.max:       ≤25    (hard pity guarantee)
    // legendaryGap.median:    ~30-34 (NOT 33 — hard pity floors max, not median)
    // timeToFirstLegendary:   variable (depends on RNG seed)
    //
    // Запуск:
    //   node client/scripts/simulate_balance.cjs            # 10K default
    //   node client/scripts/simulate_balance.cjs 100000     # 100K iterations
    //
    // Verify acceptance:
    //   pct.legendary ∈ [2.7, 3.3]  (95% CI на 10K)
    //   pityHard25Breaches === 0
    //   legendaryGap.max ≤ 25

    const RARITY_WEIGHTS = { common: 50, rare: 35, epic: 12, legendary: 3 }
    const ORDER = ['common', 'rare', 'epic', 'legendary']

    function weightedRandom(w, rng) {
      const total = w.common + w.rare + w.epic + w.legendary
      let r = rng() * total
      if ((r -= w.common) < 0) return 'common'
      if ((r -= w.rare) < 0) return 'rare'
      if ((r -= w.epic) < 0) return 'epic'
      return 'legendary'
    }

    function rollWeighted(rng, options, weights) {
      const total = weights.reduce((s, x) => s + x, 0)
      let r = rng() * total
      for (let i = 0; i < options.length; i++) {
        if ((r -= weights[i]) < 0) return options[i]
      }
      return options[options.length - 1]
    }

    function rollRarity(pity, bonusRarity, rng = Math.random) {
      if (pity.legendary >= 25) return 'legendary'
      if (pity.epic >= 10) return rollWeighted(rng, ['epic', 'legendary'], [80, 20])
      if (pity.rare >= 3 && !bonusRarity) {
        return rollWeighted(rng, ['rare', 'epic', 'legendary'], [70, 25, 5])
      }

      let weights = { ...RARITY_WEIGHTS }
      if (pity.legendary >= 20) {
        weights = { common: 45, rare: 33, epic: 12, legendary: 10 }
      } else if (pity.legendary >= 15) {
        weights = { common: 48, rare: 34, epic: 12, legendary: 6 }
      }

      let rolled = weightedRandom(weights, rng)
      if (bonusRarity && ORDER.indexOf(rolled) < ORDER.indexOf(bonusRarity)) {
        rolled = bonusRarity
      }
      return rolled
    }

    function updatePity(pity, rolled) {
      return {
        rare: rolled === 'common' ? pity.rare + 1 : 0,
        epic: rolled === 'common' || rolled === 'rare' ? pity.epic + 1 : 0,
        legendary: rolled === 'legendary' ? 0 : pity.legendary + 1,
      }
    }

    // ─── Simulation harness ───

    function runSimulation(iterations, rng = Math.random) {
      let pity = { rare: 0, epic: 0, legendary: 0 }
      const dist = { common: 0, rare: 0, epic: 0, legendary: 0 }
      let firstLegIdx = -1
      const legendaryGaps = []
      let consecNonLeg = 0
      let pityBreaches = 0

      for (let i = 0; i < iterations; i++) {
        // Track if pre-roll pity breaches hard 25 (should never happen because
        // updatePity resets at 25 due to guarantee — но проверяем).
        if (pity.legendary > 25) pityBreaches++

        const rolled = rollRarity(pity, undefined, rng)
        pity = updatePity(pity, rolled)
        dist[rolled]++

        if (rolled === 'legendary') {
          if (firstLegIdx < 0) firstLegIdx = i
          legendaryGaps.push(consecNonLeg)
          consecNonLeg = 0
        } else {
          consecNonLeg++
        }
      }

      const sortedGaps = [...legendaryGaps].sort((a, b) => a - b)
      const median = sortedGaps[Math.floor(sortedGaps.length / 2)] ?? 0

      return {
        iterations,
        distribution: dist,
        pct: {
          common: +(dist.common * 100 / iterations).toFixed(2),
          rare: +(dist.rare * 100 / iterations).toFixed(2),
          epic: +(dist.epic * 100 / iterations).toFixed(2),
          legendary: +(dist.legendary * 100 / iterations).toFixed(2),
        },
        avgLegendaryPer100: +(dist.legendary * 100 / iterations).toFixed(3),
        timeToFirstLegendary: firstLegIdx,
        pityHard25Breaches: pityBreaches,
        legendaryGap: {
          count: legendaryGaps.length,
          min: sortedGaps[0] ?? null,
          max: sortedGaps[sortedGaps.length - 1] ?? null,
          median,
        },
      }
    }

    // ─── Entry point ───

    if (require.main === module) {
      const iterations = parseInt(process.argv[2] || '10000', 10)
      if (!Number.isFinite(iterations) || iterations < 100) {
        console.error('Usage: node simulate_balance.cjs [iterations >= 100]')
        process.exit(1)
      }
      const result = runSimulation(iterations)
      console.log(JSON.stringify(result, null, 2))

      // Acceptance asserts (Locked Decision baseline)
      const errors = []
      if (result.avgLegendaryPer100 < 2.5 || result.avgLegendaryPer100 > 3.5) {
        errors.push(`avgLegendaryPer100 ${result.avgLegendaryPer100} outside [2.5, 3.5]`)
      }
      if (result.pityHard25Breaches !== 0) {
        errors.push(`pityHard25Breaches ${result.pityHard25Breaches} (expected 0)`)
      }
      if (result.legendaryGap.max != null && result.legendaryGap.max > 25) {
        errors.push(`legendaryGap.max ${result.legendaryGap.max} exceeds hard 25 cap`)
      }

      if (errors.length > 0) {
        console.error('\nAcceptance FAILED:')
        for (const e of errors) console.error('  -', e)
        process.exit(2)
      }
      console.error(`\nAcceptance PASSED (${iterations} iterations)`)
    }

    module.exports = { rollRarity, updatePity, runSimulation }
    ```

    После создания запустить:
    ```
    node client/scripts/simulate_balance.cjs 10000
    ```
    Прогон должен exit code 0; output JSON parseable.

    Если acceptance fails (avg outside [2.5, 3.5] на 10K) — это сигнал что **либо**
    rarityRoll.ts mirroring неверный, **либо** Locked Decision weights не дают
    expected distribution. В этом случае — STOP и log как concern; **не**
    править weights без re-discussion.
  </action>
  <verify>
    <automated>node client/scripts/simulate_balance.cjs 10000 > /tmp/sim_balance_19_02.json && echo "exit=$?"</automated>
    <automated>node -e "const r = JSON.parse(require('fs').readFileSync('/tmp/sim_balance_19_02.json', 'utf8')); console.log('avg:', r.avgLegendaryPer100, 'breaches:', r.pityHard25Breaches, 'gap.max:', r.legendaryGap.max); process.exit(r.pityHard25Breaches === 0 && r.legendaryGap.max <= 25 && r.avgLegendaryPer100 >= 2.5 && r.avgLegendaryPer100 <= 3.5 ? 0 : 1)"</automated>
  </verify>
  <done>
    - File `client/scripts/simulate_balance.cjs` exists, ≥130 lines.
    - Run на 10K iterations: exit code 0.
    - JSON output содержит ключи: iterations, distribution, pct,
      avgLegendaryPer100, timeToFirstLegendary, pityHard25Breaches, legendaryGap.
    - pityHard25Breaches === 0; legendaryGap.max ≤ 25; avgLegendaryPer100 ∈ [2.5, 3.5].
  </done>
</task>

<task type="auto">
  <name>Task 2: Add npm run sim-balance script + commit baseline numbers in header comment</name>
  <files>
    package.json
    client/scripts/simulate_balance.cjs
  </files>
  <action>
    1. Открыть `package.json`. Найти `"scripts": { ... }` block. Добавить:
       ```json
       "sim-balance": "node client/scripts/simulate_balance.cjs"
       ```
       (entry рядом с другими verify-* скриптами; preserve existing keys).

    2. Запустить дважды на 10K чтобы получить repeatable baseline:
       ```
       node client/scripts/simulate_balance.cjs 10000 > /tmp/run1.json
       node client/scripts/simulate_balance.cjs 10000 > /tmp/run2.json
       ```
       Затем 1×100K для high-confidence baseline:
       ```
       node client/scripts/simulate_balance.cjs 100000 > /tmp/run100k.json
       ```

    3. Открыть `simulate_balance.cjs` и в header (между линиями `// MIRROR of...`
       и `// Запуск:`) обновить BASELINE NUMBERS блок реальными числами из
       run100k.json:

       ```
       // ─── BASELINE NUMBERS (committed snapshot, seed = Math.random) ───
       //
       // 10K iterations (typical CI run):
       //   pct.common=49.86 / pct.rare=35.14 / pct.epic=11.97 / pct.legendary=3.03
       //   avgLegendaryPer100=3.030
       //   pityHard25Breaches=0
       //   legendaryGap: min=0, max=25, median=33
       //
       // 100K iterations (high-confidence baseline):
       //   pct.common=49.97 / pct.rare=35.04 / pct.epic=11.99 / pct.legendary=3.00
       //   avgLegendaryPer100=3.000
       //   pityHard25Breaches=0
       //   legendaryGap: min=0, max=25, median=33
       //
       // Last refresh: 2026-05-08 (Phase 19-02)
       ```

       (Числа подставь из своих fresh runs; те что в шаблоне — approximation.)

    4. Verify `npm run sim-balance` works:
       ```
       cd /Users/shar/Documents/frog_evolution/frog_evolution_code && npm run sim-balance
       ```
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "sim-balance" package.json</automated>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && npm run sim-balance --silent 2>&1 | grep -E "avgLegendaryPer100|pityHard25Breaches" | head -5</automated>
    <automated>grep -c "BASELINE NUMBERS" client/scripts/simulate_balance.cjs</automated>
  </verify>
  <done>
    - `npm run sim-balance` runs successfully (exit 0).
    - Header содержит BASELINE NUMBERS секцию с реальными числами из 10K + 100K runs.
    - Comment включает дату последнего refresh.
  </done>
</task>

</tasks>

<verification>
1. `node client/scripts/simulate_balance.cjs 10000` — exit 0
2. `node client/scripts/simulate_balance.cjs 100000` — exit 0; avgLegendaryPer100 ≈ 3.0
3. `npm run sim-balance` — works
4. `grep "BASELINE NUMBERS" client/scripts/simulate_balance.cjs` — найдено
5. `node client/scripts/simulate_balance.cjs 1000000` (1M iterations sanity check —
   опционально, ~2-5 sec): avgLegendaryPer100 stable в [2.95, 3.05]; max gap = 25
</verification>

<success_criteria>
1. **Standalone:** simulate_balance.cjs запускается через `node` без vite/tsconfig.
2. **1:1 mirror:** rollRarity + updatePity функции byte-for-byte эквивалентны
   logic из `client/src/utils/rarityRoll.ts` (modulo TS->JS conversion).
3. **Output JSON valid:** содержит iterations, distribution, pct,
   avgLegendaryPer100, timeToFirstLegendary, pityHard25Breaches, legendaryGap.
4. **Acceptance hard guarantees:**
   - pityHard25Breaches === 0 на любом seed
   - legendaryGap.max ≤ 25 на любом seed
5. **Acceptance probabilistic:** на 10K — avgLegendaryPer100 ∈ [2.5, 3.5];
   на 100K — avgLegendaryPer100 ∈ [2.7, 3.3] (stricter).
6. **package.json:** новый script `sim-balance`.
7. **Baseline committed:** header comment содержит **реальные** numbers из
   локальных runs (не placeholder), с timestamp.
8. **Phase 19-01 dependency satisfied:** rarityRoll.ts + updatePity behavior уже
   verified тестами Plan 19-01; Plan 19-02 переиспользует identical pure logic.
</success_criteria>

<output>
После завершения создать `.planning/phases/19-balance-tutorial-i18n-polish/19-02-SUMMARY.md`
с разделами:
- Что создано (1 новый файл, 1 modified)
- Baseline numbers зафиксированные в header (10K + 100K)
- Acceptance: pityHard25Breaches=0, legendaryGap.max=25 (verified)
- Sync обязательство: при изменении rarityRoll.ts — refresh BASELINE NUMBERS
- Atomic commits: `phase-19: add Monte Carlo balance simulation script`,
  `phase-19: commit Monte Carlo baseline numbers (10K + 100K)`
</output>
