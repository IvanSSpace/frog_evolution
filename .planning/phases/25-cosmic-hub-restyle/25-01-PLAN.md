---
phase: 25-cosmic-hub-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/CosmicHub/CosmicHubModal.tsx
autonomous: true
requirements:
  - PHASE25-SHELL
  - PHASE25-HEADER
  - PHASE25-TABSTRIP
  - PHASE25-LOCKSCREEN
estimated_hours: 2

must_haves:
  truths:
    - "Cosmic Hub фон тёмно-зелёный cosmic (#1a2e1a), не gray-950"
    - "Header показывает 🧬 emoji + title с textShadow + pink-tinted close button"
    - "Active tab имеет pink underline (3px solid #ec4899) + bobble scale 1.0↔1.02"
    - "Inactive tabs читаемы но dim (rgba(255,255,255,0.4))"
    - "Disabled tabs (когда tab.enabled=false) показывают 🔒 + opacity 0.2"
    - "Lock screen (когда !cosmosUnlocked) выглядит как WelcomeModal-style card"
    - "Все кнопки в shell имеют type=\"button\" + touchAction: manipulation"
  artifacts:
    - path: "client/src/components/CosmicHub/CosmicHubModal.tsx"
      provides: "Restyled modal shell + header + tab strip + lock screen"
      contains: "#1a2e1a"
  key_links:
    - from: "CosmicHubModal.tsx"
      to: "#ec4899 pink accent + #1a2e1a dark bg"
      via: "inline styles (заменяют bg-gray-950 / border-emerald-400 / text-white/N Tailwind utilities)"
      pattern: "style={{.*#ec4899.*}}"
    - from: "Active tab"
      to: "CSS keyframe bobble"
      via: "inline <style> block с @keyframes cosmic-tab-bobble"
      pattern: "@keyframes cosmic-tab-bobble"
---

<objective>
Restyle Cosmic Hub modal shell под единый app design language: dark cosmic theme (#1a2e1a) + pink accents (#ec4899) + WelcomeModal-style lock screen + bobble-on-active tab strip.

Purpose: Phase 25 — visual-only restyle. Plan 25-01 заменяет foundation (shell + header + tab strip + lock screen) до того как tab contents (Plan 25-02) и sub-modals (Plan 25-03) получают свой polish. Тёмно-зелёный cosmic фон matches Phase 23 WelcomeModal + Phase 24 CaptainBirthModal pattern.

Output: Restyled `CosmicHubModal.tsx` (~222 lines → ~250-280 lines after inline styles). Bundle delta target +1-2 KB gzip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-cosmic-hub-restyle/25-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design language references — READ THESE before editing:
@client/src/components/Onboarding/WelcomeModal.tsx
@client/src/ui/components/LocationStack.tsx
@client/src/components/Captain/CaptainBirthModal.tsx

# Файл который trogаем:
@client/src/components/CosmicHub/CosmicHubModal.tsx

<interfaces>
<!-- Ключевая структура CosmicHubModal — извлечено для удобства executor'а -->
<!-- Чтобы НЕ trogать функциональность: state, TABS array shape, useCosmosUnlocked, onClose — оставить нетронутым. -->

CosmicHubModal props:
```typescript
interface Props {
  onClose: () => void
}
```

TABS array shape (НЕ менять — только labels рендерятся inline-styled):
```typescript
type Tab = {
  id: CosmicTab
  label: string
  icon: string         // emoji строкой
  enabled: boolean
  lockReason?: string  // i18n key для title tooltip
}
```

State (НЕ trogать):
- activeTab: CosmicTab — текущий tab id
- setActiveTab(id: CosmicTab) — switcher
- cosmosUnlocked: boolean — gate для lock screen
- TABS[5]: Tab[] — order [scouts, station, bestiary, carriers, shop]

Lock screen render branch (около строки 172): `!cosmosUnlocked ? <lock> : <tabs+content>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle modal container + header (background, title, close button)</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <action>
Заменить shell container и header inline styles (per D-Design-Language, D-Main-Shell из CONTEXT.md):

**Container (строка ~145, `className="fixed z-50 flex flex-col bg-gray-950"`):**
- Убрать `bg-gray-950` из className (Tailwind layout utilities `fixed z-50 flex flex-col` ОСТАЁТСЯ — per D-Tailwind-Cleanup).
- Добавить inline style:
  ```
  style={{
    background: '#1a2e1a',
    color: '#fff',
    inset: 0,  // если уже есть — keep
  }}
  ```

**Header section (содержит 🧬 emoji + title + close button):**
- Оставить layout Tailwind utilities (flex, items-center, justify-between, px-N, py-N) — НЕ trogать.
- Title span (`text-white font-bold text-lg` около стр 157): убрать text utilities, заменить inline:
  ```
  style={{
    fontSize: 18,
    fontWeight: 800,
    color: '#fff',
    textShadow: '0 1px 0 rgba(0,0,0,0.4)',
  }}
  ```
  Emoji 🧬 + title text ОСТАВИТЬ как есть (i18n).
- Close button (`text-white/60 text-2xl leading-none px-2` стр ~161):
  - Заменить text utilities на inline color + size.
  - Inline style: `{ color: 'rgba(236,72,153,0.7)', fontSize: 24, lineHeight: 1, padding: '0 8px', touchAction: 'manipulation' }`.
  - Hover state не через Tailwind — добавить `onMouseEnter`/`onMouseLeave` для color flip к `#ec4899` (full opacity), ИЛИ inline через CSS-in-JS pattern с :hover нет — используй простой onMouseEnter pattern как в WelcomeModal CTA. ИЛИ оставь без hover (mobile-first demo) — задокументируй decision в comment.
  - `type="button"` подтвердить (уже стоит на стр 161 — keep).

НЕ менять: onClick={onClose}, aria-label, props, ref, любую логику.

Use Edit tool. Comment блок выше изменений: `// Phase 25-01: dark cosmic shell + pink-tinted close button`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "bg-gray-950" client/src/components/CosmicHub/CosmicHubModal.tsx | grep -q "^0$" && grep -c "#1a2e1a" client/src/components/CosmicHub/CosmicHubModal.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- `bg-gray-950` удалён из CosmicHubModal.tsx (grep returns 0)
- `#1a2e1a` присутствует ≥1 раз в файле
- `🧬` emoji и i18n title `cosmic_hub.title` остались в render
- Close button имеет `type="button"` + `touchAction: manipulation`
- TypeScript компилируется без ошибок (`npm run typecheck` или эквивалент)
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle tab strip (active/inactive/disabled states + bobble keyframe)</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <action>
Заменить tab strip rendering (строки ~187-210, TABS.map render):

**Inline `<style>` block** (mount один раз в top компонента, ПЕРЕД return, по WelcomeModal/LocationStack pattern):
```jsx
<style>{`
  @keyframes cosmic-tab-bobble {
    0%, 100% { transform: scaleY(1.0); }
    50% { transform: scaleY(1.02); }
  }
`}</style>
```

**Tab button rendering (TABS.map):**
- Container button: оставить flex/padding layout utilities, заменить text/border utilities inline.
- Active tab inline style:
  ```
  {
    color: '#fff',
    fontWeight: 700,
    borderBottom: '3px solid #ec4899',
    padding: '12px 16px',  // py-3 px-4 эквивалент — НО оставить как Tailwind py-3 px-4 если так в layout cleanup
    animation: 'cosmic-tab-bobble 1.5s ease-in-out infinite',
    transformOrigin: 'bottom center',
    touchAction: 'manipulation',
  }
  ```
- Inactive (enabled, !active):
  ```
  {
    color: 'rgba(255,255,255,0.4)',
    fontWeight: 500,
    padding: '12px 16px',
    touchAction: 'manipulation',
  }
  ```
- Disabled (!tab.enabled):
  ```
  {
    color: 'rgba(255,255,255,0.2)',
    opacity: 0.6,
    cursor: 'not-allowed',
    padding: '12px 16px',
  }
  ```

**Hover state для inactive tabs:** добавить onMouseEnter/Leave переключатель background → `rgba(236,72,153,0.1)` (pink tint), либо оставить без hover (mobile-first) и задокументировать в comment.

**Disabled tab icon:** замена `tab.enabled ? tab.icon : '🔒'` уже на месте (стр ~205) — keep, только убедиться что 🔒 рендерится корректно с новым стилем.

**`type="button"`:** проверить что есть на каждой tab кнопке (если не было — добавить).

НЕ менять: onClick logic, disabled prop binding, title prop binding, tab.id keys, активный tab detection (`activeTab === tab.id`).

Comment блок: `// Phase 25-01: tab strip pink-active underline + bobble + dim inactive/disabled`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "border-emerald-400" client/src/components/CosmicHub/CosmicHubModal.tsx | grep -q "^0$" && grep -c "cosmic-tab-bobble" client/src/components/CosmicHub/CosmicHubModal.tsx | awk '{exit !($1 >= 1)}' && grep -c "#ec4899" client/src/components/CosmicHub/CosmicHubModal.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- `border-emerald-400` удалён (grep returns 0) — pink underline заменил emerald
- `@keyframes cosmic-tab-bobble` определён в файле (grep returns ≥1)
- `#ec4899` присутствует в файле (для active underline)
- 5 tabs render correctly: active одна имеет pink underline + bobble; остальные dim; disabled показывает 🔒
- Все tab buttons имеют `type="button"`
- TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Restyle lock screen (WelcomeModal-style card когда !cosmosUnlocked)</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <action>
Заменить lock screen render branch (около строк 172-180, `!cosmosUnlocked ? <lock> : ...`):

**Lock screen — match WelcomeModal pattern (dark card + 2px border + centered):**

Текущий код (~ строки 173-180):
```jsx
<div className="flex-1 flex flex-col items-center justify-center px-6">
  <div className="text-6xl mb-4">🔒</div>
  <div className="text-white text-lg font-bold mb-2">
    {t('cosmic_hub.locked.title')}
  </div>
  <div className="text-white/60 text-sm">
    {t('cosmic_hub.locked.hint')}
  </div>
</div>
```

Заменить на (per D-Lock-Screen из CONTEXT.md):
```jsx
<div
  className="flex-1 flex flex-col items-center justify-center px-6"
  style={{ background: '#1a2e1a' /* same as shell — for safety */ }}
>
  <div
    style={{
      borderRadius: 16,
      background: '#1a2e1a',
      border: '2px solid rgba(255,255,255,0.15)',
      padding: 24,
      maxWidth: 320,
      width: '100%',
      textAlign: 'center',
      boxShadow: '0 8px 24px rgba(0,0,0,0.4)',
    }}
  >
    <div style={{ fontSize: 64, marginBottom: 16, lineHeight: 1 }}>🔒</div>
    <div
      style={{
        fontSize: 22,
        fontWeight: 800,
        color: '#fde047',  // gold accent per CONTEXT.md
        marginBottom: 8,
        textShadow: '0 1px 0 rgba(0,0,0,0.4)',
      }}
    >
      {t('cosmic_hub.locked.title')}
    </div>
    <div
      style={{
        fontSize: 14,
        fontWeight: 500,
        color: '#d4d4d8',
        lineHeight: 1.4,
      }}
    >
      {t('cosmic_hub.locked.hint')}
    </div>
  </div>
</div>
```

Decision per Claude's Discretion: gold (`#fde047`) для title accent (matches Captain Birth pattern + создаёт cosmic vibe). Если не понравится визуально — можно переключить на `#ec4899` в Plan 25-04 polish.

НЕ trogать: useCosmosUnlocked() call, conditional rendering branch `!cosmosUnlocked ? ... : ...`, i18n keys.

Comment блок: `// Phase 25-01: lock screen WelcomeModal-style dark card + gold title`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "text-6xl" client/src/components/CosmicHub/CosmicHubModal.tsx | grep -q "^0$" && grep -c "cosmic_hub.locked.title" client/src/components/CosmicHub/CosmicHubModal.tsx | awk '{exit !($1 >= 1)}' && grep -c "#fde047" client/src/components/CosmicHub/CosmicHubModal.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- `text-6xl` удалён (lock emoji теперь inline `fontSize: 64`)
- `cosmic_hub.locked.title` и `cosmic_hub.locked.hint` остались в render (i18n не trogается)
- Gold accent `#fde047` присутствует (или pink `#ec4899` если decision flipped — задокументировать)
- Lock card имеет 2px white-15% border
- TypeScript clean; `npm run dev` mount'ит modal без runtime warnings
- Manual: открыть приложение со state cosmosUnlocked=false → lock screen видим, dark card, gold title, 🔒 emoji 64px
  </done>
</task>

</tasks>

<verification>
**Build chain:**
```bash
cd /Users/shar/Documents/frog_evolution/frog_evolution_code
npm run typecheck   # tsc clean
npm run build       # vite build clean
```

**Visual smoke (manual, briefly):**
1. `npm run dev`, открыть app, навигировать к Cosmic Hub (если cosmos unlocked — через bottom bar; если нет — заведомо trigger lock screen через DEV helper).
2. Cosmic Hub shell: фон тёмно-зелёный (НЕ чёрный), header 🧬 + title с textShadow, close button × розоватый.
3. Tab strip: 5 tabs, активный имеет pink underline + лёгкий bob, остальные dim, disabled tabs показывают 🔒.
4. Lock screen (если doable): dark card 320px, gold title, hint text dim.

**Bundle delta** (фиксируем в SUMMARY, не gate):
```bash
ls -la dist/assets/ | grep -E "(index|CosmicHub)"
```
Target: +1-2 KB gzip total. Acceptable: ±5 KB per CONTEXT.md.

**НЕ trogается:**
- Функциональность tab switching, useCosmosUnlocked, onClose handler
- i18n keys (cosmic_hub.title, cosmic_hub.locked.title/hint, cosmic_hub.tab_*)
- sessionStorage SESSION_KEY logic
- TABS array shape (ids, enabled flags)
</verification>

<success_criteria>
- [x] CosmicHubModal.tsx restyled: dark cosmic bg #1a2e1a, pink-tinted close button, header textShadow
- [x] Tab strip: pink active underline + bobble keyframe, dim inactive (white/40), disabled с 🔒 + opacity
- [x] Lock screen: WelcomeModal-style dark card 320px, gold title, hint text
- [x] Все buttons имеют `type="button"` + `touchAction: manipulation`
- [x] `bg-gray-950`, `border-emerald-400`, `text-6xl` удалены из файла
- [x] Layout Tailwind utilities (flex, items-center, justify-between, px-N, py-N) **остались** (D-Tailwind-Cleanup)
- [x] Никаких изменений в логике (props, state, handlers, i18n keys)
- [x] tsc + vite build clean
- [x] Bundle delta ±5 KB gzip
</success_criteria>

<output>
After completion, create `.planning/phases/25-cosmic-hub-restyle/25-01-SUMMARY.md` с:
- Список изменений (3 секции: shell, tab strip, lock screen)
- Decisions logged (gold vs pink для lock title — что выбрали)
- Bundle delta (before/after gzip)
- Screenshots (если возможно) или textual visual confirmation
- TODO для Plan 25-02 (tabs content) и Plan 25-03 (sub-modals)
</output>
