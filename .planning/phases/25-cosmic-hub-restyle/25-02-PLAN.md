---
phase: 25-cosmic-hub-restyle
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - client/src/components/CosmicHub/ShipTab.tsx
  - client/src/components/CosmicHub/SerumInventoryTab.tsx
  - client/src/components/CosmicHub/BestiaryTab.tsx
  - client/src/components/CosmicHub/CarriersTab.tsx
  - client/src/components/CosmicHub/CarrierInfoCard.tsx
  - client/src/components/CosmicHub/CosmicShopTab.tsx
autonomous: true
requirements:
  - PHASE25-TAB-SHIP
  - PHASE25-TAB-SERUMS
  - PHASE25-TAB-BESTIARY
  - PHASE25-TAB-CARRIERS
  - PHASE25-TAB-SHOP
estimated_hours: 2

must_haves:
  truths:
    - "ShipTab: mission CTA + refresh кнопки — pink gradient pill (3D inset-shadow)"
    - "SerumInventoryTab: серум cards имеют borderRadius 12 + inset-shadow + pink count badges"
    - "BestiaryTab: filter pills имеют pink active state, grid cells inset-shadow"
    - "CarriersTab: carrier cards в WelcomeModal-card стиле (dark + 1px white-10% border)"
    - "CosmicShopTab: shop items — rounded cards, pink «Купить» CTAs"
    - "Никакой функциональности tabs не trogается (filters, sorts, drop handlers, actions)"
  artifacts:
    - path: "client/src/components/CosmicHub/ShipTab.tsx"
      provides: "Pink gradient CTA pills"
      contains: "linear-gradient(180deg, #f9a8d4"
    - path: "client/src/components/CosmicHub/SerumInventoryTab.tsx"
      provides: "Inset-shadow serum cards + pink badges"
      contains: "borderRadius"
    - path: "client/src/components/CosmicHub/BestiaryTab.tsx"
      provides: "Pink filter pills (light pass)"
    - path: "client/src/components/CosmicHub/CarriersTab.tsx"
      provides: "WelcomeModal-style carrier list cards"
    - path: "client/src/components/CosmicHub/CarrierInfoCard.tsx"
      provides: "Restyled inner card visual (dark + subtle border)"
    - path: "client/src/components/CosmicHub/CosmicShopTab.tsx"
      provides: "Rounded shop items + pink «Купить» CTAs"
  key_links:
    - from: "Каждый tab"
      to: "shell #1a2e1a фон (Plan 25-01)"
      via: "tab content sits inside CosmicHubModal — фон должен contrasting/transparent"
      pattern: "background.*rgba\\(255,255,255,0\\.06\\)"
    - from: "ShipTab CTA"
      to: "LocationStack pink gradient pattern"
      via: "linear-gradient(180deg, #f9a8d4 0%, #db2777 100%) + inset-shadow"
      pattern: "linear-gradient.*#f9a8d4.*#db2777"
---

<objective>
Light visual polish для 5 tab contents: заменить Tailwind color/text/border utilities на inline styles, привязанные к design language (dark cards + pink CTAs + inset-shadow buttons). Функциональность tabs (drop handlers, filters, actions, store integration) остаётся неизменной.

Purpose: Plan 25-01 заложил shell. Этот plan polishes контент 5 tabs так, чтобы они visually consistent с shell. Light pass — не rebuild. CarriersTab + CarrierInfoCard объединены в один scope потому что CarrierInfoCard рендерится только внутри CarriersTab list (45 строк inner component).

Output: 6 файлов с inline-style replacements. Bundle delta target +2-3 KB gzip total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-cosmic-hub-restyle/25-CONTEXT.md
@.planning/phases/25-cosmic-hub-restyle/25-01-SUMMARY.md

# Design language references — READ for button + card patterns:
@client/src/ui/components/LocationStack.tsx
@client/src/components/Onboarding/WelcomeModal.tsx

# Файлы которые trogаем:
@client/src/components/CosmicHub/ShipTab.tsx
@client/src/components/CosmicHub/SerumInventoryTab.tsx
@client/src/components/CosmicHub/BestiaryTab.tsx
@client/src/components/CosmicHub/CarriersTab.tsx
@client/src/components/CosmicHub/CarrierInfoCard.tsx
@client/src/components/CosmicHub/CosmicShopTab.tsx

<interfaces>
<!-- Design tokens (reuse внутри всех tasks этого plan'а) -->

```typescript
// Цветовая палитра (inline reuse)
const PINK = '#ec4899'
const PINK_LIGHT = '#f9a8d4'
const PINK_DARK = '#db2777'
const GOLD = '#fde047'
const CARD_BG = 'rgba(255,255,255,0.06)'  // subtle на тёмном shell
const CARD_BORDER = '1px solid rgba(255,255,255,0.1)'
const TEXT_DIM = '#d4d4d8'
const TEXT_VERY_DIM = 'rgba(255,255,255,0.4)'

// Pink gradient pill CTA (per LocationStack pattern):
const PINK_CTA_STYLE = {
  background: 'linear-gradient(180deg, #f9a8d4 0%, #db2777 100%)',
  borderRadius: 999,
  padding: '10px 20px',
  fontWeight: 800,
  color: '#fff',
  textShadow: '0 1px 0 rgba(0,0,0,0.4)',
  boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.45), 0 3px 0 rgba(0,0,0,0.3)',
  border: 'none',
  cursor: 'pointer',
  touchAction: 'manipulation' as const,
}

// Dark card (per WelcomeModal pattern):
const DARK_CARD_STYLE = {
  borderRadius: 12,
  background: 'rgba(255,255,255,0.06)',
  border: '1px solid rgba(255,255,255,0.1)',
  padding: 12,
  boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.05)',
}
```

Executor MAY извлечь эти константы в `client/src/components/CosmicHub/_styles.ts` для DRY (рекомендуется) ИЛИ заинлайнить в каждый компонент. Decision на усмотрение — главное consistency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShipTab + SerumInventoryTab restyle (pink CTAs + serum cards)</name>
  <files>client/src/components/CosmicHub/ShipTab.tsx, client/src/components/CosmicHub/SerumInventoryTab.tsx</files>
  <action>
**ShipTab.tsx (162 строки) — light pass:**

1. Прочитать файл, найти все `<button>` элементы (mission CTAs, refresh, etc).
2. Каждый primary button (mission start, «Refresh»/«Обновить» если есть, любое action CTA):
   - Убрать Tailwind color/bg utilities (`bg-emerald-*`, `bg-blue-*`, `text-white`, `rounded-*`, etc).
   - Оставить layout utilities (flex, padding, margin) если только color.
   - Применить inline `PINK_CTA_STYLE` (см. interfaces в context).
3. Headers (если есть section labels типа «Доступные миссии»): inline `{ fontWeight: 800, fontSize: 14, color: '#fff', textShadow: '0 1px 0 rgba(0,0,0,0.4)', marginBottom: 8 }`.
4. Inner cards (mission descriptions, planet info): применить `DARK_CARD_STYLE`.
5. Disabled buttons (когда нет crew или mission cooldown): добавить `style={{ ...PINK_CTA_STYLE, opacity: 0.5, cursor: 'not-allowed' }}` overrides.
6. Все buttons получают `type="button"`.

НЕ trogать: onClick handlers, props, store calls, store selectors, mission config logic, conditional rendering, i18n keys.

**SerumInventoryTab.tsx (182 строки) — light pass:**

1. Прочитать файл, найти serum item cards (вероятно map по inventory).
2. Каждая card:
   - Layout Tailwind keep (grid/flex/gap).
   - Border/bg/text utilities → inline `DARK_CARD_STYLE` + per-rarity-tint border (если уже есть rarity color logic — keep, только wrap в inline style).
3. Count badges (числовые pill бэйджи на serum cards):
   - Если есть `bg-emerald-*` / `bg-gray-*` — заменить на:
     ```
     {
       background: '#ec4899',
       color: '#fff',
       borderRadius: 999,
       padding: '2px 8px',
       fontSize: 12,
       fontWeight: 700,
       boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.3), 0 1px 0 rgba(0,0,0,0.2)',
     }
     ```
4. Empty state (если есть «Нет серумов»): применить TEXT_DIM color + center text.
5. Любые action buttons (drag handle, apply, info): pink CTA pattern если primary.

НЕ trogать: drag-and-drop logic, store integration (useGameStore selectors), serum filter logic, i18n keys.

Comment в начале каждого файла: `// Phase 25-02: visual restyle (Tailwind color utilities → inline styles)`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -E "(bg-emerald-|bg-gray-9|bg-blue-5)" client/src/components/CosmicHub/ShipTab.tsx client/src/components/CosmicHub/SerumInventoryTab.tsx | grep -v '^#' | wc -l | awk '{exit !($1 == 0)}' && grep -c "linear-gradient" client/src/components/CosmicHub/ShipTab.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- ShipTab.tsx: pink gradient CTAs визуально видны; нет `bg-emerald-*` / `bg-blue-*` Tailwind utilities в кнопках
- SerumInventoryTab.tsx: serum cards имеют borderRadius 12 + inset-shadow; count badges pink
- Все buttons имеют `type="button"`
- Функциональность нетронута (drag, apply, filter, refresh — works)
- TypeScript clean; `npm run build` clean
- Manual: открыть Ship tab → mission CTA розовый pill; открыть SerumInventory tab → cards rounded + pink count badges
  </done>
</task>

<task type="auto">
  <name>Task 2: BestiaryTab restyle (filter pills + grid cells light pass)</name>
  <files>client/src/components/CosmicHub/BestiaryTab.tsx</files>
  <action>
**BestiaryTab.tsx (166 строк) — самый «осторожный» tab, потому что это Phase 18 виртуализированный grid:**

ВНИМАНИЕ: BestiaryTab уже rebuilt в Phase 18 с @tanstack/react-virtual + FilterPills component. Этот tab — НЕ rebuild. Только color/text replacements.

**ВАЖНО:** BestiaryTab вероятно импортирует FilterPills (subdir `bestiary/` есть). Сначала прочитать BestiaryTab.tsx + проверить какие компоненты в `client/src/components/CosmicHub/bestiary/`. Если `FilterPills` отдельный компонент — НЕ trogать его в этом плане (его pink-active pill restyle отложить в Plan 25-04 polish, если нужно).

Фокус Task 2 — top-level BestiaryTab.tsx scope:

1. Найти любые Tailwind color/border utilities на top-level BestiaryTab.tsx (например header «Бестиарий», location switcher, empty state).
2. Заменить:
   - Header text → inline `{ fontWeight: 800, fontSize: 16, color: '#fff', textShadow: '0 1px 0 rgba(0,0,0,0.4)' }`
   - Empty state text → `{ color: TEXT_VERY_DIM, fontSize: 14, textAlign: 'center' }`
   - Если есть location filter tabs/buttons на уровне BestiaryTab — pink active state inline (`color: '#ec4899'`, `borderBottom: '2px solid #ec4899'`).
3. **НЕ trogать:** BestiaryCell rendering, virtualized list logic, BestiaryDetailModal, MilestoneToast.
4. **НЕ trogать:** bestiary/FilterPills.tsx, bestiary/BestiaryCell.tsx, bestiary/BestiaryGrid.tsx — это Phase 18 deep work. Если визуально явно несовместимо после Plan 25-02 — отложить в Plan 25-04 polish или Phase 26.

Comment в начале файла: `// Phase 25-02: light pass restyle (только top-level BestiaryTab; bestiary/ subdir НЕ trogается)`.

Bundle delta для этого task должен быть minimal (<0.3 KB gzip) — только header/empty/filter colors.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && npx tsc --noEmit -p client 2>&1 | grep -E "BestiaryTab.tsx" | grep -v '^#' | wc -l | awk '{exit !($1 == 0)}'</automated>
  </verify>
  <done>
- BestiaryTab.tsx top-level styles match dark cosmic theme (no `bg-gray-*`/`text-white/N` Tailwind utilities на header/empty state)
- Pink accent присутствует если location/filter tabs существуют на этом уровне
- `bestiary/` subdir НЕ trogается (grep `git status client/src/components/CosmicHub/bestiary/` показывает clean)
- Virtualized grid rendering works (manual: scroll через bestiary без glitches)
- TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 3: CarriersTab + CarrierInfoCard + CosmicShopTab restyle</name>
  <files>client/src/components/CosmicHub/CarriersTab.tsx, client/src/components/CosmicHub/CarrierInfoCard.tsx, client/src/components/CosmicHub/CosmicShopTab.tsx</files>
  <action>
**CarriersTab.tsx (68 строк) + CarrierInfoCard.tsx (45 строк) — list + inner card pair:**

1. CarriersTab: контейнер list (вероятно scrollable column). Сохранить layout Tailwind (flex flex-col gap-N px-N py-N).
   - Header «Носители» / empty state — inline стили как в Task 2 (header bold + textShadow; empty state TEXT_DIM).
2. CarrierInfoCard: каждая carrier list item.
   - Apply `DARK_CARD_STYLE` к контейнеру card.
   - Inner text: name, level, element — inline color `#fff` для primary, `TEXT_DIM` для secondary.
   - Любые badges (level, rarity, element) → mini pills:
     ```
     {
       borderRadius: 999,
       padding: '2px 8px',
       fontSize: 11,
       fontWeight: 700,
       background: 'rgba(255,255,255,0.1)',  // или element-specific tint
       color: '#fff',
     }
     ```
   - Action buttons (если есть feed/dispose/info inline): pink CTA mini variant
     ```
     { ...PINK_CTA_STYLE, padding: '6px 12px', fontSize: 12 }
     ```
   - `type="button"` + `touchAction: manipulation`.

НЕ trogать: store selectors, dispose/merge/feed handlers (они в MainScene/store actions), ceiling display logic.

**CosmicShopTab.tsx (206 строк) — самый комплексный из tabs:**

1. Shop items (6 cards, per Phase 22): list/grid с item card per shop entry.
2. Каждая card:
   - Apply `DARK_CARD_STYLE`.
   - Item icon + name + description: layout flex; цвета inline.
   - Price display: emphasis с золотом (`color: GOLD`) для essence, pink для серум currency (или GOLD везде — claude discretion).
   - «Купить» / «Buy» CTA: `PINK_CTA_STYLE` mini variant.
   - Disabled state (insufficient currency): opacity 0.5 + cursor not-allowed.
3. Headers / section labels: bold + textShadow.
4. Currency display sidebar (если есть на top): mini pills с GOLD/PINK tints.
5. **Не trogать:** purchase logic, store cosmic.buyItem call, item config, i18n keys, currency calc.

Comment в начале каждого файла: `// Phase 25-02: visual restyle (dark cards + pink CTAs)`.

**Decision на усмотрение** (документировать в SUMMARY):
- CosmicShopTab может выбрать pastel-green dark variant вместо чистого dark — обе опции approved per Design Note. Default: dark cosmic theme (consistency с shell).
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -E "(bg-emerald-|bg-gray-9|bg-blue-5)" client/src/components/CosmicHub/CarriersTab.tsx client/src/components/CosmicHub/CarrierInfoCard.tsx client/src/components/CosmicHub/CosmicShopTab.tsx | grep -v '^#' | wc -l | awk '{exit !($1 == 0)}'</automated>
  </verify>
  <done>
- 3 файла restyled, нет `bg-emerald-*` / `bg-gray-9*` / `bg-blue-5*` Tailwind utilities
- CarrierInfoCard выглядит как dark card (rounded 12 + subtle border + dim secondary text)
- CosmicShopTab: каждый shop item — dark card + pink «Купить» CTA
- Все action buttons имеют `type="button"`
- Purchase flow работает (manual: купить cheap item — currency списан, UI обновляется)
- TypeScript clean
- Bundle delta cumulative за Plan 25-02 ≤ +5 KB gzip
  </done>
</task>

</tasks>

<verification>
**Build chain:**
```bash
cd /Users/shar/Documents/frog_evolution/frog_evolution_code
npm run typecheck && npm run build
```

**Visual smoke (manual через все 5 tabs):**
1. Open Cosmic Hub (cosmos unlocked).
2. **Ship tab:** mission CTAs — pink gradient pill; planet info card — dark + rounded.
3. **SerumInventory tab:** серум cards — rounded 12, inset shadow; count badges — pink pills.
4. **Bestiary tab:** header/empty visually match; virtualized grid scrolls fine.
5. **Carriers tab:** carrier list cards — dark + subtle border + dim secondary text.
6. **Cosmic Shop tab:** 6 items — dark cards + pink «Купить»; currency display gold pills.

**Регрессионная проверка (НЕ менять функциональность):**
- Switching между tabs работает (Plan 25-01 tab strip OK)
- Drag-drop серум из Inventory работает
- Покупка в Shop работает (если есть currency)
- Feed/dispose carrier через MainScene НЕ ломается
- Bestiary scroll smooth (DOM <30 cells)

**Bundle delta** (фикс в SUMMARY):
Target: +2-3 KB gzip за весь Plan 25-02. Acceptable: ≤+5 KB.
</verification>

<success_criteria>
- [x] ShipTab: mission/action CTAs — pink gradient pills + inset-shadow
- [x] SerumInventoryTab: serum cards rounded 12 + pink count badges
- [x] BestiaryTab: light pass на top-level (bestiary/ subdir НЕ trogается)
- [x] CarriersTab + CarrierInfoCard: WelcomeModal-style dark cards
- [x] CosmicShopTab: 6 dark item cards + pink «Купить» CTAs
- [x] Все 6 файлов: Tailwind color/border/text utilities заменены inline
- [x] Layout Tailwind utilities оставлены (flex/grid/gap/px/py)
- [x] Все buttons имеют `type="button"` + `touchAction: manipulation`
- [x] Никакой функциональности не trogается (handlers/store/i18n keys intact)
- [x] tsc + vite build clean
- [x] Bundle delta cumulative ≤+5 KB gzip
</success_criteria>

<output>
After completion, create `.planning/phases/25-cosmic-hub-restyle/25-02-SUMMARY.md` с:
- Список изменений (3 tasks × ~2 файла each)
- Decisions logged (CosmicShopTab dark vs pastel-green choice; CarrierInfoCard badge stylings; вынес ли `_styles.ts` shared module)
- Bundle delta (gzip before/after per task)
- Visual confirmations (любой tab — что выглядит правильно)
- Известные visual inconsistencies для Plan 25-04 polish (если есть)
- TODO для Plan 25-03 (sub-modals)
</output>
