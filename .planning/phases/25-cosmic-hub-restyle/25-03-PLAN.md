---
phase: 25-cosmic-hub-restyle
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - client/src/components/CosmicHub/SerumModal.tsx
  - client/src/components/CosmicHub/BulkOpenSummary.tsx
  - client/src/components/CosmicHub/PityCounterDisplay.tsx
autonomous: true
requirements:
  - PHASE25-SUB-SERUM-MODAL
  - PHASE25-SUB-BULKOPEN
  - PHASE25-SUB-PITY-COUNTER
estimated_hours: 1

must_haves:
  truths:
    - "SerumModal: dark cosmic background + pink CTA + subtle border"
    - "BulkOpenSummary: stats card match dark cosmic theme + pink accents для legendary"
    - "PityCounterDisplay: progress bar pink accents + dark bg footer indicator"
    - "Sub-modals НЕ rebuild structure — только color/border/text fixes"
  artifacts:
    - path: "client/src/components/CosmicHub/SerumModal.tsx"
      provides: "Dark cosmic SerumModal с pink CTA"
      contains: "#1a2e1a"
    - path: "client/src/components/CosmicHub/BulkOpenSummary.tsx"
      provides: "Stats card в dark theme + pink legendary accents"
    - path: "client/src/components/CosmicHub/PityCounterDisplay.tsx"
      provides: "Progress bar с pink accents"
      contains: "#ec4899"
  key_links:
    - from: "SerumModal CTA"
      to: "Pink gradient pill pattern"
      via: "linear-gradient(180deg, #f9a8d4, #db2777)"
      pattern: "linear-gradient.*#f9a8d4"
    - from: "PityCounterDisplay progress fill"
      to: "Pink #ec4899 accent"
      via: "inline background style на progress bar fill div"
      pattern: "background.*#ec4899"
---

<objective>
Light pass restyle для 3 sub-modal'ей/индикаторов: SerumModal, BulkOpenSummary, PityCounterDisplay. Match dark cosmic theme + pink accents. Маленький plan (~1ч), параллельный с Plan 25-02 (нет file overlap).

Purpose: Sub-modals самостоятельны и не зависят от tabs content polish (Plan 25-02). Phase 19 закрыл PityCounterDisplay в footer-style, нужно только refresh accent colors. SerumModal — apply CTA. BulkOpenSummary — stats card после bulk open в BoxesTab/SerumsTab (НЕ trogается их functionality).

Output: 3 файла с inline-style polish. Bundle delta target +0.5-1 KB gzip total.

**ВАЖНО:** CascadeRevealModal (Phase 15, 294 строки animation overlay) НЕ входит в scope — per CONTEXT.md "Не трогаем ... CascadeRevealModal animations".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-cosmic-hub-restyle/25-CONTEXT.md
@.planning/phases/25-cosmic-hub-restyle/25-01-SUMMARY.md

# Design language references:
@client/src/components/Onboarding/WelcomeModal.tsx
@client/src/components/Captain/CaptainBirthModal.tsx

# Файлы которые trogаем:
@client/src/components/CosmicHub/SerumModal.tsx
@client/src/components/CosmicHub/BulkOpenSummary.tsx
@client/src/components/CosmicHub/PityCounterDisplay.tsx

<interfaces>
<!-- Те же design tokens что в Plan 25-02 (если был извлечён `_styles.ts` — reuse, иначе inline) -->

```typescript
const PINK = '#ec4899'
const PINK_CTA_STYLE = {
  background: 'linear-gradient(180deg, #f9a8d4 0%, #db2777 100%)',
  borderRadius: 999,
  padding: '10px 20px',
  fontWeight: 800,
  color: '#fff',
  textShadow: '0 1px 0 rgba(0,0,0,0.4)',
  boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.45), 0 3px 0 rgba(0,0,0,0.3)',
  border: 'none',
  cursor: 'pointer',
  touchAction: 'manipulation' as const,
}

const DARK_MODAL_STYLE = {
  background: '#1a2e1a',
  border: '2px solid rgba(255,255,255,0.15)',
  borderRadius: 16,
  padding: 24,
  color: '#fff',
  boxShadow: '0 8px 24px rgba(0,0,0,0.4)',
}
```

Если Plan 25-02 создал `client/src/components/CosmicHub/_styles.ts` — импортировать оттуда.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SerumModal restyle (dark cosmic + pink apply CTA)</name>
  <files>client/src/components/CosmicHub/SerumModal.tsx</files>
  <action>
**SerumModal.tsx (75 строк) — самый компактный sub-modal:**

1. Modal container: применить `DARK_MODAL_STYLE` через inline style.
2. Backdrop: если `bg-black bg-opacity-N` или подобные — заменить на:
   ```
   { background: 'rgba(0,0,0,0.6)', backdropFilter: 'blur(2px)' }
   ```
3. Title (likely «Применить серум» или название серума):
   - Inline `{ fontSize: 18, fontWeight: 800, color: '#fff', textShadow: '0 1px 0 rgba(0,0,0,0.4)', marginBottom: 12 }`.
4. Body/description text:
   - Inline `{ fontSize: 14, color: '#d4d4d8', lineHeight: 1.4, marginBottom: 16 }`.
5. Confirm/Apply CTA:
   - `PINK_CTA_STYLE` + `type="button"`.
6. Cancel/Close button (если есть):
   - Secondary style:
     ```
     {
       background: 'transparent',
       border: '1px solid rgba(255,255,255,0.2)',
       color: '#d4d4d8',
       borderRadius: 999,
       padding: '8px 16px',
       cursor: 'pointer',
       touchAction: 'manipulation',
     }
     ```
7. **НЕ trogать:** props (serum data, onConfirm, onCancel handlers), conditional logic, i18n keys.

**Cliclability checklist:**
- Backdrop click → onClose (если был — keep)
- stopPropagation на inner modal div (если был — keep, если не было и нужно — добавить)
- `type="button"` на всех кнопках
- z-index уровень модала >= 51 (выше shell z-50)

Comment в начале файла: `// Phase 25-03: SerumModal dark theme + pink apply CTA`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "#1a2e1a" client/src/components/CosmicHub/SerumModal.tsx | awk '{exit !($1 >= 1)}' && grep -c "linear-gradient" client/src/components/CosmicHub/SerumModal.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- SerumModal.tsx имеет dark cosmic bg `#1a2e1a` + 2px white-15% border
- Apply CTA — pink gradient pill
- Cancel/secondary button — outlined dim
- Backdrop с blur
- Все кнопки имеют `type="button"` + `touchAction: manipulation`
- Apply flow работает (manual: select серум → apply → carrier feed triggers correctly)
- TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 2: BulkOpenSummary restyle (stats card в dark theme + pink legendary accent)</name>
  <files>client/src/components/CosmicHub/BulkOpenSummary.tsx</files>
  <action>
**BulkOpenSummary.tsx (149 строк) — modal появляется после «Открыть все» в boxes flow:**

1. Сначала прочитать файл, identify modal container, stats grid/list, individual rarity rows.
2. Modal container: `DARK_MODAL_STYLE` (см. interfaces).
3. Title (likely «Результаты» / «Bulk Open Summary»):
   - Bold 18px white + textShadow.
4. Stats rows (по rarity: common, blue, purple, gold/legendary):
   - Каждая row как inset card:
     ```
     {
       borderRadius: 12,
       padding: '10px 14px',
       background: 'rgba(255,255,255,0.06)',
       border: '1px solid rgba(255,255,255,0.1)',
       boxShadow: 'inset 0 1px 0 rgba(255,255,255,0.05)',
       display: 'flex',
       justifyContent: 'space-between',
       marginBottom: 8,
     }
     ```
   - Rarity name (left): inline color по rarity (common: #d4d4d8, blue: #60a5fa, purple: #a78bfa, legendary: GOLD).
   - Count (right): bold pink pill badge `{ background: '#ec4899', color: '#fff', borderRadius: 999, padding: '2px 10px', fontWeight: 800 }`.
5. **Legendary glow** (если есть special highlight для legendary count > 0 — Phase 15 had это):
   - keep glow logic, но заменить любой gold/yellow Tailwind на inline:
     ```
     boxShadow: '0 0 12px 2px rgba(253,224,71,0.6), inset 0 1px 0 rgba(255,255,255,0.1)'
     ```
6. Footer/Close CTA: `PINK_CTA_STYLE` (mini variant `padding: '8px 16px', fontSize: 14`).
7. Backdrop: dark + blur (как SerumModal).
8. **НЕ trogать:** props (results data, onClose), totals calculation, conditional render (legendary banner only if count > 0).

Comment: `// Phase 25-03: BulkOpenSummary dark stats card + pink count pills + GOLD legendary glow`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "#1a2e1a" client/src/components/CosmicHub/BulkOpenSummary.tsx | awk '{exit !($1 >= 1)}' && grep -c "#ec4899" client/src/components/CosmicHub/BulkOpenSummary.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- BulkOpenSummary.tsx: dark modal bg `#1a2e1a`, rarity stat rows как inset cards
- Count badges pink pills `#ec4899`
- Legendary highlight (если был) сохранён с gold glow
- Close CTA pink pill
- Bulk open flow работает (manual: открыть 5+ боксов «Открыть все» → BulkOpenSummary показывает rarity counts correctly)
- TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 3: PityCounterDisplay restyle (pink progress + dark footer)</name>
  <files>client/src/components/CosmicHub/PityCounterDisplay.tsx</files>
  <action>
**PityCounterDisplay.tsx (67 строк) — footer indicator от Phase 19:**

Этот компонент уже compact + footer-style. Restyle минимальный.

1. Прочитать файл, identify контейнер + progress bar + label.
2. Container: dark footer bg
   ```
   {
     background: 'rgba(0,0,0,0.4)',
     borderTop: '1px solid rgba(255,255,255,0.1)',
     padding: '8px 16px',
     display: 'flex',
     alignItems: 'center',
     gap: 8,
   }
   ```
3. Label text («До легендарки» / «Pity» / etc per Phase 19 i18n keys):
   - Inline `{ fontSize: 12, fontWeight: 700, color: '#d4d4d8' }`.
4. Progress bar:
   - Track: `{ flex: 1, height: 6, background: 'rgba(255,255,255,0.1)', borderRadius: 999, overflow: 'hidden' }`.
   - Fill: pink gradient
     ```
     {
       height: '100%',
       background: 'linear-gradient(90deg, #f9a8d4 0%, #ec4899 100%)',
       borderRadius: 999,
       transition: 'width 300ms ease-out',
       width: `${percent}%`,  // existing logic для percent — keep
     }
     ```
5. Exact-reveal mode (Phase 19: hidden→dots→exact at 0/3/5 opened boxes):
   - Dots variant — pink dots: `{ background: '#ec4899', borderRadius: 999, width: 6, height: 6 }`.
   - Exact number variant — bold pink text: `{ color: '#ec4899', fontWeight: 800, fontSize: 12 }`.
6. **НЕ trogать:** progressive reveal logic (Phase 19 reveal rules: hidden if <3 opened, dots if 3-5, exact if ≥5), pity calc (current/maxPity), i18n keys, store selectors.

Comment: `// Phase 25-03: PityCounterDisplay dark footer + pink progress gradient`.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -c "#ec4899" client/src/components/CosmicHub/PityCounterDisplay.tsx | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <done>
- PityCounterDisplay.tsx: dark footer (rgba(0,0,0,0.4) bg) + top border
- Pink progress gradient `#f9a8d4 → #ec4899`
- Dots/exact variants pink-tinted
- Phase 19 progressive reveal logic preserved (hidden/dots/exact at 0/3/5 opened)
- Manual: открыть 0/3/6 боксов → display переключается hidden → dots → exact как раньше
- TypeScript clean
- Bundle delta cumulative для Plan 25-03 ≤ +1.5 KB gzip
  </done>
</task>

</tasks>

<verification>
**Build chain:**
```bash
cd /Users/shar/Documents/frog_evolution/frog_evolution_code
npm run typecheck && npm run build
```

**Visual smoke (manual):**
1. **SerumModal:** trigger через SerumInventoryTab → apply серум на carrier → modal должен показать dark bg + pink CTA. Cancel → close без crash.
2. **BulkOpenSummary:** trigger через «Открыть все» (если есть боксы; используй dev helpers если нужно) → dark stats card + pink count pills; legendary glow если есть legendary в результатах.
3. **PityCounterDisplay:** footer на BoxesTab/SerumsTab; progress fill pink-gradient; reveal mode переключается на 3 и 5 opened boxes per Phase 19 spec.

**Регрессионная проверка:**
- Apply серум работает (carrier feed/stabilize triggers)
- Bulk open results корректные (rarity counts match opened boxes)
- Pity counter числа правильные (matches store pity state)

**Bundle delta** (фикс в SUMMARY):
Target: +0.5-1 KB gzip за весь Plan 25-03. Acceptable: ≤+2 KB.
</verification>

<success_criteria>
- [x] SerumModal: dark cosmic modal + pink apply CTA + outlined cancel
- [x] BulkOpenSummary: dark stats card + pink rarity count pills + GOLD legendary glow preserved
- [x] PityCounterDisplay: dark footer + pink progress gradient + reveal modes preserved
- [x] CascadeRevealModal **НЕ trogается** (verify: `git status client/src/components/CosmicHub/CascadeRevealModal.tsx` clean)
- [x] Все 3 sub-modals: backdrop + inner modal cliclability checklist (z-index, stopPropagation, type="button", touchAction)
- [x] i18n keys нетронуты
- [x] tsc + vite build clean
- [x] Bundle delta cumulative ≤+2 KB gzip
</success_criteria>

<output>
After completion, create `.planning/phases/25-cosmic-hub-restyle/25-03-SUMMARY.md` с:
- Список изменений per file
- Decisions logged (backdrop blur opt-in? pity progress gradient direction? GOLD vs PINK legendary highlight choice)
- Bundle delta gzip
- Visual confirmations для каждого sub-modal
- Cliclability checklist подтверждение
- TODO для Plan 25-04 (smoke + finalize)
</output>
