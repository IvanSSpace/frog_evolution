---
phase: 23-onboarding-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/onboarding/types.ts
  - client/src/store/onboarding/onboardingSlice.ts
  - client/src/store/onboarding/onboardingSlice.test.ts
  - client/src/store/persistence.ts
  - client/src/store/eventBus.ts
  - client/src/components/Onboarding/OnboardingController.tsx
  - client/src/utils/onboardingDevHelpers.ts
  - client/src/utils/devHelpers.ts
  - client/src/App.tsx
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements: [PHASE23-STATE, PHASE23-CONTROLLER]
estimated_hours: 5

must_haves:
  truths:
    - "Onboarding state живёт отдельно от cosmic slice — собственный модуль store/onboarding/"
    - "localStorage ключ frog_evolution_onboarding хранит JSON: { welcomeSeen, firstBoxTapSeen, firstMergeSeen, locationsCelebrated }"
    - "load/save функции переживают corruption (try/catch → defaults) — поведение consistent с persistence.ts"
    - "Все четыре mark-actions работают и persist'ятся на каждом вызове"
    - "OnboardingController смонтирован в App.tsx и читает state через Zustand subscribe"
    - "Контроллер — пустой shell (placeholder render null) который последующие планы заполнят beat'ами"
    - "window.__resetOnboarding() и window.__skipOnboarding() работают в DEV через devHelpers"
    - "TypeScript build (tsc --noEmit) проходит"
    - "Vitest для onboardingSlice 8/8 PASS (state defaults, mark actions, locationsCelebrated map)"
  artifacts:
    - path: "client/src/store/onboarding/types.ts"
      provides: "OnboardingState interface + OnboardingActions type"
      exports: ["OnboardingState", "OnboardingActions"]
    - path: "client/src/store/onboarding/onboardingSlice.ts"
      provides: "useOnboardingStore Zustand store с subscribeWithSelector"
      exports: ["useOnboardingStore"]
    - path: "client/src/store/onboarding/onboardingSlice.test.ts"
      provides: "vitest suite ≥8 tests for store + persistence round-trip"
    - path: "client/src/components/Onboarding/OnboardingController.tsx"
      provides: "DOM-side coordinator (state machine для beats) — Wave 1 skeleton, заполняется Plan 23-02..05"
      exports: ["OnboardingController"]
    - path: "client/src/utils/onboardingDevHelpers.ts"
      provides: "installOnboardingDevHelpers() — __resetOnboarding + __skipOnboarding"
      exports: ["installOnboardingDevHelpers"]
  key_links:
    - from: "client/src/App.tsx"
      to: "OnboardingController"
      via: "JSX mount после TutorialOverlay"
      pattern: "<OnboardingController"
    - from: "client/src/store/onboarding/onboardingSlice.ts"
      to: "localStorage frog_evolution_onboarding"
      via: "loadOnboarding/saveOnboarding в persistence.ts"
      pattern: "ONBOARDING_KEY"
    - from: "client/src/utils/devHelpers.ts"
      to: "installOnboardingDevHelpers()"
      via: "DEV bootstrap"
      pattern: "installOnboardingDevHelpers"
---

<objective>
Foundation для Phase 23: отдельный Zustand store `useOnboardingStore` с persisted state,
mount'нутый координатор `OnboardingController.tsx` (пока пустой shell), dev helpers
`__resetOnboarding` / `__skipOnboarding`, и базовые i18n ключи (namespace `onboarding.*`).

Этот plan — фундамент. Все последующие планы (23-02..23-05) реализуют beat-эффекты,
которые читают state и markActions отсюда.

Purpose: чистая изолированная state-машина без вмешательства в cosmic slice
(чтобы Phase 22 migration logic не пострадала; onboarding — отдельная локальная UX-фича).

Output: store + persistence + контроллер-shell + dev helpers + skeleton i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-onboarding-flow/23-CONTEXT.md
@.planning/phases/22-carrier-merge-redesign/22-07-SUMMARY.md
@client/src/store/persistence.ts
@client/src/store/eventBus.ts
@client/src/App.tsx
@client/src/utils/cosmicSettings.ts
@client/src/utils/devHelpers.ts

<interfaces>
<!-- Существующий паттерн persistence.ts: per-key load/save с try/catch defaults -->
<!-- Существующий паттерн devHelpers.ts: install*DevHelpers() аттачит window globals в DEV -->

OnboardingState shape (создать в types.ts):
```typescript
export interface OnboardingState {
  welcomeSeen: boolean
  firstBoxTapSeen: boolean
  firstMergeSeen: boolean
  locationsCelebrated: Record<number, boolean>
}

export interface OnboardingActions {
  markWelcomeSeen: () => void
  markFirstBoxTapSeen: () => void
  markFirstMergeSeen: () => void
  markLocationCelebrated: (locationId: number) => void
  // Dev-only reset (используется внутри __resetOnboarding)
  __reset: () => void
}
```

Существующий eventBus events (НЕ менять, только добавить новый namespace):
- 'location:unlocked': { locationId: number } — уже emit'ится в MergeController
- 'cosmic:toast': CosmicToastPayload — может reuse OR создаём 'onboarding:toast'

Существующий persistence pattern:
- COSMOS_UNLOCKED_KEY = 'frog_evolution_cosmos_unlocked' — отдельный ключ от cosmic slice
- loadCosmosUnlocked()/saveCosmosUnlocked(bool) — пример minimal load/save

Существующий devHelpers паттерн:
```typescript
// installBestiaryDevHelpers() в utils/devHelpers.ts
export function installBestiaryDevHelpers() {
  if (!import.meta.env.DEV) return
  ;(window as any).__bestiaryCount = () => ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать onboarding store + types + persistence</name>
  <files>
    client/src/store/onboarding/types.ts,
    client/src/store/onboarding/onboardingSlice.ts,
    client/src/store/persistence.ts
  </files>
  <action>
    Создать новую папку `client/src/store/onboarding/` (по аналогии с `store/cosmic/`,
    но проще — это плоский slice, не subdivided slices).

    **`client/src/store/onboarding/types.ts`:**
    Экспортировать `OnboardingState` + `OnboardingActions` точно как в interfaces выше.
    `locationsCelebrated` — `Record<number, boolean>`, ключи — locationId числами
    (например 2 для Болото, 3 для Лес, 6 для Star Map).

    **`client/src/store/onboarding/onboardingSlice.ts`:**
    Использовать `zustand` + `subscribeWithSelector` middleware (consistent с gameStore).
    Создать `useOnboardingStore = create<OnboardingState & OnboardingActions>()(...)`.

    Defaults: все false, `locationsCelebrated: {}`.

    Каждая mark-action:
    1. `set((s) => ({ ...s, <field>: true }))` (или для locations — `set((s) => ({ locationsCelebrated: { ...s.locationsCelebrated, [id]: true } }))`)
    2. После set вызывать `saveOnboarding(get())` (synchronously, как в cosmicSettings).

    `__reset` action: `set(() => ({ welcomeSeen: false, firstBoxTapSeen: false, firstMergeSeen: false, locationsCelebrated: {} }))` + `saveOnboarding(...)`.

    Initial state: `loadOnboarding()` (см. Task 1.2).

    **`client/src/store/persistence.ts` (расширить):**
    Добавить вверху рядом с другими ключами:
    ```ts
    // Phase 23: onboarding flow per-device state.
    const ONBOARDING_KEY = 'frog_evolution_onboarding'
    ```

    Экспортировать:
    ```ts
    export function loadOnboarding(): OnboardingState {
      const defaults: OnboardingState = {
        welcomeSeen: false,
        firstBoxTapSeen: false,
        firstMergeSeen: false,
        locationsCelebrated: {},
      }
      try {
        const raw = localStorage.getItem(ONBOARDING_KEY)
        if (!raw) return defaults
        const parsed = JSON.parse(raw) as Partial<OnboardingState>
        return {
          welcomeSeen: typeof parsed.welcomeSeen === 'boolean' ? parsed.welcomeSeen : false,
          firstBoxTapSeen: typeof parsed.firstBoxTapSeen === 'boolean' ? parsed.firstBoxTapSeen : false,
          firstMergeSeen: typeof parsed.firstMergeSeen === 'boolean' ? parsed.firstMergeSeen : false,
          locationsCelebrated:
            parsed.locationsCelebrated && typeof parsed.locationsCelebrated === 'object'
              ? Object.fromEntries(
                  Object.entries(parsed.locationsCelebrated)
                    .filter(([k, v]) => !isNaN(Number(k)) && typeof v === 'boolean')
                    .map(([k, v]) => [Number(k), v as boolean])
                )
              : {},
        }
      } catch { return defaults }
    }

    export function saveOnboarding(state: OnboardingState): void {
      try { localStorage.setItem(ONBOARDING_KEY, JSON.stringify(state)) } catch {}
    }
    ```

    `OnboardingState` import — из `./onboarding/types`. Использовать `import type`
    чтобы избежать runtime cycle (slice импортирует persistence).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "onboarding|persistence" | head -10</automated>
    Должно быть пусто (clean compile).
  </verify>
  <done>
    Все три файла существуют. tsc clean. `useOnboardingStore.getState()` возвращает
    defaults shape. `__reset()` сбрасывает state и localStorage обновляется.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать OnboardingController skeleton + смонтировать в App.tsx</name>
  <files>
    client/src/components/Onboarding/OnboardingController.tsx,
    client/src/App.tsx
  </files>
  <action>
    Создать новую папку `client/src/components/Onboarding/` (по аналогии с `Tutorial/`).

    **`OnboardingController.tsx`:**
    Это shell — Wave 1 версия. Beat-эффекты добавит Plan 23-02 (Welcome),
    23-03 (Tap-hint), 23-04 (Merge demo), 23-05 (Location celebration).

    ```tsx
    import { useEffect } from 'react'
    import { useOnboardingStore } from '../../store/onboarding/onboardingSlice'
    import { eventBus } from '../../store/eventBus'

    /**
     * Phase 23: Soft 4-beat onboarding coordinator.
     *
     * State machine (filled by Plan 23-02..05):
     *   Beat 1: Welcome modal (if !welcomeSeen) — Plan 23-02
     *   Beat 2: Tap-hint pulse (if welcomeSeen && !firstBoxTapSeen && box landed) — Plan 23-03
     *   Beat 3: Merge demo (if firstBoxTapSeen && !firstMergeSeen && 2 L1 frogs) — Plan 23-04
     *   Beat 4: Location celebration (on 'location:unlocked' если !locationsCelebrated[id]) — Plan 23-05
     *
     * Skeleton: возвращает null, но subscribe'ится на state для future expansion.
     */
    export function OnboardingController() {
      const welcomeSeen = useOnboardingStore((s) => s.welcomeSeen)
      const firstBoxTapSeen = useOnboardingStore((s) => s.firstBoxTapSeen)
      const firstMergeSeen = useOnboardingStore((s) => s.firstMergeSeen)
      // Plan 23-05 заполнит location subscription.

      useEffect(() => {
        // Placeholder для cleanup'ов которые future plans добавят.
        return () => {}
      }, [])

      // Wave 1: shell. Beat-overlays add'ятся в render'е следующих планов.
      return null
    }
    ```

    **`client/src/App.tsx` (модифицировать):**
    Добавить import:
    ```tsx
    import { OnboardingController } from './components/Onboarding/OnboardingController'
    ```

    Внутри App JSX (рядом с `<TutorialOverlay />` около line 317):
    ```tsx
    <TutorialOverlay />
    <OnboardingController />
    ```

    OnboardingController должен mount'иться unconditionally — он сам ничего не рендерит
    пока state не triggered. Plan 23-02..05 заменят `return null` на conditional JSX.

    **Cliclability check (memory feedback_clickability):** этот controller сам ничего
    не рендерит в Wave 1, но напоминание для future plans — все overlays должны иметь
    `type="button"` и `z-index: 100+`.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "OnboardingController|App.tsx" | head -10</automated>
    Должно быть пусто.
  </verify>
  <done>
    `<OnboardingController />` появляется в App.tsx JSX. `import.meta.env.DEV` mount
    работает без runtime errors (запустить vite dev → проверить console).
  </done>
</task>

<task type="auto">
  <name>Task 3: Dev helpers + i18n bootstrap + unit tests</name>
  <files>
    client/src/utils/onboardingDevHelpers.ts,
    client/src/utils/devHelpers.ts,
    client/src/i18n/ru.json,
    client/src/i18n/en.json,
    client/src/i18n/es.json,
    client/src/store/onboarding/onboardingSlice.test.ts
  </files>
  <action>
    **`client/src/utils/onboardingDevHelpers.ts`:**
    ```ts
    import { useOnboardingStore } from '../store/onboarding/onboardingSlice'

    export function installOnboardingDevHelpers(): void {
      if (!import.meta.env.DEV) return
      const w = window as any

      w.__resetOnboarding = () => {
        useOnboardingStore.getState().__reset()
        // reload чтобы Welcome modal вышел сразу
        window.location.reload()
      }

      w.__skipOnboarding = () => {
        const s = useOnboardingStore.getState()
        s.markWelcomeSeen()
        s.markFirstBoxTapSeen()
        s.markFirstMergeSeen()
        // Cover known location ids: 2 (Болото), 3 (Лес), 6 (Star Map).
        ;[2, 3, 6].forEach((id) => s.markLocationCelebrated(id))
        console.info('[onboarding] all flags set true')
      }
    }
    ```

    **`client/src/utils/devHelpers.ts` (модифицировать):**
    Найти существующий `installBestiaryDevHelpers` и добавить рядом
    вызов `installOnboardingDevHelpers` из bootstrap (если есть orchestrator file)
    или экспортнуть новую функцию из этого файла:
    ```ts
    import { installOnboardingDevHelpers } from './onboardingDevHelpers'
    // ... existing ...
    export function installAllDevHelpers() {
      installBestiaryDevHelpers()
      installOnboardingDevHelpers()
    }
    ```
    Если `installBestiaryDevHelpers` уже вызывается напрямую в `App.tsx`, добавить
    `installOnboardingDevHelpers()` рядом в том же useEffect bootstrap'е.
    Проверить grep'ом куда `installBestiaryDevHelpers` вызывается — сохранить тот же
    callsite паттерн.

    **i18n bootstrap (RU/EN/ES) — ДОБАВИТЬ namespace `onboarding`:**
    Каждый файл `ru.json` / `en.json` / `es.json` получает блок с заглушками:
    ```json
    "onboarding": {
      "welcome": {
        "title": "Frog Evolution",
        "subtitle": "Тапай лягушек. Соединяй одинаковых. Открывай мир.",
        "cta": "Начать"
      },
      "tapHint": {
        "label": "Тапни 👆"
      },
      "mergeHint": {
        "label": "Перетащи одну на другую",
        "success": "Готово! Дальше мерджи всё подряд"
      },
      "location": {
        "unlocked": "{{name}} открыто! Тапни иконку чтобы перейти"
      }
    }
    ```

    RU — финальные строки (как в CONTEXT.md). EN/ES — точные переводы:
    - EN welcome.subtitle: "Tap frogs. Merge same. Discover the world."
    - EN tapHint.label: "Tap 👆"
    - EN mergeHint.label: "Drag one onto another"
    - EN mergeHint.success: "Done! Now merge everything"
    - EN location.unlocked: "{{name}} unlocked! Tap the icon to visit"
    - EN welcome.cta: "Start"
    - ES welcome.subtitle: "Toca ranas. Junta iguales. Descubre el mundo."
    - ES tapHint.label: "Toca 👆"
    - ES mergeHint.label: "Arrastra una sobre la otra"
    - ES mergeHint.success: "¡Listo! Ahora junta todo"
    - ES location.unlocked: "¡{{name}} desbloqueado! Toca el icono para visitar"
    - ES welcome.cta: "Empezar"

    Для location names используется существующий i18n ключ (например `locations.swamp`),
    interpolated через `{{name}}` — фактическое interpolation Plan 23-05 настроит.

    **`client/src/store/onboarding/onboardingSlice.test.ts` (vitest):**
    Минимум 8 тестов:
    1. defaults state: все false, locationsCelebrated empty
    2. markWelcomeSeen() → welcomeSeen=true, остальные false
    3. markFirstBoxTapSeen() → firstBoxTapSeen=true
    4. markFirstMergeSeen() → firstMergeSeen=true
    5. markLocationCelebrated(2) → locationsCelebrated[2]=true
    6. markLocationCelebrated(2) + markLocationCelebrated(3) → оба true, не overwrite
    7. __reset() после mark всех → возвращается к defaults
    8. persistence round-trip: после mark и create нового store, state восстановлен

    Каждый тест очищает localStorage в beforeEach: `localStorage.removeItem('frog_evolution_onboarding')`.
    Test setup использует Zustand fresh module (через `vi.resetModules()` + dynamic import,
    паттерн как в `cosmicSettings.test.ts`).
  </action>
  <verify>
    <automated>cd client && npx vitest run src/store/onboarding/onboardingSlice.test.ts 2>&1 | tail -20</automated>
    Все 8 тестов PASS.
  </verify>
  <done>
    Vitest 8/8 PASS. `npm run check-translations` (если существует) PASS — все 5
    новых ключей × 3 локали присутствуют. В DEV консоли `window.__resetOnboarding` и
    `window.__skipOnboarding` доступны.
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx tsc --noEmit` clean
2. `cd client && npx vitest run src/store/onboarding/` 8+ PASS
3. `cd client && npm run check-translations 2>&1` (если есть script) PASS
4. Manual: открыть DEV, выполнить `__resetOnboarding()` → reload → `useOnboardingStore.getState()` показывает defaults
5. Manual: `__skipOnboarding()` → state имеет все true, locationsCelebrated = {2: true, 3: true, 6: true}
</verification>

<success_criteria>
- store/onboarding/ модуль готов с types + slice + tests
- persistence.ts расширен load/saveOnboarding
- OnboardingController смонтирован в App.tsx (пока shell)
- Dev helpers работают
- i18n RU/EN/ES имеют onboarding namespace
- Все vitest PASS, tsc clean
- Plan 23-02..05 могут потреблять store и controller без изменения foundation
</success_criteria>

<output>
После завершения создать `.planning/phases/23-onboarding-flow/23-01-SUMMARY.md`
с актуальными метриками: bundle delta, vitest counts, файлы created/modified.
</output>
