---
phase: 23-onboarding-flow
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - client/src/components/Onboarding/WelcomeModal.tsx
  - client/src/components/Onboarding/welcomeModal.css
  - client/src/components/Onboarding/OnboardingController.tsx
autonomous: false
requirements: [PHASE23-BEAT1-WELCOME]
estimated_hours: 3

must_haves:
  truths:
    - "При first app open (welcomeSeen=false) появляется centered modal с backdrop dim"
    - "Backdrop click игнорируется (single-action) — закрыть можно ТОЛЬКО через CTA «Начать»"
    - "Modal содержит title «Frog Evolution», subtitle, animated frog SVG, pink CTA"
    - "Frog SVG bobbing animation работает (CSS keyframes scale 1.0↔1.05, 1.5s loop)"
    - "CTA tap → fade-out 400ms → markWelcomeSeen() → modal unmounts"
    - "После CTA click повторное reload НЕ показывает modal (welcomeSeen=true в localStorage)"
    - "Modal соответствует mobile-first (max-width 320px, viewport-safe)"
    - "Все texts читаются из i18n (welcome.title/subtitle/cta)"
    - "TypeScript build clean; no console errors на mount"
  artifacts:
    - path: "client/src/components/Onboarding/WelcomeModal.tsx"
      provides: "Beat 1 Welcome modal с CSS keyframes анимациями"
      exports: ["WelcomeModal"]
      min_lines: 80
    - path: "client/src/components/Onboarding/welcomeModal.css"
      provides: "CSS keyframes для bobbing + fade-out, button styles"
      contains: "@keyframes"
  key_links:
    - from: "client/src/components/Onboarding/OnboardingController.tsx"
      to: "WelcomeModal"
      via: "Conditional render когда !welcomeSeen"
      pattern: "<WelcomeModal"
    - from: "client/src/components/Onboarding/WelcomeModal.tsx"
      to: "useOnboardingStore.markWelcomeSeen"
      via: "CTA onClick handler"
      pattern: "markWelcomeSeen"
---

<objective>
Beat 1 onboarding: centered single-action modal с pastel gradient,
анимированной L1 frog, pink CTA «Начать». Backdrop click ignored
(welcome — единственный blocking step). После CTA — fade-out + markWelcomeSeen.

Reuse existing visual language: pink `#ec4899` gradient button, rounded 16px,
font-weight 900 title, textShadow, inset shadow.

Purpose: первый introduction для нового игрока — что это за приложение и
что делать дальше. Не объясняем mechanics здесь (это Beat 2-3) — только
positive vibe + clear «Начать».

Output: WelcomeModal component готов, integrated в OnboardingController.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-onboarding-flow/23-CONTEXT.md
@.planning/phases/23-onboarding-flow/23-01-PLAN.md
@client/src/components/Onboarding/OnboardingController.tsx
@client/src/store/onboarding/onboardingSlice.ts
@client/src/ui/components/LocationStack.tsx
@frog_obsidian/Design Notes/2026-05-18-onboarding-flow.md

<interfaces>
<!-- Reuse от Plan 23-01: -->

useOnboardingStore actions:
```typescript
const markWelcomeSeen = useOnboardingStore((s) => s.markWelcomeSeen)
```

i18n keys (созданы в Plan 23-01):
- `onboarding.welcome.title` — "Frog Evolution"
- `onboarding.welcome.subtitle` — "Тапай лягушек. Соединяй одинаковых. Открывай мир."
- `onboarding.welcome.cta` — "Начать"

<!-- Existing visual reference (LocationStack.tsx) — palette/gradients/borderRadius: -->
```typescript
// Pink CTA pattern (из existing buttons):
style={{
  background: 'linear-gradient(180deg, #f9a8d4, #ec4899)',
  borderRadius: '999px',
  padding: '12px 32px',
  color: '#fff',
  fontWeight: 900,
  fontSize: '18px',
  textShadow: '0 1px 0 rgba(0,0,0,0.4)',
  boxShadow:
    'inset 0 1px 0 rgba(255,255,255,0.45), 0 2px 0 rgba(0,0,0,0.25)',
  border: 'none',
  cursor: 'pointer',
}}
```

<!-- Pastel gradient bg (lake-blue → swamp-green из LOCATION_VISUAL[1] + [2]): -->
- lake-blue: #bae6fd (LOCATION_VISUAL[1].from)
- swamp-green: #bef264 (LOCATION_VISUAL[2].from)

<!-- React portal pattern (как TutorialOverlay.tsx) для overlay поверх Phaser canvas -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать WelcomeModal.tsx + CSS keyframes</name>
  <files>
    client/src/components/Onboarding/WelcomeModal.tsx,
    client/src/components/Onboarding/welcomeModal.css
  </files>
  <behavior>
    Component testable manually (Task 3 — checkpoint).
    Manual cases:
    - Mount → modal появляется, backdrop dim, frog bobbing
    - Backdrop click → ничего не происходит (no console, no state change)
    - CTA click → fade-out 400ms → unmounts
    - i18n switch (RU→EN→ES) → text updates correctly
  </behavior>
  <action>
    **`welcomeModal.css`:**
    ```css
    @keyframes onb-welcome-fade-in {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes onb-welcome-fade-out {
      from { opacity: 1; }
      to { opacity: 0; }
    }

    @keyframes onb-welcome-frog-bob {
      0%, 100% { transform: scale(1.0); }
      50% { transform: scale(1.05); }
    }

    .onb-welcome-backdrop {
      animation: onb-welcome-fade-in 300ms ease-out forwards;
    }

    .onb-welcome-backdrop.is-exiting {
      animation: onb-welcome-fade-out 400ms ease-in forwards;
    }

    .onb-welcome-frog {
      animation: onb-welcome-frog-bob 1500ms ease-in-out infinite;
      transform-origin: center;
      display: block;
    }

    .onb-welcome-cta:active {
      transform: scale(0.96);
      transition: transform 80ms ease-out;
    }
    ```

    **`WelcomeModal.tsx`:**
    ```tsx
    import { useEffect, useState } from 'react'
    import { createPortal } from 'react-dom'
    import { useTranslation } from 'react-i18next'
    import { useOnboardingStore } from '../../store/onboarding/onboardingSlice'
    import './welcomeModal.css'

    /**
     * Phase 23 Beat 1: Welcome modal.
     * Single-action — backdrop click игнорируется, закрытие только через CTA.
     */
    export function WelcomeModal() {
      const { t } = useTranslation()
      const markSeen = useOnboardingStore((s) => s.markWelcomeSeen)
      const [exiting, setExiting] = useState(false)

      const handleCta = () => {
        if (exiting) return
        setExiting(true)
        // wait fade-out 400ms then mark seen → parent unmounts
        window.setTimeout(() => {
          markSeen()
        }, 400)
      }

      // Backdrop click intentionally НЕ обрабатывается (single-action).

      const node = (
        <div
          className={`onb-welcome-backdrop${exiting ? ' is-exiting' : ''}`}
          role="dialog"
          aria-modal="true"
          aria-labelledby="onb-welcome-title"
          style={{
            position: 'fixed',
            inset: 0,
            zIndex: 100,
            background: 'rgba(0,0,0,0.6)',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            padding: 16,
            touchAction: 'manipulation',
          }}
        >
          <div
            onClick={(e) => e.stopPropagation()}
            style={{
              maxWidth: 320,
              width: '100%',
              padding: '32px 24px 24px',
              borderRadius: 16,
              background:
                'linear-gradient(180deg, #bae6fd 0%, #bef264 100%)',
              boxShadow:
                'inset 0 1px 0 rgba(255,255,255,0.45), 0 8px 20px rgba(0,0,0,0.35)',
              border: '2px solid rgba(255,255,255,0.6)',
              textAlign: 'center',
            }}
          >
            <h1
              id="onb-welcome-title"
              style={{
                margin: 0,
                fontSize: 32,
                fontWeight: 900,
                color: '#0c4a6e',
                textShadow: '0 1px 0 rgba(255,255,255,0.6)',
                lineHeight: 1.1,
              }}
            >
              {t('onboarding.welcome.title')}
            </h1>

            <p
              style={{
                marginTop: 8,
                marginBottom: 16,
                fontSize: 14,
                fontWeight: 600,
                color: '#365314',
                lineHeight: 1.4,
              }}
            >
              {t('onboarding.welcome.subtitle')}
            </p>

            {/* Cute L1 frog SVG. Inline SVG чтобы избежать external asset deps;
                Plan 23-06 может заменить на existing frog asset если нужно. */}
            <div
              className="onb-welcome-frog"
              style={{ margin: '8px auto 20px', width: 96, height: 96 }}
            >
              <svg viewBox="0 0 100 100" width="96" height="96">
                {/* Body */}
                <ellipse cx="50" cy="60" rx="34" ry="28" fill="#65a30d" />
                <ellipse cx="50" cy="58" rx="28" ry="20" fill="#a3e635" />
                {/* Eyes */}
                <circle cx="36" cy="36" r="12" fill="#65a30d" />
                <circle cx="64" cy="36" r="12" fill="#65a30d" />
                <circle cx="36" cy="36" r="9" fill="#fff" />
                <circle cx="64" cy="36" r="9" fill="#fff" />
                <circle cx="38" cy="38" r="5" fill="#000" />
                <circle cx="66" cy="38" r="5" fill="#000" />
                {/* Smile */}
                <path
                  d="M 38 64 Q 50 74 62 64"
                  stroke="#365314"
                  strokeWidth="3"
                  fill="none"
                  strokeLinecap="round"
                />
              </svg>
            </div>

            <button
              type="button"
              className="onb-welcome-cta"
              onClick={handleCta}
              style={{
                background: 'linear-gradient(180deg, #f9a8d4, #ec4899)',
                borderRadius: 999,
                padding: '14px 36px',
                color: '#fff',
                fontWeight: 900,
                fontSize: 18,
                textShadow: '0 1px 0 rgba(0,0,0,0.4)',
                boxShadow:
                  'inset 0 1px 0 rgba(255,255,255,0.45), 0 3px 0 rgba(0,0,0,0.3)',
                border: 'none',
                cursor: 'pointer',
                width: '100%',
              }}
            >
              {t('onboarding.welcome.cta')}
            </button>
          </div>
        </div>
      )

      return createPortal(node, document.body)
    }
    ```

    **Important — Cliclability checklist (memory):**
    - button `type="button"` ✓
    - z-index 100 ✓
    - `e.stopPropagation()` на inner modal ✓
    - Backdrop click НЕ имеет onClick handler (по дизайну — single-action) ✓
    - `touchAction: 'manipulation'` ✓

    **Important — Animation memory:**
    - Используем CSS keyframes (НЕ Lottie) ✓
    - Не tween'им frog.container.alpha — это DOM SVG, отдельная сущность ✓
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "WelcomeModal|welcomeModal" | head -10</automated>
    Должно быть пусто.
  </verify>
  <done>
    Файлы существуют. tsc clean. CSS keyframes валидны.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate WelcomeModal в OnboardingController</name>
  <files>client/src/components/Onboarding/OnboardingController.tsx</files>
  <action>
    Модифицировать OnboardingController:

    ```tsx
    import { useOnboardingStore } from '../../store/onboarding/onboardingSlice'
    import { WelcomeModal } from './WelcomeModal'
    // ... existing imports ...

    export function OnboardingController() {
      const welcomeSeen = useOnboardingStore((s) => s.welcomeSeen)
      const firstBoxTapSeen = useOnboardingStore((s) => s.firstBoxTapSeen)
      const firstMergeSeen = useOnboardingStore((s) => s.firstMergeSeen)
      // ... existing effects ...

      return (
        <>
          {!welcomeSeen && <WelcomeModal />}
          {/* Plan 23-03 добавит TapHintOverlay */}
          {/* Plan 23-04 добавит MergeHintOverlay */}
          {/* Plan 23-05 добавит LocationUnlockCelebration */}
        </>
      )
    }
    ```

    Beat 2 trigger (firstBoxTapSeen) добавит Plan 23-03; здесь только Beat 1 conditional.
    welcomeSeen `false` → WelcomeModal mount; после `markWelcomeSeen()` → re-render
    → `welcomeSeen=true` → unmount автоматически.

    NB: после Plan 23-02 пользователь увидит modal на каждом fresh open
    (если welcomeSeen=false). Plan 23-03 подхватит chain (если бокс уже упал).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "OnboardingController" | head -10</automated>
    Должно быть пусто.
  </verify>
  <done>
    Controller mount'ит WelcomeModal когда welcomeSeen=false; unmount'ит когда true.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UX verification</name>
  <what-built>
    WelcomeModal mount'ится при первом запуске (welcomeSeen=false), single-action
    (backdrop click ignored), CTA fade-out 400ms → markWelcomeSeen → modal исчезает.
  </what-built>
  <how-to-verify>
    1. `cd client && npm run dev`
    2. В DevTools console: `__resetOnboarding()` → page reloads
    3. Проверить: появилась modal по центру с pastel gradient, frog bobbing, pink CTA
    4. Кликнуть на тёмный backdrop вне modal → ничего не происходит (НЕ закрывается)
    5. Кликнуть CTA «Начать» → fade-out animation видна → modal исчезает
    6. F5 reload → modal НЕ появляется (welcomeSeen persisted)
    7. Проверить i18n: переключить на EN → `__resetOnboarding()` → texts EN
    8. Mobile viewport (DevTools 375px) → modal fits, max-width 320px ok
    9. `__resetOnboarding()` ещё раз → modal снова появляется (sanity check цикла)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (visual, animation, click behavior)</resume-signal>
</task>

</tasks>

<verification>
1. `cd client && npx tsc --noEmit` clean
2. Manual UX (Task 3) approved
3. localStorage `frog_evolution_onboarding` после CTA содержит `{"welcomeSeen":true,...}`
4. Vite build (если sanity check): `cd client && npm run build` PASS
</verification>

<success_criteria>
- WelcomeModal появляется ровно один раз per device (welcomeSeen persistence)
- Backdrop click ignored (single-action behavior)
- CTA → fade-out 400ms → state mutation → unmount
- Visual matches existing pink CTA pattern
- i18n RU/EN/ES читаются
- Mobile viewport-safe (375px ok)
- Plan 23-03 может chain Beat 2 после welcomeSeen=true
</success_criteria>

<output>
После завершения создать `.planning/phases/23-onboarding-flow/23-02-SUMMARY.md`
с visual screenshot reference (опц.), bundle delta, deviations.
</output>
