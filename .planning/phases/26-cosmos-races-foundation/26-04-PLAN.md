---
phase: 26-cosmos-races-foundation
plan: 04
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - client/src/components/CosmicHub/InventoryTab.tsx
  - client/src/components/CosmicHub/CosmicHubModal.tsx
  - client/src/store/cosmic/types.ts
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - PHASE26-INVENTORY-TAB
  - PHASE26-INVENTORY-CURRENCIES
  - PHASE26-INVENTORY-SERUMS
  - PHASE26-INVENTORY-PLACEHOLDERS
  - PHASE26-I18N-INVENTORY

must_haves:
  truths:
    - "Шестой таб 🎒 «Инвентарь» виден в CosmicHubModal после cosmos unlock"
    - "Tab переключается по нажатию (cliclability: type=button, touchAction:manipulation)"
    - "Раздел ВАЛЮТЫ показывает essence + gold (live values из gameStore)"
    - "Раздел СЫВОРОТКИ показывает все 16 элементов в grid 4×4 с count'ом (0 показывается серым)"
    - "Раздел АРТЕФАКТЫ показывает placeholder empty-state"
    - "Раздел ОТНОШЕНИЯ С РАСАМИ показывает 10 рас с '?' значениями (placeholder Phase 29)"
    - "Состояние tab сохраняется в sessionStorage (lastActiveTab) consistent с existing Tab persistence"
    - "Reuse Phase 25 design tokens (_styles.ts) — dark cosmic + pink accent consistent"
    - "i18n RU/EN/ES parity (≥20 new keys × 3 locales)"
  artifacts:
    - path: "client/src/components/CosmicHub/InventoryTab.tsx"
      provides: "Read-only inventory display: currencies + serums + artifacts placeholder + race relationships placeholder"
      exports: ["InventoryTab"]
    - path: "client/src/components/CosmicHub/CosmicHubModal.tsx"
      provides: "6th tab registration + renderTab dispatch + sessionStorage 'inventory' literal accept"
      contains: "inventory"
    - path: "client/src/store/cosmic/types.ts"
      provides: "CosmicTab union extended с 'inventory'"
      contains: "'inventory'"
  key_links:
    - from: "InventoryTab.tsx"
      to: "gameStore.ts (essence + gold + serums)"
      via: "useGameStore(s => ({essence:s.essence, gold:s.coins, serums:s.serums}))"
      pattern: "useGameStore.*essence|serums"
    - from: "InventoryTab.tsx"
      to: "config/races.ts (Plan 26-01)"
      via: "RACES iterate для relationships placeholder"
      pattern: "RACES\\."
    - from: "InventoryTab.tsx"
      to: "_styles.ts (Phase 25)"
      via: "DARK_CARD_STYLE / SECTION_HEADER_STYLE / PINK_BADGE_STYLE imports"
      pattern: "_styles"
---

<objective>
Новый таб 🎒 «Инвентарь» в Cosmic Hub. Single read-only view для всех player resources: essence + gold currencies, все 16 serum counts, placeholder секции для artifacts (Phase 27+) и race relationships (Phase 29).

**Purpose:** До Phase 26 нет одного места где игрок видит все свои ресурсы. SerumInventoryTab показывает только серумы. Cosmic Shop показывает balance в header but ephemerally. Inventory tab — единая точка истины. Также готовит UI surface для Phase 27-29 (artifacts + race relationships просто заполнят placeholder'ы).

**Output:** InventoryTab.tsx + 6-я кнопка в CosmicHubModal tab strip + расширение CosmicTab union типа + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-cosmos-races-foundation/26-CONTEXT.md
@.planning/phases/26-cosmos-races-foundation/26-01-SUMMARY.md
@.planning/STATE.md

# Existing patterns to reuse
@client/src/components/CosmicHub/CosmicHubModal.tsx
@client/src/components/CosmicHub/_styles.ts
@client/src/components/CosmicHub/SerumInventoryTab.tsx

<interfaces>
<!-- From Plan 26-01: -->
```typescript
export const RACES: readonly RaceConfig[]
interface RaceConfig {
  id: RaceId
  emojiIcon: string
  nameKey: string  // 'races.crystalloids.name'
  affinity: Element
  // ...
}
```

<!-- Existing CosmicTab type (extend): -->
```typescript
// client/src/store/cosmic/types.ts
export type CosmicTab = 'scouts' | 'boxes' | 'bestiary' | 'carriers' | 'shop'
// Plan 26-04 adds: 'inventory'
```

<!-- Existing CosmicHubModal pattern: -->
- 5 tabs registered with Tab interface: {id, label, icon, enabled, lockReason?}
- sessionStorage key 'cosmic_last_tab' restores active tab
- getInitialTab() validates literal — нужно добавить 'inventory' check
- renderTab() switch с case на каждый tab id
- Defensive cosmos gate: если !cosmosUnlocked показывает lock screen

<!-- Existing _styles.ts tokens (use these): -->
```typescript
PINK = '#ec4899'
PINK_LIGHT = '#f9a8d4'
GOLD = '#fde047'
TEXT_DIM = '#d4d4d8'
TEXT_VERY_DIM = 'rgba(255,255,255,0.4)'
DARK_CARD_STYLE (padding 12, rounded 12, glass bg)
SECTION_HEADER_STYLE (font 14, bold, white)
SECTION_HEADER_LG_STYLE (font 16)
PINK_BADGE_STYLE (rounded pill, pink bg, white text)
MINI_BADGE_STYLE (neutral white-10% bg, white text)
EMPTY_STATE_TEXT_STYLE (text-dim center)
```

<!-- gameStore relevant slices: -->
```typescript
// gameStore.ts
{
  // gold currency from base slice
  coins: number  // gold / slime
  // cosmic slice
  essence: number  // ascension reward
  serums: Record<Element, number>  // 16 entries
}
// All accessible via useGameStore(s => s.essence) etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CosmicTab union + i18n inventory namespace</name>
  <files>
    client/src/store/cosmic/types.ts,
    client/src/i18n/ru.json,
    client/src/i18n/en.json,
    client/src/i18n/es.json
  </files>
  <action>
    **A. `cosmic/types.ts`** — расширить existing CosmicTab union:
    ```ts
    // Phase 17: добавлен carriers tab.
    // Phase 22 Plan 22-05: добавлен shop tab (cosmic shop с двумя валютами).
    // Phase 26 Plan 26-04: добавлен inventory tab (read-only single-view все ресурсы).
    export type CosmicTab = 'scouts' | 'boxes' | 'bestiary' | 'carriers' | 'shop' | 'inventory'
    ```

    **B. i18n inventory namespace** — добавить в `cosmic_hub.inventory.*` (sub-namespace existing cosmic_hub, не top-level).

    RU/EN/ES parity. Подключ для пути `cosmic_hub.inventory`:

    RU:
    ```json
    "cosmic_hub": {
      // ... existing keys ...
      "tab_inventory": "Инвентарь",
      "inventory": {
        "section_currencies": "ВАЛЮТЫ",
        "section_serums": "СЫВОРОТКИ",
        "section_artifacts": "АРТЕФАКТЫ",
        "section_relationships": "ОТНОШЕНИЯ С РАСАМИ",
        "currency_essence": "Эссенция",
        "currency_gold": "Слизь",
        "placeholder_artifacts": "Скоро…",
        "placeholder_relationship_value": "?",
        "tooltip_relationship_pending": "Откроется в следующих обновлениях"
      }
    }
    ```

    EN:
    ```json
    "cosmic_hub": {
      "tab_inventory": "Inventory",
      "inventory": {
        "section_currencies": "CURRENCIES",
        "section_serums": "SERUMS",
        "section_artifacts": "ARTIFACTS",
        "section_relationships": "RELATIONS WITH RACES",
        "currency_essence": "Essence",
        "currency_gold": "Slime",
        "placeholder_artifacts": "Coming soon…",
        "placeholder_relationship_value": "?",
        "tooltip_relationship_pending": "Unlocks in future updates"
      }
    }
    ```

    ES:
    ```json
    "cosmic_hub": {
      "tab_inventory": "Inventario",
      "inventory": {
        "section_currencies": "MONEDAS",
        "section_serums": "SUEROS",
        "section_artifacts": "ARTEFACTOS",
        "section_relationships": "RELACIONES CON RAZAS",
        "currency_essence": "Esencia",
        "currency_gold": "Limo",
        "placeholder_artifacts": "Próximamente…",
        "placeholder_relationship_value": "?",
        "tooltip_relationship_pending": "Se desbloquea en futuras actualizaciones"
      }
    }
    ```

    **Не дублировать** `cosmic_hub.elements` (уже существует — 16 element labels).

    **Проверить parity** через `npm run check-translations`.
  </action>
  <verify>
    <automated>cd client && npm run check-translations 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | grep -E "CosmicTab" || echo "types clean"</automated>
  </verify>
  <done>
    `CosmicTab` union includes 'inventory'. 10 new keys × 3 locales added. check-translations clean. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: InventoryTab.tsx (read-only display)</name>
  <files>client/src/components/CosmicHub/InventoryTab.tsx (new)</files>
  <action>
    Создать `InventoryTab.tsx` с 4 секциями: currencies, serums, artifacts (placeholder), race relationships (placeholder).

    **Структура:**

    ```tsx
    // Phase 26 Plan 26-04: read-only inventory single-view.
    // Sections: currencies, serums (16-element grid), artifacts placeholder,
    //           race relationships placeholder (10 rows с '?' values).
    // Reuse Phase 25 _styles.ts design tokens.
    // Cliclability: tab активируется через CosmicHubModal tab strip; внутри 
    // нет интерактивных elements кроме scroll — read-only display.

    import { useTranslation } from 'react-i18next'
    import { useGameStore } from '../../store/gameStore'
    import { ELEMENTS, type Element } from '../../store/cosmic/types'
    import { RACES } from '../../game/config/races'
    import { ELEMENT_TINTS } from '../../game/effects/elements/elementTints'
    import {
      DARK_CARD_STYLE,
      SECTION_HEADER_STYLE,
      SECTION_HEADER_LG_STYLE,
      PINK_BADGE_STYLE,
      MINI_BADGE_STYLE,
      GOLD,
      PINK,
      TEXT_DIM,
      TEXT_VERY_DIM,
      EMPTY_STATE_TEXT_STYLE,
    } from './_styles'

    // Element emoji mapping (для inventory grid — reuse existing pattern если 
    // уже есть в SerumInventoryTab или ElementGrid; иначе локальная константа).
    const ELEMENT_EMOJI: Record<Element, string> = {
      fire: '🔥', ice: '❄️', water: '💧', forest: '🌲',
      toxic: '☠️', plasma: '⚡', shadow: '🌑', crystal: '💎',
      desert: '🏜️', gas: '☁️', ring: '💍', binary: '🪐',
      arcane: '🔮', mechanical: '⚙️', war: '⚔️', void: '🕳️',
    }

    export function InventoryTab() {
      const { t } = useTranslation()
      
      // Subscribed slices — single-field selectors для fine-grain re-render.
      const essence = useGameStore((s) => s.essence)
      const gold = useGameStore((s) => s.coins)
      const serums = useGameStore((s) => s.serums)
      
      return (
        <div style={{ padding: 12, color: '#fff' }}>
          {/* ─── ВАЛЮТЫ ─── */}
          <section style={{ marginBottom: 16 }}>
            <h3 style={SECTION_HEADER_STYLE}>
              {t('cosmic_hub.inventory.section_currencies')}
            </h3>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
              <CurrencyRow 
                icon="💎" 
                label={t('cosmic_hub.inventory.currency_essence')}
                value={essence}
                valueColor={PINK}
              />
              <CurrencyRow
                icon="💩"
                label={t('cosmic_hub.inventory.currency_gold')}
                value={gold}
                valueColor={GOLD}
              />
            </div>
          </section>

          {/* ─── СЫВОРОТКИ ─── */}
          <section style={{ marginBottom: 16 }}>
            <h3 style={SECTION_HEADER_STYLE}>
              {t('cosmic_hub.inventory.section_serums')}
            </h3>
            <div
              style={{
                display: 'grid',
                gridTemplateColumns: 'repeat(4, 1fr)',
                gap: 8,
              }}
            >
              {ELEMENTS.map((el) => {
                const count = serums[el] ?? 0
                const tint = ELEMENT_TINTS[el]
                const tintHex = '#' + tint.toString(16).padStart(6, '0')
                return (
                  <div
                    key={el}
                    style={{
                      ...DARK_CARD_STYLE,
                      display: 'flex',
                      flexDirection: 'column',
                      alignItems: 'center',
                      gap: 4,
                      padding: 8,
                      borderColor: count > 0 ? tintHex : 'rgba(255,255,255,0.1)',
                      borderWidth: 1,
                      borderStyle: 'solid',
                      opacity: count > 0 ? 1 : 0.5,
                    }}
                    title={t(`cosmic_hub.elements.${el}`)}
                  >
                    <span style={{ fontSize: 20 }}>{ELEMENT_EMOJI[el]}</span>
                    <span
                      style={{
                        fontSize: 13,
                        fontWeight: 700,
                        color: count > 0 ? '#fff' : TEXT_VERY_DIM,
                      }}
                    >
                      {count}
                    </span>
                  </div>
                )
              })}
            </div>
          </section>

          {/* ─── АРТЕФАКТЫ (placeholder Phase 27+) ─── */}
          <section style={{ marginBottom: 16 }}>
            <h3 style={SECTION_HEADER_STYLE}>
              {t('cosmic_hub.inventory.section_artifacts')}
            </h3>
            <div style={{ ...DARK_CARD_STYLE, padding: 16 }}>
              <div style={EMPTY_STATE_TEXT_STYLE}>
                {t('cosmic_hub.inventory.placeholder_artifacts')}
              </div>
            </div>
          </section>

          {/* ─── ОТНОШЕНИЯ С РАСАМИ (placeholder Phase 29) ─── */}
          <section style={{ marginBottom: 16 }}>
            <h3 style={SECTION_HEADER_STYLE}>
              {t('cosmic_hub.inventory.section_relationships')}
            </h3>
            <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
              {RACES.map((race) => (
                <div
                  key={race.id}
                  style={{
                    ...DARK_CARD_STYLE,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'space-between',
                    padding: '8px 12px',
                  }}
                  title={t('cosmic_hub.inventory.tooltip_relationship_pending')}
                >
                  <span style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                    <span style={{ fontSize: 18 }}>{race.emojiIcon}</span>
                    <span style={{ fontSize: 13, color: TEXT_DIM }}>
                      {t(race.nameKey)}
                    </span>
                  </span>
                  <span style={{ ...MINI_BADGE_STYLE, color: TEXT_VERY_DIM }}>
                    {t('cosmic_hub.inventory.placeholder_relationship_value')}
                  </span>
                </div>
              ))}
            </div>
          </section>
        </div>
      )
    }

    interface CurrencyRowProps {
      icon: string
      label: string
      value: number
      valueColor: string
    }
    
    function CurrencyRow({ icon, label, value, valueColor }: CurrencyRowProps) {
      return (
        <div
          style={{
            ...DARK_CARD_STYLE,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            padding: '10px 12px',
          }}
        >
          <span style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
            <span style={{ fontSize: 22 }}>{icon}</span>
            <span style={{ fontSize: 14, color: TEXT_DIM }}>{label}</span>
          </span>
          <span style={{ fontSize: 16, fontWeight: 800, color: valueColor }}>
            {value.toLocaleString('ru-RU')}
          </span>
        </div>
      )
    }
    ```

    **Decisions:**
    - Number formatting через `Number.toLocaleString('ru-RU')` для thousand separators. Если в проекте есть formatter helper (`numberFormat` selector в gameStore) — reuse. Проверить grep'ом: `grep -n "formatNumber\|numberFormat" client/src/utils`. Если есть — заменить toLocaleString на helper. Если нет — `toLocaleString` достаточно.
    - Serum opacity = 0.5 для count===0 (dim, не disabled).
    - Tooltip `title` атрибут — native browser tooltip (mobile-poor но acceptable для read-only display).
    - Иконка `💩` для gold/slime — reuse existing visual identity (см. CONTEXT таблицу Inventory).
    - НЕ создавать interactive elements (clicks/sorting/filters) — read-only по CONTEXT D-ReadOnly.

    **Layout grid 4×4 для 16 elements:** на mobile narrow screens (320px) каждая cell получит ~70px width — достаточно для emoji + count.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "InventoryTab" || echo "InventoryTab clean"</automated>
  </verify>
  <done>
    `InventoryTab.tsx` существует, импорты резолвятся. Рендерится без runtime errors при mount. 4 секции присутствуют. 16 element cards в grid. 10 race rows. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire InventoryTab в CosmicHubModal</name>
  <files>client/src/components/CosmicHub/CosmicHubModal.tsx</files>
  <action>
    Расширить `CosmicHubModal.tsx`:

    1. Import: `import { InventoryTab } from './InventoryTab'`.
    2. В `getInitialTab()` extend validation:
       ```ts
       if (
         saved === 'scouts' || saved === 'boxes' || saved === 'bestiary' ||
         saved === 'carriers' || saved === 'shop' || saved === 'inventory'
       ) {
         return saved
       }
       ```
    3. В `TABS` array (внутри CosmicHubModal function) ADD entry в конце:
       ```ts
       {
         id: 'inventory',
         label: t('cosmic_hub.tab_inventory'),
         icon: '🎒',
         // Phase 26: всегда виден после cosmos unlock (modal вообще lock'ит весь tab strip 
         // если !cosmosUnlocked, так что enabled: true достаточно).
         enabled: true,
       }
       ```
    4. В `renderTab()` switch ADD case:
       ```ts
       case 'inventory':
         return <InventoryTab />
       ```

    Tab strip автоматически перерендерит с 6 tabs (existing flex: 1 layout). Padding (12px 4px) сохранится. Если визуально стало слишком тесно на узком viewport — leave it для Phase 26 polish (TODO note в SUMMARY).

    **Cliclability checklist** (per memory `feedback_clickability` + CONTEXT D-Cliclability):
    - Existing tab buttons уже имеют `type="button"`, `touchAction: 'manipulation'`. Новая 6-я кнопка inherit'ит всё это автоматически (TABS.map render path).
    - z-index уже 50 (CosmicHubModal root) — соблюдено.
    - InventoryTab contents (`overflow-y-auto` parent в CosmicHubModal) уже handle'ят scroll.

    **Defensive cosmos gate** (existing): если `!cosmosUnlocked` — modal показывает lock screen вместо tabs. InventoryTab НЕ показывается до unlock — это per CONTEXT design (inventory part of cosmos UX).

    **Sessionstorage:** existing useEffect автоматически сохраняет 'inventory' при switch.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -E "CosmicHubModal|InventoryTab" || echo "wiring clean"</automated>
  </verify>
  <done>
    CosmicHubModal содержит 6 tabs (🚀 🏭 📖 🐸 🛒 🎒). Click на 🎒 → InventoryTab renders. sessionStorage сохраняет 'inventory'. Reload восстанавливает active tab. tsc clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gameStore selectors → InventoryTab | Subscribers re-render на любой store change unless granular selectors |
| sessionStorage → getInitialTab | Untrusted string → typed CosmicTab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | DoS | re-render storm если subscriber на whole store | mitigate | Granular selectors `s => s.essence` / `s => s.coins` / `s => s.serums` — Zustand selector memoization |
| T-26-04-02 | Tampering | sessionStorage 'cosmic_last_tab' = 'inventory' до того как Phase 26 ship | accept | getInitialTab() уже defensive — unknown values fall to 'scouts' default |
| T-26-04-03 | Tampering | broken serums Record (missing keys) | mitigate | `serums[el] ?? 0` fallback per-cell |
| T-26-04-04 | Info Disclosure | Tooltip с future feature hint | accept | Intentional UX hint — игрок ожидает Phase 29 expansion |

</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run check-translations` clean parity.
- Manual: открыть Cosmic Hub (post-cosmos) → видно 6 tabs → click 🎒 → InventoryTab рендерится.
- Manual: 4 секции присутствуют, currencies показывают live values, 16 serums в grid (0-count dim, >0 highlighted с element color border).
- Manual: 10 race rows с emoji + name + '?' placeholder.
- Manual: переключение на другой tab + назад → state preserved (через sessionStorage on reload).
- Manual: `useGameStore.getState().addSerum('fire')` (DEV helper) — Inventory tab показывает обновлённый count fire.
- Existing 5 tabs работают как прежде (regression check).
- vitest existing tests pass.
</verification>

<success_criteria>
1. CosmicTab union extended с 'inventory'.
2. InventoryTab.tsx renders 4 sections (currencies + serums grid + artifacts placeholder + race relationships).
3. Live store values (essence, gold, serums) — granular Zustand selectors.
4. 10 i18n keys × 3 locales + 1 tab label × 3 locales = 33 entries добавлены.
5. CosmicHubModal: 6 tabs, 'inventory' added, sessionStorage validated.
6. Cliclability: type=button, touchAction:manipulation, z-index 50.
7. Reuse Phase 25 design tokens (_styles.ts imports).
8. tsc + check-translations clean.
9. Git commit: `feat(26-04): inventory tab в Cosmic Hub + i18n keys`.
</success_criteria>

<output>
Создать `.planning/phases/26-cosmos-races-foundation/26-04-SUMMARY.md`:
- **Objective recap**
- **What was built**:
  - InventoryTab.tsx (4 sections: currencies/serums/artifacts/relationships)
  - CosmicHubModal extended (6 tabs)
  - 33 i18n entries (11 keys × 3 locales)
  - CosmicTab union extended
- **Key decisions logged**:
  - Grid 4×4 для serums (compact, не horizontal-scroll)
  - Element border color при count > 0 (visual cue)
  - Number formatting через toLocaleString (fallback если нет project formatter)
  - Native title tooltip для relationships (mobile-poor acceptable для read-only)
  - НЕ создан interactive sorting/filters (read-only по CONTEXT)
  - InventoryTab placement at end of TABS array (последний tab) — relationship-первый порядок не имеет смысла без actual data
- **Validation results**: tsc, check-translations, manual smoke
- **Polish TODO** (deferred):
  - Tab padding tweak если 6 tabs squeeze на ультра-узких viewport (320px)
  - Replace native tooltip → custom Popover если UX feedback shows mobile users miss it
  - Number-format integration with project's `numberFormat` selector если он применён в других местах
</output>
