---
phase: 26-cosmos-races-foundation
plan: 06
type: execute
wave: 4
depends_on: [26-01, 26-02, 26-03, 26-04, 26-05]
files_modified:
  - client/SMOKE_TEST_26.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PHASE26-SMOKE
  - PHASE26-I18N-PARITY
  - PHASE26-FINALIZE

must_haves:
  truths:
    - "SMOKE_TEST_26.md покрывает 7 scenarios (A-G): race config / habitable planets / star map visuals / inventory tab / first contact flow / cosmos gate / dev helpers"
    - "npm run check-translations показывает parity 100% (RU/EN/ES) после всех Plan 26-01..05 i18n additions"
    - "tsc + vitest + vite build clean без warnings"
    - "ROADMAP.md Phase 26 entry финализирован: 6 plans listed с [x], requirements заполнены, outcome paragraph"
    - "STATE.md обновлён: current_phase=26, Phase 26 row в Phase Progress table с metrics"
    - "Per-plan SUMMARY коммиты present (Plans 26-01..05)"
    - "Manual checkpoint выполнен пользователем: проверена визуальная корректность race overlays + first contact flow"
  artifacts:
    - path: "client/SMOKE_TEST_26.md"
      provides: "Manual QA checklist для phase acceptance"
      contains: "Scenario"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 entry финализирован"
      contains: "Phase 26"
    - path: ".planning/STATE.md"
      provides: "Phase 26 row + outcome paragraph"
      contains: "Phase 26"
  key_links:
    - from: "ROADMAP.md Phase 26 entry"
      to: "PHASE26-* requirement IDs (заменяют TBD)"
      via: "Requirements line"
      pattern: "PHASE26-"
---

<objective>
Phase 26 finalize: smoke test document + i18n parity verification + build chain validation + ROADMAP/STATE update. Этот plan — последний gate перед phase closure.

**Purpose:** Без structured smoke test нет confidence что 5 предыдущих plan'ов compose'ятся правильно. ROADMAP/STATE update — обязательная часть GSD workflow (precedent Phases 22-25).

**Output:** SMOKE_TEST_26.md с 7 scenarios + verified i18n parity + ROADMAP/STATE финализированы + один docs commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-cosmos-races-foundation/26-CONTEXT.md
@.planning/phases/26-cosmos-races-foundation/26-01-SUMMARY.md
@.planning/phases/26-cosmos-races-foundation/26-02-SUMMARY.md
@.planning/phases/26-cosmos-races-foundation/26-03-SUMMARY.md
@.planning/phases/26-cosmos-races-foundation/26-04-SUMMARY.md
@.planning/phases/26-cosmos-races-foundation/26-05-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Reference precedent для SMOKE_TEST format
@client/SMOKE_TEST_25.md

<interfaces>
<!-- Phase 26 deliverables: -->

Plan 26-01: RaceId/RACES + firstContactsSeen + i18n races.* + cosmos.first_contact.* + eventBus 'cosmos:first-contact'
Plan 26-02: PlanetInhabitant type + 30 habitable planets attached + getHabitable/getPlanetInhabitant/getPlanetsByRace + HABITABLE_PLANET_IDS
Plan 26-03: RaceGlowController + planetRenderer integration + popovers race info + cosmos.role_* i18n
Plan 26-04: InventoryTab + CosmicHubModal 6th tab + cosmic_hub.inventory.* i18n
Plan 26-05: FirstContactEffect + FirstContactModal + firstContactController + StarMapScene window exposure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n parity verification + build chain</name>
  <files>(no files modified — verification only)</files>
  <action>
    Запустить три проверки последовательно. ВСЕ ДОЛЖНЫ PASS перед continuation.

    **1. i18n parity:**
    ```bash
    cd client && npm run check-translations
    ```
    Ожидается: `parity clean (XXX/XXX keys × 3 locales)` где XXX > 387 (existing 337 from Phase 25 + ≥50 races + 3 cosmos.first_contact + 2 cosmos.role_* + 11 cosmic_hub.inventory.* + 1 tab_inventory = 67+ new keys per locale). Если показывает missing — пересмотреть Plans 26-01/03/04 i18n additions.

    **2. Build chain:**
    ```bash
    cd client && npx tsc --noEmit
    cd client && npx vitest run
    cd client && npx vite build
    ```
    Vite build: capture `dist/assets/index-*.js gzip` size для bundle delta calc. Запомнить baseline = Phase 25 main gzip ≈ 200.86 KB (199.88 + 0.98 от Phase 25). Cap: Phase 26 CONTEXT не указывает explicit cap, но reasonable budget = +15 KB gzip (scale similar к Phase 24-25). Если delta > 15 KB — recheck planRenderer integration для leak'ов.

    **3. Vitest:**
    Ожидается все тесты PASS включая habitablePlanets.test.ts (7 tests Plan 26-02).

    Output of these commands fixируется в Task 4 (STATE.md metrics).
  </action>
  <verify>
    <automated>cd client && npm run check-translations && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    check-translations: parity 100%. tsc clean. vitest all green. vite build success + gzip size noted. Bundle delta вычислен.
  </done>
</task>

<task type="auto">
  <name>Task 2: SMOKE_TEST_26.md — 7 scenarios</name>
  <files>client/SMOKE_TEST_26.md (new)</files>
  <action>
    Создать `client/SMOKE_TEST_26.md`. Pattern mirror SMOKE_TEST_25.md (numbered scenarios A-G + [ ] checkboxes + dev helper callouts + i18n + build chain + regression sanity).

    Содержание:

    ```markdown
    # Phase 26: Cosmos Races Foundation — Manual Smoke Test

    **Phase:** 26-cosmos-races-foundation
    **Date:** 2026-05-18+
    **Tester:** _________

    Run order: A → B → C → D → E → F → G. Каждое scenario независимо в смысле state setup (используйте dev helpers для reset между scenarios если нужно).

    ## Prerequisites

    - [ ] `npm run dev` running
    - [ ] DEV console available (window helpers exposed)
    - [ ] Player state с cosmos unlocked (`useGameStore.getState().hasCosmosUnlocked === true`).
      - Если не unlocked: triggerа через `__triggerCaptainBirth()` + L18+L18 merge, или dev tool `useGameStore.setState({hasCosmosUnlocked: true})`.

    ## Scenario A: Race config + i18n integrity

    **Goal:** Убедиться что Plan 26-01 race data доступна везде.

    - [ ] Console: `__listRaces()` → 10-row table с id/affinity/emoji
    - [ ] Console: `__firstContactsState()` → 10 entries все false
    - [ ] Console: `useGameStore.getState().firstContactsSeen` → `{crystalloids: false, ..., cometfolk: false}`
    - [ ] Locale switch (settings → RU/EN/ES) → no missing-key console warnings
    - [ ] Console: i18next get keys `races.crystalloids.name` / `races.fireworms.lore_short` — всё translation present для текущего locale
    
    ## Scenario B: Habitable planets data

    **Goal:** Убедиться что Plan 26-02 attached inhabitants корректно.

    - [ ] Console: `import('./src/game/data/habitablePlanets').then(m => console.log(m.getHabitablePlanets().length))` → 30
    - [ ] Каждая раса имеет 3 planets:
      ```
      __listRaces().forEach(r => {
        const ps = ... getPlanetsByRace(r.id)
        console.log(r.id, ps.length, ps.filter(p=>p.inhabitant.role==='home').length, 'home')
      })
      ```
      → 10 строк, каждая `<id> 3 1 home`
    - [ ] Player home planet (id='home') НЕ inhabited

    ## Scenario C: Star Map visual indicators (post-cosmos)

    **Goal:** Plan 26-03 race overlays.

    - [ ] Open Star Map (post-cosmos)
    - [ ] Visible: 10 home planets с gold pulsing halo + race emoji icon (18px bold)
    - [ ] Visible: 20 colony planets с race-color glow + smaller emoji (14px)
    - [ ] Visible: 320 uninhabited planets — без overlay (как до Phase 26)
    - [ ] Tap на home planet → popover показывает race emoji + name + "⭐ Главный мир"
    - [ ] Tap на colony planet → popover показывает race emoji + name + "· Колония"
    - [ ] Tap на uninhabited planet → popover как раньше (no race info row)
    - [ ] Close Star Map, reopen → overlays переаттач'ятся, no duplicate sprites

    ## Scenario D: Cosmos gate transparency (pre-cosmos)

    **Goal:** До cosmos unlock — habitable planets выглядят uninhabited.

    - [ ] Reset cosmos: `useGameStore.setState({hasCosmosUnlocked: false}); window.location.reload()`
    - [ ] Open Star Map → НИ ОДНОЙ race overlay не visible
    - [ ] Tap habitable planet → popover БЕЗ race info row
    - [ ] Trigger cosmos unlock: `__triggerCaptainBirth()` (Phase 24 helper) → L18+L18 merge OR direct `useGameStore.getState().markCosmosUnlocked()`
    - [ ] Без reload — Star Map overlays attach reactively (если scene still open)
    - [ ] OR reload → overlays present at next mount

    ## Scenario E: Inventory tab

    **Goal:** Plan 26-04 6-я кнопка в Cosmic Hub.

    - [ ] Open Cosmic Hub → 6 tabs visible: 🚀 Корабль / 🏭 Боксы / 📖 Бестиарий / 🐸 Карьеры / 🛒 Магазин / 🎒 Инвентарь
    - [ ] Tap 🎒 → InventoryTab renders 4 секции
    - [ ] ВАЛЮТЫ row: 💎 Эссенция = `essence` value, 💩 Слизь = `coins` value (live update if changed)
    - [ ] СЫВОРОТКИ: 4×4 grid 16 элементов. count=0 → dimmed; count>0 → element-color border + bright value
    - [ ] АРТЕФАКТЫ: "Скоро…" placeholder в dark card
    - [ ] ОТНОШЕНИЯ С РАСАМИ: 10 row, каждая с race emoji + name + "?" placeholder badge
    - [ ] Switch на другой tab + назад → state preserved
    - [ ] Close Hub → reopen → 🎒 tab restored через sessionStorage
    - [ ] DEV: `useGameStore.getState().addSerum('fire')` → 🔥 cell обновляет count

    ## Scenario F: First contact flow

    **Goal:** Plan 26-05 cinematic + modal.

    - [ ] Reset: `__resetFirstContacts()` 
    - [ ] Open Star Map → tap home planet of crystalloids race → cinematic plays ~2s (particles + ring tinted crystal color)
    - [ ] After cinematic → DOM modal: "Первый контакт" title + 💎 emoji + "Кристаллозиды" name + personality + lore_short + pink "Понятно" CTA
    - [ ] Tap CTA → modal fades + closes
    - [ ] Check: `__firstContactsState()` → crystalloids: true
    - [ ] Tap другую colony crystalloids planet → НИКАКОГО cinematic (idempotent)
    - [ ] Tap home planet fireworms race → cinematic plays (fire color!) → modal: 🔥 + "Огнечервы"
    - [ ] Modal backdrop click (вне card) → modal closes как CTA
    - [ ] DEV: `__triggerFirstContact('gasouls')` (без visit) → cinematic + modal без planet tap
    - [ ] Confirm: `__resetFirstContacts()` → replay works

    ## Scenario G: DEV helpers + Cliclability

    - [ ] `__listRaces()` — works в DEV
    - [ ] `__listRaces()` — не работает в production build (`import.meta.env.DEV` gate)
    - [ ] Mobile touch test: 🎒 Inventory tab button — single tap responds <100ms
    - [ ] Mobile touch test: First contact modal CTA — single tap responds <100ms
    - [ ] Mobile touch test: First contact backdrop — tap closes modal
    - [ ] z-index check: First contact modal над HUD + Star Map + Cosmic Hub modal (200 > 100 > 50)

    ## i18n parity

    - [ ] `npm run check-translations` → 0 missing, 0 extra
    - [ ] Switch locale RU → all races + cosmos.first_contact + cosmic_hub.inventory keys render in Russian
    - [ ] Switch locale EN → same in English (Crystalloids, Fireworms, ..., First Contact)
    - [ ] Switch locale ES → same in Spanish

    ## Build chain

    - [ ] `npx tsc --noEmit` clean
    - [ ] `npx vitest run` all green (включая habitablePlanets.test.ts: 7 tests)
    - [ ] `npx vite build` success, no warnings
    - [ ] Bundle delta gzip: _____ KB (record actual; baseline Phase 25 ~200.86 KB)

    ## Regression sanity (no breakage of prior phases)

    - [ ] Phase 24 Captain Birth flow: L18+L18 merge → cinematic + modal play (still works)
    - [ ] Phase 23 Onboarding Beat 1 (welcome modal): triggerа через `__resetOnboarding()` → welcome plays
    - [ ] Phase 22 Cosmic Shop tab: open Hub → 🛒 → items rendered с essence/serum balances
    - [ ] Phase 18 Bestiary tab: open → 6 location grids visible, virtualized
    - [ ] Phase 16 Ship tab: open → ship state rendered, can launch mission
    - [ ] Phase 11+ HUD: SerumBar + ActiveBonusesBar render normally

    ---

    **Tester signature / date:** _________
    ```
  </action>
  <verify>
    <automated>test -f client/SMOKE_TEST_26.md && wc -l client/SMOKE_TEST_26.md</automated>
  </verify>
  <done>
    SMOKE_TEST_26.md создан, 7 scenarios A-G + i18n + build chain + regression sanity sections. Mirror'ит формат SMOKE_TEST_25.md. ≥100 lines.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 26 готов к manual QA verification. Все 5 предыдущих plan'ов shipped:
    - Plan 26-01: race data model + first-contact state + i18n
    - Plan 26-02: 30 habitable planets + selection algorithm + helpers
    - Plan 26-03: Star Map race overlays + popover race info
    - Plan 26-04: Inventory tab в Cosmic Hub
    - Plan 26-05: First contact cinematic + modal + event flow
  </what-built>
  <how-to-verify>
    Открыть `client/SMOKE_TEST_26.md` и пройти scenarios A-G последовательно. Минимально критичные scenarios для approval:
    
    1. **Scenario C** (Star Map visuals) — visual integrity, gold pulse, race colors
    2. **Scenario E** (Inventory tab) — все 4 секции рендерятся правильно, sessionStorage persistence
    3. **Scenario F** (First contact flow) — cinematic + modal играют, idempotent per-race
    4. **Regression sanity** — Phase 24 captain birth still works
    
    Если визуальные issues:
    - Color/glow mismatched → recheck Plan 26-03 RaceGlowController.attach + ELEMENT_TINTS values
    - First contact modal cliclability — backdrop/CTA не отзывается → recheck stopPropagation + touchAction
    - Inventory tab tight на narrow viewport — accept as deferred polish
  </how-to-verify>
  <resume-signal>Type "approved" если все critical scenarios passed, OR описать issues для revision</resume-signal>
</task>

<task type="auto">
  <name>Task 4: ROADMAP/STATE finalize</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    После human approval — обновить planning docs.

    **A. ROADMAP.md** — Phase 26 entry:
    - Find `### Phase 26: Cosmos races foundation` (line ~685).
    - Replace `**Requirements**: TBD` с финальным списком:
      ```
      **Requirements**: PHASE26-RACES-CONFIG, PHASE26-FIRSTCONTACT-STATE, PHASE26-I18N-RACES, PHASE26-EVENTBUS, PHASE26-INHABITED-PLANETS, PHASE26-PLANET-SELECTION, PHASE26-PLANET-INHABITANT-TYPE, PHASE26-STARMAP-GLOW, PHASE26-STARMAP-ICONS, PHASE26-COSMOS-GATE-INHABITANTS, PHASE26-POPOVER-RACE-INFO, PHASE26-INVENTORY-TAB, PHASE26-INVENTORY-CURRENCIES, PHASE26-INVENTORY-SERUMS, PHASE26-INVENTORY-PLACEHOLDERS, PHASE26-I18N-INVENTORY, PHASE26-FIRSTCONTACT-EFFECT, PHASE26-FIRSTCONTACT-MODAL, PHASE26-FIRSTCONTACT-WIRING, PHASE26-FIRSTCONTACT-IDEMPOTENT, PHASE26-SMOKE, PHASE26-I18N-PARITY, PHASE26-FINALIZE
      ```
      (23 requirements total).
    - Replace `**Plans:** 0 plans` с `**Plans:** 6 plans`.
    - Replace Plans block:
      ```
      Plans:
      - [x] 26-01-PLAN.md — Race data model + firstContactsSeen state + i18n races.* + eventBus
      - [x] 26-02-PLAN.md — 30 habitable planets selection + PlanetInhabitant + helpers
      - [x] 26-03-PLAN.md — Star Map race overlays (glow + icons + gold home halo) + popover race info
      - [x] 26-04-PLAN.md — Inventory tab в Cosmic Hub (6-я кнопка)
      - [x] 26-05-PLAN.md — First contact cinematic + DOM modal + event flow
      - [x] 26-06-PLAN.md — SMOKE_TEST_26 (7 scenarios) + ROADMAP/STATE finalize
      ```
    - Add outcome paragraph (после Plans block):
      ```
      **Outcome:** Foundation для multi-phase космической экспансии (Phase 27-29). 10 рас с полным lore (Кристаллозиды/Газо-облака/Механидоны/Огнечервы/Жидко-сферы/Тенебрисы/Плазма-духи/Лесо-кореня/Время-ткачи/Кометники), 30 обитаемых планет attached deterministic seed-based selection в planetMap.json (10 home + 20 colonies × 10 рас), Star Map race-color glow + emoji icon overlays gated cosmos unlock, Inventory tab в Cosmic Hub (4 секции: currencies/serums grid 4×4/artifacts placeholder/race relationships placeholder), first contact cinematic (Phaser ~2s burst tinted race color + DOM modal lore reveal) idempotent per-race через firstContactsSeen flag server-syncable cosmic blob. Bundle delta gzip _____ KB (cap ~+15 KB). i18n RU/EN/ES parity (+67 keys × 3 locales). 7 vitest для habitablePlanets API + 7 SMOKE_TEST_26 scenarios A-G. Никакого Lottie (CSS keyframes + Phaser tweens только). НЕ trogает frog.container.alpha (overlays — отдельные GameObjects + DOM portal). 23/23 ✓ REQ-IDs.
      ```
    - Update `**Last updated:** 2026-05-18 — Phase 26 complete (6 plans, races foundation)`.

    **B. STATE.md**:
    - Update header:
      ```
      current_phase: 26 (complete); Phase 20 (Pre-release safety net) deferred до prod-релиза
      ```
    - Update last_updated timestamp.
    - В Phase Progress table добавить новую row для Phase 26:
      ```
      | 26 | Cosmos races foundation | **complete** (2026-05-18) — 6 plans, ~18h scope; 10 races config с полным lore + firstContactsSeen state (server-syncable cosmic blob) + 30 habitable planets selection (deterministic seed 19450718, Mulberry32 PRNG, affinity-first + random fallback) + helpers + 7 vitest; Star Map RaceGlowController (Phaser texture-gen pattern reuse от ConfettiBurst, radial gradient + 30 overlays = race glow + emoji icon + home gold pulsing halo) + planetRenderer integration + reactive cosmos subscribe + popovers race info badge; Inventory tab 6-я в Cosmic Hub (4 секции: currencies live values + 16-serum grid 4×4 element-color border + artifacts placeholder + 10 race relationships rows placeholder) reuse Phase 25 _styles.ts tokens; FirstContactEffect.ts (lighter 2s burst tinted race.homeColor + 1 ring + ~35 particles + window.__starMapScene exposure) + FirstContactModal.tsx (WelcomeModal pattern reuse, dark card + race emoji big + name + personality italic + lore_short + pink CTA, backdrop click closes) + firstContactController.tsx App-level (3 eventBus subscribers: starmap:planet-tapped gate → cosmos:first-contact emit → effect handler → cosmos:first-contact-effect-complete → DOM modal mount); StarMapScene getPlanetWorldCoords helper; dev helpers __listRaces/__markFirstContact/__resetFirstContacts/__firstContactsState/__triggerFirstContact; SMOKE_TEST_26.md (7 scenarios A-G); bundle delta gzip _____ KB. 23/23 ✓ REQ-IDs (PHASE26-*). |
      ```
    - Update progress:
      ```
      progress:
        total_phases: 15  # was 14
        completed_phases: 5  # was 4
        total_plans: 42    # was 36
        completed_plans: 34  # was 28
      ```
    - Optional: добавить отдельный section "## Phase 26 — Performance Metrics" с per-plan bundle delta table (mirror Phase 23/24/25 sections), если bundle data собрана детально. Минимально: per-plan commits / files modified / cumulative gzip.

    **C. Per-plan SUMMARY existence check.** Если Plans 26-01..05 уже создали свои SUMMARY (через success_criteria каждого plan'а requires SUMMARY) — verify все 5 files present:
    ```bash
    ls .planning/phases/26-cosmos-races-foundation/26-0?-SUMMARY.md
    ```
    Если какой-то missing — flag для plan re-run (не блокирует этот finalize task, но record в SMOKE_TEST_26 как deviation).
  </action>
  <verify>
    <automated>grep -n "Phase 26.*complete" .planning/STATE.md && grep -n "Phase 26: Cosmos races foundation" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    ROADMAP.md Phase 26 entry: 6 plans [x] + 23 REQ-IDs + outcome paragraph. STATE.md current_phase=26 complete + Phase Progress row populated с metrics. Bundle delta number filled. Git commit: `docs(26-06): finalize phase 26 cosmos races foundation`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual smoke test trust | Tester может miss visual edge cases — mitigation: 7 explicit scenarios + checklist style |
| ROADMAP/STATE update trust | Edit may break GSD planning tooling if frontmatter shape broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Repudiation | Phase shipped without verified smoke | mitigate | Task 3 checkpoint blocks ROADMAP update до human approval |
| T-26-06-02 | Tampering | STATE.md frontmatter broken syntax | mitigate | Validate YAML parse-ability post-edit (`python3 -c 'import yaml; yaml.safe_load(open(".planning/STATE.md").read().split("---")[1])'`) |
| T-26-06-03 | Info Disclosure | Outcome paragraph contains incorrect metric (bundle KB) | accept | Recorded во время Task 1 verification, не fabricated |

</threat_model>

<verification>
- `npm run check-translations` clean.
- `npx tsc --noEmit` clean.
- `npx vitest run` all green.
- `npx vite build` success, bundle gzip recorded.
- SMOKE_TEST_26.md present + ≥100 lines.
- Human approval received на Task 3 checkpoint.
- ROADMAP.md Phase 26 entry: 23 REQ-IDs, 6 plans [x], outcome paragraph.
- STATE.md Phase Progress row populated + current_phase=26 complete.
- Per-plan SUMMARY existence verified (Plans 01..05).
- Single docs commit финализирует phase.
</verification>

<success_criteria>
1. SMOKE_TEST_26.md создан с 7 scenarios A-G + i18n + build chain + regression.
2. Human approval checkpoint passed.
3. i18n parity check passes (100%).
4. tsc + vitest + vite build clean.
5. ROADMAP.md Phase 26 entry финализирован.
6. STATE.md current_phase=26 complete + Phase Progress row + metrics.
7. Bundle delta gzip recorded.
8. Git commit: `docs(26-06): finalize phase 26 cosmos races foundation`.
9. Phase 26 ready for closure / handoff на Phase 27 (quests).
</success_criteria>

<output>
Создать `.planning/phases/26-cosmos-races-foundation/26-06-SUMMARY.md`:
- **Objective recap**
- **What was built**:
  - SMOKE_TEST_26.md (7 scenarios)
  - ROADMAP.md финализирован (6 plans, 23 REQ-IDs)
  - STATE.md обновлён (Phase Progress row + outcome)
- **Build chain results**:
  - tsc clean
  - vitest: N/N green (включая habitablePlanets.test.ts 7/7)
  - vite build: main gzip _____ KB (delta vs Phase 25 baseline +_____ KB)
  - check-translations: ___ keys × 3 locales parity
- **Manual smoke results** (filled by tester during Task 3):
  - 7 scenarios pass status
  - Any deviations noted
- **Phase 26 final state**:
  - 6 plans shipped
  - 23 REQ-IDs covered
  - Bundle delta cumulative
- **Handoff to Phase 27** (quests):
  - 30 habitable planets с stable IDs готовы как quest targets
  - per-race firstContactsSeen flag можно extend с per-race quest progress в Phase 27
  - Inventory tab artifacts/relationships placeholder секции готовы заполниться в Phase 27/29
- **Phase 26 deferred polish TODOs**:
  - Tab strip 6 buttons squeeze на 320px viewport (Plan 26-04 known issue)
  - Custom popover tooltip для race relationship rows (vs native title)
  - Race SVG assets когда user их предоставит (replace emoji placeholders Plan 26-01)
  - i18n русские склонения для "Связь установлена с расой «{{raceName}}»" (genitive case) — приемлемо для Phase 26
</output>
