---
phase: 24-captain-creation-cinematic
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - client/src/components/Captain/CaptainBirthModal.tsx
  - client/src/components/Captain/captainBirthModal.css
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - PHASE24-CAPTAIN-MODAL
  - PHASE24-I18N
  - PHASE24-CTA-EXIT
must_haves:
  truths:
    - "Modal mounts автоматически при emit'е 'captain:birth-effect-complete'"
    - "L1 frog SVG в центре c gold drop-shadow и CSS pulse scale 1.0↔1.05 (1.5s)"
    - "Title «Вы создали Капитана» (gold #fde047), subtitle «Космос открыт. Готовы исследовать?»"
    - "CTA «В космос →» (pink #ec4899 solid, inline-block, не растягивается — pattern WelcomeModal)"
    - "Backdrop click ИЛИ CTA → fade-out 400ms → emit 'captain:birth-cta' (внутреннее событие для plan 24-04 Beat 4+5 ловит)"
    - "Modal cliclability: button type=button, z-index 200, touchAction manipulation, stopPropagation на card"
    - "i18n RU/EN/ES — все 3 строки (title/subtitle/cta) переведены"
    - "Никакого Lottie — только CSS keyframes (memory feedback_animations)"
  artifacts:
    - path: "client/src/components/Captain/CaptainBirthModal.tsx"
      provides: "Self-mounting modal component (subscribes к captain:birth-effect-complete, рендерит сам, dismisses сам с emit captain:birth-cta)"
      contains: "CaptainBirthModal"
      min_lines: 100
    - path: "client/src/components/Captain/captainBirthModal.css"
      provides: "@keyframes для pulse + fade-out"
      contains: "@keyframes"
    - path: "client/src/i18n/ru.json"
      provides: "captain.birth.{title,subtitle,cta} ключи"
      contains: "captain"
    - path: "client/src/i18n/en.json"
      provides: "captain.birth.{title,subtitle,cta} ключи"
      contains: "captain"
    - path: "client/src/i18n/es.json"
      provides: "captain.birth.{title,subtitle,cta} ключи"
      contains: "captain"
  key_links:
    - from: "eventBus 'captain:birth-effect-complete'"
      to: "client/src/components/Captain/CaptainBirthModal.tsx"
      via: "useEffect → eventBus.on, setState({visible:true})"
      pattern: "captain:birth-effect-complete"
    - from: "client/src/components/Captain/CaptainBirthModal.tsx CTA/backdrop click"
      to: "eventBus 'captain:birth-cta'"
      via: "handleDismiss() emit'ит после fade-out 400ms"
      pattern: "captain:birth-cta"
---

<objective>
Beat 3 cinematic — DOM modal «Вы создали Капитана». Mount'ится автоматически когда Phaser cinematic (Plan 24-02) заканчивает emit'ом 'captain:birth-effect-complete'. Содержит L1 frog SVG с золотым glow + pulse, gold title, subtitle, pink CTA «В космос →». Dismiss по CTA или backdrop click → fade-out 400ms → emit нового события 'captain:birth-cta' (его подцепит Plan 24-04 для Beat 4 spawn + Beat 5 starmap transition).

Purpose: модалка — кульминация cinematic, превращает «эффект» в «нарратив + intent». CTA служит и emotional payoff («Вы создали Капитана»), и practical bridge в Star Map.

Output: Captain/ subdirectory с modal + CSS, i18n keys в трёх локалях, новый eventBus event 'captain:birth-cta'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-captain-creation-cinematic/24-CONTEXT.md
@.planning/phases/24-captain-creation-cinematic/24-01-PLAN.md
@frog_obsidian/Design Notes/2026-05-18-captain-creation-cinematic.md
@client/src/components/Onboarding/WelcomeModal.tsx
@client/src/components/Onboarding/welcomeModal.css

<interfaces>
<!-- WelcomeModal.tsx — главный reference (паттерн mount/dismiss/CTA/inline-block button + cliclability checklist уже соблюдён). -->

WelcomeModal pattern key points (см. файл /client/src/components/Onboarding/WelcomeModal.tsx):
- `createPortal(node, document.body)` для root mount (избегает z-index battles)
- exiting state + setTimeout(markSeen, FADE_OUT_MS) перед store mutation
- CTA: `display: 'inline-block'`, `width: 'auto'`, `minWidth`, `maxWidth: '100%'`, `boxSizing: 'border-box'` — НЕ растягивается на весь экран (anti-bug: pink fullscreen)
- Backdrop: `position: fixed`, `inset: 0`, `display: flex`, `alignItems: center`, `justifyContent: center`, `touchAction: 'manipulation'`
- Card: `onClick={(e) => e.stopPropagation()}` — backdrop click dismiss работает, click на card — нет
- `role="dialog"`, `aria-modal="true"`, `aria-labelledby`
- Inline SVG L1 frog (НЕ external asset — zero network deps)
- i18n: `useTranslation()` + `t('namespace.key')`

CSS file (welcomeModal.css):
```css
@keyframes onb-welcome-fade-out { from { opacity: 1; } to { opacity: 0; } }
.onb-welcome-backdrop.is-exiting { animation: onb-welcome-fade-out 400ms ease-out forwards; }
```

eventBus.ts — добавим НОВОЕ событие 'captain:birth-cta' (void). Mount'ed listener — Plan 24-04 (MergeController hook + Beat 4 spawn).

Existing i18n location keys (для refernce):
```json
"onboarding": {
  "welcome": { "title": "...", "subtitle": "...", "cta": "..." }
}
```

CONTEXT.md locked tokens:
- Background: radial-gradient(ellipse at center, rgba(0,0,0,0.85) 0%, rgba(0,0,0,0.95) 70%)
- Card: max-width 320, padding 28px 20px, bg #1a2e1a, border 2px solid rgba(255,255,255,0.15), borderRadius 16
- Frog: 120×120 px, drop-shadow(0 0 24px #fde047), CSS pulse 1.0↔1.05 1.5s loop
- Title: 26px / fontWeight 900 / #fde047
- Subtitle: 14px / #d4d4d8
- CTA: #ec4899 solid (НЕ gradient), inline-block, width auto, minWidth 200, padding 14px 32px
- z-index 200 (above HUD)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys (RU/EN/ES) + новое eventBus событие</name>
  <files>
    client/src/i18n/ru.json
    client/src/i18n/en.json
    client/src/i18n/es.json
    client/src/store/eventBus.ts
  </files>
  <action>
**A. i18n** — добавь верхнеуровневый namespace `captain` после `onboarding` namespace во всех 3 файлах.

ru.json (после блока onboarding, перед закрывающей `}`):
```json
,
"captain": {
  "birth": {
    "title": "Вы создали Капитана",
    "subtitle": "Космос открыт. Готовы исследовать?",
    "cta": "В космос →"
  }
}
```

en.json (тот же путь):
```json
,
"captain": {
  "birth": {
    "title": "You Created the Captain",
    "subtitle": "The cosmos is open. Ready to explore?",
    "cta": "To Cosmos →"
  }
}
```

es.json (тот же путь):
```json
,
"captain": {
  "birth": {
    "title": "Has Creado al Capitán",
    "subtitle": "El cosmos está abierto. ¿Listo para explorar?",
    "cta": "Al Cosmos →"
  }
}
```

ВАЖНО: добавь запятую после ОБЯЗАТЕЛЬНОЙ предыдущей пары `}` (после блока onboarding), и не оставляй trailing comma после `captain` блока. JSON syntax strict.

**B. eventBus.ts** — добавь новое событие в `type Events = { ... }` map после уже добавленных Phase 24 событий (Plan 24-01):

```typescript
// Phase 24 Plan 24-03 — Captain Birth modal CTA / dismiss.
// Эмитит CaptainBirthModal когда пользователь tap'нул CTA «В космос»
// (либо backdrop click). Subscribers:
//   - Plan 24-04 hook: spawn L1 frog (Beat 4) + eventBus.emit('starmap:open') (Beat 5).
// Idempotent: повторные emit'ы no-op для subscribers (modal one-shot).
'captain:birth-cta': void
```
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('src/i18n/ru.json','utf8'))" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('src/i18n/en.json','utf8'))" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('src/i18n/es.json','utf8'))" &amp;&amp; node -e "const ru=require('./src/i18n/ru.json'); const en=require('./src/i18n/en.json'); const es=require('./src/i18n/es.json'); if(!ru.captain?.birth?.title||!en.captain?.birth?.title||!es.captain?.birth?.title) {console.error('missing captain.birth.title'); process.exit(1);} console.log('OK');" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "error TS" | head -10</automated>
  </verify>
  <done>
    - 3 i18n файла парсятся как валидный JSON
    - captain.birth.{title,subtitle,cta} существует во всех 3
    - eventBus.ts добавляет 'captain:birth-cta': void в types
    - tsc clean
    - Запусти `npm run check-translations` если такой script есть (см. package.json) — должен PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: CaptainBirthModal.tsx + captainBirthModal.css</name>
  <files>
    client/src/components/Captain/CaptainBirthModal.tsx
    client/src/components/Captain/captainBirthModal.css
  </files>
  <action>
Создай `client/src/components/Captain/captainBirthModal.css`:

```css
/* Phase 24 Plan 24-03 — Captain Birth modal animations.
   CSS keyframes only (memory feedback_animations: НЕ Lottie). */

@keyframes captain-birth-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes captain-birth-fade-out {
  from { opacity: 1; }
  to { opacity: 0; }
}

@keyframes captain-birth-pulse {
  0%, 100% { transform: scale(1.0); }
  50% { transform: scale(1.05); }
}

.captain-birth-backdrop {
  animation: captain-birth-fade-in 400ms ease-out forwards;
}

.captain-birth-backdrop.is-exiting {
  animation: captain-birth-fade-out 400ms ease-out forwards;
}

.captain-birth-frog {
  animation: captain-birth-pulse 1500ms ease-in-out infinite;
  filter: drop-shadow(0 0 24px #fde047);
  transform-origin: center center;
  will-change: transform;
}
```

Создай `client/src/components/Captain/CaptainBirthModal.tsx`:

```tsx
// Phase 24 Plan 24-03 — Captain Birth modal (Beat 3).
//
// Self-mounting modal. Subscribes к eventBus 'captain:birth-effect-complete'
// (emit'ит Plan 24-02 Phaser cinematic). Render:
//   - Centered card via flex
//   - Inline L1 frog SVG (120x120) с CSS pulse + gold drop-shadow
//   - Gold title + светло-серый subtitle + pink CTA
// Dismiss:
//   - CTA tap ИЛИ backdrop click → fade-out 400ms → emit 'captain:birth-cta' →
//     unmount. Plan 24-04 hook слушает 'captain:birth-cta' для Beat 4 (spawn L1)
//     + Beat 5 (starmap:open).
//
// Cliclability checklist (memory feedback_clickability):
//   - button type="button"
//   - z-index 200 (above HUD)
//   - backdrop click dismisses
//   - stopPropagation на inner card
//   - touchAction: manipulation
//
// IMPORTANT: НЕ Lottie (CSS keyframes only, memory feedback_animations).
// НЕ tween frog.container.alpha (memory feedback_frog_container_alpha — n/a здесь,
// это DOM SVG, отдельная сущность от Phaser).

import { useEffect, useState } from 'react'
import { createPortal } from 'react-dom'
import { useTranslation } from 'react-i18next'
import { eventBus } from '../../store/eventBus'
import './captainBirthModal.css'

const FADE_OUT_MS = 400

export function CaptainBirthModal() {
  const { t } = useTranslation()
  const [visible, setVisible] = useState(false)
  const [exiting, setExiting] = useState(false)

  useEffect(() => {
    const onComplete = () => {
      setVisible(true)
      setExiting(false)
    }
    eventBus.on('captain:birth-effect-complete', onComplete)
    return () => eventBus.off('captain:birth-effect-complete', onComplete)
  }, [])

  if (!visible) return null

  const handleDismiss = () => {
    if (exiting) return
    setExiting(true)
    window.setTimeout(() => {
      setVisible(false)
      setExiting(false)
      eventBus.emit('captain:birth-cta')
    }, FADE_OUT_MS)
  }

  const node = (
    <div
      className={`captain-birth-backdrop${exiting ? ' is-exiting' : ''}`}
      role="dialog"
      aria-modal="true"
      aria-labelledby="captain-birth-title"
      onClick={handleDismiss}
      style={{
        position: 'fixed',
        inset: 0,
        zIndex: 200,
        background:
          'radial-gradient(ellipse at center, rgba(0,0,0,0.85) 0%, rgba(0,0,0,0.95) 70%)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 16,
        touchAction: 'manipulation',
      }}
    >
      <div
        onClick={(e) => e.stopPropagation()}
        style={{
          width: '100%',
          maxWidth: 320,
          maxHeight: 'calc(100vh - 64px)',
          padding: '28px 20px 22px',
          borderRadius: 16,
          background: '#1a2e1a',
          border: '2px solid rgba(255,255,255,0.15)',
          textAlign: 'center',
          boxSizing: 'border-box',
          overflow: 'hidden',
        }}
      >
        {/* L1 frog — inline SVG (как WelcomeModal). 120x120 с pulse + glow. */}
        <div
          className="captain-birth-frog"
          style={{
            margin: '4px auto 18px',
            width: 120,
            height: 120,
            display: 'block',
          }}
        >
          <svg viewBox="0 0 100 100" width="120" height="120">
            <ellipse cx="50" cy="60" rx="34" ry="28" fill="#65a30d" />
            <ellipse cx="50" cy="58" rx="28" ry="20" fill="#a3e635" />
            <circle cx="36" cy="36" r="12" fill="#65a30d" />
            <circle cx="64" cy="36" r="12" fill="#65a30d" />
            <circle cx="36" cy="36" r="9" fill="#fff" />
            <circle cx="64" cy="36" r="9" fill="#fff" />
            <circle cx="38" cy="38" r="5" fill="#000" />
            <circle cx="66" cy="38" r="5" fill="#000" />
            <path
              d="M 38 64 Q 50 74 62 64"
              stroke="#365314"
              strokeWidth="3"
              fill="none"
              strokeLinecap="round"
            />
          </svg>
        </div>

        <h1
          id="captain-birth-title"
          style={{
            margin: 0,
            fontSize: 26,
            fontWeight: 900,
            color: '#fde047',
            lineHeight: 1.15,
            textShadow: '0 2px 12px rgba(253, 224, 71, 0.4)',
          }}
        >
          {t('captain.birth.title')}
        </h1>

        <p
          style={{
            marginTop: 12,
            marginBottom: 20,
            fontSize: 14,
            fontWeight: 500,
            color: '#d4d4d8',
            lineHeight: 1.4,
          }}
        >
          {t('captain.birth.subtitle')}
        </p>

        <div style={{ display: 'flex', justifyContent: 'center' }}>
          <button
            type="button"
            onClick={handleDismiss}
            style={{
              display: 'inline-block',
              width: 'auto',
              minWidth: 200,
              maxWidth: '100%',
              background: '#ec4899',
              borderRadius: 12,
              padding: '14px 32px',
              color: '#fff',
              fontWeight: 700,
              fontSize: 16,
              border: 'none',
              cursor: 'pointer',
              touchAction: 'manipulation',
              boxSizing: 'border-box',
            }}
          >
            {t('captain.birth.cta')}
          </button>
        </div>
      </div>
    </div>
  )

  return createPortal(node, document.body)
}
```

ВАЖНО:
- НЕ модифицируй App.tsx в этом plan'е — mount компонента делается в Plan 24-04 (вместе с MergeController hook'ом, чтобы один атомарный commit охватывал «вся cinematic-цепочка end-to-end включена»).
- Если хочешь иметь возможность визуальной проверки в DEV сейчас — можно временно вставить `<CaptainBirthModal />` в App.tsx, но удали перед commit.
- 0 ссылок на Lottie, 0 ссылок на frog.container, 0 ссылок на external assets.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "CaptainBirth\|error TS" | head -20 &amp;&amp; grep -c "frog.container\|lottie\|Lottie" src/components/Captain/CaptainBirthModal.tsx</automated>
  </verify>
  <done>
    - Файлы Captain/CaptainBirthModal.tsx + Captain/captainBirthModal.css созданы
    - tsc clean
    - 0 ссылок на frog.container, Lottie/lottie
    - useTranslation() + 3 ключа captain.birth.* используются
    - createPortal на document.body
    - z-index 200, button type="button", stopPropagation, touchAction manipulation — все cliclability points присутствуют
  </done>
</task>

</tasks>

<verification>
- JSON validate: все 3 i18n файла парсятся, captain.birth.* есть везде
- tsc clean
- DEV manual test (если хочется убедиться визуально без plan 24-04): временно вставь `<CaptainBirthModal />` в App.tsx и выполни в console:
  ```js
  eventBus.emit('captain:birth-effect-complete')
  ```
  Должна появиться модалка с frog + pulse + glow. CTA или backdrop click → fade-out → emit'тся 'captain:birth-cta'. УБЕРИ временный mount перед commit.
- cliclability checklist:
  - [ ] button type="button"
  - [ ] z-index: 200
  - [ ] backdrop click dismisses
  - [ ] stopPropagation на inner card (click на frog/title не закрывает)
  - [ ] touchAction: manipulation на backdrop И на CTA
  - [ ] CTA inline-block + width auto (не растягивается на весь экран в edge cases)
</verification>

<success_criteria>
- CaptainBirthModal.tsx и captainBirthModal.css созданы
- i18n RU/EN/ES содержат captain.birth.{title,subtitle,cta}
- eventBus добавляет 'captain:birth-cta': void
- Modal самоподписан на 'captain:birth-effect-complete'
- Modal dismiss → fade-out 400ms → emit 'captain:birth-cta' (Plan 24-04 ловит)
- Никакого Lottie, никакого frog.container.alpha
- Cliclability checklist полностью соблюдён
</success_criteria>

<output>
После завершения создай `.planning/phases/24-captain-creation-cinematic/24-03-SUMMARY.md`. Включи: какие inline SVG paths использованы (из WelcomeModal copy/paste — отметить), какой radial-gradient final (rgba values), DEV smoke snippet для триггера модалки без полного flow.
</output>
