---
phase: 24-captain-creation-cinematic
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03"]
files_modified:
  - client/src/game/scenes/main/MergeController.ts
  - client/src/App.tsx
  - client/src/components/Captain/captainBirthController.ts
autonomous: true
requirements:
  - PHASE24-MERGE-HOOK
  - PHASE24-BEAT4-SPAWN
  - PHASE24-BEAT5-STARMAP
  - PHASE24-MODAL-MOUNT
must_haves:
  truths:
    - "При первом L18+L18 normal merge (где !captainBirthSeen) MergeController вызывает markCaptainBirthSeen() и emit'ит 'captain:birth-start' в midpoint merge'а"
    - "Повторные L18+L18 — никакого cinematic (markCaptainBirthSeen idempotent)"
    - "После CTA-tap'а в модалке (event 'captain:birth-cta'): (a) addFrogToLocation(currentLocation, 1) — символический L1 spawn (Beat 4), (b) eventBus.emit('starmap:open') — переход в Star Map (Beat 5)"
    - "CaptainBirthModal mounted в App.tsx и видим в layout (рядом с другими постоянными модалками — OnboardingController, MilestoneToast)"
    - "Coordinator (captainBirthController.ts) handler установлен ОДНАЖДЫ на boot и не reactive (нет React lifecycle race с store updates)"
  artifacts:
    - path: "client/src/game/scenes/main/MergeController.ts"
      provides: "L18+L18 branch расширен: после markCosmosUnlocked() проверяет captainBirthSeen, если false — markCaptainBirthSeen() + eventBus.emit('captain:birth-start', {x:cx, y:cy})"
      contains: "captainBirthSeen"
    - path: "client/src/App.tsx"
      provides: "<CaptainBirthModal /> mounted постоянно; installCaptainBirthController() вызывается в init useEffect один раз"
      contains: "CaptainBirthModal"
    - path: "client/src/components/Captain/captainBirthController.ts"
      provides: "installCaptainBirthController() — подписка на 'captain:birth-cta' → spawn L1 + emit starmap:open; idempotent install (single listener)"
      contains: "installCaptainBirthController"
  key_links:
    - from: "client/src/game/scenes/main/MergeController.ts L18+L18 branch"
      to: "eventBus 'captain:birth-start'"
      via: "if (!storeL25.captainBirthSeen) { storeL25.markCaptainBirthSeen(); eventBus.emit('captain:birth-start', {x:cx,y:cy}) }"
      pattern: "captain:birth-start"
    - from: "eventBus 'captain:birth-cta'"
      to: "useGameStore.addFrogToLocation + eventBus 'starmap:open'"
      via: "captainBirthController handler"
      pattern: "addFrogToLocation"
---

<objective>
Beat 1→2 trigger + Beat 4 + Beat 5 — собрать всё в работающую end-to-end цепочку:
- MergeController при первом L18+L18 normal merge: после `markCosmosUnlocked()` если `!captainBirthSeen` → `markCaptainBirthSeen()` + `emit('captain:birth-start', {x:cx,y:cy})`.
- App.tsx добавляет `<CaptainBirthModal />` (Plan 24-03) в render tree.
- Новый координатор `captainBirthController.ts` подписан на `'captain:birth-cta'`: spawn'ит L1 frog на текущей локации (Beat 4) + emit'ит `'starmap:open'` (Beat 5).

Purpose: без этого plan'а cinematic-инфраструктура (Plans 24-01..03) — мёртвая. Этот plan связывает её с реальным merge-моментом и финальным переходом в Star Map.

Output: end-to-end cinematic работает на L18+L18: flash (existing) → Phaser growing effect (~3s) → modal → CTA → spawn L1 → Star Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-captain-creation-cinematic/24-CONTEXT.md
@.planning/phases/24-captain-creation-cinematic/24-01-PLAN.md
@.planning/phases/24-captain-creation-cinematic/24-02-PLAN.md
@.planning/phases/24-captain-creation-cinematic/24-03-PLAN.md
@frog_obsidian/Design Notes/2026-05-18-captain-creation-cinematic.md
@client/src/game/scenes/main/MergeController.ts
@client/src/App.tsx

<interfaces>
<!-- MergeController L18+L18 branch (существующий, строки ~273-301): -->

```typescript
// L18+L18 — special path: лягушки сгорают (уже удалены removeFrog выше),
// L19 НЕ материализуется. Тригерится unlock Звёздной карты через sentinel
// markDiscovered(19).
if (oldLevel === MAX_LEVEL && b.level === MAX_LEVEL) {
  const storeL25 = useGameStore.getState()
  const currentLocId = storeL25.currentLocation
  storeL25.removeFrogFromLocation(currentLocId, MAX_LEVEL)
  storeL25.removeFrogFromLocation(currentLocId, MAX_LEVEL)
  const wasNew = storeL25.markDiscovered(19)
  if (wasNew) {
    eventBus.emit('location:unlocked', { locationId: 6 })
  }
  // Phase 22 Plan 22-06: cosmos unlock — реактивно открывает SerumBar,
  // Cosmic Hub button, Star Map ship controls во всех gated компонентах.
  storeL25.markCosmosUnlocked()
  mergeApi(MAX_LEVEL, currentLocId).then(/* ... */).catch(/* ... */)
  return
}
```

Phase 24 вставляется ПОСЛЕ `storeL25.markCosmosUnlocked()` (storeL25 уже доступен; cx, cy — параметры из enclosing closure).

<!-- App.tsx mount section (~строки 309-345): -->
В render tree уже есть `<OnboardingController />`, `<MilestoneToast />`, `<DiscoveryModal />`. CaptainBirthModal — рядом (modal self-subscribes к eventBus, null-render'ит когда invisible).

DEV bootstrap useEffect в App.tsx уже инициализирует `installBestiaryDevHelpers()`, `installOnboardingDevHelpers()` (паттерн install-once-on-boot). Сюда добавляем `installCaptainBirthController()` — но НЕ под DEV gate (production-critical coordinator).

<!-- useGameStore.addFrogToLocation signature: -->
```typescript
addFrogToLocation: (locationId: number, level: number) => void
```

<!-- eventBus 'starmap:open' (existing): -->
LocationStack уже подписан и переключает StarMapScene активной.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: captainBirthController.ts — Beat 4 + Beat 5 coordinator</name>
  <files>client/src/components/Captain/captainBirthController.ts</files>
  <action>
Создай `client/src/components/Captain/captainBirthController.ts`. Это НЕ React-компонент — это plain JS install-once coordinator (паттерн как installOnboardingDevHelpers, но production-critical).

Содержимое:

```typescript
// Phase 24 Plan 24-04 — Captain birth Beat 4 + Beat 5 coordinator.
//
// Подписан на eventBus 'captain:birth-cta' (эмитит CaptainBirthModal в Plan 24-03
// после CTA-tap'а или backdrop click). Выполняет:
//   - Beat 4: символический spawn L1 frog на ТЕКУЩЕЙ локации игрока
//     (per CONTEXT Claude's Discretion: на currentLocation, не на Лужу).
//     FrogSpawner подхватит store update через existing spawn flow.
//   - Beat 5: eventBus.emit('starmap:open') — LocationStack subscribe'нут,
//     setStarMapActive(true), Phaser StarMapScene открывается.
//
// install() — idempotent (повторный вызов снимает старый handler и ставит новый).
// Вызывается из App.tsx один раз на boot (НЕ под DEV gate).

import { eventBus } from '../../store/eventBus'
import { useGameStore } from '../../store/gameStore'

let uninstall: (() => void) | null = null

export function installCaptainBirthController(): void {
  if (uninstall) {
    uninstall()
    uninstall = null
  }

  const handler = () => {
    // Beat 4: spawn L1 frog на текущей локации.
    const store = useGameStore.getState()
    const currentLoc = store.currentLocation
    store.addFrogToLocation(currentLoc, 1)

    // Beat 5: open Star Map.
    eventBus.emit('starmap:open')
  }

  eventBus.on('captain:birth-cta', handler)
  uninstall = () => eventBus.off('captain:birth-cta', handler)
}
```

ВАЖНО: НЕ помещай install/handler внутрь React lifecycle (useEffect в App.tsx) — это создаст race window если App unmount/remount'ится (тестовая среда, hot-reload). Module-level idempotency guard — pattern из installBestiaryDevHelpers / installOnboardingDevHelpers.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npx tsc --noEmit 2>&1 | grep -E "captainBirthController|error TS" | head -10</automated>
  </verify>
  <done>
    - Файл создан, экспортирует installCaptainBirthController функцию
    - tsc clean
    - Handler вызывает addFrogToLocation(currentLocation, 1) + emit('starmap:open')
    - Idempotent install (повторный вызов снимает старый handler)
  </done>
</task>

<task type="auto">
  <name>Task 2: MergeController — L18+L18 hook на cinematic trigger</name>
  <files>client/src/game/scenes/main/MergeController.ts</files>
  <action>
В `client/src/game/scenes/main/MergeController.ts` найди L18+L18 branch (строка ~278, начало `if (oldLevel === MAX_LEVEL && b.level === MAX_LEVEL)`). После строки `storeL25.markCosmosUnlocked()` (около строки 289), ПЕРЕД `mergeApi(MAX_LEVEL, currentLocId)` вставь:

```typescript
        // Phase 24 Plan 24-04: cinematic trigger.
        // Первый L18+L18 normal merge → cinematic (Plan 24-02 Phaser effect →
        // Plan 24-03 modal → Plan 24-04 controller spawn'ит L1 + opens Star Map).
        // markCaptainBirthSeen idempotent → повторные L18+L18 НЕ играют cinematic.
        if (!storeL25.captainBirthSeen) {
          storeL25.markCaptainBirthSeen()
          eventBus.emit('captain:birth-start', { x: cx, y: cy })
        }
```

ВАЖНО:
- `cx`, `cy` уже доступны в closure (см. строки выше — midpoint merge'а).
- `storeL25` — уже взят через `useGameStore.getState()` чуть выше; повторно не вызывай.
- НЕ перемещай `storeL25.markCosmosUnlocked()` — наш hook ПОСЛЕ него (cosmos unlock тригерит другие UI элементы, которые должны быть готовы прежде чем Star Map откроется в Beat 5).
- `eventBus` уже импортирован в MergeController (проверь top of file). Если нет — добавь импорт, следуя существующим relative paths.
- НЕ трогай carrier-merge ветку (другой блок); cinematic — только normal+normal.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npx tsc --noEmit 2>&1 | grep -E "MergeController|error TS" | head -20 && grep -nE "captainBirthSeen|captain:birth-start" src/game/scenes/main/MergeController.ts</automated>
  </verify>
  <done>
    - Найдена ровно одна вставка в L18+L18 branch
    - tsc clean
    - grep показывает 2 строки: одна `if (!storeL25.captainBirthSeen)`, одна `eventBus.emit('captain:birth-start', ...)`
    - carrier-merge ветка не затронута (grep на carrier-merge branch в MergeController не должен показывать captainBirthSeen)
  </done>
</task>

<task type="auto">
  <name>Task 3: App.tsx — mount CaptainBirthModal + install coordinator</name>
  <files>client/src/App.tsx</files>
  <action>
В `client/src/App.tsx`:

1. Добавь импорты (рядом с existing imports onboarding/captain зоны):
   ```ts
   import { CaptainBirthModal } from './components/Captain/CaptainBirthModal'
   import { installCaptainBirthController } from './components/Captain/captainBirthController'
   ```

2. В render tree найди `<OnboardingController />` (около строки 332) и сразу после него добавь:
   ```tsx
   {/* Phase 24 Plan 24-04: Captain birth modal — self-subscribes к
       eventBus 'captain:birth-effect-complete' (Plan 24-02), null-render'ит
       когда invisible. Cinematic trigger — MergeController L18+L18 branch. */}
   <CaptainBirthModal />
   ```

3. В DEV bootstrap useEffect (где вызываются `installBestiaryDevHelpers()`, `installOnboardingDevHelpers()`) — ИЛИ если такого общего useEffect нет, найди существующий init-on-boot useEffect — добавь:
   ```ts
   // Phase 24 Plan 24-04: production coordinator (НЕ DEV-only).
   installCaptainBirthController()
   ```

   Если existing useEffect защищён `if (import.meta.env.DEV)` gate — НЕ помещай туда; создай отдельный useEffect верхнего уровня без gate:
   ```ts
   useEffect(() => {
     installCaptainBirthController()
   }, [])
   ```

ВАЖНО:
- Не добавляй conditional render для CaptainBirthModal — он сам управляет visibility через internal state + eventBus subscription.
- `installCaptainBirthController()` идемпотентен — даже если StrictMode/HMR вызовет useEffect дважды, handler не задублируется.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npx tsc --noEmit 2>&1 | grep -E "App\.tsx|error TS" | head -10 && grep -nE "CaptainBirthModal|installCaptainBirthController" src/App.tsx</automated>
  </verify>
  <done>
    - CaptainBirthModal импортирован и mounted в render tree (рядом с OnboardingController)
    - installCaptainBirthController вызывается в useEffect (без DEV gate)
    - tsc clean
    - grep показывает 2 строки import + 1 строку <CaptainBirthModal /> + 1 строку install call
  </done>
</task>

</tasks>

<verification>
End-to-end ручной test (выполнять после ВСЕХ задач Plan 24-04):

1. Сбрось state: `localStorage.clear(); location.reload()` — fresh save.
2. DEV cheat: дай две L18 frog'и на текущую локацию (например через `__giveFrog(18)` если есть, или через несколько `addFrogToLocation` calls в console).
3. Drag одну L18 на другую → merge.
4. Должно произойти ПОДРЯД:
   - (Beat 1) flashAt белая вспышка — existing
   - (Beat 2) Phaser cinematic ~3s: particles + 3 rings + camera zoom
   - (Beat 3) DOM modal: L1 frog + glow + pulse + «Вы создали Капитана» + CTA «В космос →»
   - User: tap CTA (или backdrop)
   - Modal fade-out 400ms
   - (Beat 4) на текущей локации появляется L1 frog
   - (Beat 5) Star Map открывается автоматически
5. Repeat merge L18+L18 — cinematic НЕ играет (idempotent, markCaptainBirthSeen=true)
6. Проверь localStorage: `frog_evolution_captain_birth_seen` === 'true'
7. Проверь server sync: GET /game/state должен иметь `cosmic.captainBirthSeen=true` после save throttle window (~5s)

Browser console check для отслеживания:
```js
eventBus.on('captain:birth-start', p => console.log('Beat 2 start', p))
eventBus.on('captain:birth-effect-complete', () => console.log('Beat 3 mount'))
eventBus.on('captain:birth-cta', () => console.log('Beat 4+5 trigger'))
eventBus.on('starmap:open', () => console.log('Star Map opens'))
```
</verification>

<success_criteria>
- L18+L18 первый раз → 5-beat cinematic срабатывает целиком
- Повторные L18+L18 → никакого cinematic (idempotent)
- L1 frog spawn'ится на currentLocation после CTA
- Star Map открывается автоматически после CTA
- Никакого frog.container.alpha tween'инга, никакого Lottie
- tsc + vite build clean (deferred к Plan 24-05 если parallel-agent блокировка)
</success_criteria>

<output>
После завершения создай `.planning/phases/24-captain-creation-cinematic/24-04-SUMMARY.md`. Включи: точные строки вставки в MergeController (line number в финальной версии), решение о currentLocation vs forced Лужа для Beat 4 spawn (CONTEXT.md оставил это на discretion), best practices для smoke testing через console.
</output>
