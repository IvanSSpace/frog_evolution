---
phase: 24-captain-creation-cinematic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/persistence.ts
  - client/src/store/gameStore.ts
  - client/src/api/gameSync.ts
  - client/src/store/eventBus.ts
autonomous: true
requirements:
  - PHASE24-STATE
  - PHASE24-PERSISTENCE
  - PHASE24-SERVER-SYNC
  - PHASE24-MIGRATION
  - PHASE24-EVENTBUS
must_haves:
  truths:
    - "Игрок, который ещё не видел cinematic, имеет captainBirthSeen=false"
    - "Игрок с существующим сейвом где discoveredLevels включает 19 (или discovered[19]=true) получает captainBirthSeen=true автоматически (skip cinematic на uplifted сейвах)"
    - "Повторный вызов markCaptainBirthSeen — no-op"
    - "Флаг переживает reload (localStorage) и переезд устройств (server sync через cosmic.captainBirthSeen JSON field)"
    - "eventBus содержит типобезопасные события 'captain:birth-start' и 'captain:birth-effect-complete'"
  artifacts:
    - path: "client/src/store/persistence.ts"
      provides: "loadCaptainBirthSeen() + saveCaptainBirthSeen() + CAPTAIN_BIRTH_SEEN_KEY константа + legacy inference (discovered[19] -> true)"
      contains: "CAPTAIN_BIRTH_SEEN_KEY"
    - path: "client/src/store/gameStore.ts"
      provides: "captainBirthSeen: boolean + markCaptainBirthSeen() idempotent action"
      contains: "captainBirthSeen"
    - path: "client/src/api/gameSync.ts"
      provides: "snapshotForSave включает cosmic.captainBirthSeen; loadGameState hydrates его при наличии"
      contains: "captainBirthSeen"
    - path: "client/src/store/eventBus.ts"
      provides: "Два новых типизированных события — captain:birth-start (с x,y) и captain:birth-effect-complete (void)"
      contains: "captain:birth-start"
  key_links:
    - from: "client/src/store/gameStore.ts"
      to: "client/src/store/persistence.ts"
      via: "loadCaptainBirthSeen() init + saveCaptainBirthSeen(true) в markCaptainBirthSeen action"
      pattern: "loadCaptainBirthSeen\\(\\)"
    - from: "client/src/api/gameSync.ts"
      to: "useGameStore.captainBirthSeen"
      via: "snapshotForSave читает state.captainBirthSeen, loadGameState пишет useGameStore.setState({captainBirthSeen})"
      pattern: "captainBirthSeen"
---

<objective>
Заложить foundation для Phase 24 cinematic — state-флаг `captainBirthSeen` в gameStore (toplevel, отдельный localStorage key — pattern из Phase 22 `hasCosmosUnlocked`), его server-sync через существующий gameSync механизм (вписать в `cosmic` JSON blob), миграция legacy сейвов (если у игрока уже есть discovered[19] → mark seen, чтобы НЕ играть cinematic для существующих cosmos-unlocked игроков), и два новых типизированных eventBus события для координации Phaser effect ↔ DOM modal.

Purpose: 4 последующих plan'а (Phaser effect, DOM modal, MergeController hook, finalize) опираются на этот state и события. Без foundation — параллельную работу не запустить.

Output: атомарный commit «foundation» + всё typed/persisted/migrated до начала визуальной работы.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-captain-creation-cinematic/24-CONTEXT.md
@frog_obsidian/Design Notes/2026-05-18-captain-creation-cinematic.md
@client/src/store/eventBus.ts
@client/src/store/gameStore.ts
@client/src/store/persistence.ts
@client/src/api/gameSync.ts

<interfaces>
<!-- Existing pattern (Phase 22 cosmos gate) — наш captainBirthSeen строится по тому же шаблону: -->

В `client/src/store/persistence.ts` уже есть:
```typescript
const COSMOS_UNLOCKED_KEY = 'frog_evolution_cosmos_unlocked'
export function loadCosmosUnlocked(): boolean { /* ... + legacy inference from discovered[19] */ }
export function saveCosmosUnlocked(v: boolean): void { /* localStorage.setItem(...) */ }
```

В `client/src/store/gameStore.ts`:
```typescript
// В GameStateBase interface (около строки 142):
hasCosmosUnlocked: boolean
markCosmosUnlocked: () => void

// В create(...) initial state (около строки 341):
hasCosmosUnlocked: loadCosmosUnlocked(),
markCosmosUnlocked: () => {
  const s = get()
  if (s.hasCosmosUnlocked) return // idempotent
  saveCosmosUnlocked(true)
  set({ hasCosmosUnlocked: true })
  eventBus.emit('cosmic:toast', { ... })
},
```

В `client/src/api/gameSync.ts` `snapshotForSave()` возвращает payload с `cosmic: { ... }` подобъектом (около строки 52); `loadGameState()` гидратирует поля из `data.cosmic` (около строки 115). hasCosmosUnlocked НЕ ходит через cosmic blob — он toplevel + изолированный key. Phase 24 captainBirthSeen ИДЁТ через `cosmic` blob (Captain Birth — это game milestone, который имеет смысл синкать между устройствами; в отличие от Phase 23 onboarding который per-device).

В `client/src/store/eventBus.ts` mitt-events типизированы через `type Events = { ... }` map (строка 8). Добавление события — просто новый ключ с payload-shape.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistence helpers + миграция legacy сейвов</name>
  <files>client/src/store/persistence.ts</files>
  <action>
В `client/src/store/persistence.ts`:

1. Добавь константу рядом с другими storage keys (после `COSMOS_UNLOCKED_KEY`, до `ONBOARDING_KEY`):
   ```ts
   // Phase 24 Plan 24-01: captain creation cinematic — per-user milestone flag,
   // server-sync через cosmic JSON blob (не per-device как onboarding).
   const CAPTAIN_BIRTH_SEEN_KEY = 'frog_evolution_captain_birth_seen'
   ```

2. Добавь helpers по pattern `loadCosmosUnlocked` / `saveCosmosUnlocked` (после блока cosmos-unlocked, до onboarding):
   ```ts
   export function loadCaptainBirthSeen(): boolean {
     if (typeof localStorage === 'undefined') return false
     try {
       const raw = localStorage.getItem(CAPTAIN_BIRTH_SEEN_KEY)
       if (raw === 'true') return true
       if (raw === 'false') return false

       // Legacy migration (per CONTEXT.md decision): если у игрока уже есть
       // discoveredLevels[19] — он уже видел "cosmos unlock" в прошлой версии
       // (до cinematic). Mark seen чтобы НЕ играть cinematic на uplifted сейвах.
       const discRaw = localStorage.getItem(DISCOVERED_KEY)
       if (discRaw) {
         try {
           const arr = JSON.parse(discRaw)
           if (Array.isArray(arr) && arr.includes(19)) {
             saveCaptainBirthSeen(true)
             return true
           }
         } catch { /* ignore parse error */ }
       }
       // Также проверяем cosmic blob — если server-sync уже принёс captainBirthSeen
       // (Task 2 пишет туда), читаем оттуда.
       const cosmicRaw = localStorage.getItem(COSMIC_KEY)
       if (cosmicRaw) {
         try {
           const parsed = JSON.parse(cosmicRaw) as { captainBirthSeen?: boolean }
           if (parsed?.captainBirthSeen === true) {
             saveCaptainBirthSeen(true)
             return true
           }
         } catch { /* ignore */ }
       }
       return false
     } catch {
       return false
     }
   }

   export function saveCaptainBirthSeen(v: boolean): void {
     if (typeof localStorage === 'undefined') return
     try {
       localStorage.setItem(CAPTAIN_BIRTH_SEEN_KEY, v ? 'true' : 'false')
     } catch { /* ignore */ }
   }
   ```

Не трогай `loadCosmosUnlocked` — наш helper параллельный, не зависит от него.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(persistence\.ts|error TS)" | head -20</automated>
  </verify>
  <done>
    - `CAPTAIN_BIRTH_SEEN_KEY = 'frog_evolution_captain_birth_seen'` экспортируется
    - `loadCaptainBirthSeen()` возвращает true для legacy save с discovered[19]
    - `saveCaptainBirthSeen()` пишет в localStorage 'true'/'false'
    - tsc clean по persistence.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: gameStore field + action + gameSync server-sync + eventBus events</name>
  <files>
    client/src/store/gameStore.ts
    client/src/api/gameSync.ts
    client/src/store/eventBus.ts
  </files>
  <action>
**A. client/src/store/gameStore.ts**

1. Импортируй helpers (рядом с `loadCosmosUnlocked` import):
   ```ts
   import { loadCaptainBirthSeen, saveCaptainBirthSeen } from './persistence'
   ```
   (если import-блок уже подтягивает много из './persistence', добавь к существующему import statement).

2. В `interface GameStateBase` (около строки 142, после `markCosmosUnlocked`) добавь:
   ```ts
   // Phase 24 Plan 24-01: captain creation cinematic gate.
   // false до первого L18+L18 normal merge. Set'ится в MergeController
   // (Plan 24-04) перед emit'ом 'captain:birth-start'. Idempotent.
   // Server-sync через cosmic JSON blob (см. gameSync.ts).
   captainBirthSeen: boolean
   markCaptainBirthSeen: () => void
   ```

3. В `create<GameState>((set, get) => ({ ... }))` initial state (около строки 341, рядом с `hasCosmosUnlocked`):
   ```ts
   captainBirthSeen: loadCaptainBirthSeen(),
   markCaptainBirthSeen: () => {
     const s = get()
     if (s.captainBirthSeen) return // idempotent
     saveCaptainBirthSeen(true)
     set({ captainBirthSeen: true })
   },
   ```

   ВАЖНО: не добавляй `eventBus.emit('cosmic:toast', ...)` сюда — toast «Космос открыт!» уже эмитит `markCosmosUnlocked`. Дублирование не нужно. Cinematic сам по себе и есть «toast».

4. НЕ добавляй `captainBirthSeen` в `subscribe(...)` auto-persist block (строка 367+) — этот блок только для cosmic slice fields. Наш флаг персистится синхронно внутри `markCaptainBirthSeen` через `saveCaptainBirthSeen()` (как `markCosmosUnlocked` делает).

**B. client/src/api/gameSync.ts**

1. В `snapshotForSave()` (строка 38+) внутри `cosmic: { ... }` подобъекта добавь поле в конец (перед `preferences:`):
   ```ts
   // Phase 24 Plan 24-01: cross-device sync captain birth milestone.
   captainBirthSeen: s.captainBirthSeen,
   ```

2. В `loadGameState()` (около строки 115+, блок `if (data.cosmic && typeof data.cosmic === 'object')`) после строки `if ('tutorialState' in c) cosmicUpdate.tutorialState = c.tutorialState` добавь:
   ```ts
   // Phase 24 Plan 24-01: hydrate captain-birth flag from server.
   if ('captainBirthSeen' in c) cosmicUpdate.captainBirthSeen = c.captainBirthSeen
   ```

   Это попадёт в `useGameStore.setState(cosmicUpdate as Partial<typeof store>)` ниже. ВАЖНО: если server вернул `true`, нам тоже надо записать в localStorage чтобы `loadCaptainBirthSeen()` при следующем boot не игнорил. Добавь после `setState`:
   ```ts
   // Phase 24 Plan 24-01: если server принёс captainBirthSeen=true, синхронизируй
   // localStorage чтобы next boot не показал cinematic до server response.
   if (cosmicUpdate.captainBirthSeen === true) {
     const { saveCaptainBirthSeen } = await import('../store/persistence')
     saveCaptainBirthSeen(true)
   }
   ```

   (Dynamic import — потому что `gameSync.ts` уже asynс, и persistence.ts может уже быть импортирован через other paths; dynamic безопасен и избегает potential circular концерна.)

**C. client/src/store/eventBus.ts**

В `type Events = { ... }` map добавь после блока Phase 23 событий (строка 144 — после `'tutorial:firstMerge': void`):

```ts
// Phase 24 Plan 24-01 — Captain creation cinematic coordination.
// 'captain:birth-start' эмитит MergeController (Plan 24-04) после первого
// L18+L18 normal merge. Subscribers:
//   - CaptainBirthEffect (Plan 24-02, Phaser): plays cosmic growing effect
//     (particles + 3 rings + camera zoom) в данной точке.
// Coords — Phaser game-pixels (cx, cy из merge midpoint).
'captain:birth-start': { x: number; y: number }
// 'captain:birth-effect-complete' эмитит CaptainBirthEffect когда Phaser-эффект
// отыграл (~3 сек). Subscribers:
//   - CaptainBirthModal (Plan 24-03, DOM): mount modal с frog + glow + CTA.
'captain:birth-effect-complete': void
```
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "error TS" | head -20</automated>
  </verify>
  <done>
    - `useGameStore.getState().captainBirthSeen` доступно типом boolean
    - `useGameStore.getState().markCaptainBirthSeen()` существует, idempotent (2-й вызов no-op)
    - `snapshotForSave().cosmic.captainBirthSeen` присутствует
    - `loadGameState()` гидратирует флаг из server.cosmic.captainBirthSeen + sync'ает localStorage
    - `eventBus.emit('captain:birth-start', { x: 100, y: 200 })` компилируется
    - `eventBus.emit('captain:birth-effect-complete')` компилируется
    - tsc clean
  </done>
</task>

</tasks>

<verification>
- tsc clean
- ручная проверка legacy migration: при `discoveredLevels=[1,2,...,19]` в localStorage и пустом `frog_evolution_captain_birth_seen` ключе — `loadCaptainBirthSeen()` возвращает true и пишет 'true' в storage (single-shot inference, последующие loads читают напрямую)
- ручная проверка fresh save: чистый localStorage → loadCaptainBirthSeen()=false → markCaptainBirthSeen() → loadCaptainBirthSeen()=true; повторный markCaptainBirthSeen() — no-op (никаких extra setState/save calls; можно проверить через breakpoint)
- ручная проверка server sync: snapshotForSave() возвращает объект с `cosmic.captainBirthSeen`; mock server payload `{cosmic:{captainBirthSeen:true}}` → loadGameState → store.captainBirthSeen=true + localStorage='true'
- eventBus types: `eventBus.emit('captain:birth-start', {x:0,y:0})` и `eventBus.emit('captain:birth-effect-complete')` без TS-ошибок
</verification>

<success_criteria>
- captainBirthSeen state доступен через useGameStore и server-sync'ается через gameSync
- Legacy migration защищает uplifted сейвы (discovered[19] true → captainBirthSeen true, cinematic skipped)
- 2 новых eventBus event типа объявлены и типизированы
- Никакого визуального изменения для игрока — это инфраструктурный plan; cinematic появится в plans 24-02/03/04
- Файлы: persistence.ts (helpers), gameStore.ts (state+action), gameSync.ts (cosmic blob field + hydration), eventBus.ts (2 events)
</success_criteria>

<output>
После завершения создай `.planning/phases/24-captain-creation-cinematic/24-01-SUMMARY.md` с разделами: «What was built», «Files modified», «State shape», «Migration coverage», «Decisions logged» (любые отклонения от плана).
</output>
