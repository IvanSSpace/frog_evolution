---
phase: 28-quests
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - client/src/game/config/quests.ts
  - client/src/i18n/ru.json
  - client/src/i18n/en.json
  - client/src/i18n/es.json
autonomous: true
requirements:
  - PHASE28-QUEST-DATA-60-MAPPINGS
  - PHASE28-QUEST-CONFIG
  - PHASE28-REWARD-ESSENCE
  - PHASE28-REWARD-SERUM
  - PHASE28-REWARD-GOLD
  - PHASE28-REWARD-DIPLOMACY
  - PHASE28-I18N-RU
  - PHASE28-I18N-EN
  - PHASE28-I18N-ES
  - PHASE28-I18N-PARITY

must_haves:
  truths:
    - "QUESTS record in client/src/game/config/quests.ts contains 40 QuestConfig entries — one per quest_id stub from Phase 27 raceChains.ts"
    - "Every QuestConfig.id matches a quest_id from RACE_CHAINS quest_hook entries (no orphans, no missing — 40/40 coverage)"
    - "Difficulty assignment is mechanical: base id (no suffix) = easy; _b suffix = medium; _c suffix = hard"
    - "All 4 QuestType variants are represented across the 40 entries (≥5 delivery + ≥5 exploration + ≥5 merge + ≥5 diplomacy)"
    - "All 4 QuestReward kinds are exercised (≥3 essence + ≥3 serum + ≥3 gold + ≥3 relationship_and_bonus)"
    - "Reward magnitudes follow CONTEXT placeholder scaling: easy=1/1/10M, medium=3/2/100M, hard=5/3/500M (essence/serum/gold respectively)"
    - "i18n RU/EN/ES PARITY: each of 40 quests has description_key + short_key = 80 leaves per locale × 3 locales = 240 new strings"
    - "Quest description text matches the race personality from races.<id>.personality (Огнечервы demanding tone, Кристаллозиды patient geometric, Кометники cheerful, etc.)"
  artifacts:
    - path: client/src/game/config/quests.ts
      provides: QUESTS record filled with 40 entries keyed by quest_id from Phase 27 raceChains.ts
      contains: "crystalloids_silent_scout"
      min_lines: 500
    - path: client/src/i18n/ru.json
      provides: 80 new leaf keys under quests.<id>.* (description + short per quest_id × 40 quests)
      contains: "crystalloids_silent_scout"
    - path: client/src/i18n/en.json
      provides: 80 new leaf keys (PARITY)
      contains: "crystalloids_silent_scout"
    - path: client/src/i18n/es.json
      provides: 80 new leaf keys (PARITY)
      contains: "crystalloids_silent_scout"
  key_links:
    - from: client/src/game/config/quests.ts
      to: client/src/game/config/raceChains.ts
      via: "every QUESTS[questId] key matches a raceChains.ts quest_hook.quest_id literal (Plan 28-03 engine looks up by questId)"
      pattern: "quest_id:"
    - from: client/src/game/config/quests.ts
      to: client/src/i18n/{ru,en,es}.json
      via: "description_key/short_key strings reference quests.<id>.description / quests.<id>.short i18n leaves"
      pattern: "quests\\."
---

<objective>
Fill QUESTS record (skeleton from Plan 28-01) with **40 QuestConfig entries** — one for every quest_id stub in Phase 27 raceChains.ts. Each entry assigns: type (1 of 4), target (kind + params), reward (kind + magnitudes per difficulty scaling), description_key + short_key (i18n references), difficulty (easy/medium/hard per suffix convention).

Then ship i18n RU/EN/ES leaves for each quest: `quests.<questId>.description` (long body text) + `quests.<questId>.short` (card label) = 80 leaves per locale × 3 locales = **240 new i18n strings**.

Purpose: Plan 28-03 engine activates quests via `QUESTS[questId]` lookup at quest_hook accept time. Without data, the engine has nothing to activate. Mirror Phase 27 Plan 27-02 pattern (RACE_CHAINS data fill + i18n texts per chain step).

Output: 1 modified file (config/quests.ts gains ~500 LOC of QUESTS entries), 3 modified i18n files (~80 new leaves each). Bundle delta target ~+5-8 KB gzip (i18n JSON dominates; QUESTS data tree-shakeable to some extent but const record fully bundled).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-quests/28-CONTEXT.md
@.planning/phases/27-contacts-messages-relationships/27-01-SUMMARY.md
@.planning/phases/28-quests/28-01-PLAN.md

@client/CLAUDE.md
@/Users/shar/Documents/frog_evolution/CLAUDE.md

@client/src/game/config/raceChains.ts
@client/src/game/config/races.ts
@client/src/game/config/quests.ts

<interfaces>
<!-- The 40 quest_ids to fill, grouped by race (4 per race × 10 races). -->
<!-- Suffix convention: base = easy, _b = medium, _c = hard. -->
<!-- Each race owns 4 quests. Race personality should inform description tone. -->

```
crystalloids (affinity=crystal, personality=patient geometric):
  - crystalloids_silent_scout      (easy)
  - crystalloids_shard_delivery    (easy)
  - crystalloids_lattice_survey_b  (medium)
  - crystalloids_lattice_survey_c  (hard)

gasouls (affinity=gas, personality=ethereal whispers):
  - gasouls_lost_note              (easy)
  - gasouls_sunken_resonator       (easy)
  - gasouls_silent_chorus_b        (medium)
  - gasouls_silent_chorus_c        (hard)

mechanidons (affinity=mechanical, personality=protocol-obsessed):
  - mechanidons_module_delivery    (easy)
  - mechanidons_diagnostics        (easy)
  - mechanidons_audit_route_b      (medium)
  - mechanidons_audit_route_c      (hard)

fireworms (affinity=fire, personality=aggressive ritual demands):
  - fireworms_runaway_acolyte      (easy)
  - fireworms_shard_to_tenebrians  (easy)
  - fireworms_blood_oath_b         (medium)
  - fireworms_blood_oath_c         (hard)

liquidoids (affinity=water, personality=trade-focused, communal):
  - liquidoids_caravan             (easy)
  - liquidoids_stolen_cargo        (easy)
  - liquidoids_market_truce_b      (medium)
  - liquidoids_market_truce_c      (hard)

tenebrians (affinity=shadow, personality=secretive, gate-keepers):
  - tenebrians_hidden_gate         (easy)
  - tenebrians_last_shard          (easy)
  - tenebrians_veil_walk_b         (medium)
  - tenebrians_veil_walk_c         (hard)

plasmaspirits (affinity=plasma, personality=storm-chasing, kinetic):
  - plasmaspirits_lost_flock       (easy)
  - plasmaspirits_outrun           (easy)
  - plasmaspirits_storm_race_b     (medium)
  - plasmaspirits_storm_race_c     (hard)

forestcores (affinity=forest, personality=organic-growth, slow):
  - forestcores_young_forest       (easy)
  - forestcores_spore_migration    (easy)
  - forestcores_root_bridge_b      (medium)
  - forestcores_root_bridge_c      (hard)

timeweavers (affinity=void, personality=temporal-paradox):
  - timeweavers_spiral_link        (easy)
  - timeweavers_temporal_knot      (easy)
  - timeweavers_unspun_thread_b    (medium)
  - timeweavers_unspun_thread_c    (hard)

cometfolk (affinity=binary, personality=cheerful travellers):
  - cometfolk_young_comet          (easy)
  - cometfolk_lost_crest           (easy)
  - cometfolk_long_orbit_b         (medium)
  - cometfolk_long_orbit_c         (hard)
```

Reward magnitudes (CONTEXT lines 220-225, placeholder per difficulty):
- essence reward: easy=1, medium=3, hard=5
- serum reward: easy=1, medium=2, hard=3 (element = race.affinity OR 'random')
- gold reward: easy=10_000_000, medium=100_000_000, hard=500_000_000
- diplomacy reward: +1 relationship + bonus_id placeholder string (e.g. `gold_income_1pct`)

Quest type assignment guidance (distribute evenly across 4 types):
- Race affinity hints — "delivery" fits Crystalloids (shard_delivery), Mechanidons (module_delivery), Fireworms (shard_to_tenebrians), Liquidoids (caravan/stolen_cargo).
- "Exploration" fits Cometfolk (young_comet/lost_crest/long_orbit_*), Plasmaspirits (outrun/storm_race_*), Tenebrians (veil_walk_*).
- "Merge" fits Fireworms (blood_oath — ritual merging), Forestcores (root_bridge — growth), Timeweavers (unspun_thread — weaving).
- "Diplomacy" fits Gasouls (silent_chorus_* — building consensus), Crystalloids (lattice_survey_* — alignment), Mechanidons (audit_route_* — protocol agreement).
- Final tally ≥5 of each type — see `<acceptance_criteria>` below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill QUESTS record with 40 entries — config/quests.ts</name>
  <files>client/src/game/config/quests.ts</files>

  <read_first>
    - client/src/game/config/quests.ts (full file, from Plan 28-01) — current state: types defined, QUESTS empty
    - client/src/game/config/raceChains.ts (search "quest_id" — see Plan 28-01 interfaces for the full 40-id list)
    - client/src/game/config/races.ts (full file) — race affinity mapping для serum reward element choice
    - .planning/phases/28-quests/28-CONTEXT.md (lines 24-50, 87-100, 219-225) — type variants + reward magnitudes
  </read_first>

  <behavior>
    - QUESTS record contains EXACTLY 40 entries (one per quest_id from raceChains.ts)
    - Each entry has all 7 QuestConfig fields populated: id, raceId, type, target, reward, description_key, short_key, difficulty
    - description_key === `'quests.' + id + '.description'` (used in Task 2 to ensure i18n parity)
    - short_key === `'quests.' + id + '.short'`
    - difficulty determined by suffix: no suffix → easy, _b → medium, _c → hard
    - target shape matches type (delivery → serum_count|gold_amount; exploration → planets_visited|missions_complete; merge → merge_to_level|merge_count; diplomacy → raise_relationship)
    - target values scale by difficulty per CONTEXT D-Quest difficulty tiers: easy ~5 progress, medium ~25, hard ~100. Apply common sense per kind (e.g. merge_to_level: easy=L8, medium=L13, hard=L17; planets_visited: easy=5, medium=15, hard=50; serum_count: easy=5, medium=15, hard=50; gold_amount: easy=10M, medium=500M, hard=10B; merge_count: easy=10, medium=50, hard=200; missions_complete: easy=3, medium=10, hard=30; raise_relationship.tier: easy=4, medium=6, hard=8)
    - reward magnitudes per CONTEXT placeholder scaling (essence 1/3/5, serum 1/2/3, gold 10M/100M/500M, diplomacy +1 rel + bonus_id placeholder)
    - All 4 QuestType represented (≥5 of each across the 40 entries)
    - All 4 QuestReward kinds represented (≥3 of each)
    - Type/target/reward triples are internally consistent (a 'delivery' quest must have serum_count or gold_amount target; a 'diplomacy' quest must have raise_relationship target; etc.)
  </behavior>

  <action>
    Replace the empty `QUESTS = {}` skeleton with a fully populated record literal containing 40 entries. Group entries by race (4 per race × 10 races) with section dividers like `// ─── crystalloids ──────────────────────────────────`.

    **Assignment table** (apply to all 10 races — same structural pattern, per-race personality tone bleeds through into i18n strings in Task 2, NOT into config types):

    Use this **mechanical assignment** per quest_id (deterministic — no judgment calls so the file stays reviewable):

    | quest_id | type | target | reward |
    |---|---|---|---|
    | `crystalloids_silent_scout` (easy) | exploration | `{kind:'planets_visited', value:5}` | `{kind:'essence', value:1}` |
    | `crystalloids_shard_delivery` (easy) | delivery | `{kind:'serum_count', element:'crystal', value:5}` | `{kind:'gold', value:10_000_000}` |
    | `crystalloids_lattice_survey_b` (medium) | diplomacy | `{kind:'raise_relationship', raceId:'crystalloids', tier:6}` | `{kind:'relationship_and_bonus', raceId:'crystalloids', bonus_id:'gold_income_1pct'}` |
    | `crystalloids_lattice_survey_c` (hard) | merge | `{kind:'merge_to_level', level:17}` | `{kind:'essence', value:5}` |
    | `gasouls_lost_note` (easy) | delivery | `{kind:'serum_count', element:'gas', value:5}` | `{kind:'essence', value:1}` |
    | `gasouls_sunken_resonator` (easy) | exploration | `{kind:'missions_complete', value:3}` | `{kind:'serum', element:'gas', count:1}` |
    | `gasouls_silent_chorus_b` (medium) | diplomacy | `{kind:'raise_relationship', raceId:'gasouls', tier:6}` | `{kind:'gold', value:100_000_000}` |
    | `gasouls_silent_chorus_c` (hard) | merge | `{kind:'merge_count', value:200}` | `{kind:'essence', value:5}` |
    | `mechanidons_module_delivery` (easy) | delivery | `{kind:'serum_count', element:'mechanical', value:5}` | `{kind:'essence', value:1}` |
    | `mechanidons_diagnostics` (easy) | exploration | `{kind:'missions_complete', value:3}` | `{kind:'gold', value:10_000_000}` |
    | `mechanidons_audit_route_b` (medium) | diplomacy | `{kind:'raise_relationship', raceId:'mechanidons', tier:6}` | `{kind:'relationship_and_bonus', raceId:'mechanidons', bonus_id:'ship_speed_1pct'}` |
    | `mechanidons_audit_route_c` (hard) | merge | `{kind:'merge_to_level', level:17}` | `{kind:'gold', value:500_000_000}` |
    | `fireworms_runaway_acolyte` (easy) | exploration | `{kind:'planets_visited', value:5}` | `{kind:'serum', element:'fire', count:1}` |
    | `fireworms_shard_to_tenebrians` (easy) | delivery | `{kind:'serum_count', element:'fire', value:5}` | `{kind:'gold', value:10_000_000}` |
    | `fireworms_blood_oath_b` (medium) | merge | `{kind:'merge_count', value:50}` | `{kind:'essence', value:3}` |
    | `fireworms_blood_oath_c` (hard) | merge | `{kind:'merge_to_level', level:17}` | `{kind:'relationship_and_bonus', raceId:'fireworms', bonus_id:'serum_drop_1pct'}` |
    | `liquidoids_caravan` (easy) | delivery | `{kind:'serum_count', element:'water', value:5}` | `{kind:'gold', value:10_000_000}` |
    | `liquidoids_stolen_cargo` (easy) | delivery | `{kind:'gold_amount', value:50_000_000}` | `{kind:'serum', element:'water', count:1}` |
    | `liquidoids_market_truce_b` (medium) | diplomacy | `{kind:'raise_relationship', raceId:'liquidoids', tier:6}` | `{kind:'gold', value:100_000_000}` |
    | `liquidoids_market_truce_c` (hard) | diplomacy | `{kind:'raise_relationship', raceId:'liquidoids', tier:8}` | `{kind:'relationship_and_bonus', raceId:'liquidoids', bonus_id:'shop_discount_1pct'}` |
    | `tenebrians_hidden_gate` (easy) | exploration | `{kind:'planets_visited', value:5}` | `{kind:'essence', value:1}` |
    | `tenebrians_last_shard` (easy) | delivery | `{kind:'serum_count', element:'shadow', value:5}` | `{kind:'gold', value:10_000_000}` |
    | `tenebrians_veil_walk_b` (medium) | exploration | `{kind:'planets_visited', value:15}` | `{kind:'serum', element:'shadow', count:2}` |
    | `tenebrians_veil_walk_c` (hard) | exploration | `{kind:'planets_visited', value:50}` | `{kind:'essence', value:5}` |
    | `plasmaspirits_lost_flock` (easy) | exploration | `{kind:'planets_visited', value:5}` | `{kind:'serum', element:'plasma', count:1}` |
    | `plasmaspirits_outrun` (easy) | exploration | `{kind:'missions_complete', value:3}` | `{kind:'gold', value:10_000_000}` |
    | `plasmaspirits_storm_race_b` (medium) | merge | `{kind:'merge_count', value:50}` | `{kind:'serum', element:'plasma', count:2}` |
    | `plasmaspirits_storm_race_c` (hard) | exploration | `{kind:'missions_complete', value:30}` | `{kind:'essence', value:5}` |
    | `forestcores_young_forest` (easy) | merge | `{kind:'merge_count', value:10}` | `{kind:'essence', value:1}` |
    | `forestcores_spore_migration` (easy) | delivery | `{kind:'serum_count', element:'forest', value:5}` | `{kind:'serum', element:'forest', count:1}` |
    | `forestcores_root_bridge_b` (medium) | merge | `{kind:'merge_to_level', level:13}` | `{kind:'essence', value:3}` |
    | `forestcores_root_bridge_c` (hard) | diplomacy | `{kind:'raise_relationship', raceId:'forestcores', tier:8}` | `{kind:'relationship_and_bonus', raceId:'forestcores', bonus_id:'gold_income_1pct'}` |
    | `timeweavers_spiral_link` (easy) | merge | `{kind:'merge_to_level', level:8}` | `{kind:'essence', value:1}` |
    | `timeweavers_temporal_knot` (easy) | merge | `{kind:'merge_count', value:10}` | `{kind:'gold', value:10_000_000}` |
    | `timeweavers_unspun_thread_b` (medium) | merge | `{kind:'merge_to_level', level:13}` | `{kind:'serum', element:'void', count:2}` |
    | `timeweavers_unspun_thread_c` (hard) | diplomacy | `{kind:'raise_relationship', raceId:'timeweavers', tier:8}` | `{kind:'essence', value:5}` |
    | `cometfolk_young_comet` (easy) | exploration | `{kind:'planets_visited', value:5}` | `{kind:'serum', element:'binary', count:1}` |
    | `cometfolk_lost_crest` (easy) | exploration | `{kind:'missions_complete', value:3}` | `{kind:'gold', value:10_000_000}` |
    | `cometfolk_long_orbit_b` (medium) | exploration | `{kind:'planets_visited', value:15}` | `{kind:'serum', element:'binary', count:2}` |
    | `cometfolk_long_orbit_c` (hard) | exploration | `{kind:'planets_visited', value:50}` | `{kind:'relationship_and_bonus', raceId:'cometfolk', bonus_id:'ship_speed_1pct'}` |

    Tally verification (must hold):
    - delivery: 9 (crystalloids_shard_delivery, gasouls_lost_note, mechanidons_module_delivery, fireworms_shard_to_tenebrians, liquidoids_caravan, liquidoids_stolen_cargo, tenebrians_last_shard, forestcores_spore_migration) — recount carefully; if shortfall <5, swap one quest's type
    - exploration: 13
    - merge: 11
    - diplomacy: 7
    - essence reward: 11; serum reward: 11; gold reward: 9; relationship_and_bonus reward: 9
    - Each type ≥ 5 ✓ (delivery requires careful recount — if 4 then swap one easy delivery from "exploration: planets_visited" to "delivery: serum_count")

    Each entry follows this literal shape:
    ```ts
    crystalloids_silent_scout: {
      id: 'crystalloids_silent_scout',
      raceId: 'crystalloids',
      type: 'exploration',
      target: { kind: 'planets_visited', value: 5 },
      reward: { kind: 'essence', value: 1 },
      description_key: 'quests.crystalloids_silent_scout.description',
      short_key: 'quests.crystalloids_silent_scout.short',
      difficulty: 'easy',
    },
    ```

    Use 4-space indent inside the QUESTS object, single trailing comma per entry. The literal grows ~500 LOC.

    Confirm via tsc that the discriminated union narrowing works (TypeScript should accept all 40 entries without `as QuestTarget` casts — the literal shape is sufficient for inference).
  </action>

  <acceptance_criteria>
    - `grep -c "^  crystalloids_silent_scout:" client/src/game/config/quests.ts` returns 1
    - `grep -c "^  cometfolk_long_orbit_c:" client/src/game/config/quests.ts` returns 1
    - `grep -cE "^  [a-z]+_[a-z_]+(_b|_c)?: \{$" client/src/game/config/quests.ts` returns 40 (40 quest entries)
    - `grep -c "id: 'crystalloids_silent_scout'" client/src/game/config/quests.ts` returns 1
    - `grep -c "id: '" client/src/game/config/quests.ts | head` — count of `id:` literals inside QUESTS = 40
    - `grep -c "description_key:" client/src/game/config/quests.ts` returns 40
    - `grep -c "short_key:" client/src/game/config/quests.ts` returns 40
    - `grep -c "difficulty: 'easy'" client/src/game/config/quests.ts` ≥ 16 (matches table)
    - `grep -c "difficulty: 'medium'" client/src/game/config/quests.ts` ≥ 10
    - `grep -c "difficulty: 'hard'" client/src/game/config/quests.ts` ≥ 10
    - `grep -c "type: 'delivery'" client/src/game/config/quests.ts` ≥ 5
    - `grep -c "type: 'exploration'" client/src/game/config/quests.ts` ≥ 5
    - `grep -c "type: 'merge'" client/src/game/config/quests.ts` ≥ 5
    - `grep -c "type: 'diplomacy'" client/src/game/config/quests.ts` ≥ 5
    - `grep -c "kind: 'essence'" client/src/game/config/quests.ts` ≥ 3
    - `grep -c "kind: 'serum'" client/src/game/config/quests.ts` ≥ 3
    - `grep -c "kind: 'gold'" client/src/game/config/quests.ts` ≥ 3
    - `grep -c "kind: 'relationship_and_bonus'" client/src/game/config/quests.ts` ≥ 3
    - `cd client && ./node_modules/.bin/tsc --noEmit` returns 0 (proves all 40 entries are type-correct)
    - `cd client && ./node_modules/.bin/eslint src/game/config/quests.ts` shows "No issues found"
    - **Coverage proof — every raceChains.ts quest_id has a QUESTS entry:**
      `for id in $(grep -E "quest_id: '" client/src/game/config/raceChains.ts | awk -F"'" '{print $2}' | sort -u); do grep -q "  $id: {" client/src/game/config/quests.ts || echo "MISSING: $id"; done` produces NO output (no MISSING lines)
  </acceptance_criteria>

  <verify>
    <automated>cd client && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/game/config/quests.ts && echo "Coverage check:" && (cd .. && for id in $(grep -E "quest_id: '" client/src/game/config/raceChains.ts | awk -F"'" '{print $2}' | sort -u); do grep -q "  $id: {" client/src/game/config/quests.ts || echo "MISSING: $id"; done)</automated>
  </verify>

  <done>40 QuestConfig entries filled in QUESTS record covering all 40 quest_ids from raceChains.ts. Difficulty assignment matches suffix convention. All 4 type variants ≥5 entries, all 4 reward kinds ≥3 entries. tsc + eslint clean. Coverage check shows 0 MISSING quest_ids. Single atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: i18n RU/EN/ES strings — 80 leaves per locale (description + short × 40 quests)</name>
  <files>
    client/src/i18n/ru.json,
    client/src/i18n/en.json,
    client/src/i18n/es.json
  </files>

  <read_first>
    - client/src/i18n/ru.json (search "races.crystalloids.chain") — find existing per-race namespace structure
    - client/src/i18n/en.json (same)
    - client/src/i18n/es.json (same)
    - client/src/game/config/races.ts (search "personality" + race name keys) — race personality tone source
    - client/src/game/config/quests.ts (just updated in Task 1) — 40 quest_ids + raceId + type + target params для contextual i18n strings
    - .planning/phases/28-quests/28-CONTEXT.md (lines 24-50) — quest type descriptions
  </read_first>

  <behavior>
    - 40 quests × 2 keys (description + short) × 3 locales = 240 new i18n leaves total
    - Top-level namespace `"quests"` added if it does not exist (sibling to `"races"`, `"cosmic_hub"`, etc.)
    - Each quest gets a sub-object: `"quests.<questId>"` with `"description"` and `"short"` keys
    - description: 1-2 sentence narrative referencing what the player must do, in the race's voice (Огнечервы: command tone; Кристаллозиды: patient measured; Кометники: warm enthusiastic; Гасоулы: ethereal whisper; Механидоны: protocol-formal; Жидко-сферы: trade-pragmatic; Тенебрисы: hushed conspiratorial; Плазма-духи: kinetic-urgent; Лесо-корни: slow organic; Время-ткачи: cryptic-philosophical)
    - short: 1-4 word card label ("Доставить 5 кристаллических серум", "5 fire serums", etc.)
    - PARITY: RU keys == EN keys == ES keys (same paths, count, depth)
    - description references target params via natural language (e.g. "Доставить 5 кристаллических серум" embeds the count + element); does NOT use i18next interpolation (description is static per quest — interpolation reserved for dynamic counters in UI)
    - target.value / target.level mentioned naturally; quest type matched (delivery → "доставь/принеси/собери"; exploration → "посети/исследуй/заверши миссию"; merge → "объедини/сделай мерж"; diplomacy → "подними отношения")
  </behavior>

  <action>
    **Strategy:** Add a top-level `"quests"` key to each of the 3 locale JSON files. The value is an object containing 40 sub-objects (one per quest_id). Each sub-object has `description` and `short` keys.

    **Important parity rule:** JSON object key ordering does NOT affect parity (i18next reads keys, not ordering). But the parity script counts leaves — so key counts must match exactly. Use a single ordering convention (quest_ids sorted alphabetically) across all 3 locales to make diffs reviewable.

    **Insert location:** at top level of each `*.json` file. Insertion order does not matter — place at the bottom of the root object (before the closing `}`) for clean diffs.

    **Per-quest authoring guidance (40 entries — apply per-race personality):**

    Use these templates — one description per quest_id, customised per race. Below are 4 worked examples (one per type) for the easy-tier crystalloids quest as a writing reference; apply same pattern to the other 36 quest_ids:

    **`crystalloids_silent_scout` (exploration, planets_visited:5)**:
    - RU description: "Древние кристаллы предлагают своё внимание. Посетите 5 планет — каждое наблюдение укрепит решётку."
    - EN description: "The ancient crystals lend you their notice. Visit 5 planets — each observation strengthens the lattice."
    - ES description: "Los cristales antiguos te prestan su atención. Visita 5 planetas — cada observación refuerza la red."
    - short RU: "5 планет (Кристаллозиды)"
    - short EN: "5 planets (Crystalloids)"
    - short ES: "5 planetas (Cristaloides)"

    **`crystalloids_shard_delivery` (delivery, serum_count crystal:5)**:
    - RU: "Решётке нужны 5 кристаллических серум для нового резонатора. Принесите их."
    - EN: "The lattice needs 5 crystal serums for a new resonator. Bring them."
    - ES: "La red necesita 5 sueros de cristal para un nuevo resonador. Tráelos."
    - short RU: "5 кристаллических серум", EN: "5 crystal serums", ES: "5 sueros de cristal"

    **`crystalloids_lattice_survey_b` (diplomacy, raise_relationship tier:6)**:
    - RU: "Кристаллозиды зовут вас на медитацию совмещения. Поднимите отношения до уровня 6 — и решётка раскроется глубже."
    - EN: "The Crystalloids invite you to alignment meditation. Raise relations to tier 6 — and the lattice will open further."
    - ES: "Los Cristaloides te invitan a la meditación de alineación. Sube la relación al nivel 6 — y la red se abrirá más."
    - short: "Отношения 6+ (Кристаллозиды)" / "Tier 6 (Crystalloids)" / "Nivel 6 (Cristaloides)"

    **`crystalloids_lattice_survey_c` (merge, merge_to_level:17)**:
    - RU: "Глубокая решётка резонирует только с лягушкой L17. Создайте такую."
    - EN: "The deep lattice resonates only with an L17 frog. Create one."
    - ES: "La red profunda solo resuena con una rana L17. Crea una."
    - short: "L17 лягушка" / "L17 frog" / "Rana L17"

    Authoring the remaining 36 quest_ids:
    - For each, derive description from the table in Task 1 (type + target params) AND the race's personality (see CONTEXT 26-CONTEXT.md race lore — exists in `races.<id>.personality` i18n key for reference reading)
    - **Fireworms** voice: imperatives, ritualistic ("Сожги... Принеси кровь огня...")
    - **Gasouls** voice: ethereal whisper ("Услышь нас... принеси шёпот ветра...")
    - **Mechanidons** voice: protocol-formal ("Модуль X требуется... Доставьте 5 единиц...")
    - **Liquidoids** voice: trade-pragmatic ("Караван ждёт груз... 5 серум воды по цене договора")
    - **Tenebrians** voice: conspiratorial ("Тихо. Никто не должен знать... найди 5 шёпотов тени")
    - **Plasmaspirits** voice: kinetic ("Беги! Молнии не ждут — посети 5 планет до затишья!")
    - **Forestcores** voice: slow organic ("Земля просит мерж... объедини две души")
    - **Timeweavers** voice: cryptic ("Нить прошлого пересекает нить будущего... объедини на L8")
    - **Cometfolk** voice: cheerful enthusiastic ("Йоху! Полетели — посетим 5 планет вместе!")

    Keep each description 12-25 words (RU/EN), shorter for short_key (2-5 words). Use double quotes inside RU/ES if needed via JSON escape `\"`.

    **JSON structure example** (top-level of ru.json):
    ```json
    {
      "races": { ... existing ... },
      "cosmic_hub": { ... existing ... },
      "cosmos": { ... existing ... },
      "quests": {
        "crystalloids_silent_scout": {
          "description": "Древние кристаллы предлагают своё внимание. Посетите 5 планет — каждое наблюдение укрепит решётку.",
          "short": "5 планет (Кристаллозиды)"
        },
        "crystalloids_shard_delivery": {
          "description": "Решётке нужны 5 кристаллических серум для нового резонатора. Принесите их.",
          "short": "5 кристаллических серум"
        },
        ...38 more entries...
      }
    }
    ```

    Verify each JSON file remains valid (parse-able) after edits.

    Per memory `feedback_no_agents_default`: write directly via Write/Edit tool. Per project CLAUDE.md (frog_evolution): execute-plan workflow is the safe-editor pipeline — direct edits within it are sanctioned.
  </action>

  <acceptance_criteria>
    - `grep -c '"quests":' client/src/i18n/ru.json` returns 1 (top-level namespace exists)
    - `grep -c '"quests":' client/src/i18n/en.json` returns 1
    - `grep -c '"quests":' client/src/i18n/es.json` returns 1
    - `grep -c '"crystalloids_silent_scout":' client/src/i18n/ru.json` returns 1
    - `grep -c '"cometfolk_long_orbit_c":' client/src/i18n/ru.json` returns 1
    - `grep -c '"description":' client/src/i18n/ru.json` ≥ 40 (40 description leaves under quests namespace; may include other namespaces' descriptions — minimum guard)
    - `grep -c '"short":' client/src/i18n/ru.json` ≥ 40
    - Same minimums for en.json and es.json
    - All 3 files are valid JSON: `cd client && node -e "JSON.parse(require('fs').readFileSync('src/i18n/ru.json','utf-8')); JSON.parse(require('fs').readFileSync('src/i18n/en.json','utf-8')); JSON.parse(require('fs').readFileSync('src/i18n/es.json','utf-8'))"` exits 0
    - `cd client && node scripts/check-translations.cjs` exits 0 with 0 missing / 0 extra (RU leaf count === EN leaf count === ES leaf count)
    - Each questId from QUESTS has both keys present in all 3 locales:
      `for id in $(grep -E "^  [a-z_]+: \{$" client/src/game/config/quests.ts | awk '{print $1}' | tr -d ':'); do for f in ru en es; do grep -q "\"$id\":" "client/src/i18n/$f.json" || echo "MISSING $id in $f"; done; done` produces NO output
  </acceptance_criteria>

  <verify>
    <automated>cd client && node -e "JSON.parse(require('fs').readFileSync('src/i18n/ru.json','utf-8')); JSON.parse(require('fs').readFileSync('src/i18n/en.json','utf-8')); JSON.parse(require('fs').readFileSync('src/i18n/es.json','utf-8'))" && node scripts/check-translations.cjs && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>

  <done>40 quest_id sub-objects under top-level `"quests"` namespace in RU/EN/ES. Each quest has description + short keys. PARITY intact (3 locales same leaf count). Personality tone bleeds through per-race. All JSON files parse-able. check-translations passes. Single atomic commit.</done>
</task>

</tasks>

<verification>
**Build chain (run after all tasks land):**
- `cd client && ./node_modules/.bin/tsc --noEmit` exit 0
- `cd client && ./node_modules/.bin/eslint src/game/config/quests.ts` → No issues found
- `cd client && node scripts/check-translations.cjs` → 0 missing / 0 extra (parity intact)
- `cd client && ./node_modules/.bin/vitest run` → all existing tests still pass (no regressions)
- **Quest coverage proof:** every quest_id in raceChains.ts is keyed in QUESTS AND has description + short in all 3 locales (see acceptance_criteria scripts above)
</verification>

<success_criteria>
- 40 QuestConfig entries fill QUESTS record (one per quest_id from raceChains.ts)
- All 4 QuestType variants represented ≥5 times each
- All 4 QuestReward kinds represented ≥3 times each
- Difficulty assignment matches suffix convention (no suffix = easy, _b = medium, _c = hard)
- Reward magnitudes follow CONTEXT placeholder scaling
- 240 new i18n leaves (40 quests × 2 keys × 3 locales) — PARITY intact
- Race personality tones present in description strings
- tsc + eslint + check-translations + vitest gates all green
- 2 atomic commits (Task 1 quest data; Task 2 i18n)
</success_criteria>

<output>
After completion, create `.planning/phases/28-quests/28-02-SUMMARY.md` mirroring 27-02-SUMMARY.md structure.

requirements-completed list MUST include: PHASE28-QUEST-DATA-60-MAPPINGS, PHASE28-QUEST-CONFIG, PHASE28-REWARD-ESSENCE, PHASE28-REWARD-SERUM, PHASE28-REWARD-GOLD, PHASE28-REWARD-DIPLOMACY, PHASE28-I18N-RU, PHASE28-I18N-EN, PHASE28-I18N-ES, PHASE28-I18N-PARITY.

Note: PHASE28-QUEST-DATA-60-MAPPINGS REQ-ID retains the "60" label from the original phase outline (Phase 27 author's rough estimate), but factually = 40 mappings (verified via grep on raceChains.ts quest_id stubs). Document this discrepancy in SUMMARY's Decisions Made / Issues Encountered section.
</output>
