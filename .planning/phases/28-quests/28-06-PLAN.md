---
phase: 28-quests
plan: 06
type: execute
wave: 4
depends_on: [28-01, 28-02, 28-03, 28-04, 28-05]
files_modified:
  - client/SMOKE_TEST_28.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PHASE28-SMOKE
  - PHASE28-FINALIZE
  - PHASE28-I18N-PARITY
  - PHASE28-CLICLABILITY

must_haves:
  truths:
    - "Build chain validates green: tsc 0 errors / eslint clean / vitest ≥127 PASS / check-translations 0 missing 0 extra / vite build success"
    - "Bundle delta captured: main gzip vs Phase 27 baseline 220.94 KB; cap ~+15 KB (best-effort — Phase 28 adds quest engine + 40 configs + i18n + UI; expect ~+10-15 KB)"
    - "i18n parity verified: RU/EN/ES leaf count identical; Phase 27 baseline 522 → Phase 28 final = 522 + Phase 28 delta (~+95-100 leaves per locale)"
    - "DEV helpers tree-shake intact: grep production bundle for __activateQuest returns 0"
    - "SMOKE_TEST_28.md ships with ≥6 scenarios (A-F) covering: tab visibility + cosmos gate / quest activation from contacts accept / progress increment per type / completion + reward popup / manual cancel + relationship penalty / persistence cross-reload + cap enforcement"
    - "SMOKE_TEST_28.md i18n section pre-fills measurement results (parity 522 + delta, build chain green confirmations)"
    - "ROADMAP.md Phase 28 entry finalized: 28 REQ-IDs listed, all 6 plans checked [x], outcome paragraph appended, Last updated bumped"
    - "STATE.md frontmatter updated: current_phase=28 status=completed, progress totals incremented (16→17 phases, 48→54 plans), percent recalculated"
    - "Per-plan SUMMARYs verified present for plans 28-01..28-05 (28-06-SUMMARY created by this plan)"
  artifacts:
    - path: client/SMOKE_TEST_28.md
      provides: 6-scenario A-F manual QA checklist + i18n parity + build chain measurement + regression sanity for Phase 18/22/24/25/26/27 surfaces
      min_lines: 140
    - path: .planning/ROADMAP.md
      provides: Phase 28 entry finalized — TBD REQ replaced with 28 PHASE28-* IDs; 6 plan checkboxes [x]; outcome paragraph; Last updated bumped
      contains: "PHASE28-FINALIZE"
    - path: .planning/STATE.md
      provides: frontmatter current_phase: 28 (status: completed); progress totals updated; lead-in bumped; Phase Progress row added
      contains: "current_phase: 28"
  key_links:
    - from: client/SMOKE_TEST_28.md
      to: client/src/utils/devQuests.ts
      via: "scenarios reference __activateQuest / __completeQuest / __progressQuest / __resetQuests DEV helpers"
      pattern: "__activateQuest"
    - from: .planning/ROADMAP.md
      to: .planning/phases/28-quests/28-CONTEXT.md
      via: "outcome paragraph cites design source + delta numbers"
      pattern: "Phase 28"
---

<objective>
Close Phase 28: ship a manual QA checklist (SMOKE_TEST_28.md, 6 scenarios A-F), validate the build chain green, capture bundle delta vs Phase 27 baseline, verify i18n parity intact, finalize ROADMAP.md Phase 28 entry, update STATE.md to current_phase=28 (completed), update progress totals.

Purpose: Mirror Phase 27-06 finalize pattern. SMOKE_TEST is run manually by the user post-shipping (workflow.auto_advance=true auto-approves the checkpoint to unblock finalize). ROADMAP/STATE updates produce the persistent record of Phase 28 closure.

Output: 1 new file (SMOKE_TEST_28.md). 2 modified files (ROADMAP.md, STATE.md). 1 SUMMARY file (28-06-SUMMARY.md). Bundle delta documented but no further code changes — finalize plan is docs-only after build chain measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-quests/28-CONTEXT.md
@.planning/phases/28-quests/28-01-PLAN.md
@.planning/phases/28-quests/28-02-PLAN.md
@.planning/phases/28-quests/28-03-PLAN.md
@.planning/phases/28-quests/28-04-PLAN.md
@.planning/phases/28-quests/28-05-PLAN.md
@.planning/phases/27-contacts-messages-relationships/27-06-SUMMARY.md

@client/CLAUDE.md
@/Users/shar/Documents/frog_evolution/CLAUDE.md
@client/SMOKE_TEST_27.md

<interfaces>
<!-- Phase 27 baseline measurements (from 27-06-SUMMARY): -->
- vitest: 117 PASS / 0 FAIL / 1 skip (3 pre-existing Phase 22 suite-import failures excluded)
- check-translations: 522 / 522 / 522 RU/EN/ES (0 missing / 0 extra)
- vite build: main `index-*.js` gzip 220.94 KB; CosmicHubModal chunk 15.61 KB
- DEV tree-shake: `__addPending` not present in production bundle

<!-- Phase 28 expected measurement targets: -->
- vitest: ≥127 PASS (117 Phase 27 + ≥10 questEngine.test.ts new); 0 NEW failures
- check-translations: 522 + Phase 28 delta (~+95-100 leaves) per locale = ~617-622 / locale × 3 (parity)
- vite build: main gzip Phase 27 220.94 KB → Phase 28 ~230-235 KB (+10-15 KB target); CosmicHubModal chunk grows
- DEV tree-shake: __activateQuest must NOT appear in production bundle

<!-- All 28 Phase 28 REQ-IDs (from CONTEXT + grep on plan frontmatter): -->
1. PHASE28-QUEST-CONFIG (28-01)
2. PHASE28-QUEST-DATA-60-MAPPINGS (28-02; actual=40 — documented as discrepancy in 28-02 SUMMARY)
3. PHASE28-QUEST-TYPES-4 (28-01)
4. PHASE28-ACTIVE-QUESTS-STATE (28-01)
5. PHASE28-COMPLETED-QUESTS-STATE (28-01)
6. PHASE28-CAP-5 (28-01 + 28-03)
7. PHASE28-AUTO-ACTIVATE-FROM-HOOK (28-03)
8. PHASE28-AUTO-COMPLETE-PROGRESS (28-03)
9. PHASE28-MANUAL-CANCEL-PENALTY (28-03 + 28-04)
10. PHASE28-PROGRESS-HOOKS-EVENTBUS (28-03)
11. PHASE28-REWARD-ESSENCE (28-02 + 28-03)
12. PHASE28-REWARD-SERUM (28-02 + 28-03)
13. PHASE28-REWARD-GOLD (28-02 + 28-03)
14. PHASE28-REWARD-DIPLOMACY (28-02 + 28-03)
15. PHASE28-QUESTS-TAB-UI (28-04)
16. PHASE28-QUEST-CARD (28-04)
17. PHASE28-REWARD-POPUP (28-05)
18. PHASE28-PERSISTENCE (28-01)
19. PHASE28-SERVER-SYNC (28-01)
20. PHASE28-I18N-RU (28-01 + 28-02)
21. PHASE28-I18N-EN (28-01 + 28-02)
22. PHASE28-I18N-ES (28-01 + 28-02)
23. PHASE28-I18N-PARITY (28-06 measurement gate; baseline established by 28-01/28-02)
24. PHASE28-COSMOS-GATE (28-01)
25. PHASE28-DEV-HELPERS (28-03)
26. PHASE28-CLICLABILITY (28-04 + 28-05)
27. PHASE28-SMOKE (28-06)
28. PHASE28-FINALIZE (28-06)

Total = 28 REQ-IDs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build chain validation + bundle delta capture</name>
  <files></files>

  <read_first>
    - client/SMOKE_TEST_27.md (full) — Phase 27 SMOKE template structure
    - .planning/phases/27-contacts-messages-relationships/27-06-SUMMARY.md (lines 99-130) — baseline measurement table
    - .planning/STATE.md (Phase 27 row in Phase Progress section)
  </read_first>

  <behavior>
    - Run the full build chain from `client/` directory
    - Capture results into a measurement notebook (used by Task 2 to fill SMOKE_TEST_28.md inline + Task 4 to fill ROADMAP outcome paragraph)
    - Compare to Phase 27 baseline: gzip main / vitest count / i18n parity / DEV tree-shake
  </behavior>

  <action>
    Run the build chain commands (output captured to stdout — Task 2 reads these to fill SMOKE_TEST_28.md). No file modifications in this task — just measurement.

    Commands to run (record output for Task 2):
    1. `cd client && ./node_modules/.bin/tsc --noEmit` — expect exit 0, 0 errors
    2. `cd client && ./node_modules/.bin/eslint src/` (or specific Phase 28 files if scope tighter) — expect "No issues found"
    3. `cd client && ./node_modules/.bin/vitest run` — record PASS/FAIL/skip counts; expect ≥127 PASS / 0 NEW FAIL / 1 skip (3 pre-existing failures pattern from Phase 22 may still appear; document as carry-over)
    4. `cd client && ./node_modules/.bin/vitest run src/game/quests/questEngine.test.ts` — expect 10+ PASS
    5. `cd client && ./node_modules/.bin/vitest run src/api/gameSync.test.ts` — expect 2 PASS (coverage regression intact)
    6. `cd client && node scripts/check-translations.cjs` — record leaf count per locale; expect 0 missing / 0 extra; parity intact
    7. `cd client && ./node_modules/.bin/vite build` — record main gzip kB + CosmicHubModal chunk gzip kB from vite output; expect build success (chunk-size warning on phaser pre-existing)
    8. DEV tree-shake check: `grep -c "__activateQuest" client/dist/assets/index-*.js` — expect 0

    Capture these specific numbers (placeholder for SMOKE_TEST_28.md and ROADMAP outcome):
    - vitest total PASS count: _____ (target ≥127)
    - vitest questEngine PASS count: _____ (target ≥10)
    - i18n leaf count per locale (RU): _____ (target 522 + ~95-100)
    - i18n leaf count per locale (EN): _____ (must match RU)
    - i18n leaf count per locale (ES): _____ (must match RU)
    - main bundle gzip: _____ KB (target ≤235 KB)
    - CosmicHubModal chunk gzip: _____ KB
    - Phase 27 baseline main gzip: 220.94 KB (constant — for delta calc)
    - Delta: _____ KB (target ≤+15)
    - DEV helper grep on prod bundle: _____ matches (target 0)

    If any gate fails (vitest new FAIL, eslint issues, check-translations missing/extra), STOP and surface the failure as a deviation in 28-06-SUMMARY. Do NOT proceed to Task 2 with red gates.
  </action>

  <acceptance_criteria>
    - `cd client && ./node_modules/.bin/tsc --noEmit` exit 0
    - `cd client && ./node_modules/.bin/eslint src/game/config/quests.ts src/game/quests/questEngine.ts src/game/quests/questEngine.test.ts src/game/quests/questController.tsx src/utils/devQuests.ts src/components/CosmicHub/QuestsTab.tsx src/components/CosmicHub/quests/ src/components/Quests/ src/store/cosmic/types.ts src/store/cosmic/slice.ts src/store/eventBus.ts src/store/persistence.ts src/store/gameStore.ts src/api/gameSync.ts src/api/gameSync.test.ts src/components/CosmicHub/CosmicHubModal.tsx src/App.tsx` shows "No issues found"
    - `cd client && ./node_modules/.bin/vitest run` reports total PASS ≥ 127 (Phase 27 117 baseline + Phase 28 ≥10 new)
    - `cd client && ./node_modules/.bin/vitest run src/game/quests/questEngine.test.ts` reports ≥10 PASS / 0 FAIL
    - `cd client && node scripts/check-translations.cjs` exits 0 with parity intact (RU = EN = ES leaf count)
    - `cd client && ./node_modules/.bin/vite build` exits 0 (success)
    - DEV tree-shake check: `grep -c "__activateQuest" client/dist/assets/index-*.js 2>/dev/null` returns 0
  </acceptance_criteria>

  <verify>
    <automated>cd client && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/ && ./node_modules/.bin/vitest run && node scripts/check-translations.cjs && ./node_modules/.bin/vite build && (test "$(grep -c "__activateQuest" dist/assets/index-*.js 2>/dev/null)" = "0" || (echo "DEV helper leaked to prod!" && exit 1))</automated>
  </verify>

  <done>Build chain green. All measurements captured for Task 2 (SMOKE_TEST_28.md) + Task 4 (ROADMAP outcome). Numbers recorded inline in task summary commit message OR saved to a temp file for Task 2 reference. No commits in this task (measurement-only).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write SMOKE_TEST_28.md (6 scenarios A-F + i18n parity + build chain + regression sanity)</name>
  <files>client/SMOKE_TEST_28.md</files>

  <read_first>
    - client/SMOKE_TEST_27.md (full) — exact template structure (140 lines, 6 scenarios A-F, i18n section, build chain section, regression sanity section)
    - .planning/phases/28-quests/28-CONTEXT.md (all sections)
    - client/src/utils/devQuests.ts (Plan 28-03) — DEV helpers used in scenarios
    - Measurement results from Task 1 (build chain + bundle + i18n counts)
  </read_first>

  <behavior>
    - SMOKE_TEST_28.md mirrors SMOKE_TEST_27.md structure
    - ≥6 scenarios A-F, each with [ ] checkboxes
    - i18n parity section pre-filled with Task 1 measurement
    - Build chain section pre-filled with Task 1 measurement
    - Regression sanity section enumerates Phase 18/22/24/25/26/27 surfaces (carriers / ascension / captain birth / first contact / cosmic shop / inventory tab / contacts tab) for visual non-regression check
    - DEV helper callouts present in scenarios (where applicable)
    - Total LOC ~140 (matching Phase 27 SMOKE)
  </behavior>

  <action>
    Create `client/SMOKE_TEST_28.md` with this skeleton (fill measurement numbers from Task 1):

    ```markdown
    # SMOKE_TEST_28: Phase 28 Quests — Manual QA Checklist

    **Phase:** 28-quests
    **Scope:** Quest mechanic wiring; 8th Cosmic Hub tab «Квесты»; reward popup; cancel flow
    **Updated:** 2026-05-19
    **Status:** Ready for manual QA post-shipping

    Run on cosmos-unlocked save with at least one race contacted (firstContactsSeen=true для tested race). Use DEV helpers __addPending / __advanceChain / __activateQuest / __progressQuest / __completeQuest / __resetQuests where indicated.

    ---

    ## Scenario A — Tab visibility + cosmos gate

    Prerequisites: cosmos-unlocked save (otherwise modal-level lock screen). Cosmic Hub open.

    - [ ] Open Cosmic Hub → 8 tabs visible in strip (Корабль 🚀 / Боксы 🏭 / Бестиарий 📖 / Карьеры 🐸 / Магазин 🛒 / Инвентарь 🎒 / Контакты 📡 / Квесты 📜).
    - [ ] Tap «Квесты» tab → tab content swaps. Active counter «Активные квесты: 0/5» visible.
    - [ ] If activeQuests empty: empty state shows «Нет активных квестов. Открой Контакты чтобы принять.»
    - [ ] Tab strip fits 320px viewport without truncation OR scrolls horizontally without breaking layout (NOTE: if visible overflow, document in DEFERRED below — Phase 28 fallback options: reduce padding OR icon-only labels OR scroll-strip)
    - [ ] Tap any other tab and back to «Квесты» — state preserved (selectedTab persists)
    - [ ] Reload page → return to «Квесты» tab (sessionStorage lastActiveTab='quests' accepted by persistence whitelist)

    ## Scenario B — Quest activation from Contacts «Поддержать»

    Prerequisites: A race with chainProgress ≥ 5 (so a quest_hook is the next pending item) OR use DEV `__addPending('crystalloids')` repeatedly then `__advanceChain('crystalloids')` 5 times to reach a quest_hook.

    - [ ] In Contacts tab, navigate to a race with a quest_hook pending. Tap «Поддержать».
    - [ ] Quest_hook resolution: relationship +1 (visible in RelationshipBar pulse)
    - [ ] Switch to «Квесты» tab → 1 new active quest card visible. Race emoji + type icon + description + progress bar (0/N) + reward preview visible.
    - [ ] Console: `__dumpQuests()` → 1 entry in activeQuests with correct questId + raceId + progress=0
    - [ ] Trigger DEV: `__activateQuest('crystalloids_silent_scout')` → cap not reached → quest added

    ## Scenario C — Progress increment per quest type

    Note: requires specific quest types active. Use `__activateQuest('id')` for fixtures.

    - [ ] Activate a delivery serum quest (`__activateQuest('crystalloids_shard_delivery')`). Open a crystal box (or DEV-grant +1 crystal serum). Quest progress increments.
    - [ ] Activate an exploration planets quest (`__activateQuest('crystalloids_silent_scout')`). Tap any 5 different planets in Star Map. Progress reaches 5.
    - [ ] Activate a merge quest (`__activateQuest('forestcores_young_forest')` — merge_count target 10). Perform 10 merges anywhere. Progress reaches 10.
    - [ ] Activate a diplomacy quest (`__activateQuest('crystalloids_lattice_survey_b')` — raise_relationship tier 6). Use `__addPending` + accept until relationship = 6. Progress reflects current relationship score.

    ## Scenario D — Auto-complete + reward popup

    - [ ] Active any easy quest. Use `__completeQuest('<activeQuestId>')` to force completion.
    - [ ] Reward popup appears: dark card with race emoji + GOLD title «Квест выполнен!» + quest short label + reward summary + pink «Забрать» CTA. Slide-in animation visible (250ms).
    - [ ] Backdrop tap dismisses. Escape key dismisses. Auto-dismiss after 5s.
    - [ ] After dismiss: quest moves from activeQuests to completedQuests (visible in collapsible CompletedQuestsList). Reward applied: gold/essence/serum counter increments OR relationship +1 (for diplomacy reward).
    - [ ] Trigger 2 completions back-to-back. Popups queue and show sequentially.

    ## Scenario E — Manual cancel + relationship penalty

    - [ ] On a QuestCard, tap «Отказаться». Inline confirm panel appears with race-name interpolation in cancel_confirm text.
    - [ ] Tap «×» — confirm panel closes, quest still active.
    - [ ] Tap «Отказаться» (the red destructive button in confirm panel) — quest disappears from list, relationship -1 to that race (visible in Contacts tab RelationshipBar). cancelQuest action emitted 'quests:cancelled' + 'contacts:relationship-delta' events.
    - [ ] Active counter decrements.

    ## Scenario F — Persistence + cap enforcement

    - [ ] Activate 5 quests (cap reached). Active counter reads «5/5».
    - [ ] Cap notification visible: «Лимит активных квестов: 5. Завершите текущие.»
    - [ ] Try to activate a 6th via Contacts quest_hook accept. Relationship still +1 BUT quest NOT added. Active counter stays at 5/5.
    - [ ] Reload page. activeQuests survives reload (count, progress, ids intact). completedQuests survives reload.
    - [ ] Sign out + sign in on a different device (server sync test). Quests state matches (subject to game/state PUT cycle completing).

    ---

    ## i18n PARITY (build chain measurement)

    Run from `client/`:
    ```
    node scripts/check-translations.cjs
    ```
    Expected:
    - RU leaves: _____ (Phase 27 baseline 522 + Phase 28 delta ~+95-100)
    - EN leaves: _____ (must match RU)
    - ES leaves: _____ (must match RU)
    - Missing: 0 / Extra: 0

    Phase 28 i18n delta breakdown:
    - +13 cosmic_hub.quests.* + cosmic_hub.tab_quests (Plan 28-01 skeleton)
    - +80 quests.<id>.{description,short} (Plan 28-02, 40 quests × 2 keys)
    - Total per locale: ~+93 leaves

    ## BUILD CHAIN

    ```
    cd client && ./node_modules/.bin/tsc --noEmit                     # 0 errors
    cd client && ./node_modules/.bin/eslint src/                       # No issues found
    cd client && ./node_modules/.bin/vitest run                        # ≥127 PASS / 0 NEW FAIL / 1 skip
    cd client && ./node_modules/.bin/vitest run src/game/quests/       # ≥10 PASS (questEngine)
    cd client && node scripts/check-translations.cjs                  # 0 missing / 0 extra (see PARITY above)
    cd client && ./node_modules/.bin/vite build                       # Success (chunk warning on phaser pre-existing)
    ```

    Bundle delta:
    - Phase 27 baseline main gzip: 220.94 KB
    - Phase 28 actual main gzip: _____ KB (Task 1 measurement)
    - Delta: +_____ KB (target ≤+15)
    - CosmicHubModal chunk gzip: _____ KB (Phase 27 baseline 15.61 KB)
    - DEV helpers tree-shake: `grep -c "__activateQuest" dist/assets/index-*.js` → 0

    ## REGRESSION SANITY

    Verify these Phase 18-27 surfaces still work (open each, perform a basic action, confirm no console errors):

    - [ ] Phase 18 Bestiary tab — toggle, see milestone counter
    - [ ] Phase 22 Cosmic Shop tab — toggle, see essence/perma upgrades
    - [ ] Phase 22-03 Carrier ascension — feed carrier to L18 → ascends → essence +1
    - [ ] Phase 24 Captain birth cinematic — DEV `__triggerCaptainBirth()` runs cinematic + modal
    - [ ] Phase 25 Welcome modal — DEV reset onboarding → modal mounts on next refresh
    - [ ] Phase 26 First contact cinematic — DEV `__triggerFirstContact('crystalloids')` runs burst + DOM modal
    - [ ] Phase 26-04 Inventory tab — toggle, see currencies/serums/placeholders
    - [ ] Phase 27 Contacts tab — toggle, see 10 races, tap one, see RaceDetailView + RelationshipBar
    - [ ] Phase 27 Event toast — DEV `__advanceChain('crystalloids')` past an event → toast appears

    ---

    *Plan 28-06 / SMOKE_TEST_28 / Phase 28-quests*
    *Updated: 2026-05-19*
    ```

    Substitute the `_____` placeholders inline with Task 1's measured values BEFORE committing. Target line count ~140 lines (matching SMOKE_TEST_27).
  </action>

  <acceptance_criteria>
    - `test -f client/SMOKE_TEST_28.md` exit 0
    - `wc -l client/SMOKE_TEST_28.md | awk '{print $1}'` returns ≥120 (target ~140)
    - `grep -c "## Scenario [A-F]" client/SMOKE_TEST_28.md` returns ≥6
    - `grep -c "Phase 28" client/SMOKE_TEST_28.md` ≥ 3
    - `grep -cE "^- \[ \]" client/SMOKE_TEST_28.md` ≥ 25 (numerous checkbox items)
    - `grep -c "__activateQuest\|__progressQuest\|__completeQuest" client/SMOKE_TEST_28.md` ≥ 3 (DEV helpers referenced)
    - `grep -c "i18n PARITY\|BUILD CHAIN\|REGRESSION SANITY" client/SMOKE_TEST_28.md` returns 3 (3 section headings)
    - `grep -c "_____" client/SMOKE_TEST_28.md` returns 0 (all placeholders filled with Task 1 measurements)
  </acceptance_criteria>

  <verify>
    <automated>test -f client/SMOKE_TEST_28.md && test "$(wc -l < client/SMOKE_TEST_28.md)" -ge 120 && test "$(grep -c '_____' client/SMOKE_TEST_28.md)" -eq 0</automated>
  </verify>

  <done>SMOKE_TEST_28.md created with 6 scenarios A-F, i18n parity section, build chain section, regression sanity section. All measurement placeholders replaced with Task 1 actuals. Line count ≥120. Single atomic commit (docs).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke test approval gate</name>

  <what-built>
    Plans 28-01 through 28-05 shipped a complete quest mechanic: types + state + sync, 40 quest config entries + i18n RU/EN/ES (~+95 leaves per locale), engine + slice actions + DEV helpers, QuestsTab UI + QuestCard with cancel flow + CompletedQuestsList, QuestRewardPopup + queue controller.

    Task 1 validated build chain green (tsc + eslint + vitest + check-translations + vite + DEV tree-shake). Task 2 created SMOKE_TEST_28.md with 6 scenarios + measurement results.

    Per workflow `auto_advance: true` configured in `.planning/config.json` (mirror Phase 27-06 precedent), this checkpoint is auto-approved. Manual smoke run can be performed by the user post-shipping; ROADMAP/STATE finalize (Task 4) proceeds immediately.
  </what-built>

  <how-to-verify>
    Open `client/SMOKE_TEST_28.md` in editor. Read scenarios A-F. Optionally:
    1. Start dev server: `cd client && npm run dev`
    2. Open browser → cosmos-unlocked save
    3. Walk through scenarios A-F using DEV helpers
    4. Tick `- [ ]` boxes as you confirm each behavior
    5. If any scenario fails: file a tech-debt entry in `.planning/tech-debt/` with reproduction steps + suspected plan/file

    NO action required to unblock Task 4 — auto-advance protocol logs auto-approval and proceeds.
  </how-to-verify>

  <resume-signal>Auto-approved per workflow.auto_advance (Phase 27-06 precedent). Type "approved" or describe issues for explicit acknowledgment.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: ROADMAP.md + STATE.md finalize</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>

  <read_first>
    - .planning/ROADMAP.md (full Phase 28 entry section, around lines 723-745) — TBD placeholders to replace
    - .planning/STATE.md (full — particularly the frontmatter at top + Phase Progress table near line 320)
    - .planning/phases/27-contacts-messages-relationships/27-06-SUMMARY.md (lines 136-153) — ROADMAP/STATE finalization diff template
    - Task 1 measurement outputs (bundle delta + i18n + vitest counts)
  </read_first>

  <behavior>
    - ROADMAP.md Phase 28 entry: TBD → 28 PHASE28-* REQ-IDs; all 6 plan checkboxes [x] with descriptive labels; outcome paragraph appended; Last updated line bumped
    - STATE.md frontmatter: current_phase: 28 (status: completed); last_updated bumped; progress totals (16→17 phases, 48→54 plans, 83→87 percent — recompute exactly: 54/(54+remaining)); Phase Progress table gains Phase 28 row
    - Lead-in: «Status: Complete — Phase 28 closed (...)»; «Current Phase: 28 (complete)»
    - YAML parse-able after edits (verify via python or node)
  </behavior>

  <action>
    **4a) Edit `.planning/ROADMAP.md` Phase 28 entry (currently around lines 723-745):**

    1. Replace `**Requirements**: TBD (resolved при /gsd-plan-phase 28)` with:
       ```
       **Requirements**: PHASE28-QUEST-CONFIG, PHASE28-QUEST-DATA-60-MAPPINGS, PHASE28-QUEST-TYPES-4, PHASE28-ACTIVE-QUESTS-STATE, PHASE28-COMPLETED-QUESTS-STATE, PHASE28-CAP-5, PHASE28-AUTO-ACTIVATE-FROM-HOOK, PHASE28-AUTO-COMPLETE-PROGRESS, PHASE28-MANUAL-CANCEL-PENALTY, PHASE28-PROGRESS-HOOKS-EVENTBUS, PHASE28-REWARD-ESSENCE, PHASE28-REWARD-SERUM, PHASE28-REWARD-GOLD, PHASE28-REWARD-DIPLOMACY, PHASE28-QUESTS-TAB-UI, PHASE28-QUEST-CARD, PHASE28-REWARD-POPUP, PHASE28-PERSISTENCE, PHASE28-SERVER-SYNC, PHASE28-I18N-RU, PHASE28-I18N-EN, PHASE28-I18N-ES, PHASE28-I18N-PARITY, PHASE28-COSMOS-GATE, PHASE28-DEV-HELPERS, PHASE28-CLICLABILITY, PHASE28-SMOKE, PHASE28-FINALIZE
       ```
       (28 IDs total)

    2. Replace `**Plans:** TBD plans` with `**Plans:** 6 plans`

    3. Replace the empty `Plans:` block:
       ```
       Plans:
       - [ ] TBD (run /gsd-plan-phase 28 to break down)
       ```
       with:
       ```
       Plans:
       - [x] 28-01-PLAN.md — Foundation types + state + persistence + i18n skeleton + 8th tab registration
       - [x] 28-02-PLAN.md — Quest data (40 entries) + i18n RU/EN/ES (~+80 leaves)
       - [x] 28-03-PLAN.md — Pure questEngine + slice actions + 4 eventBus events + DEV helpers + ≥10 vitest
       - [x] 28-04-PLAN.md — QuestsTab + QuestCard + CompletedQuestsList + cancel flow
       - [x] 28-05-PLAN.md — QuestRewardPopup + queue controller + App wiring
       - [x] 28-06-PLAN.md — SMOKE_TEST_28 (6 scenarios) + ROADMAP/STATE finalize
       ```

    4. Append **Outcome paragraph** after the Plans list (similar to Phase 27 outcome — substantive narrative):
       ```
       **Outcome:** Quest mechanic foundation shipped. 8-я tab «Квесты» 📜 (cosmos-gated, modal-level lock inherited Phase 22-06) с активной очередью (cap 5) + collapsible completed history. 40 QuestConfig entries (config/quests.ts) — covers 100% raceChains.ts quest_hook stubs (actual = 40 vs phase-outline estimate 60; documented в 28-02 SUMMARY). 4 quest types (📦 Доставка / 🔍 Разведка / ⚡ Мерж / 🤝 Дипломатия). 4 reward kinds (essence + serum + gold + relationship_and_bonus с placeholder bonus_id). Auto-activation от Phase 27 «Поддержать» на quest_hook ChainItem (slice resolveAccept extended). Auto-complete при progress reaches target → reward popup (z-index 200 peer FirstContactModal, CSS keyframe slide-in 250ms, no Lottie). Manual cancel → -1 relationship penalty к race-owner'у (applyDeltaClamp shared с pendingEngine). Progress hooks subscribed: merge:happened (merge_count + merge_to_level через discoveredLevels), cosmic:box-opened (serum_count by element), starmap:planet-select (planets_visited), cosmic:ship-arrived (missions_complete), contacts:relationship-delta (raise_relationship). Polling-driven targets (gold_amount, raise_relationship) reconcile через triggerPendingPull-style mount effect в QuestsTab + boot-time call в questController. Pure questEngineTick deterministic side-effect-free + ≥10 vitest unit tests с vi.mock'd QUESTS fixture (independent от Plan 28-02 data fill — mirror Phase 27-03 pendingEngine pattern). Persistence + server sync через cosmic blob (snapshotForSave + loadGameState hydrate + defensive load strip unknown questId + FIFO trim completedQuests at 100 newest; gameSync.test.ts coverage REQUIRED_COSMIC_SYNC_FIELDS extended). 4 new typed eventBus events (quests:activated / cap-reached / completed / cancelled). DEV helpers __activateQuest / __progressQuest / __completeQuest / __resetQuests / __dumpQuests (Vite tree-shake confirmed — production bundle grep returns 0). RelationshipBar pulse inherited Phase 27 cancel penalty path. Cliclability checklist (type=button + touchAction:manipulation + stopPropagation + z-index hierarchy: QuestRewardPopup 200 / Cosmic Hub 100). Tab strip fit на 320px viewport — verified ___ (filled с Task 2 SMOKE_TEST_28 observation). i18n RU/EN/ES parity 522 → ___ keys × 3 locales (~+___ per locale: 13 cosmic_hub.quests.* + 80 quests.<id>.{description,short}). Bundle delta gzip main +___ KB (Phase 27 baseline 220.94 KB → ___ KB; cap ~+15 KB ___). 28/28 ✓ REQ-IDs (PHASE28-*).
       ```
       Fill `___` placeholders from Task 1 measurements before committing.

    5. Update the file-level `**Last updated:**` line at the top of ROADMAP.md to bump the date and append «Phase 28 complete (6 plans, quest mechanic, +___ KB gzip delta)».

    **4b) Edit `.planning/STATE.md`:**

    1. Frontmatter:
       - `current_phase: 27` → `current_phase: 28`
       - `status: completed` (already, but verify)
       - `last_updated: "2026-05-18T..."` → `last_updated: "2026-05-19T<current-time>Z"` (use current timestamp)
       - `progress.total_phases: 16` → `17`
       - `progress.completed_phases: 6` → `7`
       - `progress.total_plans: 48` → `54`
       - `progress.completed_plans: 40` → `46`
       - `progress.percent: 83` → recompute: 46/54 = 85% (use this value or actual rounded computation)

    2. Lead-in section (just below frontmatter):
       - `**Status:** Complete — Phase 27 closed (...)` → `Complete — Phase 28 closed (quest mechanic shipped; +___ KB gzip delta; 6 plans, 28 REQ-IDs ✓)`
       - `**Current Phase:** 27 (complete)` → `28 (complete)`

    3. Phase Progress table (around line 320 — find existing Phase 26/27 rows). Add a new Phase 28 row AFTER the Phase 27 row, BEFORE the `## v1.0 Achievement Summary` heading. Use the existing row template (Wave / Plan / Commits / Files / Bundle Delta gzip columns). Populate with Task 1 measurements + commit hashes from each Phase 28 plan completion.

    **YAML parse-ability check** (verify after edits):
    ```
    python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"
    ```
    Exit 0 = clean YAML.
  </action>

  <acceptance_criteria>
    - `grep -c "PHASE28-QUEST-CONFIG" .planning/ROADMAP.md` ≥ 1
    - `grep -c "PHASE28-FINALIZE" .planning/ROADMAP.md` ≥ 1
    - `grep -c "PHASE28-" .planning/ROADMAP.md` ≥ 28 (28 REQ-IDs listed in Phase 28 entry)
    - `grep -c "28-06-PLAN.md" .planning/ROADMAP.md` ≥ 1 (plan list updated)
    - `grep -c "TBD" .planning/ROADMAP.md` returns same or fewer matches than before (Phase 28 TBDs cleared; other TBDs in unrelated phases preserved)
    - `grep -c "current_phase: 28" .planning/STATE.md` ≥ 1
    - `grep -c "completed_phases: 7" .planning/STATE.md` ≥ 1
    - `grep -c "completed_plans: 46" .planning/STATE.md` ≥ 1
    - `grep -c "_____\|___" .planning/ROADMAP.md` returns 0 (all measurement placeholders filled — but grep may match other underscores; verify Phase 28 outcome paragraph specifically has no `___` literal)
    - YAML parse OK: `python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` exit 0
    - Phase Progress table has new row for Phase 28: `grep -c "^| .* | 28-" .planning/STATE.md` ≥ 1
  </acceptance_criteria>

  <verify>
    <automated>grep -c "PHASE28-FINALIZE" .planning/ROADMAP.md && grep -c "current_phase: 28" .planning/STATE.md && python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"</automated>
  </verify>

  <done>ROADMAP.md Phase 28 entry finalized (28 REQ-IDs + 6 plans [x] + outcome paragraph + Last updated bumped). STATE.md frontmatter updated (current_phase 28 / completed / progress 17/54). Phase Progress row 28 added. YAML parse-able. Single atomic commit (docs).</done>
</task>

</tasks>

<verification>
**Final build chain (run after all tasks land):**
- All Task 1 measurements captured and recorded in SMOKE_TEST_28.md + ROADMAP outcome
- SMOKE_TEST_28.md ≥120 lines with 6 scenarios A-F
- ROADMAP.md Phase 28 entry shows 28 REQ-IDs + 6 plans [x] + outcome paragraph
- STATE.md frontmatter shows current_phase: 28 status: completed; progress.completed_phases: 7 / completed_plans: 46
- YAML parse OK
- Per-plan SUMMARYs verified present at `.planning/phases/28-quests/28-0{1,2,3,4,5}-SUMMARY.md` (created during respective plans 28-01..28-05)
</verification>

<success_criteria>
- Build chain green: tsc + eslint + vitest (≥127 PASS) + check-translations + vite build + DEV tree-shake
- SMOKE_TEST_28.md ships 6 scenarios A-F covering tab visibility, activation, progress per type, completion + reward popup, cancel + penalty, persistence + cap
- ROADMAP.md Phase 28 entry finalized with 28 REQ-IDs + 6 plans [x] + outcome paragraph
- STATE.md frontmatter updated; Phase Progress row 28 added; YAML parse-able
- All `_____` / `___` measurement placeholders filled with Task 1 actuals
- Per-plan SUMMARYs verified present (28-01..28-05 from their respective plans; 28-06-SUMMARY from this plan's final completion step)
- 2 atomic commits (Task 2 SMOKE; Task 4 ROADMAP/STATE) + auto-approved checkpoint (Task 3)
</success_criteria>

<output>
After completion, create `.planning/phases/28-quests/28-06-SUMMARY.md` mirroring 27-06-SUMMARY.md structure:
- Frontmatter (requires enumerating all 5 prior plan SUMMARYs in `requires` for explicit dependency graph; provides + affects + tech-stack + key-files + key-decisions + requirements-completed + metrics)
- Body sections: Performance + Accomplishments + Task Commits + Build Chain Results table + Bundle Delta vs Phase 27 Baseline table + i18n Key Count Delta table + ROADMAP + STATE Finalization Diff Summary + Decisions Made + Deviations + Issues + Self-Check + Glossary Drift Report + User Setup Required + Next Phase Readiness

requirements-completed list MUST include: PHASE28-SMOKE, PHASE28-FINALIZE, PHASE28-I18N-PARITY, PHASE28-CLICLABILITY (smoke validation closes parity + cliclability for the phase).

Glossary drift report should suggest creating entries for:
- «Квест / Quest» (new domain concept; types + reward kinds + cap)
- «Активный квест очередь» (cap 5 cap-reached UX)
- «Reward popup» (auto-dismiss + queue UX)
- «Прогресс хуки» (eventBus subscriptions that drive auto-progress)

Surface for author decision only — no automated glossary edits per workspace CLAUDE.md.
</output>
