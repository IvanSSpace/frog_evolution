---
phase: 08-full-planet-uniqueness
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - client/src/audio/planetVoice.ts
  - client/src/game/scenes/StarMapScene.ts
autonomous: true
requirements: [SPEC-03]
must_haves:
  truths:
    - "buildSoundSignature(sys) возвращает tuple-string archetype|pitch|rot|inv|det|cutoff"
    - "refineSoundSeeds() выполняет до 10 attempts при коллизии sound signature"
    - "refineSoundSeeds вызывается в create() ПОСЛЕ refineAnimSeeds (порядок: texture → anim → sound)"
    - "Console output: '[StarMap] sound signatures: 1000/1000 unique, 0 unresolved'"
  artifacts:
    - path: "client/src/game/scenes/StarMapScene.ts"
      provides: "buildSoundSignature, refineSoundSeeds, и вызов refineSoundSeeds в create()"
      contains: "buildSoundSignature|refineSoundSeeds"
    - path: "client/src/audio/planetVoice.ts"
      provides: "exported deriveModulations для использования в StarMapScene"
      contains: "export function deriveModulations"
  key_links:
    - from: "refineSoundSeeds"
      to: "deriveModulations(seed, archetype)"
      via: "import из planetVoice"
      pattern: "import.*deriveModulations"
---

<objective>
Реализовать sound uniqueness pipeline (D-11..D-12). buildSoundSignature вычисляет tuple-string `archetype|pitch|rot|inv|det|cutoff` через `deriveModulations(effectiveSeed(sys), archetype)`. refineSoundSeeds выполняет до 10 attempts на mutate seed, обеспечивая 1000/1000 unique sound signatures. Вызов в create() — после refineAnimSeeds (порядок: texture → anim → sound). Каждый refine pass conservative для следующего: texture фиксирует свои signatures первым, anim вторым (signature учитывает уже изменённый rngSeed), sound третьим.

Покрывает D-11 (signature формат) и D-12 (10 attempts отдельный pass).

Purpose: Phase 4 даёт 4032 уникальных модуляций per archetype (на 36 в среднем), но без uniqueness pipeline возможны хвостовые коллизии — особенно для архетипов с малым числом планет. Refine pass гарантирует 1000/1000.

Output: buildSoundSignature + refineSoundSeeds в StarMapScene.ts; deriveModulations re-exported из planetVoice.ts; вызов добавлен в create().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-full-planet-uniqueness/08-SPEC.md
@.planning/phases/08-full-planet-uniqueness/08-CONTEXT.md
@client/src/audio/planetVoice.ts
@client/src/game/scenes/StarMapScene.ts

<interfaces>
<!-- В Plan 4 deriveModulations был внутренней (module-level) функцией.
     Plan 5 экспортирует её для использования в StarMapScene. -->

```ts
// planetVoice.ts (after Plan 4)
export function deriveModulations(seed: number, archetype: string): PlanetModulations { ... }
export interface PlanetModulations { pitchStep, rotationIdx, inversionIdx, detuneBin, cutoffBin }
```

<!-- Существующий refineAnimSeeds (StarMapScene.ts:2833+) — pattern reference для refineSoundSeeds -->
<!-- Существующий вызов refineXxxSeeds (StarMapScene.ts:270-271):
     this.refineTextureSeeds()
     this.refineAnimSeeds()
   После Plan 5:
     this.refineTextureSeeds()
     this.refineAnimSeeds()
     this.refineSoundSeeds()
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-export deriveModulations + PlanetModulations из planetVoice.ts</name>
  <read_first>
    - client/src/audio/planetVoice.ts (Plan 4 уже добавил функции — найти function deriveModulations и interface PlanetModulations)
  </read_first>
  <files>client/src/audio/planetVoice.ts</files>
  <action>
В planetVoice.ts заменить `function deriveModulations(...)` на `export function deriveModulations(...)`. PlanetModulations interface должен уже быть `export interface PlanetModulations` (так предписал Plan 4 task 2). Если нет — добавить `export`.

Дополнительно убедиться что THEME_SCALES export НЕ требуется (StarMapScene не нуждается в scale для signature — только для деривации модуляций, которая уже инкапсулирована в deriveModulations).
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && grep -E "^export function deriveModulations" src/audio/planetVoice.ts | wc -l | grep -q "^1$" && grep -E "^export interface PlanetModulations" src/audio/planetVoice.ts | wc -l | grep -q "^1$" && npx tsc --noEmit 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `export function deriveModulations` существует в planetVoice.ts
    - `export interface PlanetModulations` существует
    - tsc чистый
  </acceptance_criteria>
  <done>
    deriveModulations + PlanetModulations exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить buildSoundSignature, refineSoundSeeds в StarMapScene.ts и вызвать в create()</name>
  <read_first>
    - client/src/game/scenes/StarMapScene.ts (lines 1-30: imports; lines 260-275: create() refine call site; lines 2811-2856: refineAnimSeeds pattern; lines 932-955: animRng + mainSeedOverride + effectiveSeed добавленный в Plan 4)
    - client/src/audio/planetVoice.ts (deriveModulations, PlanetModulations — уже exported)
  </read_first>
  <files>client/src/game/scenes/StarMapScene.ts</files>
  <action>
**Шаг 1.** Добавить import в начало StarMapScene.ts (рядом с другими import statements). Если есть `import { eventBus } from ...`, добавить рядом:
```ts
import { deriveModulations } from '../../audio/planetVoice'
```

**Шаг 2.** Добавить два новых метода в class StarMapScene, рядом с refineAnimSeeds (после L2856 closing скобки `refineAnimSeeds`). Точное место: после `private hashId(id: string)` (L2858-2863):

```ts
// Phase 8: signature формат tuple-string. archetype|pitch|rot|inv|det|cutoff.
// Используется для refineSoundSeeds — гарантирует 1000/1000 unique sound signatures.
private buildSoundSignature(sys: Race | BgSystem): string {
  const archetype = (sys as BgSystem).archetype ?? sys.type
  const seed = this.effectiveSeed(sys)
  const m = deriveModulations(seed, archetype)
  return `${archetype}|${m.pitchStep}|${m.rotationIdx}|${m.inversionIdx}|${m.detuneBin}|${m.cutoffBin}`
}

// Phase 8: третий refine pass (после texture → anim → sound).
// При коллизии sound signature мутирует seed XOR ((attempt+1) * 0xc2b2ae3d).
// До 10 attempts на планету. Логирует unique/total + unresolved в консоль.
// ВАЖНО: эта мутация seed повлияет на anim signature, поэтому проводится ПОСЛЕДНЕЙ
// (после texture и anim refine). Обратная зависимость допустима: после Sound mutation
// мы НЕ возвращаемся к anim refine, потому что anim signature space уже достаточен
// (10 attempts × strict signature) для absorbing новых мутаций.
private refineSoundSeeds(): void {
  const sigs = new Map<string, string>()
  let conflicts = 0
  for (const sys of this.allSystems) {
    let attempt = 0
    let sig = this.buildSoundSignature(sys)
    while (sigs.has(sig) && attempt < 10) {
      const isBg = 'rngSeed' in sys && typeof (sys as BgSystem).rngSeed === 'number'
      const cur = isBg
        ? (sys as BgSystem).rngSeed
        : (this.mainSeedOverride.get(sys.id) ?? this.hashId(sys.id))
      const newSeed = (cur ^ ((attempt + 1) * 0xc2b2ae3d)) >>> 0
      if (isBg) {
        (sys as BgSystem).rngSeed = newSeed
      } else {
        this.mainSeedOverride.set(sys.id, newSeed)
      }
      sig = this.buildSoundSignature(sys)
      attempt++
      if (attempt === 10 && sigs.has(sig)) conflicts++
    }
    sigs.set(sig, sys.id)
  }
  // eslint-disable-next-line no-console
  console.log(`[StarMap] sound signatures: ${sigs.size}/${this.allSystems.length} unique, ${conflicts} unresolved conflicts (max 10 attempts)`)
}
```

**Шаг 3.** Добавить вызов в create() — после `this.refineAnimSeeds()` на L271. Найти эту строку и добавить новую строку:
```ts
this.refineTextureSeeds()
this.refineAnimSeeds()
this.refineSoundSeeds()  // Phase 8
```

**Constraint:** Использовать константу `0xc2b2ae3d` (FNV-1a hash multiplier) для mutate sound seed — отличается от anim mutation (`0x9e3779b9`) и texture mutation (`0x85ebca6b`), чтобы каждый pass разводил seeds в РАЗНОМ направлении и не дублировал работу другого pass.

**Note об order ([D-11 carrying texture→anim→sound]):** Каждый refine pass изменяет rngSeed для коллидирующих планет. Sound (последний) не должен ломать предыдущие unique signatures. Проверка корректности — verify_uniqueness_strict в Plan 6 даст confidence что все три passes завершают 100% unique. Если sound refine ломает anim/texture, это будет видно в Plan 6 verify scripts (anim/texture показывают <1000/<984 unique). При обнаружении такого — добавить второй проход anim refine после sound (но это редко требуется при 4032 sound combinations).
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && grep -E "import \{ deriveModulations \} from" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "private buildSoundSignature\(" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "private refineSoundSeeds\(" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "this\.refineSoundSeeds\(\)" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "0xc2b2ae3d" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && npx tsc --noEmit 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - import deriveModulations from planetVoice добавлен
    - private buildSoundSignature и private refineSoundSeeds методы существуют
    - refineSoundSeeds использует 10 attempts (`attempt < 10`)
    - Mutation использует `0xc2b2ae3d`
    - В create() вызов `this.refineSoundSeeds()` идёт ПОСЛЕ `this.refineAnimSeeds()`
    - tsc чистый
  </acceptance_criteria>
  <done>
    buildSoundSignature + refineSoundSeeds реализованы; вызов в create() корректен; tsc чистый.
  </done>
</task>

</tasks>

<verification>
Финальная проверка 1000/1000 unique sound signatures происходит в Plan 6 verify_sound_uniqueness.cjs. Здесь:
- `cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npx tsc --noEmit` — 0 errors
- `cd client && npm run build` — passes
- При запуске dev server в console должно появиться: `[StarMap] sound signatures: 1000/1000 unique`
</verification>

<success_criteria>
- buildSoundSignature + refineSoundSeeds в StarMapScene.ts
- deriveModulations exported из planetVoice
- Вызов refineSoundSeeds в create() после refineAnimSeeds
- TypeScript чистый, build проходит
- Atomic commit: `phase-8(plan-5): sound signature pipeline + refineSoundSeeds (10 attempts)`
</success_criteria>

<output>
После завершения создать `.planning/phases/08-full-planet-uniqueness/08-05-SUMMARY.md`. Включить:
- Order rationale (texture → anim → sound, с разными mutation constants)
- Sound signature формат + размер space (4032/archetype × 28 archetypes = 113K — гигантский headroom для 1000)
- Note о возможном edge-case (sound mutation breaking earlier passes — отслеживается в Plan 6)
</output>
