---
phase: 08-full-planet-uniqueness
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - client/src/game/scenes/StarMapScene.ts
autonomous: true
requirements: [SPEC-02]
must_haves:
  truths:
    - "buildTextureSignature учитывает sub-variant парам ИЛИ archetype-специфичный extra dimension для dead, повышая разрешающую способность signature space достаточно для 984/984 unique"
    - "refineTextureSeeds использует 10 attempts (вместо 5) для consistency с refineAnimSeeds"
    - "Console output: '[StarMap] texture signatures: 984/984 unique BG, 0 unresolved'"
  artifacts:
    - path: "client/src/game/scenes/StarMapScene.ts"
      provides: "extended buildTextureSignature, increased refineTextureSeeds attempts to 10"
      contains: "buildTextureSignature|refineTextureSeeds"
  key_links:
    - from: "buildTextureSignature"
      to: "renderBgPoint switch для archetype 'dead'"
      via: "учёт дополнительных variant-specific rng() calls"
      pattern: "case 'dead'"
---

<objective>
Резолюция последней texture-collision (1 unresolved: `2x dead:v2:c1-2:m1100`). Расширить `buildTextureSignature` чтобы учесть variant-specific параметры (особенно для archetype `dead` variant 2 — bare), и увеличить refine attempts до 10. Цель: 984/984 unique.

Покрывает SPEC requirement #2.

Purpose: Phase 7 при scaling до 984 BG-планет даёт 1 collision из-за слишком грубого signature, который не различает количество кратеров между двумя случайными "bare" dead планетами с одинаковым `c1`/`c2` в первых 2 rng() bins. Дополнительные dimensions решают.

Output: Расширенный `buildTextureSignature`; `refineTextureSeeds` с 10 attempts; console-log подтверждает 984/984.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-full-planet-uniqueness/08-SPEC.md
@.planning/phases/08-full-planet-uniqueness/08-CONTEXT.md
@client/src/game/scenes/StarMapScene.ts

<interfaces>
<!-- StarMapScene.ts:2868-2904 — buildTextureSignature текущий -->
<!-- StarMapScene.ts:2906-2928 — refineTextureSeeds текущий (5 attempts) -->
<!-- StarMapScene.ts:4836-4889 — case 'dead' в renderBgPoint (3 sub-variants) -->

Текущая signature формат:
`${archetype}:v${variant}:c${c1}-${c2}:m${surfaceLines}${gradientBands}${multiSpots}${stackedRings}`

Проблема: для dead variant 2 (bare) после `c1`/`c2` (которые мало варьируют для bare-варианта)
происходит коллизия. Решение: добавить ещё 2-3 параметра уровня pre-modifier для расширения space.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить buildTextureSignature дополнительными dimensions и увеличить refineTextureSeeds до 10 attempts</name>
  <read_first>
    - client/src/game/scenes/StarMapScene.ts (lines 2868-2904: buildTextureSignature; lines 2906-2928: refineTextureSeeds; lines 4836-4889: case 'dead' в renderBgPoint — 3 sub-variants для понимания какие параметры захватить)
    - .planning/phases/07-unique-planet-animations/PLAN.md (Task 11: Phase 7 паттерн buildTextureSignature)
    - .planning/phases/08-full-planet-uniqueness/08-SPEC.md (Requirement 2: Texture uniqueness 100%)
  </read_first>
  <files>client/src/game/scenes/StarMapScene.ts</files>
  <action>
**Шаг 1.** Расширить `buildTextureSignature` (L2868-2904) добавив 2 дополнительных bins **после** текущего `m{surfaceLines}{gradientBands}{multiSpots}{stackedRings}`:
- **`c3`**: ещё один Math.floor(rng() * 5) — третий count, расширяет diff space для archetypes с большим количеством параметров (dead/lava/desert)
- **`asym`**: rng() < 0.2 ? 1 : 0 — флаг asymmetric atmosphere (universal modifier 5)
- **`speckle`**: rng() < 0.25 ? 1 : 0 — флаг color speckle (universal modifier 6)

Финальная signature становится:
`${bg.archetype}:v${variant}:c${c1}-${c2}-${c3}:m${surfaceLines}${gradientBands}${multiSpots}${stackedRings}${asym}${speckle}`

**Тело функции после изменений:**
```ts
private buildTextureSignature(sys: Race | BgSystem): string {
  if (!('archetype' in sys)) return `main:${sys.id}`
  const bg = sys as BgSystem
  const rng = mulberry32(bg.rngSeed)
  // 1) sparkle decision
  rng()
  // 2) aura
  const showAura =
    bg.archetype !== 'dead' &&
    bg.archetype !== 'mineral' &&
    bg.archetype !== 'desert'
  if (showAura) {
    rng() // auraR
    rng() // auraAlpha
    if (rng() < 0.3) {
      rng() // double aura
    }
  }
  // 3) base color shift
  rng() // 0.92 + rng() * 0.07
  rng() // ringOffsetAng
  rng() // ringOffsetMag
  rng() // size factor
  // 4) baseRotation
  rng()
  // 5) sub-variant choice
  const variant = Math.floor(rng() * 3)
  // 6-7) первые 2 counts
  const c1 = Math.floor(rng() * 5)
  const c2 = Math.floor(rng() * 5)
  // Phase 8: third count для расширения signature space
  const c3 = Math.floor(rng() * 5)
  // 8) modifier flags (universal modifiers)
  const surfaceLines = rng() < 0.15 ? 1 : 0
  const gradientBands = rng() < 0.12 ? 1 : 0
  const multiSpots = rng() < 0.15 ? 1 : 0
  const stackedRings = rng() < 0.08 ? 1 : 0
  // Phase 8: asymmetric atmosphere + color speckle modifiers (последние 2 universal modifiers)
  const asym = rng() < 0.2 ? 1 : 0
  const speckle = rng() < 0.25 ? 1 : 0
  return `${bg.archetype}:v${variant}:c${c1}-${c2}-${c3}:m${surfaceLines}${gradientBands}${multiSpots}${stackedRings}${asym}${speckle}`
}
```

**Шаг 2.** Увеличить `refineTextureSeeds` attempts с 5 на 10. В L2917 заменить `attempt < 5` на `attempt < 10`, в L2922 — `attempt === 5` на `attempt === 10`.

Обновлённый метод:
```ts
private refineTextureSeeds(): void {
  const sigs = new Map<string, string>()
  let conflicts = 0
  let bgCount = 0
  for (const sys of this.allSystems) {
    if (!('archetype' in sys)) continue // skip main
    bgCount++
    const bg = sys as BgSystem
    let attempt = 0
    let sig = this.buildTextureSignature(bg)
    while (sigs.has(sig) && attempt < 10) {
      const cur = bg.rngSeed
      bg.rngSeed = (cur ^ ((attempt + 1) * 0x85ebca6b)) >>> 0
      sig = this.buildTextureSignature(bg)
      attempt++
      if (attempt === 10 && sigs.has(sig)) conflicts++
    }
    sigs.set(sig, sys.id)
  }
  // eslint-disable-next-line no-console
  console.log(`[StarMap] texture signatures: ${sigs.size}/${bgCount} unique BG, ${conflicts} unresolved`)
}
```

**Note:** Расширение signature расходует дополнительные 3 rng() calls (c3, asym, speckle) до того как мы попадаем в `surfaceLines/gradientBands/multiSpots/stackedRings` rng order. **ВАЖНО:** мы НЕ ломаем порядок rng() в renderBgPoint, потому что buildTextureSignature не должен реплицировать каждый rng() в renderBgPoint — он лишь захватывает достаточно entropy чтобы различать планеты. Чем больше captures — тем шире signature space.

Универсальные modifiers asymmetric atmosphere и color speckle уже существуют в renderBgPoint (см. StarMapScene.ts вокруг section "УНИВЕРСАЛЬНЫЕ МОДИФИКАТОРЫ ПОВЕРХ"). Мы захватываем их флаги в signature без каких-либо изменений в renderBgPoint.
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && grep -E "const c3 = Math\.floor\(rng\(\) \* 5\)" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "const asym = rng\(\) < 0\.2" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "const speckle = rng\(\) < 0\.25" src/game/scenes/StarMapScene.ts | wc -l | grep -q "^1$" && grep -E "attempt < 10" src/game/scenes/StarMapScene.ts | wc -l | awk '{if($1<2){exit 1}}' && npx tsc --noEmit 2>&1 | grep -E "error" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - В `buildTextureSignature` присутствуют переменные `c3`, `asym`, `speckle`
    - Финальная signature string содержит сегменты `c${c1}-${c2}-${c3}` и `m${...}{asym}{speckle}` (6 modifier flags вместо 4)
    - `refineTextureSeeds` использует `while (... && attempt < 10)` (Phase 7 было 5)
    - `npx tsc --noEmit` — 0 errors
  </acceptance_criteria>
  <done>
    buildTextureSignature расширена 3 дополнительными dimensions (c3, asym, speckle); refineTextureSeeds с 10 attempts; tsc чистый.
  </done>
</task>

</tasks>

<verification>
Проверка 984/984 unique будет в Plan 6 (verify script). Здесь:
- `cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npx tsc --noEmit` — 0 errors
- `cd client && npm run build` — passes
</verification>

<success_criteria>
- buildTextureSignature расширена тремя новыми dimensions (c3, asym, speckle)
- refineTextureSeeds с 10 attempts
- TypeScript чистый
- Atomic commit: `phase-8(plan-3): extend texture signature dimensions, 10 refine attempts`
</success_criteria>

<output>
После завершения создать `.planning/phases/08-full-planet-uniqueness/08-03-SUMMARY.md` со списком новых dimensions и заметкой что финальная проверка 984/984 в Plan 6 verify script.
</output>
