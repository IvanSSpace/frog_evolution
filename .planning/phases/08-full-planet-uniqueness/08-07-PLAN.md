---
phase: 08-full-planet-uniqueness
plan: 07
type: execute
wave: 7
depends_on: [08-06]
files_modified:
  - .planning/phases/08-full-planet-uniqueness/08-SMOKE.md
  - .planning/STATE.md
autonomous: false
requirements: [SPEC-06]
must_haves:
  truths:
    - "tsc --noEmit чистый (0 errors)"
    - "npm run build проходит без warnings уровня error"
    - "Index chunk gzipped прирост ≤50KB к baseline (~196KB → ≤246KB)"
    - "npm run verify-uniqueness возвращает exit code 0 (все 3 verifier'а pass)"
    - "Manual smoke: для каждого из ≥6 sampled archetypes 5 случайных планет звучат различимо и анимируются по-разному"
    - "STATE.md обновлён: Phase 8 = complete"
  artifacts:
    - path: ".planning/phases/08-full-planet-uniqueness/08-SMOKE.md"
      provides: "Smoke test report — tsc + build + verify + manual checklist results"
      min_lines: 30
    - path: ".planning/STATE.md"
      provides: "Updated phase progress (Phase 8 → complete)"
      contains: "current_phase: 8"
  key_links:
    - from: "build size baseline"
      to: "post-Phase-8 build size"
      via: "сравнение через `du -sh client/dist`"
      pattern: "index.*gzip"
---

<objective>
Финальная верификация Phase 8: автоматические checks (tsc, build, verify-uniqueness, build-size delta) + manual smoke test (per archetype 5 случайных планет звучат и анимируются различимо). Закрытие фазы — обновление STATE.md с phase=complete.

Покрывает SPEC.md acceptance criteria #8, #9, #10.

Purpose: Без smoke не можем доказать что 1000/1000 unique signatures транслируются в реально различимые игровые ощущения. Build size check защищает от bundle регрессии (D-17 gate).

Output: 08-SMOKE.md с отчётом, обновлённый STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-full-planet-uniqueness/08-SPEC.md
@.planning/phases/08-full-planet-uniqueness/08-CONTEXT.md
@client/src/game/data/planetMap.json

<interfaces>
<!-- Build size baseline:
     SPEC.md #6 — текущий index chunk ~196KB gzipped.
     После Phase 8 — index chunk ≤ 246KB gzipped.
     Tone.js chunk не растёт (Phase 8 не добавляет audio dependencies, только модулирует).

     Команда измерения (post-build):
     `cd client && npm run build` → output показывает kB / gzip kB per chunk.
     Можно также: `gzip -c client/dist/assets/index-*.js | wc -c` — фактический size.

     Smoke test methodology:
     - Открыть `npm run dev` в браузере
     - StarMap scene активна (Bottombar → планетка)
     - Камера zoom 0.4 — видно 30+ планет
     - Sample 5 случайных планет на archetype: ice, gas_giant, lava, dead, mineral, plasma (≥6 archetype'ов)
     - Тапать на каждую — animation должна выглядеть отлично, sound — different (pitch/timbre/cutoff)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Автоматические проверки (tsc + build + verify-uniqueness + build size delta)</name>
  <read_first>
    - .planning/phases/08-full-planet-uniqueness/08-SPEC.md (acceptance criteria #1-9)
    - client/package.json (build, verify-uniqueness scripts)
  </read_first>
  <files>.planning/phases/08-full-planet-uniqueness/08-SMOKE.md</files>
  <action>
**Шаг 1.** Запусти автоматические проверки и собери output:

```bash
# (a) TypeScript
cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client
npx tsc --noEmit > /tmp/p8_tsc.txt 2>&1
TSC_CODE=$?

# (b) Build
npm run build > /tmp/p8_build.txt 2>&1
BUILD_CODE=$?

# (c) Verify uniqueness
npm run verify-uniqueness > /tmp/p8_verify.txt 2>&1
VERIFY_CODE=$?

# (d) Build size — index chunk gzipped (Vite показывает 'gzip' в build log)
INDEX_GZIP=$(grep -E "index-.*\.js" /tmp/p8_build.txt | grep -oE "[0-9]+\.[0-9]+ kB │ gzip: [0-9]+\.[0-9]+ kB" | tail -1)
```

**Шаг 2.** Создай `.planning/phases/08-full-planet-uniqueness/08-SMOKE.md` со структурой:

```markdown
# Phase 8: Smoke & Final Verification Report

**Date:** {today}
**Status:** {pass | partial | fail}

## Automated Checks

### 1. TypeScript (tsc --noEmit)
- Exit code: {0 = pass}
- Output: `0 errors` или вырезка ошибок если есть

### 2. Build (npm run build)
- Exit code: {0 = pass}
- Index chunk gzip: {X.X kB} (baseline ~196 kB, budget ≤246 kB → delta {Δ kB})
- Tone.js chunk gzip: {Y.Y kB} (не растёт)
- Bundle gate: {pass | fail (превышение)}

### 3. Verify Uniqueness (npm run verify-uniqueness)
- Exit code: {0 = pass}
- Anim strict: {1000/1000 unique}
- Texture: {984/984 unique after refine}
- Sound: {1000/1000 unique after refine}

## Manual Smoke (Task 2)

См. ниже — отдельный чек-лист per archetype.

## SPEC.md Acceptance Criteria Status

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 1. Anim strict 1000/1000 | ✓/✗ | verify_anim_uniqueness_strict.cjs |
| 2. Texture 984/984 | ✓/✗ | verify_texture_uniqueness.cjs |
| 3. Sound 1000/1000 | ✓/✗ | verify_sound_uniqueness.cjs |
| 4. switch ≥96 cases | ✓/✗ | grep -c "case [0-9]+:" StarMapScene.ts |
| 5. COMP_DURATIONS_MS ≥96 | ✓/✗ | inspect Record |
| 6. THEME_COMPONENTS pools ≥12 | ✓/✗ | (target ≥14 met) |
| 7. play() differs by seed | ✓/✗ | manual smoke |
| 8. tsc --noEmit clean | ✓/✗ | tsc output |
| 9. build + bundle ≤+50KB | ✓/✗ | build output |
| 10. Manual smoke per archetype | ✓/✗ | manual smoke checklist |

## Conclusion

{Phase 8 complete | regressions found, see ...}
```

Заполни плейсхолдеры реальными значениями из output'ов.

**Шаг 3.** Если build-size delta > 50KB (фейл по criterion #9), добавь recommendation: "Либо optimize THEME_SCALES (сжать до compact form), либо tree-shake unused tone.js sub-modules. Не блокирует Phase 8 если single regression — фолбэк-tier."

**Шаг 4.** Обнови `.planning/STATE.md`:
- `current_phase: 8`
- В таблице Phase Progress добавь `| 8 — Full Planet Uniqueness | complete |`
- `progress.completed_phases` инкрементируй до 2 (был 1; Phase 7 уже complete; теперь Phase 8)
- `progress.total_phases` обнови до 8
- `last_updated`: today (формат ISO)
- В Notes добавь строчку: `Phase 8 (today): 1000/1000/1000 unique по anim/texture/sound, 96 анимационных компонентов, per-planet sound modulation, all theme pools ≥14.`
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && test -f .planning/phases/08-full-planet-uniqueness/08-SMOKE.md && grep -E "1000/1000|984/984" .planning/phases/08-full-planet-uniqueness/08-SMOKE.md | wc -l | awk '{if($1<3){exit 1}}' && grep -E "current_phase: 8" .planning/STATE.md | wc -l | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - 08-SMOKE.md существует и содержит ≥30 строк
    - Все 10 acceptance criteria из SPEC.md перечислены с status
    - STATE.md обновлён: current_phase=8, Phase 8 в таблице со status complete
    - tsc, build, verify exit codes — 0 (если не 0, отметить как partial и указать что fixed в этой сессии)
    - Index chunk gzip delta ≤50KB задокументирован
  </acceptance_criteria>
  <done>
    Все автоматические проверки пройдены и задокументированы; STATE.md обновлён.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — 5 случайных планет per archetype</name>
  <what-built>
    - Phase 8 fully implemented (Plans 1-6 merged)
    - Per-planet sound modulation активна для всех 1000 планет
    - 96 анимационных компонентов
    - 1000/1000 unique по всем трём axes (verify-uniqueness pass)
  </what-built>
  <how-to-verify>
1. Открой `cd /Users/shar/Documents/frog_evolution/frog_evolution_code/client && npm run dev`
2. В браузере открой http://localhost:5173 (или порт из vite output)
3. Включи планет-голос: settings → sound effects toggle (если требуется)
4. Открой StarMap (📖 → bottombar planet icon, или другой entrypoint)
5. Установи zoom 0.4 (mousewheel)
6. **Для КАЖДОГО из этих 6 archetypes** найди 5 планет одного archetype и тапни:
   - **ice** (cyan-голубые) — 5 планет: каждая должна звучать различимо (pitch + timbre), animation — разная
   - **gas_giant** (yellow-orange полосатые) — 5 планет: разные звуки и разные анимации
   - **lava** (red/orange горящие) — 5 планет: pitch/cutoff/анимация различимы
   - **dead** (серые) — 5 планет: разные тембры + animation
   - **mineral** (purple/cyan кристаллические) — 5 планет
   - **plasma** (электрические разноцветные) — 5 планет
7. **Обрати внимание:**
   - Звуки в одном archetype должны быть в одной "тональности" (например все ice звучат светло, все lava — тёмно), но СРЕДИ планет одного archetype различимы по pitch/inversion/cutoff
   - Анимации не повторяются (recipe + modifier params)
   - 96 компонентов работают (Phase 8 новые: bouncingBall/digitalGlitch/ringPulsar/swarmParticles/prismRefract/lifeBloom/windRibbons/wreckageOrbit) — поищи их в plasma/destroyed/mineral/organic/mist
8. Также протестируй ≥3 main races (home, crystal, rocky) — должны иметь свои уникальные signature звуки

**Если что-то ломается:**
- Не звучит — проверь что включена audio mute (Settings → 🔊)
- Звучит идентично между планетами — debug в console: должно быть [StarMap] sound signatures: 1000/1000 unique
- Анимация лагает — отдалить камеру до zoom=0.4 (меньше планет рендерится)
  </how-to-verify>
  <resume-signal>Type "approved" если все 6 archetypes показали диверсификацию, или опиши проблемы (например "ice 5 планет звучат идентично" — это блокер).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Финализация и git commit</name>
  <read_first>
    - 08-SMOKE.md (только что создан)
    - .planning/STATE.md (обновлённый)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
**Шаг 1.** Если manual smoke (Task 2) выявил regressions — задокументируй в 08-SMOKE.md в секции `## Issues Found`. Если все pass — добавь финальный блок:
```markdown
## Conclusion

✓ Phase 8 — все 10 acceptance criteria из SPEC.md пройдены.
1000/1000 unique anim, 984/984 unique texture, 1000/1000 unique sound.
Bundle delta: +{X}KB (≤50KB cap).
Manual smoke confirms diversity in 6 sampled archetypes (ice, gas_giant, lava, dead, mineral, plasma) + 3 main races (home, crystal, rocky).

Phase 8 complete: {today}.
```

**Шаг 2.** Atomic commit от Plan 7:
```bash
cd /Users/shar/Documents/frog_evolution/frog_evolution_code
git add .planning/phases/08-full-planet-uniqueness/08-SMOKE.md .planning/STATE.md
git commit -m "phase-8(plan-7): smoke + final verification, Phase 8 complete"
```
(Если в репо нет git — пропустить commit step.)

**Шаг 3.** Финальный summary в console:
```
✓ Phase 8 complete
  - 1000/1000 unique animations (strict criterion)
  - 984/984 unique textures
  - 1000/1000 unique sound signatures
  - 96 animation components
  - All theme pools ≥14
  - Bundle delta: ≤50KB
  - Manual smoke: 6 archetypes + 3 main races verified
```
  </action>
  <verify>
    <automated>cd /Users/shar/Documents/frog_evolution/frog_evolution_code && grep -E "Phase 8.*complete|Phase 8 complete" .planning/STATE.md | wc -l | awk '{if($1<1){exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - 08-SMOKE.md содержит секцию `## Conclusion` с финальным результатом
    - STATE.md содержит "Phase 8" со статусом `complete`
    - (Опционально) git commit phase-8(plan-7) присутствует
  </acceptance_criteria>
  <done>
    Phase 8 полностью верифицирована и закрыта.
  </done>
</task>

</tasks>

<verification>
- 08-SMOKE.md содержит финальный отчёт со всеми 10 acceptance criteria из SPEC.md
- STATE.md обновлён до phase 8 complete
- (Manual) 6 archetypes показали diversity per smoke checklist
</verification>

<success_criteria>
- Все 10 acceptance criteria SPEC.md verified (либо автоматически, либо manual)
- 08-SMOKE.md задокументировал результаты
- STATE.md отражает Phase 8 complete
- Atomic commit: `phase-8(plan-7): smoke + final verification, Phase 8 complete`
</success_criteria>

<output>
После завершения создать `.planning/phases/08-full-planet-uniqueness/08-07-SUMMARY.md` со ссылкой на 08-SMOKE.md и финальный статус. Также обновить `.planning/RETROSPECTIVE.md` если он существует — добавить заметку о Phase 8 patterns ("Strict signature pattern", "Per-planet sound modulation", "Verify scripts in repo").
</output>
